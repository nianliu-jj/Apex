# Apex——v0.1 系统分析分卷：S09 生命周期至 S16 Task 工具

> 文档状态：待审核，禁止据此进入代码开发。
> 本分卷覆盖 S09 至 S16。

## 0. 分卷使用规则
审核通过前只能修改文档，不得修改 apps、crates、Cargo 配置或运行代码。每个条目的代码注释要求是后续开发强制门禁。

## 1. 功能索引
| 编号 | 功能 | 计划验收 | 依赖 |
|---|---|---|---|
| S09-001 | Project 创建和 workspace 路径校验 | 非目录、越界路径被拒绝 | S08 |
| S09-002 | Session 创建/选择/关闭 | TUI 可列出并选择项目会话 | S09-001 |
| S09-003 | Run/Turn 状态机 | queued/running/waiting/completed/failed/interrupted 转换单测 | S09-002 |
| S09-004 | command_id 幂等 | 重复提交不重复产生业务结果 | S09-003 |
| S09-005 | lifecycle projection | 重启后恢复 active run 和 cursor | S09-004 |
| S09-006 | `v0.1-project-session` demo | 创建项目→会话→Run→Turn→恢复 | S09-005 |
| S10-001 | `Provider` trait | FakeProvider 和测试 adapter 共用 contract | S09 |
| S10-002 | provider config schema | model/base_url/timeout/retry 可验证 | S10-001 |
| S10-003 | credential metadata | 仅引用 credential_id，不读取原文到日志 | S10-002 |
| S10-004 | FakeProvider stream/error/timeout | fixture 可重复模拟 | S10-003 |
| S10-005 | OpenAI-compatible adapter | 真实请求、流式 delta、usage、错误映射 | S10-004 |
| S10-006 | 第二 Provider adapter | 切换不破坏历史和 session | S10-005 |
| S10-007 | retry/backoff/cancel | 只重试 provider 请求，不重试已执行工具 | S10-006 |
| S10-008 | `v0.1-provider-alpha` demo | 配置 provider 后完成一次真实或 fake turn | S10-007 |
| S11-001 | RunContext | project/session/run/turn/operation/correlation 全量关联 | S09、S10 |
| S11-002 | prompt/context builder | system、rules、spec、history、user input 分层 | S11-001 |
| S11-003 | provider response parser | text/tool_call/finish/usage/error 分流 | S11-002 |
| S11-004 | linear Agent Loop | user→provider→assistant 可完成 | S11-003 |
| S11-005 | tool-call continuation | provider tool call→approval/tool result→继续推理 | S11-004 |
| S11-006 | cancel/timeout/failure | 任意 turn 可中断并保存最终状态 | S11-005 |
| S11-007 | restart/recover run | Core 重启后不重复已完成 operation | S11-006 |
| S11-008 | `v0.1-agent-alpha` demo | 一次 Agent turn 全链路可查询 | S11-007 |
| S12-001 | PermissionRule schema | allow/deny/ask/always/never 可解析 | S11 |
| S12-002 | path classification | workspace-relative/outside/sensitive 正确分类 | S12-001 |
| S12-003 | Bash AST command classification | `rm -rf`、网络、重定向、管道等分类 | S12-002 |
| S12-004 | policy evaluator | 同样输入得到确定 decision | S12-003 |
| S12-005 | Approval lifecycle | create/approve/deny/expire/revoke 均产生事件 | S12-004 |
| S12-006 | priority control lane | cancel/security/approval 不被普通 delta 阻塞 | S12-005 |
| S12-007 | denial/approval TUI | TUI 可显示理由和下一步动作 | S12-006 |
| S12-008 | `v0.1-permission-beta` demo | 安全命令被阻断，用户批准后才继续 | S12-007 |
| S13-001 | ToolDefinition/operation_id | schema、版本、超时、取消字段可序列化 | S12 |
| S13-002 | Read tool | 读取 workspace 文件，输出限长、行号和 digest | S13-001 |
| S13-003 | Glob tool | glob 结果排序、数量/字节上限和越界阻断 | S13-002 |
| S13-004 | Grep tool | 搜索结果脱敏、截断、文件类型过滤 | S13-003 |
| S13-005 | result normalizer | success/denied/timeout/cancelled/error 统一格式 | S13-004 |
| S13-006 | tool audit projection | 工具调用耗时、状态、参数摘要可查 | S13-005 |
| S13-007 | `v0.1-read-tools-beta` demo | TUI 输入查询并看到结果和日志 | S13-006 |
| S14-001 | write preflight | workspace、符号链接、大小、编码检查 | S13 |
| S14-002 | Write tool | 仅经 Permission + ToolGateway 写文件 | S14-001 |
| S14-003 | Edit patch parser | 单文件 patch、上下文匹配和冲突检测 | S14-002 |
| S14-004 | Edit tool | 成功修改、匹配失败和冲突均可恢复 | S14-003 |
| S14-005 | PostToolUse event | 文件变更产生 digest、size、diff summary | S14-004 |
| S14-006 | rollback-on-failure | 写入后 gate 失败可回到变更前状态 | S14-005 |
| S14-007 | `v0.1-write-tools-beta` demo | 创建文件→编辑→失败回滚 | S14-006 |
| S15-001 | process supervisor | 子进程生命周期、stdout/stderr、退出码可回收 | S14 |
| S15-002 | command allowlist | `cargo test` 等安全命令可执行 | S15-001 |
| S15-003 | dangerous command block | 删除、提权、网络外传、后台驻留被阻断 | S15-002 |
| S15-004 | timeout/cancel/process-tree cleanup | Ctrl-C 后子孙进程结束 | S15-003 |
| S15-005 | output truncation | stdout/stderr 限长，digest 和 byte count 保留 | S15-004 |
| S15-006 | shell audit | command fingerprint、decision、exit class 可查询 | S15-005 |
| S15-007 | `v0.1-bash-beta` demo | 执行安全测试命令并拒绝危险命令 | S15-006 |
| S16-001 | Task schema | parent_run、task_id、write_scope、status 可持久化 | S15 |
| S16-002 | sequential task runner | 依赖顺序执行，不实现并行 DAG | S16-001 |
| S16-003 | child context | 子任务继承安全上下文但拥有独立 operation | S16-002 |
| S16-004 | child failure handling | 失败暂停父任务并显示原因 | S16-003 |
| S16-005 | Task TUI panel | 子任务状态、进度、输出摘要 | S16-004 |
| S16-006 | `v0.1-task-beta` demo | 父任务运行两个线性子任务并恢复 | S16-005 |

## 2. 详细系统分析
<!-- DETAILS BEGIN -->
## S09-001：Project 创建和 workspace 路径校验

### 1. 功能目标
本功能属于 S09。建立 Project、Session、Run、Turn 的生命周期、幂等命令和重启恢复。 本项聚焦于领域功能、应用服务和可运行入口，必须形成可以独立启动、测试或演示的最小闭环。

### 2. 范围与边界
- 包含：Project 创建和 workspace 路径校验所需契约、状态转换、错误返回、日志事件和自动化验收。
- 不包含：未列入本阶段的真实 Provider、跨阶段优化或视觉润色；依赖能力只能通过 Port、Fake 或 fixture 接入。
- 上游依赖：S08。依赖未 verified 时，本功能只能处于 blocked。
- 主要产物：领域事件、查询投影、日志和验收结果。

### 3. 输入、输出与处理流程
输入来自上游命令、查询、事件、配置、用户动作或 fixture，必须具备稳定标识、版本和必要授权上下文。

处理顺序：首先校验格式、当前状态、权限和依赖版本；其次，围绕功能 [Project 创建和 workspace 路径校验] 完成输入校验、领域处理、持久化或投影、用户反馈和可重复验收。；然后产生领域事件或标准结果并更新必要投影；最后返回成功、等待、失败或取消结果，并记录阶段进度。

输出为领域事件、查询投影、日志和验收结果。计划中的最小验收为：非目录、越界路径被拒绝。

### 4. 状态、数据与一致性
- 状态变化必须遵守领域模型与事件规范，禁止直接修改 projection 绕过 domain command。
- SQLite Event Store，或本阶段明确声明的 Fake Store，是业务事实来源；日志只用于诊断。
- 外部副作用必须具有 operation_id、幂等策略、取消边界和恢复结论。
- 并发写入遵循单写者或 Write Claim；查询需要说明一致性级别和 cursor。

### 5. 异常、取消与恢复
必须设计输入无效、依赖不可用、权限拒绝、超时、用户取消、进程重启和重复提交路径。每类路径都要定义稳定 error_code、用户提示、是否可重试、是否产生事件和重启动作。不安全时进入明确的 blocked、failed 或 interrupted，不能静默成功。

### 6. 日志与安全要求
入口初始化日志，记录 stage.started、关键进度、stage.completed 或 stage.failed。日志采用非 JSON、非 key=value 的单行位置式格式：时间、级别、PID、任务链 UUID、线程或协程、logger、最后一级文件名和行号、主消息。不记录客户端地址；诊断上下文仅在 DEBUG 级别展示并置于主消息前，INFO 及以上隐藏。必须保持 Port、幂等、取消、恢复和审计边界。

### 7. 测试与可运行验收
- 必须提供 cargo 命令、CLI 子命令、集成测试或可重复 demo。
- 至少覆盖成功、失败、取消、重复调用以及适用的恢复或回滚路径。
- 日志断言任务链 UUID、稳定 message code、进度闭环、真实文件名和行号、无敏感信息泄漏。
- 回滚只能删除本阶段临时产物或恢复快照，不得删除用户数据或覆盖外部修改。

### 8. 后续代码注释要求
编码前必须通过本条目审核。实现代码需要为公共类型和函数、状态转换、协议字段、数据库迁移、异步任务边界、错误分支和安全判定添加中文详细注释。注释应解释设计原因、约束和失败恢复；复杂算法、并发或锁、取消传播、幂等键和安全边界还要写明前置条件、后置条件及不变量。测试代码需要说明 fixture 意图和验收理由。

### 9. 审核结论栏
- 需求理解：待审核
- 架构边界：待审核
- 接口和事件：待审核
- 异常与恢复：待审核
- 安全与日志：待审核
- 允许进入代码开发：否

<!-- END S09-001 -->
## S09-002：Session 创建/选择/关闭

### 1. 功能目标
本功能属于 S09。建立 Project、Session、Run、Turn 的生命周期、幂等命令和重启恢复。 本项聚焦于领域功能、应用服务和可运行入口，必须形成可以独立启动、测试或演示的最小闭环。

### 2. 范围与边界
- 包含：Session 创建/选择/关闭所需契约、状态转换、错误返回、日志事件和自动化验收。
- 不包含：未列入本阶段的真实 Provider、跨阶段优化或视觉润色；依赖能力只能通过 Port、Fake 或 fixture 接入。
- 上游依赖：S09-001。依赖未 verified 时，本功能只能处于 blocked。
- 主要产物：领域事件、查询投影、日志和验收结果。

### 3. 输入、输出与处理流程
输入来自上游命令、查询、事件、配置、用户动作或 fixture，必须具备稳定标识、版本和必要授权上下文。

处理顺序：首先校验格式、当前状态、权限和依赖版本；其次，围绕功能 [Session 创建/选择/关闭] 完成输入校验、领域处理、持久化或投影、用户反馈和可重复验收。；然后产生领域事件或标准结果并更新必要投影；最后返回成功、等待、失败或取消结果，并记录阶段进度。

输出为领域事件、查询投影、日志和验收结果。计划中的最小验收为：TUI 可列出并选择项目会话。

### 4. 状态、数据与一致性
- 状态变化必须遵守领域模型与事件规范，禁止直接修改 projection 绕过 domain command。
- SQLite Event Store，或本阶段明确声明的 Fake Store，是业务事实来源；日志只用于诊断。
- 外部副作用必须具有 operation_id、幂等策略、取消边界和恢复结论。
- 并发写入遵循单写者或 Write Claim；查询需要说明一致性级别和 cursor。

### 5. 异常、取消与恢复
必须设计输入无效、依赖不可用、权限拒绝、超时、用户取消、进程重启和重复提交路径。每类路径都要定义稳定 error_code、用户提示、是否可重试、是否产生事件和重启动作。不安全时进入明确的 blocked、failed 或 interrupted，不能静默成功。

### 6. 日志与安全要求
入口初始化日志，记录 stage.started、关键进度、stage.completed 或 stage.failed。日志采用非 JSON、非 key=value 的单行位置式格式：时间、级别、PID、任务链 UUID、线程或协程、logger、最后一级文件名和行号、主消息。不记录客户端地址；诊断上下文仅在 DEBUG 级别展示并置于主消息前，INFO 及以上隐藏。必须保持 Port、幂等、取消、恢复和审计边界。

### 7. 测试与可运行验收
- 必须提供 cargo 命令、CLI 子命令、集成测试或可重复 demo。
- 至少覆盖成功、失败、取消、重复调用以及适用的恢复或回滚路径。
- 日志断言任务链 UUID、稳定 message code、进度闭环、真实文件名和行号、无敏感信息泄漏。
- 回滚只能删除本阶段临时产物或恢复快照，不得删除用户数据或覆盖外部修改。

### 8. 后续代码注释要求
编码前必须通过本条目审核。实现代码需要为公共类型和函数、状态转换、协议字段、数据库迁移、异步任务边界、错误分支和安全判定添加中文详细注释。注释应解释设计原因、约束和失败恢复；复杂算法、并发或锁、取消传播、幂等键和安全边界还要写明前置条件、后置条件及不变量。测试代码需要说明 fixture 意图和验收理由。

### 9. 审核结论栏
- 需求理解：待审核
- 架构边界：待审核
- 接口和事件：待审核
- 异常与恢复：待审核
- 安全与日志：待审核
- 允许进入代码开发：否

<!-- END S09-002 -->
## S09-003：Run/Turn 状态机

### 1. 功能目标
本功能属于 S09。建立 Project、Session、Run、Turn 的生命周期、幂等命令和重启恢复。 本项聚焦于领域功能、应用服务和可运行入口，必须形成可以独立启动、测试或演示的最小闭环。

### 2. 范围与边界
- 包含：Run/Turn 状态机所需契约、状态转换、错误返回、日志事件和自动化验收。
- 不包含：未列入本阶段的真实 Provider、跨阶段优化或视觉润色；依赖能力只能通过 Port、Fake 或 fixture 接入。
- 上游依赖：S09-002。依赖未 verified 时，本功能只能处于 blocked。
- 主要产物：领域事件、查询投影、日志和验收结果。

### 3. 输入、输出与处理流程
输入来自上游命令、查询、事件、配置、用户动作或 fixture，必须具备稳定标识、版本和必要授权上下文。

处理顺序：首先校验格式、当前状态、权限和依赖版本；其次，围绕功能 [Run/Turn 状态机] 完成输入校验、领域处理、持久化或投影、用户反馈和可重复验收。；然后产生领域事件或标准结果并更新必要投影；最后返回成功、等待、失败或取消结果，并记录阶段进度。

输出为领域事件、查询投影、日志和验收结果。计划中的最小验收为：queued/running/waiting/completed/failed/interrupted 转换单测。

### 4. 状态、数据与一致性
- 状态变化必须遵守领域模型与事件规范，禁止直接修改 projection 绕过 domain command。
- SQLite Event Store，或本阶段明确声明的 Fake Store，是业务事实来源；日志只用于诊断。
- 外部副作用必须具有 operation_id、幂等策略、取消边界和恢复结论。
- 并发写入遵循单写者或 Write Claim；查询需要说明一致性级别和 cursor。

### 5. 异常、取消与恢复
必须设计输入无效、依赖不可用、权限拒绝、超时、用户取消、进程重启和重复提交路径。每类路径都要定义稳定 error_code、用户提示、是否可重试、是否产生事件和重启动作。不安全时进入明确的 blocked、failed 或 interrupted，不能静默成功。

### 6. 日志与安全要求
入口初始化日志，记录 stage.started、关键进度、stage.completed 或 stage.failed。日志采用非 JSON、非 key=value 的单行位置式格式：时间、级别、PID、任务链 UUID、线程或协程、logger、最后一级文件名和行号、主消息。不记录客户端地址；诊断上下文仅在 DEBUG 级别展示并置于主消息前，INFO 及以上隐藏。必须保持 Port、幂等、取消、恢复和审计边界。

### 7. 测试与可运行验收
- 必须提供 cargo 命令、CLI 子命令、集成测试或可重复 demo。
- 至少覆盖成功、失败、取消、重复调用以及适用的恢复或回滚路径。
- 日志断言任务链 UUID、稳定 message code、进度闭环、真实文件名和行号、无敏感信息泄漏。
- 回滚只能删除本阶段临时产物或恢复快照，不得删除用户数据或覆盖外部修改。

### 8. 后续代码注释要求
编码前必须通过本条目审核。实现代码需要为公共类型和函数、状态转换、协议字段、数据库迁移、异步任务边界、错误分支和安全判定添加中文详细注释。注释应解释设计原因、约束和失败恢复；复杂算法、并发或锁、取消传播、幂等键和安全边界还要写明前置条件、后置条件及不变量。测试代码需要说明 fixture 意图和验收理由。

### 9. 审核结论栏
- 需求理解：待审核
- 架构边界：待审核
- 接口和事件：待审核
- 异常与恢复：待审核
- 安全与日志：待审核
- 允许进入代码开发：否

<!-- END S09-003 -->
## S09-004：command_id 幂等

### 1. 功能目标
本功能属于 S09。建立 Project、Session、Run、Turn 的生命周期、幂等命令和重启恢复。 本项聚焦于领域功能、应用服务和可运行入口，必须形成可以独立启动、测试或演示的最小闭环。

### 2. 范围与边界
- 包含：command_id 幂等所需契约、状态转换、错误返回、日志事件和自动化验收。
- 不包含：未列入本阶段的真实 Provider、跨阶段优化或视觉润色；依赖能力只能通过 Port、Fake 或 fixture 接入。
- 上游依赖：S09-003。依赖未 verified 时，本功能只能处于 blocked。
- 主要产物：领域事件、查询投影、日志和验收结果。

### 3. 输入、输出与处理流程
输入来自上游命令、查询、事件、配置、用户动作或 fixture，必须具备稳定标识、版本和必要授权上下文。

处理顺序：首先校验格式、当前状态、权限和依赖版本；其次，以 command_id 建立唯一约束和结果缓存；重复命令返回原结果，不重复副作用。；然后产生领域事件或标准结果并更新必要投影；最后返回成功、等待、失败或取消结果，并记录阶段进度。

输出为领域事件、查询投影、日志和验收结果。计划中的最小验收为：重复提交不重复产生业务结果。

### 4. 状态、数据与一致性
- 状态变化必须遵守领域模型与事件规范，禁止直接修改 projection 绕过 domain command。
- SQLite Event Store，或本阶段明确声明的 Fake Store，是业务事实来源；日志只用于诊断。
- 外部副作用必须具有 operation_id、幂等策略、取消边界和恢复结论。
- 并发写入遵循单写者或 Write Claim；查询需要说明一致性级别和 cursor。

### 5. 异常、取消与恢复
必须设计输入无效、依赖不可用、权限拒绝、超时、用户取消、进程重启和重复提交路径。每类路径都要定义稳定 error_code、用户提示、是否可重试、是否产生事件和重启动作。不安全时进入明确的 blocked、failed 或 interrupted，不能静默成功。

### 6. 日志与安全要求
入口初始化日志，记录 stage.started、关键进度、stage.completed 或 stage.failed。日志采用非 JSON、非 key=value 的单行位置式格式：时间、级别、PID、任务链 UUID、线程或协程、logger、最后一级文件名和行号、主消息。不记录客户端地址；诊断上下文仅在 DEBUG 级别展示并置于主消息前，INFO 及以上隐藏。必须保持 Port、幂等、取消、恢复和审计边界。

### 7. 测试与可运行验收
- 必须提供 cargo 命令、CLI 子命令、集成测试或可重复 demo。
- 至少覆盖成功、失败、取消、重复调用以及适用的恢复或回滚路径。
- 日志断言任务链 UUID、稳定 message code、进度闭环、真实文件名和行号、无敏感信息泄漏。
- 回滚只能删除本阶段临时产物或恢复快照，不得删除用户数据或覆盖外部修改。

### 8. 后续代码注释要求
编码前必须通过本条目审核。实现代码需要为公共类型和函数、状态转换、协议字段、数据库迁移、异步任务边界、错误分支和安全判定添加中文详细注释。注释应解释设计原因、约束和失败恢复；复杂算法、并发或锁、取消传播、幂等键和安全边界还要写明前置条件、后置条件及不变量。测试代码需要说明 fixture 意图和验收理由。

### 9. 审核结论栏
- 需求理解：待审核
- 架构边界：待审核
- 接口和事件：待审核
- 异常与恢复：待审核
- 安全与日志：待审核
- 允许进入代码开发：否

<!-- END S09-004 -->
## S09-005：lifecycle projection

### 1. 功能目标
本功能属于 S09。建立 Project、Session、Run、Turn 的生命周期、幂等命令和重启恢复。 本项聚焦于事件存储、事务和可重建投影，必须形成可以独立启动、测试或演示的最小闭环。

### 2. 范围与边界
- 包含：lifecycle projection所需契约、状态转换、错误返回、日志事件和自动化验收。
- 不包含：未列入本阶段的真实 Provider、跨阶段优化或视觉润色；依赖能力只能通过 Port、Fake 或 fixture 接入。
- 上游依赖：S09-004。依赖未 verified 时，本功能只能处于 blocked。
- 主要产物：事件序列、投影版本、游标和恢复结果。

### 3. 输入、输出与处理流程
输入来自上游命令、查询、事件、配置、用户动作或 fixture，必须具备稳定标识、版本和必要授权上下文。

处理顺序：首先校验格式、当前状态、权限和依赖版本；其次，围绕功能 [lifecycle projection] 完成输入校验、领域处理、持久化或投影、用户反馈和可重复验收。；然后产生领域事件或标准结果并更新必要投影；最后返回成功、等待、失败或取消结果，并记录阶段进度。

输出为事件序列、投影版本、游标和恢复结果。计划中的最小验收为：重启后恢复 active run 和 cursor。

### 4. 状态、数据与一致性
- 状态变化必须遵守领域模型与事件规范，禁止直接修改 projection 绕过 domain command。
- SQLite Event Store，或本阶段明确声明的 Fake Store，是业务事实来源；日志只用于诊断。
- 外部副作用必须具有 operation_id、幂等策略、取消边界和恢复结论。
- 并发写入遵循单写者或 Write Claim；查询需要说明一致性级别和 cursor。

### 5. 异常、取消与恢复
必须设计输入无效、依赖不可用、权限拒绝、超时、用户取消、进程重启和重复提交路径。每类路径都要定义稳定 error_code、用户提示、是否可重试、是否产生事件和重启动作。不安全时进入明确的 blocked、failed 或 interrupted，不能静默成功。

### 6. 日志与安全要求
入口初始化日志，记录 stage.started、关键进度、stage.completed 或 stage.failed。日志采用非 JSON、非 key=value 的单行位置式格式：时间、级别、PID、任务链 UUID、线程或协程、logger、最后一级文件名和行号、主消息。不记录客户端地址；诊断上下文仅在 DEBUG 级别展示并置于主消息前，INFO 及以上隐藏。SQLite 事件是业务事实，事务不得产生半提交。

### 7. 测试与可运行验收
- 必须提供 cargo 命令、CLI 子命令、集成测试或可重复 demo。
- 至少覆盖成功、失败、取消、重复调用以及适用的恢复或回滚路径。
- 日志断言任务链 UUID、稳定 message code、进度闭环、真实文件名和行号、无敏感信息泄漏。
- 回滚只能删除本阶段临时产物或恢复快照，不得删除用户数据或覆盖外部修改。

### 8. 后续代码注释要求
编码前必须通过本条目审核。实现代码需要为公共类型和函数、状态转换、协议字段、数据库迁移、异步任务边界、错误分支和安全判定添加中文详细注释。注释应解释设计原因、约束和失败恢复；复杂算法、并发或锁、取消传播、幂等键和安全边界还要写明前置条件、后置条件及不变量。测试代码需要说明 fixture 意图和验收理由。

### 9. 审核结论栏
- 需求理解：待审核
- 架构边界：待审核
- 接口和事件：待审核
- 异常与恢复：待审核
- 安全与日志：待审核
- 允许进入代码开发：否

<!-- END S09-005 -->
## S09-006：`v0.1-project-session` demo

### 1. 功能目标
本功能属于 S09。建立 Project、Session、Run、Turn 的生命周期、幂等命令和重启恢复。 本项聚焦于领域功能、应用服务和可运行入口，必须形成可以独立启动、测试或演示的最小闭环。

### 2. 范围与边界
- 包含：`v0.1-project-session` demo所需契约、状态转换、错误返回、日志事件和自动化验收。
- 不包含：未列入本阶段的真实 Provider、跨阶段优化或视觉润色；依赖能力只能通过 Port、Fake 或 fixture 接入。
- 上游依赖：S09-005。依赖未 verified 时，本功能只能处于 blocked。
- 主要产物：领域事件、查询投影、日志和验收结果。

### 3. 输入、输出与处理流程
输入来自上游命令、查询、事件、配置、用户动作或 fixture，必须具备稳定标识、版本和必要授权上下文。

处理顺序：首先校验格式、当前状态、权限和依赖版本；其次，围绕功能 [`v0.1-project-session` demo] 完成输入校验、领域处理、持久化或投影、用户反馈和可重复验收。；然后产生领域事件或标准结果并更新必要投影；最后返回成功、等待、失败或取消结果，并记录阶段进度。

输出为领域事件、查询投影、日志和验收结果。计划中的最小验收为：创建项目→会话→Run→Turn→恢复。

### 4. 状态、数据与一致性
- 状态变化必须遵守领域模型与事件规范，禁止直接修改 projection 绕过 domain command。
- SQLite Event Store，或本阶段明确声明的 Fake Store，是业务事实来源；日志只用于诊断。
- 外部副作用必须具有 operation_id、幂等策略、取消边界和恢复结论。
- 并发写入遵循单写者或 Write Claim；查询需要说明一致性级别和 cursor。

### 5. 异常、取消与恢复
必须设计输入无效、依赖不可用、权限拒绝、超时、用户取消、进程重启和重复提交路径。每类路径都要定义稳定 error_code、用户提示、是否可重试、是否产生事件和重启动作。不安全时进入明确的 blocked、failed 或 interrupted，不能静默成功。

### 6. 日志与安全要求
入口初始化日志，记录 stage.started、关键进度、stage.completed 或 stage.failed。日志采用非 JSON、非 key=value 的单行位置式格式：时间、级别、PID、任务链 UUID、线程或协程、logger、最后一级文件名和行号、主消息。不记录客户端地址；诊断上下文仅在 DEBUG 级别展示并置于主消息前，INFO 及以上隐藏。必须保持 Port、幂等、取消、恢复和审计边界。

### 7. 测试与可运行验收
- 必须提供 cargo 命令、CLI 子命令、集成测试或可重复 demo。
- 至少覆盖成功、失败、取消、重复调用以及适用的恢复或回滚路径。
- 日志断言任务链 UUID、稳定 message code、进度闭环、真实文件名和行号、无敏感信息泄漏。
- 回滚只能删除本阶段临时产物或恢复快照，不得删除用户数据或覆盖外部修改。

### 8. 后续代码注释要求
编码前必须通过本条目审核。实现代码需要为公共类型和函数、状态转换、协议字段、数据库迁移、异步任务边界、错误分支和安全判定添加中文详细注释。注释应解释设计原因、约束和失败恢复；复杂算法、并发或锁、取消传播、幂等键和安全边界还要写明前置条件、后置条件及不变量。测试代码需要说明 fixture 意图和验收理由。

### 9. 审核结论栏
- 需求理解：待审核
- 架构边界：待审核
- 接口和事件：待审核
- 异常与恢复：待审核
- 安全与日志：待审核
- 允许进入代码开发：否

<!-- END S09-006 -->
## S10-001：`Provider` trait

### 1. 功能目标
本功能属于 S10。建立 Provider Port、配置、凭据引用、流式响应、错误映射、重试和取消。 本项聚焦于Provider Port、流式响应和上下文边界，必须形成可以独立启动、测试或演示的最小闭环。

### 2. 范围与边界
- 包含：`Provider` trait所需契约、状态转换、错误返回、日志事件和自动化验收。
- 不包含：未列入本阶段的真实 Provider、跨阶段优化或视觉润色；依赖能力只能通过 Port、Fake 或 fixture 接入。
- 上游依赖：S09。依赖未 verified 时，本功能只能处于 blocked。
- 主要产物：标准响应、usage、错误分类和可恢复状态。

### 3. 输入、输出与处理流程
输入来自上游命令、查询、事件、配置、用户动作或 fixture，必须具备稳定标识、版本和必要授权上下文。

处理顺序：首先校验格式、当前状态、权限和依赖版本；其次，围绕功能 [`Provider` trait] 完成输入校验、领域处理、持久化或投影、用户反馈和可重复验收。；然后产生领域事件或标准结果并更新必要投影；最后返回成功、等待、失败或取消结果，并记录阶段进度。

输出为标准响应、usage、错误分类和可恢复状态。计划中的最小验收为：FakeProvider 和测试 adapter 共用 contract。

### 4. 状态、数据与一致性
- 状态变化必须遵守领域模型与事件规范，禁止直接修改 projection 绕过 domain command。
- SQLite Event Store，或本阶段明确声明的 Fake Store，是业务事实来源；日志只用于诊断。
- 外部副作用必须具有 operation_id、幂等策略、取消边界和恢复结论。
- 并发写入遵循单写者或 Write Claim；查询需要说明一致性级别和 cursor。

### 5. 异常、取消与恢复
必须设计输入无效、依赖不可用、权限拒绝、超时、用户取消、进程重启和重复提交路径。每类路径都要定义稳定 error_code、用户提示、是否可重试、是否产生事件和重启动作。不安全时进入明确的 blocked、failed 或 interrupted，不能静默成功。

### 6. 日志与安全要求
入口初始化日志，记录 stage.started、关键进度、stage.completed 或 stage.failed。日志采用非 JSON、非 key=value 的单行位置式格式：时间、级别、PID、任务链 UUID、线程或协程、logger、最后一级文件名和行号、主消息。不记录客户端地址；诊断上下文仅在 DEBUG 级别展示并置于主消息前，INFO 及以上隐藏。不得记录 Prompt 原文、Provider 原文和凭据明文。

### 7. 测试与可运行验收
- 必须提供 cargo 命令、CLI 子命令、集成测试或可重复 demo。
- 至少覆盖成功、失败、取消、重复调用以及适用的恢复或回滚路径。
- 日志断言任务链 UUID、稳定 message code、进度闭环、真实文件名和行号、无敏感信息泄漏。
- 回滚只能删除本阶段临时产物或恢复快照，不得删除用户数据或覆盖外部修改。

### 8. 后续代码注释要求
编码前必须通过本条目审核。实现代码需要为公共类型和函数、状态转换、协议字段、数据库迁移、异步任务边界、错误分支和安全判定添加中文详细注释。注释应解释设计原因、约束和失败恢复；复杂算法、并发或锁、取消传播、幂等键和安全边界还要写明前置条件、后置条件及不变量。测试代码需要说明 fixture 意图和验收理由。

### 9. 审核结论栏
- 需求理解：待审核
- 架构边界：待审核
- 接口和事件：待审核
- 异常与恢复：待审核
- 安全与日志：待审核
- 允许进入代码开发：否

<!-- END S10-001 -->
## S10-002：provider config schema

### 1. 功能目标
本功能属于 S10。建立 Provider Port、配置、凭据引用、流式响应、错误映射、重试和取消。 本项聚焦于Provider Port、流式响应和上下文边界，必须形成可以独立启动、测试或演示的最小闭环。

### 2. 范围与边界
- 包含：provider config schema所需契约、状态转换、错误返回、日志事件和自动化验收。
- 不包含：未列入本阶段的真实 Provider、跨阶段优化或视觉润色；依赖能力只能通过 Port、Fake 或 fixture 接入。
- 上游依赖：S10-001。依赖未 verified 时，本功能只能处于 blocked。
- 主要产物：标准响应、usage、错误分类和可恢复状态。

### 3. 输入、输出与处理流程
输入来自上游命令、查询、事件、配置、用户动作或 fixture，必须具备稳定标识、版本和必要授权上下文。

处理顺序：首先校验格式、当前状态、权限和依赖版本；其次，围绕功能 [provider config schema] 完成输入校验、领域处理、持久化或投影、用户反馈和可重复验收。；然后产生领域事件或标准结果并更新必要投影；最后返回成功、等待、失败或取消结果，并记录阶段进度。

输出为标准响应、usage、错误分类和可恢复状态。计划中的最小验收为：model/base_url/timeout/retry 可验证。

### 4. 状态、数据与一致性
- 状态变化必须遵守领域模型与事件规范，禁止直接修改 projection 绕过 domain command。
- SQLite Event Store，或本阶段明确声明的 Fake Store，是业务事实来源；日志只用于诊断。
- 外部副作用必须具有 operation_id、幂等策略、取消边界和恢复结论。
- 并发写入遵循单写者或 Write Claim；查询需要说明一致性级别和 cursor。

### 5. 异常、取消与恢复
必须设计输入无效、依赖不可用、权限拒绝、超时、用户取消、进程重启和重复提交路径。每类路径都要定义稳定 error_code、用户提示、是否可重试、是否产生事件和重启动作。不安全时进入明确的 blocked、failed 或 interrupted，不能静默成功。

### 6. 日志与安全要求
入口初始化日志，记录 stage.started、关键进度、stage.completed 或 stage.failed。日志采用非 JSON、非 key=value 的单行位置式格式：时间、级别、PID、任务链 UUID、线程或协程、logger、最后一级文件名和行号、主消息。不记录客户端地址；诊断上下文仅在 DEBUG 级别展示并置于主消息前，INFO 及以上隐藏。不得记录 Prompt 原文、Provider 原文和凭据明文。

### 7. 测试与可运行验收
- 必须提供 cargo 命令、CLI 子命令、集成测试或可重复 demo。
- 至少覆盖成功、失败、取消、重复调用以及适用的恢复或回滚路径。
- 日志断言任务链 UUID、稳定 message code、进度闭环、真实文件名和行号、无敏感信息泄漏。
- 回滚只能删除本阶段临时产物或恢复快照，不得删除用户数据或覆盖外部修改。

### 8. 后续代码注释要求
编码前必须通过本条目审核。实现代码需要为公共类型和函数、状态转换、协议字段、数据库迁移、异步任务边界、错误分支和安全判定添加中文详细注释。注释应解释设计原因、约束和失败恢复；复杂算法、并发或锁、取消传播、幂等键和安全边界还要写明前置条件、后置条件及不变量。测试代码需要说明 fixture 意图和验收理由。

### 9. 审核结论栏
- 需求理解：待审核
- 架构边界：待审核
- 接口和事件：待审核
- 异常与恢复：待审核
- 安全与日志：待审核
- 允许进入代码开发：否

<!-- END S10-002 -->
## S10-003：credential metadata

### 1. 功能目标
本功能属于 S10。建立 Provider Port、配置、凭据引用、流式响应、错误映射、重试和取消。 本项聚焦于领域功能、应用服务和可运行入口，必须形成可以独立启动、测试或演示的最小闭环。

### 2. 范围与边界
- 包含：credential metadata所需契约、状态转换、错误返回、日志事件和自动化验收。
- 不包含：未列入本阶段的真实 Provider、跨阶段优化或视觉润色；依赖能力只能通过 Port、Fake 或 fixture 接入。
- 上游依赖：S10-002。依赖未 verified 时，本功能只能处于 blocked。
- 主要产物：领域事件、查询投影、日志和验收结果。

### 3. 输入、输出与处理流程
输入来自上游命令、查询、事件、配置、用户动作或 fixture，必须具备稳定标识、版本和必要授权上下文。

处理顺序：首先校验格式、当前状态、权限和依赖版本；其次，围绕功能 [credential metadata] 完成输入校验、领域处理、持久化或投影、用户反馈和可重复验收。；然后产生领域事件或标准结果并更新必要投影；最后返回成功、等待、失败或取消结果，并记录阶段进度。

输出为领域事件、查询投影、日志和验收结果。计划中的最小验收为：仅引用 credential_id，不读取原文到日志。

### 4. 状态、数据与一致性
- 状态变化必须遵守领域模型与事件规范，禁止直接修改 projection 绕过 domain command。
- SQLite Event Store，或本阶段明确声明的 Fake Store，是业务事实来源；日志只用于诊断。
- 外部副作用必须具有 operation_id、幂等策略、取消边界和恢复结论。
- 并发写入遵循单写者或 Write Claim；查询需要说明一致性级别和 cursor。

### 5. 异常、取消与恢复
必须设计输入无效、依赖不可用、权限拒绝、超时、用户取消、进程重启和重复提交路径。每类路径都要定义稳定 error_code、用户提示、是否可重试、是否产生事件和重启动作。不安全时进入明确的 blocked、failed 或 interrupted，不能静默成功。

### 6. 日志与安全要求
入口初始化日志，记录 stage.started、关键进度、stage.completed 或 stage.failed。日志采用非 JSON、非 key=value 的单行位置式格式：时间、级别、PID、任务链 UUID、线程或协程、logger、最后一级文件名和行号、主消息。不记录客户端地址；诊断上下文仅在 DEBUG 级别展示并置于主消息前，INFO 及以上隐藏。必须保持 Port、幂等、取消、恢复和审计边界。

### 7. 测试与可运行验收
- 必须提供 cargo 命令、CLI 子命令、集成测试或可重复 demo。
- 至少覆盖成功、失败、取消、重复调用以及适用的恢复或回滚路径。
- 日志断言任务链 UUID、稳定 message code、进度闭环、真实文件名和行号、无敏感信息泄漏。
- 回滚只能删除本阶段临时产物或恢复快照，不得删除用户数据或覆盖外部修改。

### 8. 后续代码注释要求
编码前必须通过本条目审核。实现代码需要为公共类型和函数、状态转换、协议字段、数据库迁移、异步任务边界、错误分支和安全判定添加中文详细注释。注释应解释设计原因、约束和失败恢复；复杂算法、并发或锁、取消传播、幂等键和安全边界还要写明前置条件、后置条件及不变量。测试代码需要说明 fixture 意图和验收理由。

### 9. 审核结论栏
- 需求理解：待审核
- 架构边界：待审核
- 接口和事件：待审核
- 异常与恢复：待审核
- 安全与日志：待审核
- 允许进入代码开发：否

<!-- END S10-003 -->
## S10-004：FakeProvider stream/error/timeout

### 1. 功能目标
本功能属于 S10。建立 Provider Port、配置、凭据引用、流式响应、错误映射、重试和取消。 本项聚焦于Provider Port、流式响应和上下文边界，必须形成可以独立启动、测试或演示的最小闭环。

### 2. 范围与边界
- 包含：FakeProvider stream/error/timeout所需契约、状态转换、错误返回、日志事件和自动化验收。
- 不包含：未列入本阶段的真实 Provider、跨阶段优化或视觉润色；依赖能力只能通过 Port、Fake 或 fixture 接入。
- 上游依赖：S10-003。依赖未 verified 时，本功能只能处于 blocked。
- 主要产物：标准响应、usage、错误分类和可恢复状态。

### 3. 输入、输出与处理流程
输入来自上游命令、查询、事件、配置、用户动作或 fixture，必须具备稳定标识、版本和必要授权上下文。

处理顺序：首先校验格式、当前状态、权限和依赖版本；其次，围绕功能 [FakeProvider stream/error/timeout] 完成输入校验、领域处理、持久化或投影、用户反馈和可重复验收。；然后产生领域事件或标准结果并更新必要投影；最后返回成功、等待、失败或取消结果，并记录阶段进度。

输出为标准响应、usage、错误分类和可恢复状态。计划中的最小验收为：fixture 可重复模拟。

### 4. 状态、数据与一致性
- 状态变化必须遵守领域模型与事件规范，禁止直接修改 projection 绕过 domain command。
- SQLite Event Store，或本阶段明确声明的 Fake Store，是业务事实来源；日志只用于诊断。
- 外部副作用必须具有 operation_id、幂等策略、取消边界和恢复结论。
- 并发写入遵循单写者或 Write Claim；查询需要说明一致性级别和 cursor。

### 5. 异常、取消与恢复
必须设计输入无效、依赖不可用、权限拒绝、超时、用户取消、进程重启和重复提交路径。每类路径都要定义稳定 error_code、用户提示、是否可重试、是否产生事件和重启动作。不安全时进入明确的 blocked、failed 或 interrupted，不能静默成功。

### 6. 日志与安全要求
入口初始化日志，记录 stage.started、关键进度、stage.completed 或 stage.failed。日志采用非 JSON、非 key=value 的单行位置式格式：时间、级别、PID、任务链 UUID、线程或协程、logger、最后一级文件名和行号、主消息。不记录客户端地址；诊断上下文仅在 DEBUG 级别展示并置于主消息前，INFO 及以上隐藏。不得记录 Prompt 原文、Provider 原文和凭据明文。

### 7. 测试与可运行验收
- 必须提供 cargo 命令、CLI 子命令、集成测试或可重复 demo。
- 至少覆盖成功、失败、取消、重复调用以及适用的恢复或回滚路径。
- 日志断言任务链 UUID、稳定 message code、进度闭环、真实文件名和行号、无敏感信息泄漏。
- 回滚只能删除本阶段临时产物或恢复快照，不得删除用户数据或覆盖外部修改。

### 8. 后续代码注释要求
编码前必须通过本条目审核。实现代码需要为公共类型和函数、状态转换、协议字段、数据库迁移、异步任务边界、错误分支和安全判定添加中文详细注释。注释应解释设计原因、约束和失败恢复；复杂算法、并发或锁、取消传播、幂等键和安全边界还要写明前置条件、后置条件及不变量。测试代码需要说明 fixture 意图和验收理由。

### 9. 审核结论栏
- 需求理解：待审核
- 架构边界：待审核
- 接口和事件：待审核
- 异常与恢复：待审核
- 安全与日志：待审核
- 允许进入代码开发：否

<!-- END S10-004 -->
## S10-005：OpenAI-compatible adapter

### 1. 功能目标
本功能属于 S10。建立 Provider Port、配置、凭据引用、流式响应、错误映射、重试和取消。 本项聚焦于领域功能、应用服务和可运行入口，必须形成可以独立启动、测试或演示的最小闭环。

### 2. 范围与边界
- 包含：OpenAI-compatible adapter所需契约、状态转换、错误返回、日志事件和自动化验收。
- 不包含：未列入本阶段的真实 Provider、跨阶段优化或视觉润色；依赖能力只能通过 Port、Fake 或 fixture 接入。
- 上游依赖：S10-004。依赖未 verified 时，本功能只能处于 blocked。
- 主要产物：领域事件、查询投影、日志和验收结果。

### 3. 输入、输出与处理流程
输入来自上游命令、查询、事件、配置、用户动作或 fixture，必须具备稳定标识、版本和必要授权上下文。

处理顺序：首先校验格式、当前状态、权限和依赖版本；其次，围绕功能 [OpenAI-compatible adapter] 完成输入校验、领域处理、持久化或投影、用户反馈和可重复验收。；然后产生领域事件或标准结果并更新必要投影；最后返回成功、等待、失败或取消结果，并记录阶段进度。

输出为领域事件、查询投影、日志和验收结果。计划中的最小验收为：真实请求、流式 delta、usage、错误映射。

### 4. 状态、数据与一致性
- 状态变化必须遵守领域模型与事件规范，禁止直接修改 projection 绕过 domain command。
- SQLite Event Store，或本阶段明确声明的 Fake Store，是业务事实来源；日志只用于诊断。
- 外部副作用必须具有 operation_id、幂等策略、取消边界和恢复结论。
- 并发写入遵循单写者或 Write Claim；查询需要说明一致性级别和 cursor。

### 5. 异常、取消与恢复
必须设计输入无效、依赖不可用、权限拒绝、超时、用户取消、进程重启和重复提交路径。每类路径都要定义稳定 error_code、用户提示、是否可重试、是否产生事件和重启动作。不安全时进入明确的 blocked、failed 或 interrupted，不能静默成功。

### 6. 日志与安全要求
入口初始化日志，记录 stage.started、关键进度、stage.completed 或 stage.failed。日志采用非 JSON、非 key=value 的单行位置式格式：时间、级别、PID、任务链 UUID、线程或协程、logger、最后一级文件名和行号、主消息。不记录客户端地址；诊断上下文仅在 DEBUG 级别展示并置于主消息前，INFO 及以上隐藏。必须保持 Port、幂等、取消、恢复和审计边界。

### 7. 测试与可运行验收
- 必须提供 cargo 命令、CLI 子命令、集成测试或可重复 demo。
- 至少覆盖成功、失败、取消、重复调用以及适用的恢复或回滚路径。
- 日志断言任务链 UUID、稳定 message code、进度闭环、真实文件名和行号、无敏感信息泄漏。
- 回滚只能删除本阶段临时产物或恢复快照，不得删除用户数据或覆盖外部修改。

### 8. 后续代码注释要求
编码前必须通过本条目审核。实现代码需要为公共类型和函数、状态转换、协议字段、数据库迁移、异步任务边界、错误分支和安全判定添加中文详细注释。注释应解释设计原因、约束和失败恢复；复杂算法、并发或锁、取消传播、幂等键和安全边界还要写明前置条件、后置条件及不变量。测试代码需要说明 fixture 意图和验收理由。

### 9. 审核结论栏
- 需求理解：待审核
- 架构边界：待审核
- 接口和事件：待审核
- 异常与恢复：待审核
- 安全与日志：待审核
- 允许进入代码开发：否

<!-- END S10-005 -->
## S10-006：第二 Provider adapter

### 1. 功能目标
本功能属于 S10。建立 Provider Port、配置、凭据引用、流式响应、错误映射、重试和取消。 本项聚焦于Provider Port、流式响应和上下文边界，必须形成可以独立启动、测试或演示的最小闭环。

### 2. 范围与边界
- 包含：第二 Provider adapter所需契约、状态转换、错误返回、日志事件和自动化验收。
- 不包含：未列入本阶段的真实 Provider、跨阶段优化或视觉润色；依赖能力只能通过 Port、Fake 或 fixture 接入。
- 上游依赖：S10-005。依赖未 verified 时，本功能只能处于 blocked。
- 主要产物：标准响应、usage、错误分类和可恢复状态。

### 3. 输入、输出与处理流程
输入来自上游命令、查询、事件、配置、用户动作或 fixture，必须具备稳定标识、版本和必要授权上下文。

处理顺序：首先校验格式、当前状态、权限和依赖版本；其次，围绕功能 [第二 Provider adapter] 完成输入校验、领域处理、持久化或投影、用户反馈和可重复验收。；然后产生领域事件或标准结果并更新必要投影；最后返回成功、等待、失败或取消结果，并记录阶段进度。

输出为标准响应、usage、错误分类和可恢复状态。计划中的最小验收为：切换不破坏历史和 session。

### 4. 状态、数据与一致性
- 状态变化必须遵守领域模型与事件规范，禁止直接修改 projection 绕过 domain command。
- SQLite Event Store，或本阶段明确声明的 Fake Store，是业务事实来源；日志只用于诊断。
- 外部副作用必须具有 operation_id、幂等策略、取消边界和恢复结论。
- 并发写入遵循单写者或 Write Claim；查询需要说明一致性级别和 cursor。

### 5. 异常、取消与恢复
必须设计输入无效、依赖不可用、权限拒绝、超时、用户取消、进程重启和重复提交路径。每类路径都要定义稳定 error_code、用户提示、是否可重试、是否产生事件和重启动作。不安全时进入明确的 blocked、failed 或 interrupted，不能静默成功。

### 6. 日志与安全要求
入口初始化日志，记录 stage.started、关键进度、stage.completed 或 stage.failed。日志采用非 JSON、非 key=value 的单行位置式格式：时间、级别、PID、任务链 UUID、线程或协程、logger、最后一级文件名和行号、主消息。不记录客户端地址；诊断上下文仅在 DEBUG 级别展示并置于主消息前，INFO 及以上隐藏。不得记录 Prompt 原文、Provider 原文和凭据明文。

### 7. 测试与可运行验收
- 必须提供 cargo 命令、CLI 子命令、集成测试或可重复 demo。
- 至少覆盖成功、失败、取消、重复调用以及适用的恢复或回滚路径。
- 日志断言任务链 UUID、稳定 message code、进度闭环、真实文件名和行号、无敏感信息泄漏。
- 回滚只能删除本阶段临时产物或恢复快照，不得删除用户数据或覆盖外部修改。

### 8. 后续代码注释要求
编码前必须通过本条目审核。实现代码需要为公共类型和函数、状态转换、协议字段、数据库迁移、异步任务边界、错误分支和安全判定添加中文详细注释。注释应解释设计原因、约束和失败恢复；复杂算法、并发或锁、取消传播、幂等键和安全边界还要写明前置条件、后置条件及不变量。测试代码需要说明 fixture 意图和验收理由。

### 9. 审核结论栏
- 需求理解：待审核
- 架构边界：待审核
- 接口和事件：待审核
- 异常与恢复：待审核
- 安全与日志：待审核
- 允许进入代码开发：否

<!-- END S10-006 -->
## S10-007：retry/backoff/cancel

### 1. 功能目标
本功能属于 S10。建立 Provider Port、配置、凭据引用、流式响应、错误映射、重试和取消。 本项聚焦于Provider Port、流式响应和上下文边界，必须形成可以独立启动、测试或演示的最小闭环。

### 2. 范围与边界
- 包含：retry/backoff/cancel所需契约、状态转换、错误返回、日志事件和自动化验收。
- 不包含：未列入本阶段的真实 Provider、跨阶段优化或视觉润色；依赖能力只能通过 Port、Fake 或 fixture 接入。
- 上游依赖：S10-006。依赖未 verified 时，本功能只能处于 blocked。
- 主要产物：标准响应、usage、错误分类和可恢复状态。

### 3. 输入、输出与处理流程
输入来自上游命令、查询、事件、配置、用户动作或 fixture，必须具备稳定标识、版本和必要授权上下文。

处理顺序：首先校验格式、当前状态、权限和依赖版本；其次，只对连接失败、429 和临时 5xx 等可重试错误退避；已执行工具和用户拒绝不得自动重试。；然后产生领域事件或标准结果并更新必要投影；最后返回成功、等待、失败或取消结果，并记录阶段进度。

输出为标准响应、usage、错误分类和可恢复状态。计划中的最小验收为：只重试 provider 请求，不重试已执行工具。

### 4. 状态、数据与一致性
- 状态变化必须遵守领域模型与事件规范，禁止直接修改 projection 绕过 domain command。
- SQLite Event Store，或本阶段明确声明的 Fake Store，是业务事实来源；日志只用于诊断。
- 外部副作用必须具有 operation_id、幂等策略、取消边界和恢复结论。
- 并发写入遵循单写者或 Write Claim；查询需要说明一致性级别和 cursor。

### 5. 异常、取消与恢复
必须设计输入无效、依赖不可用、权限拒绝、超时、用户取消、进程重启和重复提交路径。每类路径都要定义稳定 error_code、用户提示、是否可重试、是否产生事件和重启动作。不安全时进入明确的 blocked、failed 或 interrupted，不能静默成功。

### 6. 日志与安全要求
入口初始化日志，记录 stage.started、关键进度、stage.completed 或 stage.failed。日志采用非 JSON、非 key=value 的单行位置式格式：时间、级别、PID、任务链 UUID、线程或协程、logger、最后一级文件名和行号、主消息。不记录客户端地址；诊断上下文仅在 DEBUG 级别展示并置于主消息前，INFO 及以上隐藏。不得记录 Prompt 原文、Provider 原文和凭据明文。

### 7. 测试与可运行验收
- 必须提供 cargo 命令、CLI 子命令、集成测试或可重复 demo。
- 至少覆盖成功、失败、取消、重复调用以及适用的恢复或回滚路径。
- 日志断言任务链 UUID、稳定 message code、进度闭环、真实文件名和行号、无敏感信息泄漏。
- 回滚只能删除本阶段临时产物或恢复快照，不得删除用户数据或覆盖外部修改。

### 8. 后续代码注释要求
编码前必须通过本条目审核。实现代码需要为公共类型和函数、状态转换、协议字段、数据库迁移、异步任务边界、错误分支和安全判定添加中文详细注释。注释应解释设计原因、约束和失败恢复；复杂算法、并发或锁、取消传播、幂等键和安全边界还要写明前置条件、后置条件及不变量。测试代码需要说明 fixture 意图和验收理由。

### 9. 审核结论栏
- 需求理解：待审核
- 架构边界：待审核
- 接口和事件：待审核
- 异常与恢复：待审核
- 安全与日志：待审核
- 允许进入代码开发：否

<!-- END S10-007 -->
## S10-008：`v0.1-provider-alpha` demo

### 1. 功能目标
本功能属于 S10。建立 Provider Port、配置、凭据引用、流式响应、错误映射、重试和取消。 本项聚焦于Provider Port、流式响应和上下文边界，必须形成可以独立启动、测试或演示的最小闭环。

### 2. 范围与边界
- 包含：`v0.1-provider-alpha` demo所需契约、状态转换、错误返回、日志事件和自动化验收。
- 不包含：未列入本阶段的真实 Provider、跨阶段优化或视觉润色；依赖能力只能通过 Port、Fake 或 fixture 接入。
- 上游依赖：S10-007。依赖未 verified 时，本功能只能处于 blocked。
- 主要产物：标准响应、usage、错误分类和可恢复状态。

### 3. 输入、输出与处理流程
输入来自上游命令、查询、事件、配置、用户动作或 fixture，必须具备稳定标识、版本和必要授权上下文。

处理顺序：首先校验格式、当前状态、权限和依赖版本；其次，围绕功能 [`v0.1-provider-alpha` demo] 完成输入校验、领域处理、持久化或投影、用户反馈和可重复验收。；然后产生领域事件或标准结果并更新必要投影；最后返回成功、等待、失败或取消结果，并记录阶段进度。

输出为标准响应、usage、错误分类和可恢复状态。计划中的最小验收为：配置 provider 后完成一次真实或 fake turn。

### 4. 状态、数据与一致性
- 状态变化必须遵守领域模型与事件规范，禁止直接修改 projection 绕过 domain command。
- SQLite Event Store，或本阶段明确声明的 Fake Store，是业务事实来源；日志只用于诊断。
- 外部副作用必须具有 operation_id、幂等策略、取消边界和恢复结论。
- 并发写入遵循单写者或 Write Claim；查询需要说明一致性级别和 cursor。

### 5. 异常、取消与恢复
必须设计输入无效、依赖不可用、权限拒绝、超时、用户取消、进程重启和重复提交路径。每类路径都要定义稳定 error_code、用户提示、是否可重试、是否产生事件和重启动作。不安全时进入明确的 blocked、failed 或 interrupted，不能静默成功。

### 6. 日志与安全要求
入口初始化日志，记录 stage.started、关键进度、stage.completed 或 stage.failed。日志采用非 JSON、非 key=value 的单行位置式格式：时间、级别、PID、任务链 UUID、线程或协程、logger、最后一级文件名和行号、主消息。不记录客户端地址；诊断上下文仅在 DEBUG 级别展示并置于主消息前，INFO 及以上隐藏。不得记录 Prompt 原文、Provider 原文和凭据明文。

### 7. 测试与可运行验收
- 必须提供 cargo 命令、CLI 子命令、集成测试或可重复 demo。
- 至少覆盖成功、失败、取消、重复调用以及适用的恢复或回滚路径。
- 日志断言任务链 UUID、稳定 message code、进度闭环、真实文件名和行号、无敏感信息泄漏。
- 回滚只能删除本阶段临时产物或恢复快照，不得删除用户数据或覆盖外部修改。

### 8. 后续代码注释要求
编码前必须通过本条目审核。实现代码需要为公共类型和函数、状态转换、协议字段、数据库迁移、异步任务边界、错误分支和安全判定添加中文详细注释。注释应解释设计原因、约束和失败恢复；复杂算法、并发或锁、取消传播、幂等键和安全边界还要写明前置条件、后置条件及不变量。测试代码需要说明 fixture 意图和验收理由。

### 9. 审核结论栏
- 需求理解：待审核
- 架构边界：待审核
- 接口和事件：待审核
- 异常与恢复：待审核
- 安全与日志：待审核
- 允许进入代码开发：否

<!-- END S10-008 -->
## S11-001：RunContext

### 1. 功能目标
本功能属于 S11。建立单会话 Agent Loop 的上下文、Provider、工具续接、取消和恢复闭环。 本项聚焦于上下文条目、预算和 Checkpoint，必须形成可以独立启动、测试或演示的最小闭环。

### 2. 范围与边界
- 包含：RunContext所需契约、状态转换、错误返回、日志事件和自动化验收。
- 不包含：未列入本阶段的真实 Provider、跨阶段优化或视觉润色；依赖能力只能通过 Port、Fake 或 fixture 接入。
- 上游依赖：S09、S10。依赖未 verified 时，本功能只能处于 blocked。
- 主要产物：预算报告、摘要、版本和恢复上下文。

### 3. 输入、输出与处理流程
输入来自上游命令、查询、事件、配置、用户动作或 fixture，必须具备稳定标识、版本和必要授权上下文。

处理顺序：首先校验格式、当前状态、权限和依赖版本；其次，围绕功能 [RunContext] 完成输入校验、领域处理、持久化或投影、用户反馈和可重复验收。；然后产生领域事件或标准结果并更新必要投影；最后返回成功、等待、失败或取消结果，并记录阶段进度。

输出为预算报告、摘要、版本和恢复上下文。计划中的最小验收为：project/session/run/turn/operation/correlation 全量关联。

### 4. 状态、数据与一致性
- 状态变化必须遵守领域模型与事件规范，禁止直接修改 projection 绕过 domain command。
- SQLite Event Store，或本阶段明确声明的 Fake Store，是业务事实来源；日志只用于诊断。
- 外部副作用必须具有 operation_id、幂等策略、取消边界和恢复结论。
- 并发写入遵循单写者或 Write Claim；查询需要说明一致性级别和 cursor。

### 5. 异常、取消与恢复
必须设计输入无效、依赖不可用、权限拒绝、超时、用户取消、进程重启和重复提交路径。每类路径都要定义稳定 error_code、用户提示、是否可重试、是否产生事件和重启动作。不安全时进入明确的 blocked、failed 或 interrupted，不能静默成功。

### 6. 日志与安全要求
入口初始化日志，记录 stage.started、关键进度、stage.completed 或 stage.failed。日志采用非 JSON、非 key=value 的单行位置式格式：时间、级别、PID、任务链 UUID、线程或协程、logger、最后一级文件名和行号、主消息。不记录客户端地址；诊断上下文仅在 DEBUG 级别展示并置于主消息前，INFO 及以上隐藏。压缩不得丢失任务约束、工具事实和用户确认。

### 7. 测试与可运行验收
- 必须提供 cargo 命令、CLI 子命令、集成测试或可重复 demo。
- 至少覆盖成功、失败、取消、重复调用以及适用的恢复或回滚路径。
- 日志断言任务链 UUID、稳定 message code、进度闭环、真实文件名和行号、无敏感信息泄漏。
- 回滚只能删除本阶段临时产物或恢复快照，不得删除用户数据或覆盖外部修改。

### 8. 后续代码注释要求
编码前必须通过本条目审核。实现代码需要为公共类型和函数、状态转换、协议字段、数据库迁移、异步任务边界、错误分支和安全判定添加中文详细注释。注释应解释设计原因、约束和失败恢复；复杂算法、并发或锁、取消传播、幂等键和安全边界还要写明前置条件、后置条件及不变量。测试代码需要说明 fixture 意图和验收理由。

### 9. 审核结论栏
- 需求理解：待审核
- 架构边界：待审核
- 接口和事件：待审核
- 异常与恢复：待审核
- 安全与日志：待审核
- 允许进入代码开发：否

<!-- END S11-001 -->
## S11-002：prompt/context builder

### 1. 功能目标
本功能属于 S11。建立单会话 Agent Loop 的上下文、Provider、工具续接、取消和恢复闭环。 本项聚焦于Provider Port、流式响应和上下文边界，必须形成可以独立启动、测试或演示的最小闭环。

### 2. 范围与边界
- 包含：prompt/context builder所需契约、状态转换、错误返回、日志事件和自动化验收。
- 不包含：未列入本阶段的真实 Provider、跨阶段优化或视觉润色；依赖能力只能通过 Port、Fake 或 fixture 接入。
- 上游依赖：S11-001。依赖未 verified 时，本功能只能处于 blocked。
- 主要产物：标准响应、usage、错误分类和可恢复状态。

### 3. 输入、输出与处理流程
输入来自上游命令、查询、事件、配置、用户动作或 fixture，必须具备稳定标识、版本和必要授权上下文。

处理顺序：首先校验格式、当前状态、权限和依赖版本；其次，围绕功能 [prompt/context builder] 完成输入校验、领域处理、持久化或投影、用户反馈和可重复验收。；然后产生领域事件或标准结果并更新必要投影；最后返回成功、等待、失败或取消结果，并记录阶段进度。

输出为标准响应、usage、错误分类和可恢复状态。计划中的最小验收为：system、rules、spec、history、user input 分层。

### 4. 状态、数据与一致性
- 状态变化必须遵守领域模型与事件规范，禁止直接修改 projection 绕过 domain command。
- SQLite Event Store，或本阶段明确声明的 Fake Store，是业务事实来源；日志只用于诊断。
- 外部副作用必须具有 operation_id、幂等策略、取消边界和恢复结论。
- 并发写入遵循单写者或 Write Claim；查询需要说明一致性级别和 cursor。

### 5. 异常、取消与恢复
必须设计输入无效、依赖不可用、权限拒绝、超时、用户取消、进程重启和重复提交路径。每类路径都要定义稳定 error_code、用户提示、是否可重试、是否产生事件和重启动作。不安全时进入明确的 blocked、failed 或 interrupted，不能静默成功。

### 6. 日志与安全要求
入口初始化日志，记录 stage.started、关键进度、stage.completed 或 stage.failed。日志采用非 JSON、非 key=value 的单行位置式格式：时间、级别、PID、任务链 UUID、线程或协程、logger、最后一级文件名和行号、主消息。不记录客户端地址；诊断上下文仅在 DEBUG 级别展示并置于主消息前，INFO 及以上隐藏。不得记录 Prompt 原文、Provider 原文和凭据明文。

### 7. 测试与可运行验收
- 必须提供 cargo 命令、CLI 子命令、集成测试或可重复 demo。
- 至少覆盖成功、失败、取消、重复调用以及适用的恢复或回滚路径。
- 日志断言任务链 UUID、稳定 message code、进度闭环、真实文件名和行号、无敏感信息泄漏。
- 回滚只能删除本阶段临时产物或恢复快照，不得删除用户数据或覆盖外部修改。

### 8. 后续代码注释要求
编码前必须通过本条目审核。实现代码需要为公共类型和函数、状态转换、协议字段、数据库迁移、异步任务边界、错误分支和安全判定添加中文详细注释。注释应解释设计原因、约束和失败恢复；复杂算法、并发或锁、取消传播、幂等键和安全边界还要写明前置条件、后置条件及不变量。测试代码需要说明 fixture 意图和验收理由。

### 9. 审核结论栏
- 需求理解：待审核
- 架构边界：待审核
- 接口和事件：待审核
- 异常与恢复：待审核
- 安全与日志：待审核
- 允许进入代码开发：否

<!-- END S11-002 -->
## S11-003：provider response parser

### 1. 功能目标
本功能属于 S11。建立单会话 Agent Loop 的上下文、Provider、工具续接、取消和恢复闭环。 本项聚焦于输入事件、纯状态转换和终端渲染，必须形成可以独立启动、测试或演示的最小闭环。

### 2. 范围与边界
- 包含：provider response parser所需契约、状态转换、错误返回、日志事件和自动化验收。
- 不包含：未列入本阶段的真实 Provider、跨阶段优化或视觉润色；依赖能力只能通过 Port、Fake 或 fixture 接入。
- 上游依赖：S11-002。依赖未 verified 时，本功能只能处于 blocked。
- 主要产物：AppState、Effect、渲染帧和用户提示。

### 3. 输入、输出与处理流程
输入来自上游命令、查询、事件、配置、用户动作或 fixture，必须具备稳定标识、版本和必要授权上下文。

处理顺序：首先校验格式、当前状态、权限和依赖版本；其次，围绕功能 [provider response parser] 完成输入校验、领域处理、持久化或投影、用户反馈和可重复验收。；然后产生领域事件或标准结果并更新必要投影；最后返回成功、等待、失败或取消结果，并记录阶段进度。

输出为AppState、Effect、渲染帧和用户提示。计划中的最小验收为：text/tool_call/finish/usage/error 分流。

### 4. 状态、数据与一致性
- 状态变化必须遵守领域模型与事件规范，禁止直接修改 projection 绕过 domain command。
- SQLite Event Store，或本阶段明确声明的 Fake Store，是业务事实来源；日志只用于诊断。
- 外部副作用必须具有 operation_id、幂等策略、取消边界和恢复结论。
- 并发写入遵循单写者或 Write Claim；查询需要说明一致性级别和 cursor。

### 5. 异常、取消与恢复
必须设计输入无效、依赖不可用、权限拒绝、超时、用户取消、进程重启和重复提交路径。每类路径都要定义稳定 error_code、用户提示、是否可重试、是否产生事件和重启动作。不安全时进入明确的 blocked、failed 或 interrupted，不能静默成功。

### 6. 日志与安全要求
入口初始化日志，记录 stage.started、关键进度、stage.completed 或 stage.failed。日志采用非 JSON、非 key=value 的单行位置式格式：时间、级别、PID、任务链 UUID、线程或协程、logger、最后一级文件名和行号、主消息。不记录客户端地址；诊断上下文仅在 DEBUG 级别展示并置于主消息前，INFO 及以上隐藏。渲染层不得产生业务副作用，窄终端和 Unicode 不得越界。

### 7. 测试与可运行验收
- 必须提供 cargo 命令、CLI 子命令、集成测试或可重复 demo。
- 至少覆盖成功、失败、取消、重复调用以及适用的恢复或回滚路径。
- 日志断言任务链 UUID、稳定 message code、进度闭环、真实文件名和行号、无敏感信息泄漏。
- 回滚只能删除本阶段临时产物或恢复快照，不得删除用户数据或覆盖外部修改。

### 8. 后续代码注释要求
编码前必须通过本条目审核。实现代码需要为公共类型和函数、状态转换、协议字段、数据库迁移、异步任务边界、错误分支和安全判定添加中文详细注释。注释应解释设计原因、约束和失败恢复；复杂算法、并发或锁、取消传播、幂等键和安全边界还要写明前置条件、后置条件及不变量。测试代码需要说明 fixture 意图和验收理由。

### 9. 审核结论栏
- 需求理解：待审核
- 架构边界：待审核
- 接口和事件：待审核
- 异常与恢复：待审核
- 安全与日志：待审核
- 允许进入代码开发：否

<!-- END S11-003 -->
## S11-004：linear Agent Loop

### 1. 功能目标
本功能属于 S11。建立单会话 Agent Loop 的上下文、Provider、工具续接、取消和恢复闭环。 本项聚焦于领域功能、应用服务和可运行入口，必须形成可以独立启动、测试或演示的最小闭环。

### 2. 范围与边界
- 包含：linear Agent Loop所需契约、状态转换、错误返回、日志事件和自动化验收。
- 不包含：未列入本阶段的真实 Provider、跨阶段优化或视觉润色；依赖能力只能通过 Port、Fake 或 fixture 接入。
- 上游依赖：S11-003。依赖未 verified 时，本功能只能处于 blocked。
- 主要产物：领域事件、查询投影、日志和验收结果。

### 3. 输入、输出与处理流程
输入来自上游命令、查询、事件、配置、用户动作或 fixture，必须具备稳定标识、版本和必要授权上下文。

处理顺序：首先校验格式、当前状态、权限和依赖版本；其次，围绕功能 [linear Agent Loop] 完成输入校验、领域处理、持久化或投影、用户反馈和可重复验收。；然后产生领域事件或标准结果并更新必要投影；最后返回成功、等待、失败或取消结果，并记录阶段进度。

输出为领域事件、查询投影、日志和验收结果。计划中的最小验收为：user→provider→assistant 可完成。

### 4. 状态、数据与一致性
- 状态变化必须遵守领域模型与事件规范，禁止直接修改 projection 绕过 domain command。
- SQLite Event Store，或本阶段明确声明的 Fake Store，是业务事实来源；日志只用于诊断。
- 外部副作用必须具有 operation_id、幂等策略、取消边界和恢复结论。
- 并发写入遵循单写者或 Write Claim；查询需要说明一致性级别和 cursor。

### 5. 异常、取消与恢复
必须设计输入无效、依赖不可用、权限拒绝、超时、用户取消、进程重启和重复提交路径。每类路径都要定义稳定 error_code、用户提示、是否可重试、是否产生事件和重启动作。不安全时进入明确的 blocked、failed 或 interrupted，不能静默成功。

### 6. 日志与安全要求
入口初始化日志，记录 stage.started、关键进度、stage.completed 或 stage.failed。日志采用非 JSON、非 key=value 的单行位置式格式：时间、级别、PID、任务链 UUID、线程或协程、logger、最后一级文件名和行号、主消息。不记录客户端地址；诊断上下文仅在 DEBUG 级别展示并置于主消息前，INFO 及以上隐藏。必须保持 Port、幂等、取消、恢复和审计边界。

### 7. 测试与可运行验收
- 必须提供 cargo 命令、CLI 子命令、集成测试或可重复 demo。
- 至少覆盖成功、失败、取消、重复调用以及适用的恢复或回滚路径。
- 日志断言任务链 UUID、稳定 message code、进度闭环、真实文件名和行号、无敏感信息泄漏。
- 回滚只能删除本阶段临时产物或恢复快照，不得删除用户数据或覆盖外部修改。

### 8. 后续代码注释要求
编码前必须通过本条目审核。实现代码需要为公共类型和函数、状态转换、协议字段、数据库迁移、异步任务边界、错误分支和安全判定添加中文详细注释。注释应解释设计原因、约束和失败恢复；复杂算法、并发或锁、取消传播、幂等键和安全边界还要写明前置条件、后置条件及不变量。测试代码需要说明 fixture 意图和验收理由。

### 9. 审核结论栏
- 需求理解：待审核
- 架构边界：待审核
- 接口和事件：待审核
- 异常与恢复：待审核
- 安全与日志：待审核
- 允许进入代码开发：否

<!-- END S11-004 -->
## S11-005：tool-call continuation

### 1. 功能目标
本功能属于 S11。建立单会话 Agent Loop 的上下文、Provider、工具续接、取消和恢复闭环。 本项聚焦于Tool Gateway、工具契约和副作用隔离，必须形成可以独立启动、测试或演示的最小闭环。

### 2. 范围与边界
- 包含：tool-call continuation所需契约、状态转换、错误返回、日志事件和自动化验收。
- 不包含：未列入本阶段的真实 Provider、跨阶段优化或视觉润色；依赖能力只能通过 Port、Fake 或 fixture 接入。
- 上游依赖：S11-004。依赖未 verified 时，本功能只能处于 blocked。
- 主要产物：ToolCall、标准结果、变更和快照摘要。

### 3. 输入、输出与处理流程
输入来自上游命令、查询、事件、配置、用户动作或 fixture，必须具备稳定标识、版本和必要授权上下文。

处理顺序：首先校验格式、当前状态、权限和依赖版本；其次，保存 tool_call 后进入审批，执行工具并写入 tool result，再以同一 operation_id 继续推理。；然后产生领域事件或标准结果并更新必要投影；最后返回成功、等待、失败或取消结果，并记录阶段进度。

输出为ToolCall、标准结果、变更和快照摘要。计划中的最小验收为：provider tool call→approval/tool result→继续推理。

### 4. 状态、数据与一致性
- 状态变化必须遵守领域模型与事件规范，禁止直接修改 projection 绕过 domain command。
- SQLite Event Store，或本阶段明确声明的 Fake Store，是业务事实来源；日志只用于诊断。
- 外部副作用必须具有 operation_id、幂等策略、取消边界和恢复结论。
- 并发写入遵循单写者或 Write Claim；查询需要说明一致性级别和 cursor。

### 5. 异常、取消与恢复
必须设计输入无效、依赖不可用、权限拒绝、超时、用户取消、进程重启和重复提交路径。每类路径都要定义稳定 error_code、用户提示、是否可重试、是否产生事件和重启动作。不安全时进入明确的 blocked、failed 或 interrupted，不能静默成功。

### 6. 日志与安全要求
入口初始化日志，记录 stage.started、关键进度、stage.completed 或 stage.failed。日志采用非 JSON、非 key=value 的单行位置式格式：时间、级别、PID、任务链 UUID、线程或协程、logger、最后一级文件名和行号、主消息。不记录客户端地址；诊断上下文仅在 DEBUG 级别展示并置于主消息前，INFO 及以上隐藏。工具不得绕过 Gateway、权限、取消、超时和日志门禁。

### 7. 测试与可运行验收
- 必须提供 cargo 命令、CLI 子命令、集成测试或可重复 demo。
- 至少覆盖成功、失败、取消、重复调用以及适用的恢复或回滚路径。
- 日志断言任务链 UUID、稳定 message code、进度闭环、真实文件名和行号、无敏感信息泄漏。
- 回滚只能删除本阶段临时产物或恢复快照，不得删除用户数据或覆盖外部修改。

### 8. 后续代码注释要求
编码前必须通过本条目审核。实现代码需要为公共类型和函数、状态转换、协议字段、数据库迁移、异步任务边界、错误分支和安全判定添加中文详细注释。注释应解释设计原因、约束和失败恢复；复杂算法、并发或锁、取消传播、幂等键和安全边界还要写明前置条件、后置条件及不变量。测试代码需要说明 fixture 意图和验收理由。

### 9. 审核结论栏
- 需求理解：待审核
- 架构边界：待审核
- 接口和事件：待审核
- 异常与恢复：待审核
- 安全与日志：待审核
- 允许进入代码开发：否

<!-- END S11-005 -->
## S11-006：cancel/timeout/failure

### 1. 功能目标
本功能属于 S11。建立单会话 Agent Loop 的上下文、Provider、工具续接、取消和恢复闭环。 本项聚焦于领域功能、应用服务和可运行入口，必须形成可以独立启动、测试或演示的最小闭环。

### 2. 范围与边界
- 包含：cancel/timeout/failure所需契约、状态转换、错误返回、日志事件和自动化验收。
- 不包含：未列入本阶段的真实 Provider、跨阶段优化或视觉润色；依赖能力只能通过 Port、Fake 或 fixture 接入。
- 上游依赖：S11-005。依赖未 verified 时，本功能只能处于 blocked。
- 主要产物：领域事件、查询投影、日志和验收结果。

### 3. 输入、输出与处理流程
输入来自上游命令、查询、事件、配置、用户动作或 fixture，必须具备稳定标识、版本和必要授权上下文。

处理顺序：首先校验格式、当前状态、权限和依赖版本；其次，围绕功能 [cancel/timeout/failure] 完成输入校验、领域处理、持久化或投影、用户反馈和可重复验收。；然后产生领域事件或标准结果并更新必要投影；最后返回成功、等待、失败或取消结果，并记录阶段进度。

输出为领域事件、查询投影、日志和验收结果。计划中的最小验收为：任意 turn 可中断并保存最终状态。

### 4. 状态、数据与一致性
- 状态变化必须遵守领域模型与事件规范，禁止直接修改 projection 绕过 domain command。
- SQLite Event Store，或本阶段明确声明的 Fake Store，是业务事实来源；日志只用于诊断。
- 外部副作用必须具有 operation_id、幂等策略、取消边界和恢复结论。
- 并发写入遵循单写者或 Write Claim；查询需要说明一致性级别和 cursor。

### 5. 异常、取消与恢复
必须设计输入无效、依赖不可用、权限拒绝、超时、用户取消、进程重启和重复提交路径。每类路径都要定义稳定 error_code、用户提示、是否可重试、是否产生事件和重启动作。不安全时进入明确的 blocked、failed 或 interrupted，不能静默成功。

### 6. 日志与安全要求
入口初始化日志，记录 stage.started、关键进度、stage.completed 或 stage.failed。日志采用非 JSON、非 key=value 的单行位置式格式：时间、级别、PID、任务链 UUID、线程或协程、logger、最后一级文件名和行号、主消息。不记录客户端地址；诊断上下文仅在 DEBUG 级别展示并置于主消息前，INFO 及以上隐藏。必须保持 Port、幂等、取消、恢复和审计边界。

### 7. 测试与可运行验收
- 必须提供 cargo 命令、CLI 子命令、集成测试或可重复 demo。
- 至少覆盖成功、失败、取消、重复调用以及适用的恢复或回滚路径。
- 日志断言任务链 UUID、稳定 message code、进度闭环、真实文件名和行号、无敏感信息泄漏。
- 回滚只能删除本阶段临时产物或恢复快照，不得删除用户数据或覆盖外部修改。

### 8. 后续代码注释要求
编码前必须通过本条目审核。实现代码需要为公共类型和函数、状态转换、协议字段、数据库迁移、异步任务边界、错误分支和安全判定添加中文详细注释。注释应解释设计原因、约束和失败恢复；复杂算法、并发或锁、取消传播、幂等键和安全边界还要写明前置条件、后置条件及不变量。测试代码需要说明 fixture 意图和验收理由。

### 9. 审核结论栏
- 需求理解：待审核
- 架构边界：待审核
- 接口和事件：待审核
- 异常与恢复：待审核
- 安全与日志：待审核
- 允许进入代码开发：否

<!-- END S11-006 -->
## S11-007：restart/recover run

### 1. 功能目标
本功能属于 S11。建立单会话 Agent Loop 的上下文、Provider、工具续接、取消和恢复闭环。 本项聚焦于发布集成、故障演练和运维生命周期，必须形成可以独立启动、测试或演示的最小闭环。

### 2. 范围与边界
- 包含：restart/recover run所需契约、状态转换、错误返回、日志事件和自动化验收。
- 不包含：未列入本阶段的真实 Provider、跨阶段优化或视觉润色；依赖能力只能通过 Port、Fake 或 fixture 接入。
- 上游依赖：S11-006。依赖未 verified 时，本功能只能处于 blocked。
- 主要产物：报告、制品、manifest、digest 和恢复证明。

### 3. 输入、输出与处理流程
输入来自上游命令、查询、事件、配置、用户动作或 fixture，必须具备稳定标识、版本和必要授权上下文。

处理顺序：首先校验格式、当前状态、权限和依赖版本；其次，围绕功能 [restart/recover run] 完成输入校验、领域处理、持久化或投影、用户反馈和可重复验收。；然后产生领域事件或标准结果并更新必要投影；最后返回成功、等待、失败或取消结果，并记录阶段进度。

输出为报告、制品、manifest、digest 和恢复证明。计划中的最小验收为：Core 重启后不重复已完成 operation。

### 4. 状态、数据与一致性
- 状态变化必须遵守领域模型与事件规范，禁止直接修改 projection 绕过 domain command。
- SQLite Event Store，或本阶段明确声明的 Fake Store，是业务事实来源；日志只用于诊断。
- 外部副作用必须具有 operation_id、幂等策略、取消边界和恢复结论。
- 并发写入遵循单写者或 Write Claim；查询需要说明一致性级别和 cursor。

### 5. 异常、取消与恢复
必须设计输入无效、依赖不可用、权限拒绝、超时、用户取消、进程重启和重复提交路径。每类路径都要定义稳定 error_code、用户提示、是否可重试、是否产生事件和重启动作。不安全时进入明确的 blocked、failed 或 interrupted，不能静默成功。

### 6. 日志与安全要求
入口初始化日志，记录 stage.started、关键进度、stage.completed 或 stage.failed。日志采用非 JSON、非 key=value 的单行位置式格式：时间、级别、PID、任务链 UUID、线程或协程、logger、最后一级文件名和行号、主消息。不记录客户端地址；诊断上下文仅在 DEBUG 级别展示并置于主消息前，INFO 及以上隐藏。失败必须可定位、可重试、可回滚且不破坏用户数据。

### 7. 测试与可运行验收
- 必须提供 cargo 命令、CLI 子命令、集成测试或可重复 demo。
- 至少覆盖成功、失败、取消、重复调用以及适用的恢复或回滚路径。
- 日志断言任务链 UUID、稳定 message code、进度闭环、真实文件名和行号、无敏感信息泄漏。
- 回滚只能删除本阶段临时产物或恢复快照，不得删除用户数据或覆盖外部修改。

### 8. 后续代码注释要求
编码前必须通过本条目审核。实现代码需要为公共类型和函数、状态转换、协议字段、数据库迁移、异步任务边界、错误分支和安全判定添加中文详细注释。注释应解释设计原因、约束和失败恢复；复杂算法、并发或锁、取消传播、幂等键和安全边界还要写明前置条件、后置条件及不变量。测试代码需要说明 fixture 意图和验收理由。

### 9. 审核结论栏
- 需求理解：待审核
- 架构边界：待审核
- 接口和事件：待审核
- 异常与恢复：待审核
- 安全与日志：待审核
- 允许进入代码开发：否

<!-- END S11-007 -->
## S11-008：`v0.1-agent-alpha` demo

### 1. 功能目标
本功能属于 S11。建立单会话 Agent Loop 的上下文、Provider、工具续接、取消和恢复闭环。 本项聚焦于领域功能、应用服务和可运行入口，必须形成可以独立启动、测试或演示的最小闭环。

### 2. 范围与边界
- 包含：`v0.1-agent-alpha` demo所需契约、状态转换、错误返回、日志事件和自动化验收。
- 不包含：未列入本阶段的真实 Provider、跨阶段优化或视觉润色；依赖能力只能通过 Port、Fake 或 fixture 接入。
- 上游依赖：S11-007。依赖未 verified 时，本功能只能处于 blocked。
- 主要产物：领域事件、查询投影、日志和验收结果。

### 3. 输入、输出与处理流程
输入来自上游命令、查询、事件、配置、用户动作或 fixture，必须具备稳定标识、版本和必要授权上下文。

处理顺序：首先校验格式、当前状态、权限和依赖版本；其次，围绕功能 [`v0.1-agent-alpha` demo] 完成输入校验、领域处理、持久化或投影、用户反馈和可重复验收。；然后产生领域事件或标准结果并更新必要投影；最后返回成功、等待、失败或取消结果，并记录阶段进度。

输出为领域事件、查询投影、日志和验收结果。计划中的最小验收为：一次 Agent turn 全链路可查询。

### 4. 状态、数据与一致性
- 状态变化必须遵守领域模型与事件规范，禁止直接修改 projection 绕过 domain command。
- SQLite Event Store，或本阶段明确声明的 Fake Store，是业务事实来源；日志只用于诊断。
- 外部副作用必须具有 operation_id、幂等策略、取消边界和恢复结论。
- 并发写入遵循单写者或 Write Claim；查询需要说明一致性级别和 cursor。

### 5. 异常、取消与恢复
必须设计输入无效、依赖不可用、权限拒绝、超时、用户取消、进程重启和重复提交路径。每类路径都要定义稳定 error_code、用户提示、是否可重试、是否产生事件和重启动作。不安全时进入明确的 blocked、failed 或 interrupted，不能静默成功。

### 6. 日志与安全要求
入口初始化日志，记录 stage.started、关键进度、stage.completed 或 stage.failed。日志采用非 JSON、非 key=value 的单行位置式格式：时间、级别、PID、任务链 UUID、线程或协程、logger、最后一级文件名和行号、主消息。不记录客户端地址；诊断上下文仅在 DEBUG 级别展示并置于主消息前，INFO 及以上隐藏。必须保持 Port、幂等、取消、恢复和审计边界。

### 7. 测试与可运行验收
- 必须提供 cargo 命令、CLI 子命令、集成测试或可重复 demo。
- 至少覆盖成功、失败、取消、重复调用以及适用的恢复或回滚路径。
- 日志断言任务链 UUID、稳定 message code、进度闭环、真实文件名和行号、无敏感信息泄漏。
- 回滚只能删除本阶段临时产物或恢复快照，不得删除用户数据或覆盖外部修改。

### 8. 后续代码注释要求
编码前必须通过本条目审核。实现代码需要为公共类型和函数、状态转换、协议字段、数据库迁移、异步任务边界、错误分支和安全判定添加中文详细注释。注释应解释设计原因、约束和失败恢复；复杂算法、并发或锁、取消传播、幂等键和安全边界还要写明前置条件、后置条件及不变量。测试代码需要说明 fixture 意图和验收理由。

### 9. 审核结论栏
- 需求理解：待审核
- 架构边界：待审核
- 接口和事件：待审核
- 异常与恢复：待审核
- 安全与日志：待审核
- 允许进入代码开发：否

<!-- END S11-008 -->
## S12-001：PermissionRule schema

### 1. 功能目标
本功能属于 S12。建立路径和命令分类、权限策略、审批生命周期及高优先级控制通道。 本项聚焦于安全策略、授权上下文和审批生命周期，必须形成可以独立启动、测试或演示的最小闭环。

### 2. 范围与边界
- 包含：PermissionRule schema所需契约、状态转换、错误返回、日志事件和自动化验收。
- 不包含：未列入本阶段的真实 Provider、跨阶段优化或视觉润色；依赖能力只能通过 Port、Fake 或 fixture 接入。
- 上游依赖：S11。依赖未 verified 时，本功能只能处于 blocked。
- 主要产物：决策、理由、审批事件和安全报告。

### 3. 输入、输出与处理流程
输入来自上游命令、查询、事件、配置、用户动作或 fixture，必须具备稳定标识、版本和必要授权上下文。

处理顺序：首先校验格式、当前状态、权限和依赖版本；其次，围绕功能 [PermissionRule schema] 完成输入校验、领域处理、持久化或投影、用户反馈和可重复验收。；然后产生领域事件或标准结果并更新必要投影；最后返回成功、等待、失败或取消结果，并记录阶段进度。

输出为决策、理由、审批事件和安全报告。计划中的最小验收为：allow/deny/ask/always/never 可解析。

### 4. 状态、数据与一致性
- 状态变化必须遵守领域模型与事件规范，禁止直接修改 projection 绕过 domain command。
- SQLite Event Store，或本阶段明确声明的 Fake Store，是业务事实来源；日志只用于诊断。
- 外部副作用必须具有 operation_id、幂等策略、取消边界和恢复结论。
- 并发写入遵循单写者或 Write Claim；查询需要说明一致性级别和 cursor。

### 5. 异常、取消与恢复
必须设计输入无效、依赖不可用、权限拒绝、超时、用户取消、进程重启和重复提交路径。每类路径都要定义稳定 error_code、用户提示、是否可重试、是否产生事件和重启动作。不安全时进入明确的 blocked、failed 或 interrupted，不能静默成功。

### 6. 日志与安全要求
入口初始化日志，记录 stage.started、关键进度、stage.completed 或 stage.failed。日志采用非 JSON、非 key=value 的单行位置式格式：时间、级别、PID、任务链 UUID、线程或协程、logger、最后一级文件名和行号、主消息。不记录客户端地址；诊断上下文仅在 DEBUG 级别展示并置于主消息前，INFO 及以上隐藏。高风险操作默认拒绝，任何放行必须可审计。

### 7. 测试与可运行验收
- 必须提供 cargo 命令、CLI 子命令、集成测试或可重复 demo。
- 至少覆盖成功、失败、取消、重复调用以及适用的恢复或回滚路径。
- 日志断言任务链 UUID、稳定 message code、进度闭环、真实文件名和行号、无敏感信息泄漏。
- 回滚只能删除本阶段临时产物或恢复快照，不得删除用户数据或覆盖外部修改。

### 8. 后续代码注释要求
编码前必须通过本条目审核。实现代码需要为公共类型和函数、状态转换、协议字段、数据库迁移、异步任务边界、错误分支和安全判定添加中文详细注释。注释应解释设计原因、约束和失败恢复；复杂算法、并发或锁、取消传播、幂等键和安全边界还要写明前置条件、后置条件及不变量。测试代码需要说明 fixture 意图和验收理由。

### 9. 审核结论栏
- 需求理解：待审核
- 架构边界：待审核
- 接口和事件：待审核
- 异常与恢复：待审核
- 安全与日志：待审核
- 允许进入代码开发：否

<!-- END S12-001 -->
## S12-002：path classification

### 1. 功能目标
本功能属于 S12。建立路径和命令分类、权限策略、审批生命周期及高优先级控制通道。 本项聚焦于安全策略、授权上下文和审批生命周期，必须形成可以独立启动、测试或演示的最小闭环。

### 2. 范围与边界
- 包含：path classification所需契约、状态转换、错误返回、日志事件和自动化验收。
- 不包含：未列入本阶段的真实 Provider、跨阶段优化或视觉润色；依赖能力只能通过 Port、Fake 或 fixture 接入。
- 上游依赖：S12-001。依赖未 verified 时，本功能只能处于 blocked。
- 主要产物：决策、理由、审批事件和安全报告。

### 3. 输入、输出与处理流程
输入来自上游命令、查询、事件、配置、用户动作或 fixture，必须具备稳定标识、版本和必要授权上下文。

处理顺序：首先校验格式、当前状态、权限和依赖版本；其次，围绕功能 [path classification] 完成输入校验、领域处理、持久化或投影、用户反馈和可重复验收。；然后产生领域事件或标准结果并更新必要投影；最后返回成功、等待、失败或取消结果，并记录阶段进度。

输出为决策、理由、审批事件和安全报告。计划中的最小验收为：workspace-relative/outside/sensitive 正确分类。

### 4. 状态、数据与一致性
- 状态变化必须遵守领域模型与事件规范，禁止直接修改 projection 绕过 domain command。
- SQLite Event Store，或本阶段明确声明的 Fake Store，是业务事实来源；日志只用于诊断。
- 外部副作用必须具有 operation_id、幂等策略、取消边界和恢复结论。
- 并发写入遵循单写者或 Write Claim；查询需要说明一致性级别和 cursor。

### 5. 异常、取消与恢复
必须设计输入无效、依赖不可用、权限拒绝、超时、用户取消、进程重启和重复提交路径。每类路径都要定义稳定 error_code、用户提示、是否可重试、是否产生事件和重启动作。不安全时进入明确的 blocked、failed 或 interrupted，不能静默成功。

### 6. 日志与安全要求
入口初始化日志，记录 stage.started、关键进度、stage.completed 或 stage.failed。日志采用非 JSON、非 key=value 的单行位置式格式：时间、级别、PID、任务链 UUID、线程或协程、logger、最后一级文件名和行号、主消息。不记录客户端地址；诊断上下文仅在 DEBUG 级别展示并置于主消息前，INFO 及以上隐藏。高风险操作默认拒绝，任何放行必须可审计。

### 7. 测试与可运行验收
- 必须提供 cargo 命令、CLI 子命令、集成测试或可重复 demo。
- 至少覆盖成功、失败、取消、重复调用以及适用的恢复或回滚路径。
- 日志断言任务链 UUID、稳定 message code、进度闭环、真实文件名和行号、无敏感信息泄漏。
- 回滚只能删除本阶段临时产物或恢复快照，不得删除用户数据或覆盖外部修改。

### 8. 后续代码注释要求
编码前必须通过本条目审核。实现代码需要为公共类型和函数、状态转换、协议字段、数据库迁移、异步任务边界、错误分支和安全判定添加中文详细注释。注释应解释设计原因、约束和失败恢复；复杂算法、并发或锁、取消传播、幂等键和安全边界还要写明前置条件、后置条件及不变量。测试代码需要说明 fixture 意图和验收理由。

### 9. 审核结论栏
- 需求理解：待审核
- 架构边界：待审核
- 接口和事件：待审核
- 异常与恢复：待审核
- 安全与日志：待审核
- 允许进入代码开发：否

<!-- END S12-002 -->
## S12-003：Bash AST command classification

### 1. 功能目标
本功能属于 S12。建立路径和命令分类、权限策略、审批生命周期及高优先级控制通道。 本项聚焦于安全策略、授权上下文和审批生命周期，必须形成可以独立启动、测试或演示的最小闭环。

### 2. 范围与边界
- 包含：Bash AST command classification所需契约、状态转换、错误返回、日志事件和自动化验收。
- 不包含：未列入本阶段的真实 Provider、跨阶段优化或视觉润色；依赖能力只能通过 Port、Fake 或 fixture 接入。
- 上游依赖：S12-002。依赖未 verified 时，本功能只能处于 blocked。
- 主要产物：决策、理由、审批事件和安全报告。

### 3. 输入、输出与处理流程
输入来自上游命令、查询、事件、配置、用户动作或 fixture，必须具备稳定标识、版本和必要授权上下文。

处理顺序：首先校验格式、当前状态、权限和依赖版本；其次，围绕功能 [Bash AST command classification] 完成输入校验、领域处理、持久化或投影、用户反馈和可重复验收。；然后产生领域事件或标准结果并更新必要投影；最后返回成功、等待、失败或取消结果，并记录阶段进度。

输出为决策、理由、审批事件和安全报告。计划中的最小验收为：`rm -rf`、网络、重定向、管道等分类。

### 4. 状态、数据与一致性
- 状态变化必须遵守领域模型与事件规范，禁止直接修改 projection 绕过 domain command。
- SQLite Event Store，或本阶段明确声明的 Fake Store，是业务事实来源；日志只用于诊断。
- 外部副作用必须具有 operation_id、幂等策略、取消边界和恢复结论。
- 并发写入遵循单写者或 Write Claim；查询需要说明一致性级别和 cursor。

### 5. 异常、取消与恢复
必须设计输入无效、依赖不可用、权限拒绝、超时、用户取消、进程重启和重复提交路径。每类路径都要定义稳定 error_code、用户提示、是否可重试、是否产生事件和重启动作。不安全时进入明确的 blocked、failed 或 interrupted，不能静默成功。

### 6. 日志与安全要求
入口初始化日志，记录 stage.started、关键进度、stage.completed 或 stage.failed。日志采用非 JSON、非 key=value 的单行位置式格式：时间、级别、PID、任务链 UUID、线程或协程、logger、最后一级文件名和行号、主消息。不记录客户端地址；诊断上下文仅在 DEBUG 级别展示并置于主消息前，INFO 及以上隐藏。高风险操作默认拒绝，任何放行必须可审计。

### 7. 测试与可运行验收
- 必须提供 cargo 命令、CLI 子命令、集成测试或可重复 demo。
- 至少覆盖成功、失败、取消、重复调用以及适用的恢复或回滚路径。
- 日志断言任务链 UUID、稳定 message code、进度闭环、真实文件名和行号、无敏感信息泄漏。
- 回滚只能删除本阶段临时产物或恢复快照，不得删除用户数据或覆盖外部修改。

### 8. 后续代码注释要求
编码前必须通过本条目审核。实现代码需要为公共类型和函数、状态转换、协议字段、数据库迁移、异步任务边界、错误分支和安全判定添加中文详细注释。注释应解释设计原因、约束和失败恢复；复杂算法、并发或锁、取消传播、幂等键和安全边界还要写明前置条件、后置条件及不变量。测试代码需要说明 fixture 意图和验收理由。

### 9. 审核结论栏
- 需求理解：待审核
- 架构边界：待审核
- 接口和事件：待审核
- 异常与恢复：待审核
- 安全与日志：待审核
- 允许进入代码开发：否

<!-- END S12-003 -->
## S12-004：policy evaluator

### 1. 功能目标
本功能属于 S12。建立路径和命令分类、权限策略、审批生命周期及高优先级控制通道。 本项聚焦于安全策略、授权上下文和审批生命周期，必须形成可以独立启动、测试或演示的最小闭环。

### 2. 范围与边界
- 包含：policy evaluator所需契约、状态转换、错误返回、日志事件和自动化验收。
- 不包含：未列入本阶段的真实 Provider、跨阶段优化或视觉润色；依赖能力只能通过 Port、Fake 或 fixture 接入。
- 上游依赖：S12-003。依赖未 verified 时，本功能只能处于 blocked。
- 主要产物：决策、理由、审批事件和安全报告。

### 3. 输入、输出与处理流程
输入来自上游命令、查询、事件、配置、用户动作或 fixture，必须具备稳定标识、版本和必要授权上下文。

处理顺序：首先校验格式、当前状态、权限和依赖版本；其次，把路径类别、命令类别、项目可信度、规则版本和用户模式输入纯策略求值器。；然后产生领域事件或标准结果并更新必要投影；最后返回成功、等待、失败或取消结果，并记录阶段进度。

输出为决策、理由、审批事件和安全报告。计划中的最小验收为：同样输入得到确定 decision。

### 4. 状态、数据与一致性
- 状态变化必须遵守领域模型与事件规范，禁止直接修改 projection 绕过 domain command。
- SQLite Event Store，或本阶段明确声明的 Fake Store，是业务事实来源；日志只用于诊断。
- 外部副作用必须具有 operation_id、幂等策略、取消边界和恢复结论。
- 并发写入遵循单写者或 Write Claim；查询需要说明一致性级别和 cursor。

### 5. 异常、取消与恢复
必须设计输入无效、依赖不可用、权限拒绝、超时、用户取消、进程重启和重复提交路径。每类路径都要定义稳定 error_code、用户提示、是否可重试、是否产生事件和重启动作。不安全时进入明确的 blocked、failed 或 interrupted，不能静默成功。

### 6. 日志与安全要求
入口初始化日志，记录 stage.started、关键进度、stage.completed 或 stage.failed。日志采用非 JSON、非 key=value 的单行位置式格式：时间、级别、PID、任务链 UUID、线程或协程、logger、最后一级文件名和行号、主消息。不记录客户端地址；诊断上下文仅在 DEBUG 级别展示并置于主消息前，INFO 及以上隐藏。高风险操作默认拒绝，任何放行必须可审计。

### 7. 测试与可运行验收
- 必须提供 cargo 命令、CLI 子命令、集成测试或可重复 demo。
- 至少覆盖成功、失败、取消、重复调用以及适用的恢复或回滚路径。
- 日志断言任务链 UUID、稳定 message code、进度闭环、真实文件名和行号、无敏感信息泄漏。
- 回滚只能删除本阶段临时产物或恢复快照，不得删除用户数据或覆盖外部修改。

### 8. 后续代码注释要求
编码前必须通过本条目审核。实现代码需要为公共类型和函数、状态转换、协议字段、数据库迁移、异步任务边界、错误分支和安全判定添加中文详细注释。注释应解释设计原因、约束和失败恢复；复杂算法、并发或锁、取消传播、幂等键和安全边界还要写明前置条件、后置条件及不变量。测试代码需要说明 fixture 意图和验收理由。

### 9. 审核结论栏
- 需求理解：待审核
- 架构边界：待审核
- 接口和事件：待审核
- 异常与恢复：待审核
- 安全与日志：待审核
- 允许进入代码开发：否

<!-- END S12-004 -->
## S12-005：Approval lifecycle

### 1. 功能目标
本功能属于 S12。建立路径和命令分类、权限策略、审批生命周期及高优先级控制通道。 本项聚焦于安全策略、授权上下文和审批生命周期，必须形成可以独立启动、测试或演示的最小闭环。

### 2. 范围与边界
- 包含：Approval lifecycle所需契约、状态转换、错误返回、日志事件和自动化验收。
- 不包含：未列入本阶段的真实 Provider、跨阶段优化或视觉润色；依赖能力只能通过 Port、Fake 或 fixture 接入。
- 上游依赖：S12-004。依赖未 verified 时，本功能只能处于 blocked。
- 主要产物：决策、理由、审批事件和安全报告。

### 3. 输入、输出与处理流程
输入来自上游命令、查询、事件、配置、用户动作或 fixture，必须具备稳定标识、版本和必要授权上下文。

处理顺序：首先校验格式、当前状态、权限和依赖版本；其次，围绕功能 [Approval lifecycle] 完成输入校验、领域处理、持久化或投影、用户反馈和可重复验收。；然后产生领域事件或标准结果并更新必要投影；最后返回成功、等待、失败或取消结果，并记录阶段进度。

输出为决策、理由、审批事件和安全报告。计划中的最小验收为：create/approve/deny/expire/revoke 均产生事件。

### 4. 状态、数据与一致性
- 状态变化必须遵守领域模型与事件规范，禁止直接修改 projection 绕过 domain command。
- SQLite Event Store，或本阶段明确声明的 Fake Store，是业务事实来源；日志只用于诊断。
- 外部副作用必须具有 operation_id、幂等策略、取消边界和恢复结论。
- 并发写入遵循单写者或 Write Claim；查询需要说明一致性级别和 cursor。

### 5. 异常、取消与恢复
必须设计输入无效、依赖不可用、权限拒绝、超时、用户取消、进程重启和重复提交路径。每类路径都要定义稳定 error_code、用户提示、是否可重试、是否产生事件和重启动作。不安全时进入明确的 blocked、failed 或 interrupted，不能静默成功。

### 6. 日志与安全要求
入口初始化日志，记录 stage.started、关键进度、stage.completed 或 stage.failed。日志采用非 JSON、非 key=value 的单行位置式格式：时间、级别、PID、任务链 UUID、线程或协程、logger、最后一级文件名和行号、主消息。不记录客户端地址；诊断上下文仅在 DEBUG 级别展示并置于主消息前，INFO 及以上隐藏。高风险操作默认拒绝，任何放行必须可审计。

### 7. 测试与可运行验收
- 必须提供 cargo 命令、CLI 子命令、集成测试或可重复 demo。
- 至少覆盖成功、失败、取消、重复调用以及适用的恢复或回滚路径。
- 日志断言任务链 UUID、稳定 message code、进度闭环、真实文件名和行号、无敏感信息泄漏。
- 回滚只能删除本阶段临时产物或恢复快照，不得删除用户数据或覆盖外部修改。

### 8. 后续代码注释要求
编码前必须通过本条目审核。实现代码需要为公共类型和函数、状态转换、协议字段、数据库迁移、异步任务边界、错误分支和安全判定添加中文详细注释。注释应解释设计原因、约束和失败恢复；复杂算法、并发或锁、取消传播、幂等键和安全边界还要写明前置条件、后置条件及不变量。测试代码需要说明 fixture 意图和验收理由。

### 9. 审核结论栏
- 需求理解：待审核
- 架构边界：待审核
- 接口和事件：待审核
- 异常与恢复：待审核
- 安全与日志：待审核
- 允许进入代码开发：否

<!-- END S12-005 -->
## S12-006：priority control lane

### 1. 功能目标
本功能属于 S12。建立路径和命令分类、权限策略、审批生命周期及高优先级控制通道。 本项聚焦于领域功能、应用服务和可运行入口，必须形成可以独立启动、测试或演示的最小闭环。

### 2. 范围与边界
- 包含：priority control lane所需契约、状态转换、错误返回、日志事件和自动化验收。
- 不包含：未列入本阶段的真实 Provider、跨阶段优化或视觉润色；依赖能力只能通过 Port、Fake 或 fixture 接入。
- 上游依赖：S12-005。依赖未 verified 时，本功能只能处于 blocked。
- 主要产物：领域事件、查询投影、日志和验收结果。

### 3. 输入、输出与处理流程
输入来自上游命令、查询、事件、配置、用户动作或 fixture，必须具备稳定标识、版本和必要授权上下文。

处理顺序：首先校验格式、当前状态、权限和依赖版本；其次，围绕功能 [priority control lane] 完成输入校验、领域处理、持久化或投影、用户反馈和可重复验收。；然后产生领域事件或标准结果并更新必要投影；最后返回成功、等待、失败或取消结果，并记录阶段进度。

输出为领域事件、查询投影、日志和验收结果。计划中的最小验收为：cancel/security/approval 不被普通 delta 阻塞。

### 4. 状态、数据与一致性
- 状态变化必须遵守领域模型与事件规范，禁止直接修改 projection 绕过 domain command。
- SQLite Event Store，或本阶段明确声明的 Fake Store，是业务事实来源；日志只用于诊断。
- 外部副作用必须具有 operation_id、幂等策略、取消边界和恢复结论。
- 并发写入遵循单写者或 Write Claim；查询需要说明一致性级别和 cursor。

### 5. 异常、取消与恢复
必须设计输入无效、依赖不可用、权限拒绝、超时、用户取消、进程重启和重复提交路径。每类路径都要定义稳定 error_code、用户提示、是否可重试、是否产生事件和重启动作。不安全时进入明确的 blocked、failed 或 interrupted，不能静默成功。

### 6. 日志与安全要求
入口初始化日志，记录 stage.started、关键进度、stage.completed 或 stage.failed。日志采用非 JSON、非 key=value 的单行位置式格式：时间、级别、PID、任务链 UUID、线程或协程、logger、最后一级文件名和行号、主消息。不记录客户端地址；诊断上下文仅在 DEBUG 级别展示并置于主消息前，INFO 及以上隐藏。必须保持 Port、幂等、取消、恢复和审计边界。

### 7. 测试与可运行验收
- 必须提供 cargo 命令、CLI 子命令、集成测试或可重复 demo。
- 至少覆盖成功、失败、取消、重复调用以及适用的恢复或回滚路径。
- 日志断言任务链 UUID、稳定 message code、进度闭环、真实文件名和行号、无敏感信息泄漏。
- 回滚只能删除本阶段临时产物或恢复快照，不得删除用户数据或覆盖外部修改。

### 8. 后续代码注释要求
编码前必须通过本条目审核。实现代码需要为公共类型和函数、状态转换、协议字段、数据库迁移、异步任务边界、错误分支和安全判定添加中文详细注释。注释应解释设计原因、约束和失败恢复；复杂算法、并发或锁、取消传播、幂等键和安全边界还要写明前置条件、后置条件及不变量。测试代码需要说明 fixture 意图和验收理由。

### 9. 审核结论栏
- 需求理解：待审核
- 架构边界：待审核
- 接口和事件：待审核
- 异常与恢复：待审核
- 安全与日志：待审核
- 允许进入代码开发：否

<!-- END S12-006 -->
## S12-007：denial/approval TUI

### 1. 功能目标
本功能属于 S12。建立路径和命令分类、权限策略、审批生命周期及高优先级控制通道。 本项聚焦于输入事件、纯状态转换和终端渲染，必须形成可以独立启动、测试或演示的最小闭环。

### 2. 范围与边界
- 包含：denial/approval TUI所需契约、状态转换、错误返回、日志事件和自动化验收。
- 不包含：未列入本阶段的真实 Provider、跨阶段优化或视觉润色；依赖能力只能通过 Port、Fake 或 fixture 接入。
- 上游依赖：S12-006。依赖未 verified 时，本功能只能处于 blocked。
- 主要产物：AppState、Effect、渲染帧和用户提示。

### 3. 输入、输出与处理流程
输入来自上游命令、查询、事件、配置、用户动作或 fixture，必须具备稳定标识、版本和必要授权上下文。

处理顺序：首先校验格式、当前状态、权限和依赖版本；其次，围绕功能 [denial/approval TUI] 完成输入校验、领域处理、持久化或投影、用户反馈和可重复验收。；然后产生领域事件或标准结果并更新必要投影；最后返回成功、等待、失败或取消结果，并记录阶段进度。

输出为AppState、Effect、渲染帧和用户提示。计划中的最小验收为：TUI 可显示理由和下一步动作。

### 4. 状态、数据与一致性
- 状态变化必须遵守领域模型与事件规范，禁止直接修改 projection 绕过 domain command。
- SQLite Event Store，或本阶段明确声明的 Fake Store，是业务事实来源；日志只用于诊断。
- 外部副作用必须具有 operation_id、幂等策略、取消边界和恢复结论。
- 并发写入遵循单写者或 Write Claim；查询需要说明一致性级别和 cursor。

### 5. 异常、取消与恢复
必须设计输入无效、依赖不可用、权限拒绝、超时、用户取消、进程重启和重复提交路径。每类路径都要定义稳定 error_code、用户提示、是否可重试、是否产生事件和重启动作。不安全时进入明确的 blocked、failed 或 interrupted，不能静默成功。

### 6. 日志与安全要求
入口初始化日志，记录 stage.started、关键进度、stage.completed 或 stage.failed。日志采用非 JSON、非 key=value 的单行位置式格式：时间、级别、PID、任务链 UUID、线程或协程、logger、最后一级文件名和行号、主消息。不记录客户端地址；诊断上下文仅在 DEBUG 级别展示并置于主消息前，INFO 及以上隐藏。渲染层不得产生业务副作用，窄终端和 Unicode 不得越界。

### 7. 测试与可运行验收
- 必须提供 cargo 命令、CLI 子命令、集成测试或可重复 demo。
- 至少覆盖成功、失败、取消、重复调用以及适用的恢复或回滚路径。
- 日志断言任务链 UUID、稳定 message code、进度闭环、真实文件名和行号、无敏感信息泄漏。
- 回滚只能删除本阶段临时产物或恢复快照，不得删除用户数据或覆盖外部修改。

### 8. 后续代码注释要求
编码前必须通过本条目审核。实现代码需要为公共类型和函数、状态转换、协议字段、数据库迁移、异步任务边界、错误分支和安全判定添加中文详细注释。注释应解释设计原因、约束和失败恢复；复杂算法、并发或锁、取消传播、幂等键和安全边界还要写明前置条件、后置条件及不变量。测试代码需要说明 fixture 意图和验收理由。

### 9. 审核结论栏
- 需求理解：待审核
- 架构边界：待审核
- 接口和事件：待审核
- 异常与恢复：待审核
- 安全与日志：待审核
- 允许进入代码开发：否

<!-- END S12-007 -->
## S12-008：`v0.1-permission-beta` demo

### 1. 功能目标
本功能属于 S12。建立路径和命令分类、权限策略、审批生命周期及高优先级控制通道。 本项聚焦于安全策略、授权上下文和审批生命周期，必须形成可以独立启动、测试或演示的最小闭环。

### 2. 范围与边界
- 包含：`v0.1-permission-beta` demo所需契约、状态转换、错误返回、日志事件和自动化验收。
- 不包含：未列入本阶段的真实 Provider、跨阶段优化或视觉润色；依赖能力只能通过 Port、Fake 或 fixture 接入。
- 上游依赖：S12-007。依赖未 verified 时，本功能只能处于 blocked。
- 主要产物：决策、理由、审批事件和安全报告。

### 3. 输入、输出与处理流程
输入来自上游命令、查询、事件、配置、用户动作或 fixture，必须具备稳定标识、版本和必要授权上下文。

处理顺序：首先校验格式、当前状态、权限和依赖版本；其次，围绕功能 [`v0.1-permission-beta` demo] 完成输入校验、领域处理、持久化或投影、用户反馈和可重复验收。；然后产生领域事件或标准结果并更新必要投影；最后返回成功、等待、失败或取消结果，并记录阶段进度。

输出为决策、理由、审批事件和安全报告。计划中的最小验收为：安全命令被阻断，用户批准后才继续。

### 4. 状态、数据与一致性
- 状态变化必须遵守领域模型与事件规范，禁止直接修改 projection 绕过 domain command。
- SQLite Event Store，或本阶段明确声明的 Fake Store，是业务事实来源；日志只用于诊断。
- 外部副作用必须具有 operation_id、幂等策略、取消边界和恢复结论。
- 并发写入遵循单写者或 Write Claim；查询需要说明一致性级别和 cursor。

### 5. 异常、取消与恢复
必须设计输入无效、依赖不可用、权限拒绝、超时、用户取消、进程重启和重复提交路径。每类路径都要定义稳定 error_code、用户提示、是否可重试、是否产生事件和重启动作。不安全时进入明确的 blocked、failed 或 interrupted，不能静默成功。

### 6. 日志与安全要求
入口初始化日志，记录 stage.started、关键进度、stage.completed 或 stage.failed。日志采用非 JSON、非 key=value 的单行位置式格式：时间、级别、PID、任务链 UUID、线程或协程、logger、最后一级文件名和行号、主消息。不记录客户端地址；诊断上下文仅在 DEBUG 级别展示并置于主消息前，INFO 及以上隐藏。高风险操作默认拒绝，任何放行必须可审计。

### 7. 测试与可运行验收
- 必须提供 cargo 命令、CLI 子命令、集成测试或可重复 demo。
- 至少覆盖成功、失败、取消、重复调用以及适用的恢复或回滚路径。
- 日志断言任务链 UUID、稳定 message code、进度闭环、真实文件名和行号、无敏感信息泄漏。
- 回滚只能删除本阶段临时产物或恢复快照，不得删除用户数据或覆盖外部修改。

### 8. 后续代码注释要求
编码前必须通过本条目审核。实现代码需要为公共类型和函数、状态转换、协议字段、数据库迁移、异步任务边界、错误分支和安全判定添加中文详细注释。注释应解释设计原因、约束和失败恢复；复杂算法、并发或锁、取消传播、幂等键和安全边界还要写明前置条件、后置条件及不变量。测试代码需要说明 fixture 意图和验收理由。

### 9. 审核结论栏
- 需求理解：待审核
- 架构边界：待审核
- 接口和事件：待审核
- 异常与恢复：待审核
- 安全与日志：待审核
- 允许进入代码开发：否

<!-- END S12-008 -->
## S13-001：ToolDefinition/operation_id

### 1. 功能目标
本功能属于 S13。建立 Read、Glob、Grep 只读工具的契约、边界、裁剪、取消和结果标准化。 本项聚焦于Tool Gateway、工具契约和副作用隔离，必须形成可以独立启动、测试或演示的最小闭环。

### 2. 范围与边界
- 包含：ToolDefinition/operation_id所需契约、状态转换、错误返回、日志事件和自动化验收。
- 不包含：未列入本阶段的真实 Provider、跨阶段优化或视觉润色；依赖能力只能通过 Port、Fake 或 fixture 接入。
- 上游依赖：S12。依赖未 verified 时，本功能只能处于 blocked。
- 主要产物：ToolCall、标准结果、变更和快照摘要。

### 3. 输入、输出与处理流程
输入来自上游命令、查询、事件、配置、用户动作或 fixture，必须具备稳定标识、版本和必要授权上下文。

处理顺序：首先校验格式、当前状态、权限和依赖版本；其次，围绕功能 [ToolDefinition/operation_id] 完成输入校验、领域处理、持久化或投影、用户反馈和可重复验收。；然后产生领域事件或标准结果并更新必要投影；最后返回成功、等待、失败或取消结果，并记录阶段进度。

输出为ToolCall、标准结果、变更和快照摘要。计划中的最小验收为：schema、版本、超时、取消字段可序列化。

### 4. 状态、数据与一致性
- 状态变化必须遵守领域模型与事件规范，禁止直接修改 projection 绕过 domain command。
- SQLite Event Store，或本阶段明确声明的 Fake Store，是业务事实来源；日志只用于诊断。
- 外部副作用必须具有 operation_id、幂等策略、取消边界和恢复结论。
- 并发写入遵循单写者或 Write Claim；查询需要说明一致性级别和 cursor。

### 5. 异常、取消与恢复
必须设计输入无效、依赖不可用、权限拒绝、超时、用户取消、进程重启和重复提交路径。每类路径都要定义稳定 error_code、用户提示、是否可重试、是否产生事件和重启动作。不安全时进入明确的 blocked、failed 或 interrupted，不能静默成功。

### 6. 日志与安全要求
入口初始化日志，记录 stage.started、关键进度、stage.completed 或 stage.failed。日志采用非 JSON、非 key=value 的单行位置式格式：时间、级别、PID、任务链 UUID、线程或协程、logger、最后一级文件名和行号、主消息。不记录客户端地址；诊断上下文仅在 DEBUG 级别展示并置于主消息前，INFO 及以上隐藏。工具不得绕过 Gateway、权限、取消、超时和日志门禁。

### 7. 测试与可运行验收
- 必须提供 cargo 命令、CLI 子命令、集成测试或可重复 demo。
- 至少覆盖成功、失败、取消、重复调用以及适用的恢复或回滚路径。
- 日志断言任务链 UUID、稳定 message code、进度闭环、真实文件名和行号、无敏感信息泄漏。
- 回滚只能删除本阶段临时产物或恢复快照，不得删除用户数据或覆盖外部修改。

### 8. 后续代码注释要求
编码前必须通过本条目审核。实现代码需要为公共类型和函数、状态转换、协议字段、数据库迁移、异步任务边界、错误分支和安全判定添加中文详细注释。注释应解释设计原因、约束和失败恢复；复杂算法、并发或锁、取消传播、幂等键和安全边界还要写明前置条件、后置条件及不变量。测试代码需要说明 fixture 意图和验收理由。

### 9. 审核结论栏
- 需求理解：待审核
- 架构边界：待审核
- 接口和事件：待审核
- 异常与恢复：待审核
- 安全与日志：待审核
- 允许进入代码开发：否

<!-- END S13-001 -->
## S13-002：Read tool

### 1. 功能目标
本功能属于 S13。建立 Read、Glob、Grep 只读工具的契约、边界、裁剪、取消和结果标准化。 本项聚焦于Tool Gateway、工具契约和副作用隔离，必须形成可以独立启动、测试或演示的最小闭环。

### 2. 范围与边界
- 包含：Read tool所需契约、状态转换、错误返回、日志事件和自动化验收。
- 不包含：未列入本阶段的真实 Provider、跨阶段优化或视觉润色；依赖能力只能通过 Port、Fake 或 fixture 接入。
- 上游依赖：S13-001。依赖未 verified 时，本功能只能处于 blocked。
- 主要产物：ToolCall、标准结果、变更和快照摘要。

### 3. 输入、输出与处理流程
输入来自上游命令、查询、事件、配置、用户动作或 fixture，必须具备稳定标识、版本和必要授权上下文。

处理顺序：首先校验格式、当前状态、权限和依赖版本；其次，围绕功能 [Read tool] 完成输入校验、领域处理、持久化或投影、用户反馈和可重复验收。；然后产生领域事件或标准结果并更新必要投影；最后返回成功、等待、失败或取消结果，并记录阶段进度。

输出为ToolCall、标准结果、变更和快照摘要。计划中的最小验收为：读取 workspace 文件，输出限长、行号和 digest。

### 4. 状态、数据与一致性
- 状态变化必须遵守领域模型与事件规范，禁止直接修改 projection 绕过 domain command。
- SQLite Event Store，或本阶段明确声明的 Fake Store，是业务事实来源；日志只用于诊断。
- 外部副作用必须具有 operation_id、幂等策略、取消边界和恢复结论。
- 并发写入遵循单写者或 Write Claim；查询需要说明一致性级别和 cursor。

### 5. 异常、取消与恢复
必须设计输入无效、依赖不可用、权限拒绝、超时、用户取消、进程重启和重复提交路径。每类路径都要定义稳定 error_code、用户提示、是否可重试、是否产生事件和重启动作。不安全时进入明确的 blocked、failed 或 interrupted，不能静默成功。

### 6. 日志与安全要求
入口初始化日志，记录 stage.started、关键进度、stage.completed 或 stage.failed。日志采用非 JSON、非 key=value 的单行位置式格式：时间、级别、PID、任务链 UUID、线程或协程、logger、最后一级文件名和行号、主消息。不记录客户端地址；诊断上下文仅在 DEBUG 级别展示并置于主消息前，INFO 及以上隐藏。工具不得绕过 Gateway、权限、取消、超时和日志门禁。

### 7. 测试与可运行验收
- 必须提供 cargo 命令、CLI 子命令、集成测试或可重复 demo。
- 至少覆盖成功、失败、取消、重复调用以及适用的恢复或回滚路径。
- 日志断言任务链 UUID、稳定 message code、进度闭环、真实文件名和行号、无敏感信息泄漏。
- 回滚只能删除本阶段临时产物或恢复快照，不得删除用户数据或覆盖外部修改。

### 8. 后续代码注释要求
编码前必须通过本条目审核。实现代码需要为公共类型和函数、状态转换、协议字段、数据库迁移、异步任务边界、错误分支和安全判定添加中文详细注释。注释应解释设计原因、约束和失败恢复；复杂算法、并发或锁、取消传播、幂等键和安全边界还要写明前置条件、后置条件及不变量。测试代码需要说明 fixture 意图和验收理由。

### 9. 审核结论栏
- 需求理解：待审核
- 架构边界：待审核
- 接口和事件：待审核
- 异常与恢复：待审核
- 安全与日志：待审核
- 允许进入代码开发：否

<!-- END S13-002 -->
## S13-003：Glob tool

### 1. 功能目标
本功能属于 S13。建立 Read、Glob、Grep 只读工具的契约、边界、裁剪、取消和结果标准化。 本项聚焦于Tool Gateway、工具契约和副作用隔离，必须形成可以独立启动、测试或演示的最小闭环。

### 2. 范围与边界
- 包含：Glob tool所需契约、状态转换、错误返回、日志事件和自动化验收。
- 不包含：未列入本阶段的真实 Provider、跨阶段优化或视觉润色；依赖能力只能通过 Port、Fake 或 fixture 接入。
- 上游依赖：S13-002。依赖未 verified 时，本功能只能处于 blocked。
- 主要产物：ToolCall、标准结果、变更和快照摘要。

### 3. 输入、输出与处理流程
输入来自上游命令、查询、事件、配置、用户动作或 fixture，必须具备稳定标识、版本和必要授权上下文。

处理顺序：首先校验格式、当前状态、权限和依赖版本；其次，围绕功能 [Glob tool] 完成输入校验、领域处理、持久化或投影、用户反馈和可重复验收。；然后产生领域事件或标准结果并更新必要投影；最后返回成功、等待、失败或取消结果，并记录阶段进度。

输出为ToolCall、标准结果、变更和快照摘要。计划中的最小验收为：glob 结果排序、数量/字节上限和越界阻断。

### 4. 状态、数据与一致性
- 状态变化必须遵守领域模型与事件规范，禁止直接修改 projection 绕过 domain command。
- SQLite Event Store，或本阶段明确声明的 Fake Store，是业务事实来源；日志只用于诊断。
- 外部副作用必须具有 operation_id、幂等策略、取消边界和恢复结论。
- 并发写入遵循单写者或 Write Claim；查询需要说明一致性级别和 cursor。

### 5. 异常、取消与恢复
必须设计输入无效、依赖不可用、权限拒绝、超时、用户取消、进程重启和重复提交路径。每类路径都要定义稳定 error_code、用户提示、是否可重试、是否产生事件和重启动作。不安全时进入明确的 blocked、failed 或 interrupted，不能静默成功。

### 6. 日志与安全要求
入口初始化日志，记录 stage.started、关键进度、stage.completed 或 stage.failed。日志采用非 JSON、非 key=value 的单行位置式格式：时间、级别、PID、任务链 UUID、线程或协程、logger、最后一级文件名和行号、主消息。不记录客户端地址；诊断上下文仅在 DEBUG 级别展示并置于主消息前，INFO 及以上隐藏。工具不得绕过 Gateway、权限、取消、超时和日志门禁。

### 7. 测试与可运行验收
- 必须提供 cargo 命令、CLI 子命令、集成测试或可重复 demo。
- 至少覆盖成功、失败、取消、重复调用以及适用的恢复或回滚路径。
- 日志断言任务链 UUID、稳定 message code、进度闭环、真实文件名和行号、无敏感信息泄漏。
- 回滚只能删除本阶段临时产物或恢复快照，不得删除用户数据或覆盖外部修改。

### 8. 后续代码注释要求
编码前必须通过本条目审核。实现代码需要为公共类型和函数、状态转换、协议字段、数据库迁移、异步任务边界、错误分支和安全判定添加中文详细注释。注释应解释设计原因、约束和失败恢复；复杂算法、并发或锁、取消传播、幂等键和安全边界还要写明前置条件、后置条件及不变量。测试代码需要说明 fixture 意图和验收理由。

### 9. 审核结论栏
- 需求理解：待审核
- 架构边界：待审核
- 接口和事件：待审核
- 异常与恢复：待审核
- 安全与日志：待审核
- 允许进入代码开发：否

<!-- END S13-003 -->
## S13-004：Grep tool

### 1. 功能目标
本功能属于 S13。建立 Read、Glob、Grep 只读工具的契约、边界、裁剪、取消和结果标准化。 本项聚焦于Tool Gateway、工具契约和副作用隔离，必须形成可以独立启动、测试或演示的最小闭环。

### 2. 范围与边界
- 包含：Grep tool所需契约、状态转换、错误返回、日志事件和自动化验收。
- 不包含：未列入本阶段的真实 Provider、跨阶段优化或视觉润色；依赖能力只能通过 Port、Fake 或 fixture 接入。
- 上游依赖：S13-003。依赖未 verified 时，本功能只能处于 blocked。
- 主要产物：ToolCall、标准结果、变更和快照摘要。

### 3. 输入、输出与处理流程
输入来自上游命令、查询、事件、配置、用户动作或 fixture，必须具备稳定标识、版本和必要授权上下文。

处理顺序：首先校验格式、当前状态、权限和依赖版本；其次，围绕功能 [Grep tool] 完成输入校验、领域处理、持久化或投影、用户反馈和可重复验收。；然后产生领域事件或标准结果并更新必要投影；最后返回成功、等待、失败或取消结果，并记录阶段进度。

输出为ToolCall、标准结果、变更和快照摘要。计划中的最小验收为：搜索结果脱敏、截断、文件类型过滤。

### 4. 状态、数据与一致性
- 状态变化必须遵守领域模型与事件规范，禁止直接修改 projection 绕过 domain command。
- SQLite Event Store，或本阶段明确声明的 Fake Store，是业务事实来源；日志只用于诊断。
- 外部副作用必须具有 operation_id、幂等策略、取消边界和恢复结论。
- 并发写入遵循单写者或 Write Claim；查询需要说明一致性级别和 cursor。

### 5. 异常、取消与恢复
必须设计输入无效、依赖不可用、权限拒绝、超时、用户取消、进程重启和重复提交路径。每类路径都要定义稳定 error_code、用户提示、是否可重试、是否产生事件和重启动作。不安全时进入明确的 blocked、failed 或 interrupted，不能静默成功。

### 6. 日志与安全要求
入口初始化日志，记录 stage.started、关键进度、stage.completed 或 stage.failed。日志采用非 JSON、非 key=value 的单行位置式格式：时间、级别、PID、任务链 UUID、线程或协程、logger、最后一级文件名和行号、主消息。不记录客户端地址；诊断上下文仅在 DEBUG 级别展示并置于主消息前，INFO 及以上隐藏。工具不得绕过 Gateway、权限、取消、超时和日志门禁。

### 7. 测试与可运行验收
- 必须提供 cargo 命令、CLI 子命令、集成测试或可重复 demo。
- 至少覆盖成功、失败、取消、重复调用以及适用的恢复或回滚路径。
- 日志断言任务链 UUID、稳定 message code、进度闭环、真实文件名和行号、无敏感信息泄漏。
- 回滚只能删除本阶段临时产物或恢复快照，不得删除用户数据或覆盖外部修改。

### 8. 后续代码注释要求
编码前必须通过本条目审核。实现代码需要为公共类型和函数、状态转换、协议字段、数据库迁移、异步任务边界、错误分支和安全判定添加中文详细注释。注释应解释设计原因、约束和失败恢复；复杂算法、并发或锁、取消传播、幂等键和安全边界还要写明前置条件、后置条件及不变量。测试代码需要说明 fixture 意图和验收理由。

### 9. 审核结论栏
- 需求理解：待审核
- 架构边界：待审核
- 接口和事件：待审核
- 异常与恢复：待审核
- 安全与日志：待审核
- 允许进入代码开发：否

<!-- END S13-004 -->
## S13-005：result normalizer

### 1. 功能目标
本功能属于 S13。建立 Read、Glob、Grep 只读工具的契约、边界、裁剪、取消和结果标准化。 本项聚焦于领域功能、应用服务和可运行入口，必须形成可以独立启动、测试或演示的最小闭环。

### 2. 范围与边界
- 包含：result normalizer所需契约、状态转换、错误返回、日志事件和自动化验收。
- 不包含：未列入本阶段的真实 Provider、跨阶段优化或视觉润色；依赖能力只能通过 Port、Fake 或 fixture 接入。
- 上游依赖：S13-004。依赖未 verified 时，本功能只能处于 blocked。
- 主要产物：领域事件、查询投影、日志和验收结果。

### 3. 输入、输出与处理流程
输入来自上游命令、查询、事件、配置、用户动作或 fixture，必须具备稳定标识、版本和必要授权上下文。

处理顺序：首先校验格式、当前状态、权限和依赖版本；其次，围绕功能 [result normalizer] 完成输入校验、领域处理、持久化或投影、用户反馈和可重复验收。；然后产生领域事件或标准结果并更新必要投影；最后返回成功、等待、失败或取消结果，并记录阶段进度。

输出为领域事件、查询投影、日志和验收结果。计划中的最小验收为：success/denied/timeout/cancelled/error 统一格式。

### 4. 状态、数据与一致性
- 状态变化必须遵守领域模型与事件规范，禁止直接修改 projection 绕过 domain command。
- SQLite Event Store，或本阶段明确声明的 Fake Store，是业务事实来源；日志只用于诊断。
- 外部副作用必须具有 operation_id、幂等策略、取消边界和恢复结论。
- 并发写入遵循单写者或 Write Claim；查询需要说明一致性级别和 cursor。

### 5. 异常、取消与恢复
必须设计输入无效、依赖不可用、权限拒绝、超时、用户取消、进程重启和重复提交路径。每类路径都要定义稳定 error_code、用户提示、是否可重试、是否产生事件和重启动作。不安全时进入明确的 blocked、failed 或 interrupted，不能静默成功。

### 6. 日志与安全要求
入口初始化日志，记录 stage.started、关键进度、stage.completed 或 stage.failed。日志采用非 JSON、非 key=value 的单行位置式格式：时间、级别、PID、任务链 UUID、线程或协程、logger、最后一级文件名和行号、主消息。不记录客户端地址；诊断上下文仅在 DEBUG 级别展示并置于主消息前，INFO 及以上隐藏。必须保持 Port、幂等、取消、恢复和审计边界。

### 7. 测试与可运行验收
- 必须提供 cargo 命令、CLI 子命令、集成测试或可重复 demo。
- 至少覆盖成功、失败、取消、重复调用以及适用的恢复或回滚路径。
- 日志断言任务链 UUID、稳定 message code、进度闭环、真实文件名和行号、无敏感信息泄漏。
- 回滚只能删除本阶段临时产物或恢复快照，不得删除用户数据或覆盖外部修改。

### 8. 后续代码注释要求
编码前必须通过本条目审核。实现代码需要为公共类型和函数、状态转换、协议字段、数据库迁移、异步任务边界、错误分支和安全判定添加中文详细注释。注释应解释设计原因、约束和失败恢复；复杂算法、并发或锁、取消传播、幂等键和安全边界还要写明前置条件、后置条件及不变量。测试代码需要说明 fixture 意图和验收理由。

### 9. 审核结论栏
- 需求理解：待审核
- 架构边界：待审核
- 接口和事件：待审核
- 异常与恢复：待审核
- 安全与日志：待审核
- 允许进入代码开发：否

<!-- END S13-005 -->
## S13-006：tool audit projection

### 1. 功能目标
本功能属于 S13。建立 Read、Glob、Grep 只读工具的契约、边界、裁剪、取消和结果标准化。 本项聚焦于事件存储、事务和可重建投影，必须形成可以独立启动、测试或演示的最小闭环。

### 2. 范围与边界
- 包含：tool audit projection所需契约、状态转换、错误返回、日志事件和自动化验收。
- 不包含：未列入本阶段的真实 Provider、跨阶段优化或视觉润色；依赖能力只能通过 Port、Fake 或 fixture 接入。
- 上游依赖：S13-005。依赖未 verified 时，本功能只能处于 blocked。
- 主要产物：事件序列、投影版本、游标和恢复结果。

### 3. 输入、输出与处理流程
输入来自上游命令、查询、事件、配置、用户动作或 fixture，必须具备稳定标识、版本和必要授权上下文。

处理顺序：首先校验格式、当前状态、权限和依赖版本；其次，围绕功能 [tool audit projection] 完成输入校验、领域处理、持久化或投影、用户反馈和可重复验收。；然后产生领域事件或标准结果并更新必要投影；最后返回成功、等待、失败或取消结果，并记录阶段进度。

输出为事件序列、投影版本、游标和恢复结果。计划中的最小验收为：工具调用耗时、状态、参数摘要可查。

### 4. 状态、数据与一致性
- 状态变化必须遵守领域模型与事件规范，禁止直接修改 projection 绕过 domain command。
- SQLite Event Store，或本阶段明确声明的 Fake Store，是业务事实来源；日志只用于诊断。
- 外部副作用必须具有 operation_id、幂等策略、取消边界和恢复结论。
- 并发写入遵循单写者或 Write Claim；查询需要说明一致性级别和 cursor。

### 5. 异常、取消与恢复
必须设计输入无效、依赖不可用、权限拒绝、超时、用户取消、进程重启和重复提交路径。每类路径都要定义稳定 error_code、用户提示、是否可重试、是否产生事件和重启动作。不安全时进入明确的 blocked、failed 或 interrupted，不能静默成功。

### 6. 日志与安全要求
入口初始化日志，记录 stage.started、关键进度、stage.completed 或 stage.failed。日志采用非 JSON、非 key=value 的单行位置式格式：时间、级别、PID、任务链 UUID、线程或协程、logger、最后一级文件名和行号、主消息。不记录客户端地址；诊断上下文仅在 DEBUG 级别展示并置于主消息前，INFO 及以上隐藏。SQLite 事件是业务事实，事务不得产生半提交。

### 7. 测试与可运行验收
- 必须提供 cargo 命令、CLI 子命令、集成测试或可重复 demo。
- 至少覆盖成功、失败、取消、重复调用以及适用的恢复或回滚路径。
- 日志断言任务链 UUID、稳定 message code、进度闭环、真实文件名和行号、无敏感信息泄漏。
- 回滚只能删除本阶段临时产物或恢复快照，不得删除用户数据或覆盖外部修改。

### 8. 后续代码注释要求
编码前必须通过本条目审核。实现代码需要为公共类型和函数、状态转换、协议字段、数据库迁移、异步任务边界、错误分支和安全判定添加中文详细注释。注释应解释设计原因、约束和失败恢复；复杂算法、并发或锁、取消传播、幂等键和安全边界还要写明前置条件、后置条件及不变量。测试代码需要说明 fixture 意图和验收理由。

### 9. 审核结论栏
- 需求理解：待审核
- 架构边界：待审核
- 接口和事件：待审核
- 异常与恢复：待审核
- 安全与日志：待审核
- 允许进入代码开发：否

<!-- END S13-006 -->
## S13-007：`v0.1-read-tools-beta` demo

### 1. 功能目标
本功能属于 S13。建立 Read、Glob、Grep 只读工具的契约、边界、裁剪、取消和结果标准化。 本项聚焦于Tool Gateway、工具契约和副作用隔离，必须形成可以独立启动、测试或演示的最小闭环。

### 2. 范围与边界
- 包含：`v0.1-read-tools-beta` demo所需契约、状态转换、错误返回、日志事件和自动化验收。
- 不包含：未列入本阶段的真实 Provider、跨阶段优化或视觉润色；依赖能力只能通过 Port、Fake 或 fixture 接入。
- 上游依赖：S13-006。依赖未 verified 时，本功能只能处于 blocked。
- 主要产物：ToolCall、标准结果、变更和快照摘要。

### 3. 输入、输出与处理流程
输入来自上游命令、查询、事件、配置、用户动作或 fixture，必须具备稳定标识、版本和必要授权上下文。

处理顺序：首先校验格式、当前状态、权限和依赖版本；其次，围绕功能 [`v0.1-read-tools-beta` demo] 完成输入校验、领域处理、持久化或投影、用户反馈和可重复验收。；然后产生领域事件或标准结果并更新必要投影；最后返回成功、等待、失败或取消结果，并记录阶段进度。

输出为ToolCall、标准结果、变更和快照摘要。计划中的最小验收为：TUI 输入查询并看到结果和日志。

### 4. 状态、数据与一致性
- 状态变化必须遵守领域模型与事件规范，禁止直接修改 projection 绕过 domain command。
- SQLite Event Store，或本阶段明确声明的 Fake Store，是业务事实来源；日志只用于诊断。
- 外部副作用必须具有 operation_id、幂等策略、取消边界和恢复结论。
- 并发写入遵循单写者或 Write Claim；查询需要说明一致性级别和 cursor。

### 5. 异常、取消与恢复
必须设计输入无效、依赖不可用、权限拒绝、超时、用户取消、进程重启和重复提交路径。每类路径都要定义稳定 error_code、用户提示、是否可重试、是否产生事件和重启动作。不安全时进入明确的 blocked、failed 或 interrupted，不能静默成功。

### 6. 日志与安全要求
入口初始化日志，记录 stage.started、关键进度、stage.completed 或 stage.failed。日志采用非 JSON、非 key=value 的单行位置式格式：时间、级别、PID、任务链 UUID、线程或协程、logger、最后一级文件名和行号、主消息。不记录客户端地址；诊断上下文仅在 DEBUG 级别展示并置于主消息前，INFO 及以上隐藏。工具不得绕过 Gateway、权限、取消、超时和日志门禁。

### 7. 测试与可运行验收
- 必须提供 cargo 命令、CLI 子命令、集成测试或可重复 demo。
- 至少覆盖成功、失败、取消、重复调用以及适用的恢复或回滚路径。
- 日志断言任务链 UUID、稳定 message code、进度闭环、真实文件名和行号、无敏感信息泄漏。
- 回滚只能删除本阶段临时产物或恢复快照，不得删除用户数据或覆盖外部修改。

### 8. 后续代码注释要求
编码前必须通过本条目审核。实现代码需要为公共类型和函数、状态转换、协议字段、数据库迁移、异步任务边界、错误分支和安全判定添加中文详细注释。注释应解释设计原因、约束和失败恢复；复杂算法、并发或锁、取消传播、幂等键和安全边界还要写明前置条件、后置条件及不变量。测试代码需要说明 fixture 意图和验收理由。

### 9. 审核结论栏
- 需求理解：待审核
- 架构边界：待审核
- 接口和事件：待审核
- 异常与恢复：待审核
- 安全与日志：待审核
- 允许进入代码开发：否

<!-- END S13-007 -->
## S14-001：write preflight

### 1. 功能目标
本功能属于 S14。建立 Write、Edit 的 Write Claim、权限、原子写入、快照和回滚流程。 本项聚焦于Tool Gateway、工具契约和副作用隔离，必须形成可以独立启动、测试或演示的最小闭环。

### 2. 范围与边界
- 包含：write preflight所需契约、状态转换、错误返回、日志事件和自动化验收。
- 不包含：未列入本阶段的真实 Provider、跨阶段优化或视觉润色；依赖能力只能通过 Port、Fake 或 fixture 接入。
- 上游依赖：S13。依赖未 verified 时，本功能只能处于 blocked。
- 主要产物：ToolCall、标准结果、变更和快照摘要。

### 3. 输入、输出与处理流程
输入来自上游命令、查询、事件、配置、用户动作或 fixture，必须具备稳定标识、版本和必要授权上下文。

处理顺序：首先校验格式、当前状态、权限和依赖版本；其次，围绕功能 [write preflight] 完成输入校验、领域处理、持久化或投影、用户反馈和可重复验收。；然后产生领域事件或标准结果并更新必要投影；最后返回成功、等待、失败或取消结果，并记录阶段进度。

输出为ToolCall、标准结果、变更和快照摘要。计划中的最小验收为：workspace、符号链接、大小、编码检查。

### 4. 状态、数据与一致性
- 状态变化必须遵守领域模型与事件规范，禁止直接修改 projection 绕过 domain command。
- SQLite Event Store，或本阶段明确声明的 Fake Store，是业务事实来源；日志只用于诊断。
- 外部副作用必须具有 operation_id、幂等策略、取消边界和恢复结论。
- 并发写入遵循单写者或 Write Claim；查询需要说明一致性级别和 cursor。

### 5. 异常、取消与恢复
必须设计输入无效、依赖不可用、权限拒绝、超时、用户取消、进程重启和重复提交路径。每类路径都要定义稳定 error_code、用户提示、是否可重试、是否产生事件和重启动作。不安全时进入明确的 blocked、failed 或 interrupted，不能静默成功。

### 6. 日志与安全要求
入口初始化日志，记录 stage.started、关键进度、stage.completed 或 stage.failed。日志采用非 JSON、非 key=value 的单行位置式格式：时间、级别、PID、任务链 UUID、线程或协程、logger、最后一级文件名和行号、主消息。不记录客户端地址；诊断上下文仅在 DEBUG 级别展示并置于主消息前，INFO 及以上隐藏。工具不得绕过 Gateway、权限、取消、超时和日志门禁。

### 7. 测试与可运行验收
- 必须提供 cargo 命令、CLI 子命令、集成测试或可重复 demo。
- 至少覆盖成功、失败、取消、重复调用以及适用的恢复或回滚路径。
- 日志断言任务链 UUID、稳定 message code、进度闭环、真实文件名和行号、无敏感信息泄漏。
- 回滚只能删除本阶段临时产物或恢复快照，不得删除用户数据或覆盖外部修改。

### 8. 后续代码注释要求
编码前必须通过本条目审核。实现代码需要为公共类型和函数、状态转换、协议字段、数据库迁移、异步任务边界、错误分支和安全判定添加中文详细注释。注释应解释设计原因、约束和失败恢复；复杂算法、并发或锁、取消传播、幂等键和安全边界还要写明前置条件、后置条件及不变量。测试代码需要说明 fixture 意图和验收理由。

### 9. 审核结论栏
- 需求理解：待审核
- 架构边界：待审核
- 接口和事件：待审核
- 异常与恢复：待审核
- 安全与日志：待审核
- 允许进入代码开发：否

<!-- END S14-001 -->
## S14-002：Write tool

### 1. 功能目标
本功能属于 S14。建立 Write、Edit 的 Write Claim、权限、原子写入、快照和回滚流程。 本项聚焦于Tool Gateway、工具契约和副作用隔离，必须形成可以独立启动、测试或演示的最小闭环。

### 2. 范围与边界
- 包含：Write tool所需契约、状态转换、错误返回、日志事件和自动化验收。
- 不包含：未列入本阶段的真实 Provider、跨阶段优化或视觉润色；依赖能力只能通过 Port、Fake 或 fixture 接入。
- 上游依赖：S14-001。依赖未 verified 时，本功能只能处于 blocked。
- 主要产物：ToolCall、标准结果、变更和快照摘要。

### 3. 输入、输出与处理流程
输入来自上游命令、查询、事件、配置、用户动作或 fixture，必须具备稳定标识、版本和必要授权上下文。

处理顺序：首先校验格式、当前状态、权限和依赖版本；其次，围绕功能 [Write tool] 完成输入校验、领域处理、持久化或投影、用户反馈和可重复验收。；然后产生领域事件或标准结果并更新必要投影；最后返回成功、等待、失败或取消结果，并记录阶段进度。

输出为ToolCall、标准结果、变更和快照摘要。计划中的最小验收为：仅经 Permission + ToolGateway 写文件。

### 4. 状态、数据与一致性
- 状态变化必须遵守领域模型与事件规范，禁止直接修改 projection 绕过 domain command。
- SQLite Event Store，或本阶段明确声明的 Fake Store，是业务事实来源；日志只用于诊断。
- 外部副作用必须具有 operation_id、幂等策略、取消边界和恢复结论。
- 并发写入遵循单写者或 Write Claim；查询需要说明一致性级别和 cursor。

### 5. 异常、取消与恢复
必须设计输入无效、依赖不可用、权限拒绝、超时、用户取消、进程重启和重复提交路径。每类路径都要定义稳定 error_code、用户提示、是否可重试、是否产生事件和重启动作。不安全时进入明确的 blocked、failed 或 interrupted，不能静默成功。

### 6. 日志与安全要求
入口初始化日志，记录 stage.started、关键进度、stage.completed 或 stage.failed。日志采用非 JSON、非 key=value 的单行位置式格式：时间、级别、PID、任务链 UUID、线程或协程、logger、最后一级文件名和行号、主消息。不记录客户端地址；诊断上下文仅在 DEBUG 级别展示并置于主消息前，INFO 及以上隐藏。工具不得绕过 Gateway、权限、取消、超时和日志门禁。

### 7. 测试与可运行验收
- 必须提供 cargo 命令、CLI 子命令、集成测试或可重复 demo。
- 至少覆盖成功、失败、取消、重复调用以及适用的恢复或回滚路径。
- 日志断言任务链 UUID、稳定 message code、进度闭环、真实文件名和行号、无敏感信息泄漏。
- 回滚只能删除本阶段临时产物或恢复快照，不得删除用户数据或覆盖外部修改。

### 8. 后续代码注释要求
编码前必须通过本条目审核。实现代码需要为公共类型和函数、状态转换、协议字段、数据库迁移、异步任务边界、错误分支和安全判定添加中文详细注释。注释应解释设计原因、约束和失败恢复；复杂算法、并发或锁、取消传播、幂等键和安全边界还要写明前置条件、后置条件及不变量。测试代码需要说明 fixture 意图和验收理由。

### 9. 审核结论栏
- 需求理解：待审核
- 架构边界：待审核
- 接口和事件：待审核
- 异常与恢复：待审核
- 安全与日志：待审核
- 允许进入代码开发：否

<!-- END S14-002 -->
## S14-003：Edit patch parser

### 1. 功能目标
本功能属于 S14。建立 Write、Edit 的 Write Claim、权限、原子写入、快照和回滚流程。 本项聚焦于输入事件、纯状态转换和终端渲染，必须形成可以独立启动、测试或演示的最小闭环。

### 2. 范围与边界
- 包含：Edit patch parser所需契约、状态转换、错误返回、日志事件和自动化验收。
- 不包含：未列入本阶段的真实 Provider、跨阶段优化或视觉润色；依赖能力只能通过 Port、Fake 或 fixture 接入。
- 上游依赖：S14-002。依赖未 verified 时，本功能只能处于 blocked。
- 主要产物：AppState、Effect、渲染帧和用户提示。

### 3. 输入、输出与处理流程
输入来自上游命令、查询、事件、配置、用户动作或 fixture，必须具备稳定标识、版本和必要授权上下文。

处理顺序：首先校验格式、当前状态、权限和依赖版本；其次，取得 Write Claim 后校验摘要和权限，写临时文件并原子替换；失败时保留原文件。；然后产生领域事件或标准结果并更新必要投影；最后返回成功、等待、失败或取消结果，并记录阶段进度。

输出为AppState、Effect、渲染帧和用户提示。计划中的最小验收为：单文件 patch、上下文匹配和冲突检测。

### 4. 状态、数据与一致性
- 状态变化必须遵守领域模型与事件规范，禁止直接修改 projection 绕过 domain command。
- SQLite Event Store，或本阶段明确声明的 Fake Store，是业务事实来源；日志只用于诊断。
- 外部副作用必须具有 operation_id、幂等策略、取消边界和恢复结论。
- 并发写入遵循单写者或 Write Claim；查询需要说明一致性级别和 cursor。

### 5. 异常、取消与恢复
必须设计输入无效、依赖不可用、权限拒绝、超时、用户取消、进程重启和重复提交路径。每类路径都要定义稳定 error_code、用户提示、是否可重试、是否产生事件和重启动作。不安全时进入明确的 blocked、failed 或 interrupted，不能静默成功。

### 6. 日志与安全要求
入口初始化日志，记录 stage.started、关键进度、stage.completed 或 stage.failed。日志采用非 JSON、非 key=value 的单行位置式格式：时间、级别、PID、任务链 UUID、线程或协程、logger、最后一级文件名和行号、主消息。不记录客户端地址；诊断上下文仅在 DEBUG 级别展示并置于主消息前，INFO 及以上隐藏。渲染层不得产生业务副作用，窄终端和 Unicode 不得越界。

### 7. 测试与可运行验收
- 必须提供 cargo 命令、CLI 子命令、集成测试或可重复 demo。
- 至少覆盖成功、失败、取消、重复调用以及适用的恢复或回滚路径。
- 日志断言任务链 UUID、稳定 message code、进度闭环、真实文件名和行号、无敏感信息泄漏。
- 回滚只能删除本阶段临时产物或恢复快照，不得删除用户数据或覆盖外部修改。

### 8. 后续代码注释要求
编码前必须通过本条目审核。实现代码需要为公共类型和函数、状态转换、协议字段、数据库迁移、异步任务边界、错误分支和安全判定添加中文详细注释。注释应解释设计原因、约束和失败恢复；复杂算法、并发或锁、取消传播、幂等键和安全边界还要写明前置条件、后置条件及不变量。测试代码需要说明 fixture 意图和验收理由。

### 9. 审核结论栏
- 需求理解：待审核
- 架构边界：待审核
- 接口和事件：待审核
- 异常与恢复：待审核
- 安全与日志：待审核
- 允许进入代码开发：否

<!-- END S14-003 -->
## S14-004：Edit tool

### 1. 功能目标
本功能属于 S14。建立 Write、Edit 的 Write Claim、权限、原子写入、快照和回滚流程。 本项聚焦于Tool Gateway、工具契约和副作用隔离，必须形成可以独立启动、测试或演示的最小闭环。

### 2. 范围与边界
- 包含：Edit tool所需契约、状态转换、错误返回、日志事件和自动化验收。
- 不包含：未列入本阶段的真实 Provider、跨阶段优化或视觉润色；依赖能力只能通过 Port、Fake 或 fixture 接入。
- 上游依赖：S14-003。依赖未 verified 时，本功能只能处于 blocked。
- 主要产物：ToolCall、标准结果、变更和快照摘要。

### 3. 输入、输出与处理流程
输入来自上游命令、查询、事件、配置、用户动作或 fixture，必须具备稳定标识、版本和必要授权上下文。

处理顺序：首先校验格式、当前状态、权限和依赖版本；其次，围绕功能 [Edit tool] 完成输入校验、领域处理、持久化或投影、用户反馈和可重复验收。；然后产生领域事件或标准结果并更新必要投影；最后返回成功、等待、失败或取消结果，并记录阶段进度。

输出为ToolCall、标准结果、变更和快照摘要。计划中的最小验收为：成功修改、匹配失败和冲突均可恢复。

### 4. 状态、数据与一致性
- 状态变化必须遵守领域模型与事件规范，禁止直接修改 projection 绕过 domain command。
- SQLite Event Store，或本阶段明确声明的 Fake Store，是业务事实来源；日志只用于诊断。
- 外部副作用必须具有 operation_id、幂等策略、取消边界和恢复结论。
- 并发写入遵循单写者或 Write Claim；查询需要说明一致性级别和 cursor。

### 5. 异常、取消与恢复
必须设计输入无效、依赖不可用、权限拒绝、超时、用户取消、进程重启和重复提交路径。每类路径都要定义稳定 error_code、用户提示、是否可重试、是否产生事件和重启动作。不安全时进入明确的 blocked、failed 或 interrupted，不能静默成功。

### 6. 日志与安全要求
入口初始化日志，记录 stage.started、关键进度、stage.completed 或 stage.failed。日志采用非 JSON、非 key=value 的单行位置式格式：时间、级别、PID、任务链 UUID、线程或协程、logger、最后一级文件名和行号、主消息。不记录客户端地址；诊断上下文仅在 DEBUG 级别展示并置于主消息前，INFO 及以上隐藏。工具不得绕过 Gateway、权限、取消、超时和日志门禁。

### 7. 测试与可运行验收
- 必须提供 cargo 命令、CLI 子命令、集成测试或可重复 demo。
- 至少覆盖成功、失败、取消、重复调用以及适用的恢复或回滚路径。
- 日志断言任务链 UUID、稳定 message code、进度闭环、真实文件名和行号、无敏感信息泄漏。
- 回滚只能删除本阶段临时产物或恢复快照，不得删除用户数据或覆盖外部修改。

### 8. 后续代码注释要求
编码前必须通过本条目审核。实现代码需要为公共类型和函数、状态转换、协议字段、数据库迁移、异步任务边界、错误分支和安全判定添加中文详细注释。注释应解释设计原因、约束和失败恢复；复杂算法、并发或锁、取消传播、幂等键和安全边界还要写明前置条件、后置条件及不变量。测试代码需要说明 fixture 意图和验收理由。

### 9. 审核结论栏
- 需求理解：待审核
- 架构边界：待审核
- 接口和事件：待审核
- 异常与恢复：待审核
- 安全与日志：待审核
- 允许进入代码开发：否

<!-- END S14-004 -->
## S14-005：PostToolUse event

### 1. 功能目标
本功能属于 S14。建立 Write、Edit 的 Write Claim、权限、原子写入、快照和回滚流程。 本项聚焦于事件存储、事务和可重建投影，必须形成可以独立启动、测试或演示的最小闭环。

### 2. 范围与边界
- 包含：PostToolUse event所需契约、状态转换、错误返回、日志事件和自动化验收。
- 不包含：未列入本阶段的真实 Provider、跨阶段优化或视觉润色；依赖能力只能通过 Port、Fake 或 fixture 接入。
- 上游依赖：S14-004。依赖未 verified 时，本功能只能处于 blocked。
- 主要产物：事件序列、投影版本、游标和恢复结果。

### 3. 输入、输出与处理流程
输入来自上游命令、查询、事件、配置、用户动作或 fixture，必须具备稳定标识、版本和必要授权上下文。

处理顺序：首先校验格式、当前状态、权限和依赖版本；其次，围绕功能 [PostToolUse event] 完成输入校验、领域处理、持久化或投影、用户反馈和可重复验收。；然后产生领域事件或标准结果并更新必要投影；最后返回成功、等待、失败或取消结果，并记录阶段进度。

输出为事件序列、投影版本、游标和恢复结果。计划中的最小验收为：文件变更产生 digest、size、diff summary。

### 4. 状态、数据与一致性
- 状态变化必须遵守领域模型与事件规范，禁止直接修改 projection 绕过 domain command。
- SQLite Event Store，或本阶段明确声明的 Fake Store，是业务事实来源；日志只用于诊断。
- 外部副作用必须具有 operation_id、幂等策略、取消边界和恢复结论。
- 并发写入遵循单写者或 Write Claim；查询需要说明一致性级别和 cursor。

### 5. 异常、取消与恢复
必须设计输入无效、依赖不可用、权限拒绝、超时、用户取消、进程重启和重复提交路径。每类路径都要定义稳定 error_code、用户提示、是否可重试、是否产生事件和重启动作。不安全时进入明确的 blocked、failed 或 interrupted，不能静默成功。

### 6. 日志与安全要求
入口初始化日志，记录 stage.started、关键进度、stage.completed 或 stage.failed。日志采用非 JSON、非 key=value 的单行位置式格式：时间、级别、PID、任务链 UUID、线程或协程、logger、最后一级文件名和行号、主消息。不记录客户端地址；诊断上下文仅在 DEBUG 级别展示并置于主消息前，INFO 及以上隐藏。SQLite 事件是业务事实，事务不得产生半提交。

### 7. 测试与可运行验收
- 必须提供 cargo 命令、CLI 子命令、集成测试或可重复 demo。
- 至少覆盖成功、失败、取消、重复调用以及适用的恢复或回滚路径。
- 日志断言任务链 UUID、稳定 message code、进度闭环、真实文件名和行号、无敏感信息泄漏。
- 回滚只能删除本阶段临时产物或恢复快照，不得删除用户数据或覆盖外部修改。

### 8. 后续代码注释要求
编码前必须通过本条目审核。实现代码需要为公共类型和函数、状态转换、协议字段、数据库迁移、异步任务边界、错误分支和安全判定添加中文详细注释。注释应解释设计原因、约束和失败恢复；复杂算法、并发或锁、取消传播、幂等键和安全边界还要写明前置条件、后置条件及不变量。测试代码需要说明 fixture 意图和验收理由。

### 9. 审核结论栏
- 需求理解：待审核
- 架构边界：待审核
- 接口和事件：待审核
- 异常与恢复：待审核
- 安全与日志：待审核
- 允许进入代码开发：否

<!-- END S14-005 -->
## S14-006：rollback-on-failure

### 1. 功能目标
本功能属于 S14。建立 Write、Edit 的 Write Claim、权限、原子写入、快照和回滚流程。 本项聚焦于领域功能、应用服务和可运行入口，必须形成可以独立启动、测试或演示的最小闭环。

### 2. 范围与边界
- 包含：rollback-on-failure所需契约、状态转换、错误返回、日志事件和自动化验收。
- 不包含：未列入本阶段的真实 Provider、跨阶段优化或视觉润色；依赖能力只能通过 Port、Fake 或 fixture 接入。
- 上游依赖：S14-005。依赖未 verified 时，本功能只能处于 blocked。
- 主要产物：领域事件、查询投影、日志和验收结果。

### 3. 输入、输出与处理流程
输入来自上游命令、查询、事件、配置、用户动作或 fixture，必须具备稳定标识、版本和必要授权上下文。

处理顺序：首先校验格式、当前状态、权限和依赖版本；其次，围绕功能 [rollback-on-failure] 完成输入校验、领域处理、持久化或投影、用户反馈和可重复验收。；然后产生领域事件或标准结果并更新必要投影；最后返回成功、等待、失败或取消结果，并记录阶段进度。

输出为领域事件、查询投影、日志和验收结果。计划中的最小验收为：写入后 gate 失败可回到变更前状态。

### 4. 状态、数据与一致性
- 状态变化必须遵守领域模型与事件规范，禁止直接修改 projection 绕过 domain command。
- SQLite Event Store，或本阶段明确声明的 Fake Store，是业务事实来源；日志只用于诊断。
- 外部副作用必须具有 operation_id、幂等策略、取消边界和恢复结论。
- 并发写入遵循单写者或 Write Claim；查询需要说明一致性级别和 cursor。

### 5. 异常、取消与恢复
必须设计输入无效、依赖不可用、权限拒绝、超时、用户取消、进程重启和重复提交路径。每类路径都要定义稳定 error_code、用户提示、是否可重试、是否产生事件和重启动作。不安全时进入明确的 blocked、failed 或 interrupted，不能静默成功。

### 6. 日志与安全要求
入口初始化日志，记录 stage.started、关键进度、stage.completed 或 stage.failed。日志采用非 JSON、非 key=value 的单行位置式格式：时间、级别、PID、任务链 UUID、线程或协程、logger、最后一级文件名和行号、主消息。不记录客户端地址；诊断上下文仅在 DEBUG 级别展示并置于主消息前，INFO 及以上隐藏。必须保持 Port、幂等、取消、恢复和审计边界。

### 7. 测试与可运行验收
- 必须提供 cargo 命令、CLI 子命令、集成测试或可重复 demo。
- 至少覆盖成功、失败、取消、重复调用以及适用的恢复或回滚路径。
- 日志断言任务链 UUID、稳定 message code、进度闭环、真实文件名和行号、无敏感信息泄漏。
- 回滚只能删除本阶段临时产物或恢复快照，不得删除用户数据或覆盖外部修改。

### 8. 后续代码注释要求
编码前必须通过本条目审核。实现代码需要为公共类型和函数、状态转换、协议字段、数据库迁移、异步任务边界、错误分支和安全判定添加中文详细注释。注释应解释设计原因、约束和失败恢复；复杂算法、并发或锁、取消传播、幂等键和安全边界还要写明前置条件、后置条件及不变量。测试代码需要说明 fixture 意图和验收理由。

### 9. 审核结论栏
- 需求理解：待审核
- 架构边界：待审核
- 接口和事件：待审核
- 异常与恢复：待审核
- 安全与日志：待审核
- 允许进入代码开发：否

<!-- END S14-006 -->
## S14-007：`v0.1-write-tools-beta` demo

### 1. 功能目标
本功能属于 S14。建立 Write、Edit 的 Write Claim、权限、原子写入、快照和回滚流程。 本项聚焦于Tool Gateway、工具契约和副作用隔离，必须形成可以独立启动、测试或演示的最小闭环。

### 2. 范围与边界
- 包含：`v0.1-write-tools-beta` demo所需契约、状态转换、错误返回、日志事件和自动化验收。
- 不包含：未列入本阶段的真实 Provider、跨阶段优化或视觉润色；依赖能力只能通过 Port、Fake 或 fixture 接入。
- 上游依赖：S14-006。依赖未 verified 时，本功能只能处于 blocked。
- 主要产物：ToolCall、标准结果、变更和快照摘要。

### 3. 输入、输出与处理流程
输入来自上游命令、查询、事件、配置、用户动作或 fixture，必须具备稳定标识、版本和必要授权上下文。

处理顺序：首先校验格式、当前状态、权限和依赖版本；其次，围绕功能 [`v0.1-write-tools-beta` demo] 完成输入校验、领域处理、持久化或投影、用户反馈和可重复验收。；然后产生领域事件或标准结果并更新必要投影；最后返回成功、等待、失败或取消结果，并记录阶段进度。

输出为ToolCall、标准结果、变更和快照摘要。计划中的最小验收为：创建文件→编辑→失败回滚。

### 4. 状态、数据与一致性
- 状态变化必须遵守领域模型与事件规范，禁止直接修改 projection 绕过 domain command。
- SQLite Event Store，或本阶段明确声明的 Fake Store，是业务事实来源；日志只用于诊断。
- 外部副作用必须具有 operation_id、幂等策略、取消边界和恢复结论。
- 并发写入遵循单写者或 Write Claim；查询需要说明一致性级别和 cursor。

### 5. 异常、取消与恢复
必须设计输入无效、依赖不可用、权限拒绝、超时、用户取消、进程重启和重复提交路径。每类路径都要定义稳定 error_code、用户提示、是否可重试、是否产生事件和重启动作。不安全时进入明确的 blocked、failed 或 interrupted，不能静默成功。

### 6. 日志与安全要求
入口初始化日志，记录 stage.started、关键进度、stage.completed 或 stage.failed。日志采用非 JSON、非 key=value 的单行位置式格式：时间、级别、PID、任务链 UUID、线程或协程、logger、最后一级文件名和行号、主消息。不记录客户端地址；诊断上下文仅在 DEBUG 级别展示并置于主消息前，INFO 及以上隐藏。工具不得绕过 Gateway、权限、取消、超时和日志门禁。

### 7. 测试与可运行验收
- 必须提供 cargo 命令、CLI 子命令、集成测试或可重复 demo。
- 至少覆盖成功、失败、取消、重复调用以及适用的恢复或回滚路径。
- 日志断言任务链 UUID、稳定 message code、进度闭环、真实文件名和行号、无敏感信息泄漏。
- 回滚只能删除本阶段临时产物或恢复快照，不得删除用户数据或覆盖外部修改。

### 8. 后续代码注释要求
编码前必须通过本条目审核。实现代码需要为公共类型和函数、状态转换、协议字段、数据库迁移、异步任务边界、错误分支和安全判定添加中文详细注释。注释应解释设计原因、约束和失败恢复；复杂算法、并发或锁、取消传播、幂等键和安全边界还要写明前置条件、后置条件及不变量。测试代码需要说明 fixture 意图和验收理由。

### 9. 审核结论栏
- 需求理解：待审核
- 架构边界：待审核
- 接口和事件：待审核
- 异常与恢复：待审核
- 安全与日志：待审核
- 允许进入代码开发：否

<!-- END S14-007 -->
## S15-001：process supervisor

### 1. 功能目标
本功能属于 S15。建立 Bash AST 风险分类、审批、隔离执行、超时取消和输出截断。 本项聚焦于领域功能、应用服务和可运行入口，必须形成可以独立启动、测试或演示的最小闭环。

### 2. 范围与边界
- 包含：process supervisor所需契约、状态转换、错误返回、日志事件和自动化验收。
- 不包含：未列入本阶段的真实 Provider、跨阶段优化或视觉润色；依赖能力只能通过 Port、Fake 或 fixture 接入。
- 上游依赖：S14。依赖未 verified 时，本功能只能处于 blocked。
- 主要产物：领域事件、查询投影、日志和验收结果。

### 3. 输入、输出与处理流程
输入来自上游命令、查询、事件、配置、用户动作或 fixture，必须具备稳定标识、版本和必要授权上下文。

处理顺序：首先校验格式、当前状态、权限和依赖版本；其次，围绕功能 [process supervisor] 完成输入校验、领域处理、持久化或投影、用户反馈和可重复验收。；然后产生领域事件或标准结果并更新必要投影；最后返回成功、等待、失败或取消结果，并记录阶段进度。

输出为领域事件、查询投影、日志和验收结果。计划中的最小验收为：子进程生命周期、stdout/stderr、退出码可回收。

### 4. 状态、数据与一致性
- 状态变化必须遵守领域模型与事件规范，禁止直接修改 projection 绕过 domain command。
- SQLite Event Store，或本阶段明确声明的 Fake Store，是业务事实来源；日志只用于诊断。
- 外部副作用必须具有 operation_id、幂等策略、取消边界和恢复结论。
- 并发写入遵循单写者或 Write Claim；查询需要说明一致性级别和 cursor。

### 5. 异常、取消与恢复
必须设计输入无效、依赖不可用、权限拒绝、超时、用户取消、进程重启和重复提交路径。每类路径都要定义稳定 error_code、用户提示、是否可重试、是否产生事件和重启动作。不安全时进入明确的 blocked、failed 或 interrupted，不能静默成功。

### 6. 日志与安全要求
入口初始化日志，记录 stage.started、关键进度、stage.completed 或 stage.failed。日志采用非 JSON、非 key=value 的单行位置式格式：时间、级别、PID、任务链 UUID、线程或协程、logger、最后一级文件名和行号、主消息。不记录客户端地址；诊断上下文仅在 DEBUG 级别展示并置于主消息前，INFO 及以上隐藏。必须保持 Port、幂等、取消、恢复和审计边界。

### 7. 测试与可运行验收
- 必须提供 cargo 命令、CLI 子命令、集成测试或可重复 demo。
- 至少覆盖成功、失败、取消、重复调用以及适用的恢复或回滚路径。
- 日志断言任务链 UUID、稳定 message code、进度闭环、真实文件名和行号、无敏感信息泄漏。
- 回滚只能删除本阶段临时产物或恢复快照，不得删除用户数据或覆盖外部修改。

### 8. 后续代码注释要求
编码前必须通过本条目审核。实现代码需要为公共类型和函数、状态转换、协议字段、数据库迁移、异步任务边界、错误分支和安全判定添加中文详细注释。注释应解释设计原因、约束和失败恢复；复杂算法、并发或锁、取消传播、幂等键和安全边界还要写明前置条件、后置条件及不变量。测试代码需要说明 fixture 意图和验收理由。

### 9. 审核结论栏
- 需求理解：待审核
- 架构边界：待审核
- 接口和事件：待审核
- 异常与恢复：待审核
- 安全与日志：待审核
- 允许进入代码开发：否

<!-- END S15-001 -->
## S15-002：command allowlist

### 1. 功能目标
本功能属于 S15。建立 Bash AST 风险分类、审批、隔离执行、超时取消和输出截断。 本项聚焦于领域功能、应用服务和可运行入口，必须形成可以独立启动、测试或演示的最小闭环。

### 2. 范围与边界
- 包含：command allowlist所需契约、状态转换、错误返回、日志事件和自动化验收。
- 不包含：未列入本阶段的真实 Provider、跨阶段优化或视觉润色；依赖能力只能通过 Port、Fake 或 fixture 接入。
- 上游依赖：S15-001。依赖未 verified 时，本功能只能处于 blocked。
- 主要产物：领域事件、查询投影、日志和验收结果。

### 3. 输入、输出与处理流程
输入来自上游命令、查询、事件、配置、用户动作或 fixture，必须具备稳定标识、版本和必要授权上下文。

处理顺序：首先校验格式、当前状态、权限和依赖版本；其次，围绕功能 [command allowlist] 完成输入校验、领域处理、持久化或投影、用户反馈和可重复验收。；然后产生领域事件或标准结果并更新必要投影；最后返回成功、等待、失败或取消结果，并记录阶段进度。

输出为领域事件、查询投影、日志和验收结果。计划中的最小验收为：`cargo test` 等安全命令可执行。

### 4. 状态、数据与一致性
- 状态变化必须遵守领域模型与事件规范，禁止直接修改 projection 绕过 domain command。
- SQLite Event Store，或本阶段明确声明的 Fake Store，是业务事实来源；日志只用于诊断。
- 外部副作用必须具有 operation_id、幂等策略、取消边界和恢复结论。
- 并发写入遵循单写者或 Write Claim；查询需要说明一致性级别和 cursor。

### 5. 异常、取消与恢复
必须设计输入无效、依赖不可用、权限拒绝、超时、用户取消、进程重启和重复提交路径。每类路径都要定义稳定 error_code、用户提示、是否可重试、是否产生事件和重启动作。不安全时进入明确的 blocked、failed 或 interrupted，不能静默成功。

### 6. 日志与安全要求
入口初始化日志，记录 stage.started、关键进度、stage.completed 或 stage.failed。日志采用非 JSON、非 key=value 的单行位置式格式：时间、级别、PID、任务链 UUID、线程或协程、logger、最后一级文件名和行号、主消息。不记录客户端地址；诊断上下文仅在 DEBUG 级别展示并置于主消息前，INFO 及以上隐藏。必须保持 Port、幂等、取消、恢复和审计边界。

### 7. 测试与可运行验收
- 必须提供 cargo 命令、CLI 子命令、集成测试或可重复 demo。
- 至少覆盖成功、失败、取消、重复调用以及适用的恢复或回滚路径。
- 日志断言任务链 UUID、稳定 message code、进度闭环、真实文件名和行号、无敏感信息泄漏。
- 回滚只能删除本阶段临时产物或恢复快照，不得删除用户数据或覆盖外部修改。

### 8. 后续代码注释要求
编码前必须通过本条目审核。实现代码需要为公共类型和函数、状态转换、协议字段、数据库迁移、异步任务边界、错误分支和安全判定添加中文详细注释。注释应解释设计原因、约束和失败恢复；复杂算法、并发或锁、取消传播、幂等键和安全边界还要写明前置条件、后置条件及不变量。测试代码需要说明 fixture 意图和验收理由。

### 9. 审核结论栏
- 需求理解：待审核
- 架构边界：待审核
- 接口和事件：待审核
- 异常与恢复：待审核
- 安全与日志：待审核
- 允许进入代码开发：否

<!-- END S15-002 -->
## S15-003：dangerous command block

### 1. 功能目标
本功能属于 S15。建立 Bash AST 风险分类、审批、隔离执行、超时取消和输出截断。 本项聚焦于领域功能、应用服务和可运行入口，必须形成可以独立启动、测试或演示的最小闭环。

### 2. 范围与边界
- 包含：dangerous command block所需契约、状态转换、错误返回、日志事件和自动化验收。
- 不包含：未列入本阶段的真实 Provider、跨阶段优化或视觉润色；依赖能力只能通过 Port、Fake 或 fixture 接入。
- 上游依赖：S15-002。依赖未 verified 时，本功能只能处于 blocked。
- 主要产物：领域事件、查询投影、日志和验收结果。

### 3. 输入、输出与处理流程
输入来自上游命令、查询、事件、配置、用户动作或 fixture，必须具备稳定标识、版本和必要授权上下文。

处理顺序：首先校验格式、当前状态、权限和依赖版本；其次，解析命令 AST，识别递归删除、网络、重定向、管道和环境修改，不以字符串前缀判定安全。；然后产生领域事件或标准结果并更新必要投影；最后返回成功、等待、失败或取消结果，并记录阶段进度。

输出为领域事件、查询投影、日志和验收结果。计划中的最小验收为：删除、提权、网络外传、后台驻留被阻断。

### 4. 状态、数据与一致性
- 状态变化必须遵守领域模型与事件规范，禁止直接修改 projection 绕过 domain command。
- SQLite Event Store，或本阶段明确声明的 Fake Store，是业务事实来源；日志只用于诊断。
- 外部副作用必须具有 operation_id、幂等策略、取消边界和恢复结论。
- 并发写入遵循单写者或 Write Claim；查询需要说明一致性级别和 cursor。

### 5. 异常、取消与恢复
必须设计输入无效、依赖不可用、权限拒绝、超时、用户取消、进程重启和重复提交路径。每类路径都要定义稳定 error_code、用户提示、是否可重试、是否产生事件和重启动作。不安全时进入明确的 blocked、failed 或 interrupted，不能静默成功。

### 6. 日志与安全要求
入口初始化日志，记录 stage.started、关键进度、stage.completed 或 stage.failed。日志采用非 JSON、非 key=value 的单行位置式格式：时间、级别、PID、任务链 UUID、线程或协程、logger、最后一级文件名和行号、主消息。不记录客户端地址；诊断上下文仅在 DEBUG 级别展示并置于主消息前，INFO 及以上隐藏。必须保持 Port、幂等、取消、恢复和审计边界。

### 7. 测试与可运行验收
- 必须提供 cargo 命令、CLI 子命令、集成测试或可重复 demo。
- 至少覆盖成功、失败、取消、重复调用以及适用的恢复或回滚路径。
- 日志断言任务链 UUID、稳定 message code、进度闭环、真实文件名和行号、无敏感信息泄漏。
- 回滚只能删除本阶段临时产物或恢复快照，不得删除用户数据或覆盖外部修改。

### 8. 后续代码注释要求
编码前必须通过本条目审核。实现代码需要为公共类型和函数、状态转换、协议字段、数据库迁移、异步任务边界、错误分支和安全判定添加中文详细注释。注释应解释设计原因、约束和失败恢复；复杂算法、并发或锁、取消传播、幂等键和安全边界还要写明前置条件、后置条件及不变量。测试代码需要说明 fixture 意图和验收理由。

### 9. 审核结论栏
- 需求理解：待审核
- 架构边界：待审核
- 接口和事件：待审核
- 异常与恢复：待审核
- 安全与日志：待审核
- 允许进入代码开发：否

<!-- END S15-003 -->
## S15-004：timeout/cancel/process-tree cleanup

### 1. 功能目标
本功能属于 S15。建立 Bash AST 风险分类、审批、隔离执行、超时取消和输出截断。 本项聚焦于领域功能、应用服务和可运行入口，必须形成可以独立启动、测试或演示的最小闭环。

### 2. 范围与边界
- 包含：timeout/cancel/process-tree cleanup所需契约、状态转换、错误返回、日志事件和自动化验收。
- 不包含：未列入本阶段的真实 Provider、跨阶段优化或视觉润色；依赖能力只能通过 Port、Fake 或 fixture 接入。
- 上游依赖：S15-003。依赖未 verified 时，本功能只能处于 blocked。
- 主要产物：领域事件、查询投影、日志和验收结果。

### 3. 输入、输出与处理流程
输入来自上游命令、查询、事件、配置、用户动作或 fixture，必须具备稳定标识、版本和必要授权上下文。

处理顺序：首先校验格式、当前状态、权限和依赖版本；其次，围绕功能 [timeout/cancel/process-tree cleanup] 完成输入校验、领域处理、持久化或投影、用户反馈和可重复验收。；然后产生领域事件或标准结果并更新必要投影；最后返回成功、等待、失败或取消结果，并记录阶段进度。

输出为领域事件、查询投影、日志和验收结果。计划中的最小验收为：Ctrl-C 后子孙进程结束。

### 4. 状态、数据与一致性
- 状态变化必须遵守领域模型与事件规范，禁止直接修改 projection 绕过 domain command。
- SQLite Event Store，或本阶段明确声明的 Fake Store，是业务事实来源；日志只用于诊断。
- 外部副作用必须具有 operation_id、幂等策略、取消边界和恢复结论。
- 并发写入遵循单写者或 Write Claim；查询需要说明一致性级别和 cursor。

### 5. 异常、取消与恢复
必须设计输入无效、依赖不可用、权限拒绝、超时、用户取消、进程重启和重复提交路径。每类路径都要定义稳定 error_code、用户提示、是否可重试、是否产生事件和重启动作。不安全时进入明确的 blocked、failed 或 interrupted，不能静默成功。

### 6. 日志与安全要求
入口初始化日志，记录 stage.started、关键进度、stage.completed 或 stage.failed。日志采用非 JSON、非 key=value 的单行位置式格式：时间、级别、PID、任务链 UUID、线程或协程、logger、最后一级文件名和行号、主消息。不记录客户端地址；诊断上下文仅在 DEBUG 级别展示并置于主消息前，INFO 及以上隐藏。必须保持 Port、幂等、取消、恢复和审计边界。

### 7. 测试与可运行验收
- 必须提供 cargo 命令、CLI 子命令、集成测试或可重复 demo。
- 至少覆盖成功、失败、取消、重复调用以及适用的恢复或回滚路径。
- 日志断言任务链 UUID、稳定 message code、进度闭环、真实文件名和行号、无敏感信息泄漏。
- 回滚只能删除本阶段临时产物或恢复快照，不得删除用户数据或覆盖外部修改。

### 8. 后续代码注释要求
编码前必须通过本条目审核。实现代码需要为公共类型和函数、状态转换、协议字段、数据库迁移、异步任务边界、错误分支和安全判定添加中文详细注释。注释应解释设计原因、约束和失败恢复；复杂算法、并发或锁、取消传播、幂等键和安全边界还要写明前置条件、后置条件及不变量。测试代码需要说明 fixture 意图和验收理由。

### 9. 审核结论栏
- 需求理解：待审核
- 架构边界：待审核
- 接口和事件：待审核
- 异常与恢复：待审核
- 安全与日志：待审核
- 允许进入代码开发：否

<!-- END S15-004 -->
## S15-005：output truncation

### 1. 功能目标
本功能属于 S15。建立 Bash AST 风险分类、审批、隔离执行、超时取消和输出截断。 本项聚焦于领域功能、应用服务和可运行入口，必须形成可以独立启动、测试或演示的最小闭环。

### 2. 范围与边界
- 包含：output truncation所需契约、状态转换、错误返回、日志事件和自动化验收。
- 不包含：未列入本阶段的真实 Provider、跨阶段优化或视觉润色；依赖能力只能通过 Port、Fake 或 fixture 接入。
- 上游依赖：S15-004。依赖未 verified 时，本功能只能处于 blocked。
- 主要产物：领域事件、查询投影、日志和验收结果。

### 3. 输入、输出与处理流程
输入来自上游命令、查询、事件、配置、用户动作或 fixture，必须具备稳定标识、版本和必要授权上下文。

处理顺序：首先校验格式、当前状态、权限和依赖版本；其次，围绕功能 [output truncation] 完成输入校验、领域处理、持久化或投影、用户反馈和可重复验收。；然后产生领域事件或标准结果并更新必要投影；最后返回成功、等待、失败或取消结果，并记录阶段进度。

输出为领域事件、查询投影、日志和验收结果。计划中的最小验收为：stdout/stderr 限长，digest 和 byte count 保留。

### 4. 状态、数据与一致性
- 状态变化必须遵守领域模型与事件规范，禁止直接修改 projection 绕过 domain command。
- SQLite Event Store，或本阶段明确声明的 Fake Store，是业务事实来源；日志只用于诊断。
- 外部副作用必须具有 operation_id、幂等策略、取消边界和恢复结论。
- 并发写入遵循单写者或 Write Claim；查询需要说明一致性级别和 cursor。

### 5. 异常、取消与恢复
必须设计输入无效、依赖不可用、权限拒绝、超时、用户取消、进程重启和重复提交路径。每类路径都要定义稳定 error_code、用户提示、是否可重试、是否产生事件和重启动作。不安全时进入明确的 blocked、failed 或 interrupted，不能静默成功。

### 6. 日志与安全要求
入口初始化日志，记录 stage.started、关键进度、stage.completed 或 stage.failed。日志采用非 JSON、非 key=value 的单行位置式格式：时间、级别、PID、任务链 UUID、线程或协程、logger、最后一级文件名和行号、主消息。不记录客户端地址；诊断上下文仅在 DEBUG 级别展示并置于主消息前，INFO 及以上隐藏。必须保持 Port、幂等、取消、恢复和审计边界。

### 7. 测试与可运行验收
- 必须提供 cargo 命令、CLI 子命令、集成测试或可重复 demo。
- 至少覆盖成功、失败、取消、重复调用以及适用的恢复或回滚路径。
- 日志断言任务链 UUID、稳定 message code、进度闭环、真实文件名和行号、无敏感信息泄漏。
- 回滚只能删除本阶段临时产物或恢复快照，不得删除用户数据或覆盖外部修改。

### 8. 后续代码注释要求
编码前必须通过本条目审核。实现代码需要为公共类型和函数、状态转换、协议字段、数据库迁移、异步任务边界、错误分支和安全判定添加中文详细注释。注释应解释设计原因、约束和失败恢复；复杂算法、并发或锁、取消传播、幂等键和安全边界还要写明前置条件、后置条件及不变量。测试代码需要说明 fixture 意图和验收理由。

### 9. 审核结论栏
- 需求理解：待审核
- 架构边界：待审核
- 接口和事件：待审核
- 异常与恢复：待审核
- 安全与日志：待审核
- 允许进入代码开发：否

<!-- END S15-005 -->
## S15-006：shell audit

### 1. 功能目标
本功能属于 S15。建立 Bash AST 风险分类、审批、隔离执行、超时取消和输出截断。 本项聚焦于安全策略、授权上下文和审批生命周期，必须形成可以独立启动、测试或演示的最小闭环。

### 2. 范围与边界
- 包含：shell audit所需契约、状态转换、错误返回、日志事件和自动化验收。
- 不包含：未列入本阶段的真实 Provider、跨阶段优化或视觉润色；依赖能力只能通过 Port、Fake 或 fixture 接入。
- 上游依赖：S15-005。依赖未 verified 时，本功能只能处于 blocked。
- 主要产物：决策、理由、审批事件和安全报告。

### 3. 输入、输出与处理流程
输入来自上游命令、查询、事件、配置、用户动作或 fixture，必须具备稳定标识、版本和必要授权上下文。

处理顺序：首先校验格式、当前状态、权限和依赖版本；其次，围绕功能 [shell audit] 完成输入校验、领域处理、持久化或投影、用户反馈和可重复验收。；然后产生领域事件或标准结果并更新必要投影；最后返回成功、等待、失败或取消结果，并记录阶段进度。

输出为决策、理由、审批事件和安全报告。计划中的最小验收为：command fingerprint、decision、exit class 可查询。

### 4. 状态、数据与一致性
- 状态变化必须遵守领域模型与事件规范，禁止直接修改 projection 绕过 domain command。
- SQLite Event Store，或本阶段明确声明的 Fake Store，是业务事实来源；日志只用于诊断。
- 外部副作用必须具有 operation_id、幂等策略、取消边界和恢复结论。
- 并发写入遵循单写者或 Write Claim；查询需要说明一致性级别和 cursor。

### 5. 异常、取消与恢复
必须设计输入无效、依赖不可用、权限拒绝、超时、用户取消、进程重启和重复提交路径。每类路径都要定义稳定 error_code、用户提示、是否可重试、是否产生事件和重启动作。不安全时进入明确的 blocked、failed 或 interrupted，不能静默成功。

### 6. 日志与安全要求
入口初始化日志，记录 stage.started、关键进度、stage.completed 或 stage.failed。日志采用非 JSON、非 key=value 的单行位置式格式：时间、级别、PID、任务链 UUID、线程或协程、logger、最后一级文件名和行号、主消息。不记录客户端地址；诊断上下文仅在 DEBUG 级别展示并置于主消息前，INFO 及以上隐藏。高风险操作默认拒绝，任何放行必须可审计。

### 7. 测试与可运行验收
- 必须提供 cargo 命令、CLI 子命令、集成测试或可重复 demo。
- 至少覆盖成功、失败、取消、重复调用以及适用的恢复或回滚路径。
- 日志断言任务链 UUID、稳定 message code、进度闭环、真实文件名和行号、无敏感信息泄漏。
- 回滚只能删除本阶段临时产物或恢复快照，不得删除用户数据或覆盖外部修改。

### 8. 后续代码注释要求
编码前必须通过本条目审核。实现代码需要为公共类型和函数、状态转换、协议字段、数据库迁移、异步任务边界、错误分支和安全判定添加中文详细注释。注释应解释设计原因、约束和失败恢复；复杂算法、并发或锁、取消传播、幂等键和安全边界还要写明前置条件、后置条件及不变量。测试代码需要说明 fixture 意图和验收理由。

### 9. 审核结论栏
- 需求理解：待审核
- 架构边界：待审核
- 接口和事件：待审核
- 异常与恢复：待审核
- 安全与日志：待审核
- 允许进入代码开发：否

<!-- END S15-006 -->
## S15-007：`v0.1-bash-beta` demo

### 1. 功能目标
本功能属于 S15。建立 Bash AST 风险分类、审批、隔离执行、超时取消和输出截断。 本项聚焦于Tool Gateway、工具契约和副作用隔离，必须形成可以独立启动、测试或演示的最小闭环。

### 2. 范围与边界
- 包含：`v0.1-bash-beta` demo所需契约、状态转换、错误返回、日志事件和自动化验收。
- 不包含：未列入本阶段的真实 Provider、跨阶段优化或视觉润色；依赖能力只能通过 Port、Fake 或 fixture 接入。
- 上游依赖：S15-006。依赖未 verified 时，本功能只能处于 blocked。
- 主要产物：ToolCall、标准结果、变更和快照摘要。

### 3. 输入、输出与处理流程
输入来自上游命令、查询、事件、配置、用户动作或 fixture，必须具备稳定标识、版本和必要授权上下文。

处理顺序：首先校验格式、当前状态、权限和依赖版本；其次，围绕功能 [`v0.1-bash-beta` demo] 完成输入校验、领域处理、持久化或投影、用户反馈和可重复验收。；然后产生领域事件或标准结果并更新必要投影；最后返回成功、等待、失败或取消结果，并记录阶段进度。

输出为ToolCall、标准结果、变更和快照摘要。计划中的最小验收为：执行安全测试命令并拒绝危险命令。

### 4. 状态、数据与一致性
- 状态变化必须遵守领域模型与事件规范，禁止直接修改 projection 绕过 domain command。
- SQLite Event Store，或本阶段明确声明的 Fake Store，是业务事实来源；日志只用于诊断。
- 外部副作用必须具有 operation_id、幂等策略、取消边界和恢复结论。
- 并发写入遵循单写者或 Write Claim；查询需要说明一致性级别和 cursor。

### 5. 异常、取消与恢复
必须设计输入无效、依赖不可用、权限拒绝、超时、用户取消、进程重启和重复提交路径。每类路径都要定义稳定 error_code、用户提示、是否可重试、是否产生事件和重启动作。不安全时进入明确的 blocked、failed 或 interrupted，不能静默成功。

### 6. 日志与安全要求
入口初始化日志，记录 stage.started、关键进度、stage.completed 或 stage.failed。日志采用非 JSON、非 key=value 的单行位置式格式：时间、级别、PID、任务链 UUID、线程或协程、logger、最后一级文件名和行号、主消息。不记录客户端地址；诊断上下文仅在 DEBUG 级别展示并置于主消息前，INFO 及以上隐藏。工具不得绕过 Gateway、权限、取消、超时和日志门禁。

### 7. 测试与可运行验收
- 必须提供 cargo 命令、CLI 子命令、集成测试或可重复 demo。
- 至少覆盖成功、失败、取消、重复调用以及适用的恢复或回滚路径。
- 日志断言任务链 UUID、稳定 message code、进度闭环、真实文件名和行号、无敏感信息泄漏。
- 回滚只能删除本阶段临时产物或恢复快照，不得删除用户数据或覆盖外部修改。

### 8. 后续代码注释要求
编码前必须通过本条目审核。实现代码需要为公共类型和函数、状态转换、协议字段、数据库迁移、异步任务边界、错误分支和安全判定添加中文详细注释。注释应解释设计原因、约束和失败恢复；复杂算法、并发或锁、取消传播、幂等键和安全边界还要写明前置条件、后置条件及不变量。测试代码需要说明 fixture 意图和验收理由。

### 9. 审核结论栏
- 需求理解：待审核
- 架构边界：待审核
- 接口和事件：待审核
- 异常与恢复：待审核
- 安全与日志：待审核
- 允许进入代码开发：否

<!-- END S15-007 -->
## S16-001：Task schema

### 1. 功能目标
本功能属于 S16。建立受限线性子任务的委派、预算、取消、结果回传和失败隔离。 本项聚焦于领域功能、应用服务和可运行入口，必须形成可以独立启动、测试或演示的最小闭环。

### 2. 范围与边界
- 包含：Task schema所需契约、状态转换、错误返回、日志事件和自动化验收。
- 不包含：未列入本阶段的真实 Provider、跨阶段优化或视觉润色；依赖能力只能通过 Port、Fake 或 fixture 接入。
- 上游依赖：S15。依赖未 verified 时，本功能只能处于 blocked。
- 主要产物：领域事件、查询投影、日志和验收结果。

### 3. 输入、输出与处理流程
输入来自上游命令、查询、事件、配置、用户动作或 fixture，必须具备稳定标识、版本和必要授权上下文。

处理顺序：首先校验格式、当前状态、权限和依赖版本；其次，围绕功能 [Task schema] 完成输入校验、领域处理、持久化或投影、用户反馈和可重复验收。；然后产生领域事件或标准结果并更新必要投影；最后返回成功、等待、失败或取消结果，并记录阶段进度。

输出为领域事件、查询投影、日志和验收结果。计划中的最小验收为：parent_run、task_id、write_scope、status 可持久化。

### 4. 状态、数据与一致性
- 状态变化必须遵守领域模型与事件规范，禁止直接修改 projection 绕过 domain command。
- SQLite Event Store，或本阶段明确声明的 Fake Store，是业务事实来源；日志只用于诊断。
- 外部副作用必须具有 operation_id、幂等策略、取消边界和恢复结论。
- 并发写入遵循单写者或 Write Claim；查询需要说明一致性级别和 cursor。

### 5. 异常、取消与恢复
必须设计输入无效、依赖不可用、权限拒绝、超时、用户取消、进程重启和重复提交路径。每类路径都要定义稳定 error_code、用户提示、是否可重试、是否产生事件和重启动作。不安全时进入明确的 blocked、failed 或 interrupted，不能静默成功。

### 6. 日志与安全要求
入口初始化日志，记录 stage.started、关键进度、stage.completed 或 stage.failed。日志采用非 JSON、非 key=value 的单行位置式格式：时间、级别、PID、任务链 UUID、线程或协程、logger、最后一级文件名和行号、主消息。不记录客户端地址；诊断上下文仅在 DEBUG 级别展示并置于主消息前，INFO 及以上隐藏。必须保持 Port、幂等、取消、恢复和审计边界。

### 7. 测试与可运行验收
- 必须提供 cargo 命令、CLI 子命令、集成测试或可重复 demo。
- 至少覆盖成功、失败、取消、重复调用以及适用的恢复或回滚路径。
- 日志断言任务链 UUID、稳定 message code、进度闭环、真实文件名和行号、无敏感信息泄漏。
- 回滚只能删除本阶段临时产物或恢复快照，不得删除用户数据或覆盖外部修改。

### 8. 后续代码注释要求
编码前必须通过本条目审核。实现代码需要为公共类型和函数、状态转换、协议字段、数据库迁移、异步任务边界、错误分支和安全判定添加中文详细注释。注释应解释设计原因、约束和失败恢复；复杂算法、并发或锁、取消传播、幂等键和安全边界还要写明前置条件、后置条件及不变量。测试代码需要说明 fixture 意图和验收理由。

### 9. 审核结论栏
- 需求理解：待审核
- 架构边界：待审核
- 接口和事件：待审核
- 异常与恢复：待审核
- 安全与日志：待审核
- 允许进入代码开发：否

<!-- END S16-001 -->
## S16-002：sequential task runner

### 1. 功能目标
本功能属于 S16。建立受限线性子任务的委派、预算、取消、结果回传和失败隔离。 本项聚焦于领域功能、应用服务和可运行入口，必须形成可以独立启动、测试或演示的最小闭环。

### 2. 范围与边界
- 包含：sequential task runner所需契约、状态转换、错误返回、日志事件和自动化验收。
- 不包含：未列入本阶段的真实 Provider、跨阶段优化或视觉润色；依赖能力只能通过 Port、Fake 或 fixture 接入。
- 上游依赖：S16-001。依赖未 verified 时，本功能只能处于 blocked。
- 主要产物：领域事件、查询投影、日志和验收结果。

### 3. 输入、输出与处理流程
输入来自上游命令、查询、事件、配置、用户动作或 fixture，必须具备稳定标识、版本和必要授权上下文。

处理顺序：首先校验格式、当前状态、权限和依赖版本；其次，围绕功能 [sequential task runner] 完成输入校验、领域处理、持久化或投影、用户反馈和可重复验收。；然后产生领域事件或标准结果并更新必要投影；最后返回成功、等待、失败或取消结果，并记录阶段进度。

输出为领域事件、查询投影、日志和验收结果。计划中的最小验收为：依赖顺序执行，不实现并行 DAG。

### 4. 状态、数据与一致性
- 状态变化必须遵守领域模型与事件规范，禁止直接修改 projection 绕过 domain command。
- SQLite Event Store，或本阶段明确声明的 Fake Store，是业务事实来源；日志只用于诊断。
- 外部副作用必须具有 operation_id、幂等策略、取消边界和恢复结论。
- 并发写入遵循单写者或 Write Claim；查询需要说明一致性级别和 cursor。

### 5. 异常、取消与恢复
必须设计输入无效、依赖不可用、权限拒绝、超时、用户取消、进程重启和重复提交路径。每类路径都要定义稳定 error_code、用户提示、是否可重试、是否产生事件和重启动作。不安全时进入明确的 blocked、failed 或 interrupted，不能静默成功。

### 6. 日志与安全要求
入口初始化日志，记录 stage.started、关键进度、stage.completed 或 stage.failed。日志采用非 JSON、非 key=value 的单行位置式格式：时间、级别、PID、任务链 UUID、线程或协程、logger、最后一级文件名和行号、主消息。不记录客户端地址；诊断上下文仅在 DEBUG 级别展示并置于主消息前，INFO 及以上隐藏。必须保持 Port、幂等、取消、恢复和审计边界。

### 7. 测试与可运行验收
- 必须提供 cargo 命令、CLI 子命令、集成测试或可重复 demo。
- 至少覆盖成功、失败、取消、重复调用以及适用的恢复或回滚路径。
- 日志断言任务链 UUID、稳定 message code、进度闭环、真实文件名和行号、无敏感信息泄漏。
- 回滚只能删除本阶段临时产物或恢复快照，不得删除用户数据或覆盖外部修改。

### 8. 后续代码注释要求
编码前必须通过本条目审核。实现代码需要为公共类型和函数、状态转换、协议字段、数据库迁移、异步任务边界、错误分支和安全判定添加中文详细注释。注释应解释设计原因、约束和失败恢复；复杂算法、并发或锁、取消传播、幂等键和安全边界还要写明前置条件、后置条件及不变量。测试代码需要说明 fixture 意图和验收理由。

### 9. 审核结论栏
- 需求理解：待审核
- 架构边界：待审核
- 接口和事件：待审核
- 异常与恢复：待审核
- 安全与日志：待审核
- 允许进入代码开发：否

<!-- END S16-002 -->
## S16-003：child context

### 1. 功能目标
本功能属于 S16。建立受限线性子任务的委派、预算、取消、结果回传和失败隔离。 本项聚焦于上下文条目、预算和 Checkpoint，必须形成可以独立启动、测试或演示的最小闭环。

### 2. 范围与边界
- 包含：child context所需契约、状态转换、错误返回、日志事件和自动化验收。
- 不包含：未列入本阶段的真实 Provider、跨阶段优化或视觉润色；依赖能力只能通过 Port、Fake 或 fixture 接入。
- 上游依赖：S16-002。依赖未 verified 时，本功能只能处于 blocked。
- 主要产物：预算报告、摘要、版本和恢复上下文。

### 3. 输入、输出与处理流程
输入来自上游命令、查询、事件、配置、用户动作或 fixture，必须具备稳定标识、版本和必要授权上下文。

处理顺序：首先校验格式、当前状态、权限和依赖版本；其次，围绕功能 [child context] 完成输入校验、领域处理、持久化或投影、用户反馈和可重复验收。；然后产生领域事件或标准结果并更新必要投影；最后返回成功、等待、失败或取消结果，并记录阶段进度。

输出为预算报告、摘要、版本和恢复上下文。计划中的最小验收为：子任务继承安全上下文但拥有独立 operation。

### 4. 状态、数据与一致性
- 状态变化必须遵守领域模型与事件规范，禁止直接修改 projection 绕过 domain command。
- SQLite Event Store，或本阶段明确声明的 Fake Store，是业务事实来源；日志只用于诊断。
- 外部副作用必须具有 operation_id、幂等策略、取消边界和恢复结论。
- 并发写入遵循单写者或 Write Claim；查询需要说明一致性级别和 cursor。

### 5. 异常、取消与恢复
必须设计输入无效、依赖不可用、权限拒绝、超时、用户取消、进程重启和重复提交路径。每类路径都要定义稳定 error_code、用户提示、是否可重试、是否产生事件和重启动作。不安全时进入明确的 blocked、failed 或 interrupted，不能静默成功。

### 6. 日志与安全要求
入口初始化日志，记录 stage.started、关键进度、stage.completed 或 stage.failed。日志采用非 JSON、非 key=value 的单行位置式格式：时间、级别、PID、任务链 UUID、线程或协程、logger、最后一级文件名和行号、主消息。不记录客户端地址；诊断上下文仅在 DEBUG 级别展示并置于主消息前，INFO 及以上隐藏。压缩不得丢失任务约束、工具事实和用户确认。

### 7. 测试与可运行验收
- 必须提供 cargo 命令、CLI 子命令、集成测试或可重复 demo。
- 至少覆盖成功、失败、取消、重复调用以及适用的恢复或回滚路径。
- 日志断言任务链 UUID、稳定 message code、进度闭环、真实文件名和行号、无敏感信息泄漏。
- 回滚只能删除本阶段临时产物或恢复快照，不得删除用户数据或覆盖外部修改。

### 8. 后续代码注释要求
编码前必须通过本条目审核。实现代码需要为公共类型和函数、状态转换、协议字段、数据库迁移、异步任务边界、错误分支和安全判定添加中文详细注释。注释应解释设计原因、约束和失败恢复；复杂算法、并发或锁、取消传播、幂等键和安全边界还要写明前置条件、后置条件及不变量。测试代码需要说明 fixture 意图和验收理由。

### 9. 审核结论栏
- 需求理解：待审核
- 架构边界：待审核
- 接口和事件：待审核
- 异常与恢复：待审核
- 安全与日志：待审核
- 允许进入代码开发：否

<!-- END S16-003 -->
## S16-004：child failure handling

### 1. 功能目标
本功能属于 S16。建立受限线性子任务的委派、预算、取消、结果回传和失败隔离。 本项聚焦于领域功能、应用服务和可运行入口，必须形成可以独立启动、测试或演示的最小闭环。

### 2. 范围与边界
- 包含：child failure handling所需契约、状态转换、错误返回、日志事件和自动化验收。
- 不包含：未列入本阶段的真实 Provider、跨阶段优化或视觉润色；依赖能力只能通过 Port、Fake 或 fixture 接入。
- 上游依赖：S16-003。依赖未 verified 时，本功能只能处于 blocked。
- 主要产物：领域事件、查询投影、日志和验收结果。

### 3. 输入、输出与处理流程
输入来自上游命令、查询、事件、配置、用户动作或 fixture，必须具备稳定标识、版本和必要授权上下文。

处理顺序：首先校验格式、当前状态、权限和依赖版本；其次，围绕功能 [child failure handling] 完成输入校验、领域处理、持久化或投影、用户反馈和可重复验收。；然后产生领域事件或标准结果并更新必要投影；最后返回成功、等待、失败或取消结果，并记录阶段进度。

输出为领域事件、查询投影、日志和验收结果。计划中的最小验收为：失败暂停父任务并显示原因。

### 4. 状态、数据与一致性
- 状态变化必须遵守领域模型与事件规范，禁止直接修改 projection 绕过 domain command。
- SQLite Event Store，或本阶段明确声明的 Fake Store，是业务事实来源；日志只用于诊断。
- 外部副作用必须具有 operation_id、幂等策略、取消边界和恢复结论。
- 并发写入遵循单写者或 Write Claim；查询需要说明一致性级别和 cursor。

### 5. 异常、取消与恢复
必须设计输入无效、依赖不可用、权限拒绝、超时、用户取消、进程重启和重复提交路径。每类路径都要定义稳定 error_code、用户提示、是否可重试、是否产生事件和重启动作。不安全时进入明确的 blocked、failed 或 interrupted，不能静默成功。

### 6. 日志与安全要求
入口初始化日志，记录 stage.started、关键进度、stage.completed 或 stage.failed。日志采用非 JSON、非 key=value 的单行位置式格式：时间、级别、PID、任务链 UUID、线程或协程、logger、最后一级文件名和行号、主消息。不记录客户端地址；诊断上下文仅在 DEBUG 级别展示并置于主消息前，INFO 及以上隐藏。必须保持 Port、幂等、取消、恢复和审计边界。

### 7. 测试与可运行验收
- 必须提供 cargo 命令、CLI 子命令、集成测试或可重复 demo。
- 至少覆盖成功、失败、取消、重复调用以及适用的恢复或回滚路径。
- 日志断言任务链 UUID、稳定 message code、进度闭环、真实文件名和行号、无敏感信息泄漏。
- 回滚只能删除本阶段临时产物或恢复快照，不得删除用户数据或覆盖外部修改。

### 8. 后续代码注释要求
编码前必须通过本条目审核。实现代码需要为公共类型和函数、状态转换、协议字段、数据库迁移、异步任务边界、错误分支和安全判定添加中文详细注释。注释应解释设计原因、约束和失败恢复；复杂算法、并发或锁、取消传播、幂等键和安全边界还要写明前置条件、后置条件及不变量。测试代码需要说明 fixture 意图和验收理由。

### 9. 审核结论栏
- 需求理解：待审核
- 架构边界：待审核
- 接口和事件：待审核
- 异常与恢复：待审核
- 安全与日志：待审核
- 允许进入代码开发：否

<!-- END S16-004 -->
## S16-005：Task TUI panel

### 1. 功能目标
本功能属于 S16。建立受限线性子任务的委派、预算、取消、结果回传和失败隔离。 本项聚焦于输入事件、纯状态转换和终端渲染，必须形成可以独立启动、测试或演示的最小闭环。

### 2. 范围与边界
- 包含：Task TUI panel所需契约、状态转换、错误返回、日志事件和自动化验收。
- 不包含：未列入本阶段的真实 Provider、跨阶段优化或视觉润色；依赖能力只能通过 Port、Fake 或 fixture 接入。
- 上游依赖：S16-004。依赖未 verified 时，本功能只能处于 blocked。
- 主要产物：AppState、Effect、渲染帧和用户提示。

### 3. 输入、输出与处理流程
输入来自上游命令、查询、事件、配置、用户动作或 fixture，必须具备稳定标识、版本和必要授权上下文。

处理顺序：首先校验格式、当前状态、权限和依赖版本；其次，围绕功能 [Task TUI panel] 完成输入校验、领域处理、持久化或投影、用户反馈和可重复验收。；然后产生领域事件或标准结果并更新必要投影；最后返回成功、等待、失败或取消结果，并记录阶段进度。

输出为AppState、Effect、渲染帧和用户提示。计划中的最小验收为：子任务状态、进度、输出摘要。

### 4. 状态、数据与一致性
- 状态变化必须遵守领域模型与事件规范，禁止直接修改 projection 绕过 domain command。
- SQLite Event Store，或本阶段明确声明的 Fake Store，是业务事实来源；日志只用于诊断。
- 外部副作用必须具有 operation_id、幂等策略、取消边界和恢复结论。
- 并发写入遵循单写者或 Write Claim；查询需要说明一致性级别和 cursor。

### 5. 异常、取消与恢复
必须设计输入无效、依赖不可用、权限拒绝、超时、用户取消、进程重启和重复提交路径。每类路径都要定义稳定 error_code、用户提示、是否可重试、是否产生事件和重启动作。不安全时进入明确的 blocked、failed 或 interrupted，不能静默成功。

### 6. 日志与安全要求
入口初始化日志，记录 stage.started、关键进度、stage.completed 或 stage.failed。日志采用非 JSON、非 key=value 的单行位置式格式：时间、级别、PID、任务链 UUID、线程或协程、logger、最后一级文件名和行号、主消息。不记录客户端地址；诊断上下文仅在 DEBUG 级别展示并置于主消息前，INFO 及以上隐藏。渲染层不得产生业务副作用，窄终端和 Unicode 不得越界。

### 7. 测试与可运行验收
- 必须提供 cargo 命令、CLI 子命令、集成测试或可重复 demo。
- 至少覆盖成功、失败、取消、重复调用以及适用的恢复或回滚路径。
- 日志断言任务链 UUID、稳定 message code、进度闭环、真实文件名和行号、无敏感信息泄漏。
- 回滚只能删除本阶段临时产物或恢复快照，不得删除用户数据或覆盖外部修改。

### 8. 后续代码注释要求
编码前必须通过本条目审核。实现代码需要为公共类型和函数、状态转换、协议字段、数据库迁移、异步任务边界、错误分支和安全判定添加中文详细注释。注释应解释设计原因、约束和失败恢复；复杂算法、并发或锁、取消传播、幂等键和安全边界还要写明前置条件、后置条件及不变量。测试代码需要说明 fixture 意图和验收理由。

### 9. 审核结论栏
- 需求理解：待审核
- 架构边界：待审核
- 接口和事件：待审核
- 异常与恢复：待审核
- 安全与日志：待审核
- 允许进入代码开发：否

<!-- END S16-005 -->
## S16-006：`v0.1-task-beta` demo

### 1. 功能目标
本功能属于 S16。建立受限线性子任务的委派、预算、取消、结果回传和失败隔离。 本项聚焦于领域功能、应用服务和可运行入口，必须形成可以独立启动、测试或演示的最小闭环。

### 2. 范围与边界
- 包含：`v0.1-task-beta` demo所需契约、状态转换、错误返回、日志事件和自动化验收。
- 不包含：未列入本阶段的真实 Provider、跨阶段优化或视觉润色；依赖能力只能通过 Port、Fake 或 fixture 接入。
- 上游依赖：S16-005。依赖未 verified 时，本功能只能处于 blocked。
- 主要产物：领域事件、查询投影、日志和验收结果。

### 3. 输入、输出与处理流程
输入来自上游命令、查询、事件、配置、用户动作或 fixture，必须具备稳定标识、版本和必要授权上下文。

处理顺序：首先校验格式、当前状态、权限和依赖版本；其次，围绕功能 [`v0.1-task-beta` demo] 完成输入校验、领域处理、持久化或投影、用户反馈和可重复验收。；然后产生领域事件或标准结果并更新必要投影；最后返回成功、等待、失败或取消结果，并记录阶段进度。

输出为领域事件、查询投影、日志和验收结果。计划中的最小验收为：父任务运行两个线性子任务并恢复。

### 4. 状态、数据与一致性
- 状态变化必须遵守领域模型与事件规范，禁止直接修改 projection 绕过 domain command。
- SQLite Event Store，或本阶段明确声明的 Fake Store，是业务事实来源；日志只用于诊断。
- 外部副作用必须具有 operation_id、幂等策略、取消边界和恢复结论。
- 并发写入遵循单写者或 Write Claim；查询需要说明一致性级别和 cursor。

### 5. 异常、取消与恢复
必须设计输入无效、依赖不可用、权限拒绝、超时、用户取消、进程重启和重复提交路径。每类路径都要定义稳定 error_code、用户提示、是否可重试、是否产生事件和重启动作。不安全时进入明确的 blocked、failed 或 interrupted，不能静默成功。

### 6. 日志与安全要求
入口初始化日志，记录 stage.started、关键进度、stage.completed 或 stage.failed。日志采用非 JSON、非 key=value 的单行位置式格式：时间、级别、PID、任务链 UUID、线程或协程、logger、最后一级文件名和行号、主消息。不记录客户端地址；诊断上下文仅在 DEBUG 级别展示并置于主消息前，INFO 及以上隐藏。必须保持 Port、幂等、取消、恢复和审计边界。

### 7. 测试与可运行验收
- 必须提供 cargo 命令、CLI 子命令、集成测试或可重复 demo。
- 至少覆盖成功、失败、取消、重复调用以及适用的恢复或回滚路径。
- 日志断言任务链 UUID、稳定 message code、进度闭环、真实文件名和行号、无敏感信息泄漏。
- 回滚只能删除本阶段临时产物或恢复快照，不得删除用户数据或覆盖外部修改。

### 8. 后续代码注释要求
编码前必须通过本条目审核。实现代码需要为公共类型和函数、状态转换、协议字段、数据库迁移、异步任务边界、错误分支和安全判定添加中文详细注释。注释应解释设计原因、约束和失败恢复；复杂算法、并发或锁、取消传播、幂等键和安全边界还要写明前置条件、后置条件及不变量。测试代码需要说明 fixture 意图和验收理由。

### 9. 审核结论栏
- 需求理解：待审核
- 架构边界：待审核
- 接口和事件：待审核
- 异常与恢复：待审核
- 安全与日志：待审核
- 允许进入代码开发：否

<!-- END S16-006 -->
