# 进度日志

## 会话：2026-08-12

### Phase 10：版本迭代执行计划（参考实现提交史分析）
- **状态：** complete
- **目标：** 基于 Reasonix/Pi 真实提交历史分析其开发迭代流程，生成按版本切片（v0.1–v1.3）的原子化执行计划；v1.0 及之前仅 TUI 端，Desktop/Web 顺延至 v1.1+。
- 执行的操作：
  - 分析 Reasonix（4492 条提交）最早约 220 条与 Pi（5543 条提交）v0.5.x–v0.84 全程，提炼 10 条共同迭代模式（P1–P10：内核先行、设计文档先行、编号工作包、纵向切片、CI 先行、changelog 纪律、测试扫荡、pin test、生态后置、兼容层收敛）。
  - 新建 `docs/17-version-iteration-execution-plan.md`：13 个版本（v0.1 Core Loop → v1.3 Trinity），每版本含目标、入口门、WI 原子任务表（映射文档 16 的 EP 编号）、退出标准；v0.1/v0.2/v0.3 细化到 0.5–5 人日粒度的 WI 工作项。
  - 按"EP/VAL 只追加不重用"规则新增注册编号 EP-1201–EP-1208、VAL-214–VAL-221（附录 B 登记）。
  - 关键范围决策：v0.1 权限用简化清单模式（EP-1201，预登记 v0.3 Superseded）；EP-0814 音频顺延 v1.1；EP-0308/0309 控制租约顺延 v1.2（TUI 单控制端退化恒成立）。
  - 更新 `README.md` 阅读顺序第 18 项，注册新文档。
- 创建/修改的文件：
  - `docs/17-version-iteration-execution-plan.md`（新建）
  - `README.md`（导航追加）
  - `progress.md`（本条目）

## 会话：2026-08-11

### Phase 6：原子化功能开发执行计划
- **状态：** complete
- **目标：** 只生成可执行的细粒度开发计划，不实现代码。
- 执行的操作：
  - 重新读取 L4 Spec 驱动与 planning-with-files 技能要求。
  - 回读 task_plan、progress、findings，确认既有架构文档为本计划的约束来源。
  - 新建 `docs/16-implementation-execution-plan.md`，按 S0–S11 拆分 214 个原子 EP 任务。
  - 为每个 EP 指定依赖、需求/验收映射、产出和独立 VAL；补充 G-0–G-8 阶段门。
  - 补充全局任务流程、S1/S3/S5/S7/S9/S10/S11 验证流程图，以及失败修复和发布回滚流程图。
  - 更新根 README、docs 总册和质量路线图导航。

### 阶段 1：需求基线与资料盘点
- **状态：** complete
- **开始时间：** 2026-08-11
- 执行的操作：
  - 完成 115 项逐一澄清并获得用户对需求基线的明确确认。
  - 读取 Spec 驱动编码与文件规划技能的完整说明。
  - 盘点当前项目 Markdown 文档、Cargo 配置和工作区状态。
  - 确认现有代码删除项属于任务外改动，不进行恢复。
  - 将旧 README 与 docs 顶层 27 份 Markdown 归档到 `docs/archive/legacy-2026-08-11/`。
  - 阅读 AiAgent 的跨项目横向索引，提取 Context、权限、多 Agent、会话恢复、安全和扩展系统的成熟模式。
  - 阅读 DeepSeek-TUI 的架构、Runtime API、Subagent、Memory、MCP 与 Cargo workspace 文档。
  - 阅读 claude-code-rust 的进程桥接和发布架构，以及 pi 的协议与 Agent Loop 契约。
  - 阅读 opencode V2 的 Session、Context Epoch、Provider、Tool 和配置规格，并定位 Reasonix/CodeWhale 的调度与权限实现入口。
  - 回读 Reasonix 的 Scheduler、Write Claim、SnipHinter 和权限策略源码，以及 CodeWhale 的授权顺序契约。
  - 回读 MiMo-Code 的 Checkpoint 模板、校验器、章节预算和阈值触发实现。
  - 回读旧 Apex 总体架构、领域模型、API 协议和两轮一致性审查，确认重复定义、命名漂移与 ADR 回写不可验证是主要失败模式。
- 创建/修改的文件：
  - `task_plan.md`
  - `findings.md`
  - `progress.md`
  - `docs/archive/legacy-2026-08-11/**`（移动归档）

### 阶段 2：文档信息架构与跨文档契约
- **状态：** complete
- 执行的操作：
  - 冻结四级权威关系：需求 What、架构/领域边界、Trait/协议稳定契约、主题流程说明。
  - 确定采用编号文档集和单一 ADR 注册表，主题文档不得复制权威枚举或协议定义。
  - 新建根 README 和文档总册，定义权威顺序、阅读路径与变更规则。
  - 将 115 项已确认要求编号为 `RQ-001`–`RQ-115`，建立目标文档落点和 20 项产品验收标准。
  - 生成总体逻辑架构、部署架构、核心运行时序、信任边界与异常恢复流程。
  - 建立 15 项 ADR 注册表，记录关键取舍、代价与重审条件。
  - 冻结 Cargo Workspace 分层、crate 职责、依赖禁令与生成/发布边界。
  - 冻结领域术语、UUIDv7/内容哈希 ID、状态枚举、事件信封、错误码和消息分层。
  - 定义 Event/File Fact、Session/Lease、Spec/Rules、Permission/Tool、DAG/Context/Provider/Extension 等核心 Trait。
  - 定义本地 gRPC、REST/WebSocket、版本协商、快照+事件重连、Web 认证和三端能力矩阵。
- 创建/修改的文件：
  - `../../../README.md`
  - `README.md`
  - `docs/01-requirements.md`
  - `docs/02-system-architecture.md`
  - `docs/adr/README.md`
  - `docs/03-workspace-and-crates.md`
  - `docs/04-domain-model.md`
  - `docs/05-trait-contracts.md`
  - `docs/06-protocol-and-clients.md`

### 阶段 3：生成全新文档集
- **状态：** complete
- 执行的操作：
  - 已完成 README、需求、架构、工程结构、领域模型、Trait 与协议文档。
  - 完成存储/文件事实/日志/归档设计，并分别给出 Session JSONL 与系统文本日志样例。
  - 完成 Spec/Rules/Verification、三 Shell 静态权限、Tool Gateway 与终端设计。
  - 完成 Context/Checkpoint/Memory、Agent/DAG/Claim/Snapshot/Replay/补偿设计。
  - 完成 Provider/多模态、Skills/MCP/Plugin、安装升级运维设计。
  - 完成风险登记册、覆盖率/NFR/安全发布门与完整产品实施计划。
- 创建/修改的文件：
  - `docs/07-storage-files-logging.md`
  - `docs/08-spec-rules-verification.md`
  - `docs/09-tool-permission-terminal.md`
  - `docs/10-context-checkpoint-memory.md`
  - `docs/11-agent-dag-snapshot-replay.md`
  - `docs/12-provider-multimodal.md`
  - `docs/13-skills-mcp-plugins.md`
  - `docs/14-install-upgrade-operations.md`
  - `docs/15-quality-risks-roadmap.md`

### 阶段 4：一致性与可实现性验证
- **状态：** complete
- 执行的操作：
  - 校验 18 份新权威文档全部存在。
  - 校验 `RQ-001`–`RQ-115` 与 `AC-001`–`AC-020` 连续、唯一且均有有效落点。
  - 校验全部相对链接、Markdown 围栏、Trait 唯一权威和 `.apex` 路径。
  - 校验 30 个 Mermaid 图块的围栏与图类型结构；包含 ER、状态机、Gantt、流程和时序图。
  - 解析验证 JSON、JSONL、TOML 样例；会话 JSONL 每条均有 `trace_id`。
  - 校验 28 份旧 Markdown 与 Git HEAD 逐字节一致地保存在归档目录。
  - 校验无新增非文档改动；用户原有 `apex-log-demo`/`apex-observability` 删除项保持不变。
  - 执行 `git diff --check`，无空白错误。
- 创建/修改的文件：
  - `progress.md`
  - `task_plan.md`

### 阶段 5：交付
- **状态：** complete
- 执行的操作：
  - README 已提供新文档导航、权威顺序和旧版归档入口。
  - 已汇总文档、关键设计与验证证据，准备交由用户架构评审。
- 创建/修改的文件：
  - 无新增。

## 需求基线摘要
- 单用户全局 `apexd`、统一 SQLite、TUI/Tauri/Web 三端。
- Markdown 是 Spec、Checkpoint、Memory、最终验证报告的事实源。
- 强制 Spec、静态权限、Checkpoint-first、多 Provider、多 Agent DAG、Skills/MCP/原生 Plugin。
- 会话 JSONL 签名日志、系统文本日志、120/365 天归档策略。
- 全平台完整产品设计，不实现代码；旧文档归档后全部重建。

## 测试结果
| 测试 | 输入 | 预期结果 | 实际结果 | 状态 |
|------|------|---------|---------|------|
| 工作区状态检查 | `git status --short` | 识别用户已有改动 | 发现代码删除项，已标记为不可触碰 | 通过 |
| 需求审核门 | 用户确认 | 明确允许进入设计阶段 | 已回复“确认” | 通过 |
| 新文档完整性 | 18 个预期路径 | 全部存在 | 18/18 | 通过 |
| 需求追踪 | `RQ-001..115`、`AC-001..020` | 连续唯一 | 115/115、20/20 | 通过 |
| 链接与围栏 | 新 Markdown 集 | 0 断链、0 未闭合围栏 | 0/0 | 通过 |
| Mermaid 结构 | 30 个图块 | 合法图类型与平衡围栏 | 30/30（结构检查） | 通过 |
| 示例格式 | JSON/JSONL/TOML | 可解析且日志有 trace | 全部通过 | 通过 |
| 旧文档归档 | 28 份 HEAD Markdown | 逐字节一致 | 28/28 | 通过 |
| 非文档边界 | Git status | 无新增代码改动 | 0 个意外非文档改动 | 通过 |
| Git 空白检查 | `git diff --check` | 无错误 | 无输出 | 通过 |
| 原子计划结构 | `docs/16-implementation-execution-plan.md` | 214 个唯一 EP、214 个唯一 VAL 引用 | 通过 | 通过 |
| 计划 Mermaid | 执行计划图块 | 9 个图块、围栏平衡 | 通过 | 通过 |
| 计划链接/兼容性 | 全部非归档 Markdown | 无断链；RQ/AC 仍为 115/20 | 通过 | 通过 |

## 错误日志
| 时间戳 | 错误 | 尝试次数 | 解决方案 |
|--------|------|---------|---------|
| 2026-08-11 | 首轮 smoke check 对 4 个标准关键词未命中，虽然语义已存在 | 1 | 在协议/权限/Provider 权威文档补充 Unix Domain Socket、Named Pipe、零 Token、不支持实时视频的显式表述后重验 |
| 2026-08-11 | planning skill 的完成检查脚本只识别英文 `Phase`/`Status`，中文计划格式被计为 0/0 | 1 | 将 task_plan 的阶段/状态标签改为兼容格式，不改变计划内容 |

## 五问重启检查
| 问题 | 答案 |
|------|------|
| 我在哪里？ | 原子化功能开发执行计划已完成，等待用户评审 |
| 我要去哪里？ | 用户确认计划后，未来按 EP/Feature Spec 进入实现（本轮不编码） |
| 目标是什么？ | 只通过文档完整重构 Apex 的目标系统方案 |
| 我学到了什么？ | 见 findings.md 与需求基线摘要 |
| 我做了什么？ | 完成旧文档归档、全量架构重建、原子执行计划和一致性验证 |

---
*每个阶段完成后或遇到错误时更新此文件*

### 阶段 7：项目术语与缩写表
- **状态：** complete
- 执行的操作：
  - 从现行权威文档提取项目编号、领域生命周期、Spec/验证、Context、日志、并发恢复、协议安全、Provider/扩展和发布质量术语。
  - 新增 `docs/00-glossary.md`，明确 `RQ`、`AC`、`EP`、`VAL`、`G`、`RC` 等缩写及其关系。
  - 区分 Session/Run/Turn、Validation/Verification、Checkpoint/Snapshot/Query Snapshot、State Replay/Re-execution Replay。
  - 说明文档权威层级与 Spec 复杂度两套 `L1`–`L4` 的不同语义。
  - 更新根 README、文档总册和领域模型的术语导航。
  - 校验全部非归档 Markdown 的相对链接与代码围栏，校验术语表必备词和消歧说明，并通过 `git diff --check`。
- 创建/修改的文件：
  - `docs/00-glossary.md`
  - `../../../README.md`
  - `README.md`
  - `docs/04-domain-model.md`
  - `task_plan.md`
  - `progress.md`

## 会话：2026-08-12

### 阶段 8：执行计划三端拆分与 TUI 优先化
- **状态：** complete
- **目标：** 只调整文档中的执行计划结构，不实现代码。
- 执行的操作：
  - 重新读取 task_plan、progress、findings，恢复当前计划上下文。
  - 定位 `docs/16-implementation-execution-plan.md` 中的 S10、G-7 和相关引用。
  - 检查 README、文档总册和质量路线图中的计划入口与阶段总览。
  - 准备将 S10 拆分为 TUI / Desktop / Web 三个独立轨道，并在 TUI 前插入测试 demo。
  - 实际完成 S10 结构重排，新增 TUI demo 前置任务，Desktop/Web 后移为独立轨道。
  - 同步更新 task_plan.md 与 findings.md 的执行记录。
- 创建/修改的文件：
  - `task_plan.md`
  - `progress.md`
  - `findings.md`
  - `docs/16-implementation-execution-plan.md`

### 阶段 9：执行计划一致性收尾
- **状态：** complete
- **目标：** 解除残留依赖耦合，并统一高层路线图和执行计划的客户端顺序。
- 执行的操作：
  - 检查 `EP-1025`、`EP-1014` 和 `docs/15-quality-risks-roadmap.md` 的客户端顺序表达。
  - 准备补齐 `docs/16-implementation-execution-plan.md` 与 `docs/15-quality-risks-roadmap.md` 的措辞一致性。
  - 继续同步 `task_plan.md`、`findings.md` 与 `progress.md` 的收尾记录。
  - 实际完成 `EP-1025` 的依赖解耦，以及 `docs/15` 的 TUI 优先化对齐。
- 创建/修改的文件：
  - `task_plan.md`
  - `progress.md`

## 阶段 7 测试结果
| 测试 | 预期结果 | 实际结果 | 状态 |
|---|---|---|---|
| 术语覆盖 | RQ/AC/EP/VAL/G/RC/ADR/NFR 与核心生命周期词均有定义 | 全部命中 | 通过 |
| 易混淆语义 | RC、两套 L1–L4、Session/Run/Turn、三类 Snapshot 可区分 | 均有专节或对照表 | 通过 |
| 相对链接 | 全部非归档 Markdown 无断链 | 无断链 | 通过 |
| Markdown 围栏 | 全部非归档 Markdown 围栏成对 | 通过 | 通过 |
| Git 空白检查 | `git diff --check` 无错误 | 无输出 | 通过 |
| 修改边界 | 不新增代码修改，不触碰既有代码删除项 | 仅新增/更新文档与规划记录 | 通过 |
