# Apex——v0.1 系统分析分卷：S17 Spec 至 S24 发布验收

> 文档状态：待审核，禁止据此进入代码开发。
> 本分卷覆盖 S17 至 S24。

## 0. 分卷使用规则
审核通过前只能修改文档，不得修改 apps、crates、Cargo 配置或运行代码。每个条目的代码注释要求是后续开发强制门禁。

## 1. 功能索引
| 编号 | 功能 | 计划验收 | 依赖 |
|---|---|---|---|
| S17-001 | Spec aggregate/schema | feature、revision、stage、status 可持久化 | S16 |
| S17-002 | requirements artifact | 生成带 frontmatter 的 requirements.md | S17-001 |
| S17-003 | requirements review gate | 用户 approve/reject/edit 后状态正确 | S17-002 |
| S17-004 | design artifact | 生成 design.md 并嵌入 rules/acceptance | S17-003 |
| S17-005 | design review gate | 修改后产生 revision、下游失效 | S17-004 |
| S17-006 | tasks artifact | 生成 tasks.md、依赖和写路径声明 | S17-005 |
| S17-007 | tasks review gate | 确认后才能进入 implementation | S17-006 |
| S17-008 | `v0.1-spec-plan-rc1` demo | 需求→设计→任务三道门逐一确认 | S17-007 |
| S18-001 | implementation runner | 按 tasks 顺序调用 Agent Loop/Tool | S17 |
| S18-002 | artifact versioning | 用户编辑/Agent 修改均有 revision/diff | S18-001 |
| S18-003 | `/skip-spec` command | 跳过决策带理由和 spec_skipped=true | S18-002 |
| S18-004 | verification artifact | 生成 verification.md 和逐条证据 | S18-003 |
| S18-005 | verification review gate | 用户确认后 feature completed | S18-004 |
| S18-006 | invalidation | 上游 requirements/design 变更使下游失效 | S18-005 |
| S18-007 | `v0.1-spec-full-rc1` demo | 正常流水线和 skip-spec 各完成一次 | S18-006 |
| S19-001 | rule source loader | 加载 `apex/rules`、AGENTS/CLAUDE、全局规则 | S18 |
| S19-002 | rule compiler | 规则解析、优先级、revision、diagnostic code | S19-001 |
| S19-003 | changed-file detector | 只检查本次变更文件 | S19-002 |
| S19-004 | format/lint/test adapter | 运行规则命令并收集结果 | S19-003 |
| S19-005 | PostToolUse hook | 每次 Write/Edit 后自动触发增量检查 | S19-004 |
| S19-006 | repair task | error 级问题生成修复子任务 | S19-005 |
| S19-007 | verification gate | gate 阻断或放行有事件和安全理由 | S19-006 |
| S19-008 | rules quality panel | TUI 显示通过、警告、错误、修复中 | S19-007 |
| S19-009 | `v0.1-rules-rc1` demo | 故意引入违规代码，流程自动阻断并修复 | S19-008 |
| S20-001 | context item 分类 | system/rule/spec/history/tool/external 分层 | S19 |
| S20-002 | token budget calculator | 预算、保留区、超限原因可查询 | S20-001 |
| S20-003 | Checkpoint schema | 输入、稳定前缀、摘要、usage、revision 可保存 | S20-002 |
| S20-004 | checkpoint materializer | turn 完成、cancel、tool 后生成 checkpoint | S20-003 |
| S20-005 | compaction strategy | checkpoint-first，分级摘要兜底 | S20-004 |
| S20-006 | prefix cache layout | 稳定前缀和易变后缀分离 | S20-005 |
| S20-007 | restore context | 重启后根据 checkpoint 恢复下一 turn | S20-006 |
| S20-008 | `v0.1-context-rc1` demo | 超长历史压缩后继续完成任务 | S20-007 |
| S21-001 | shadow repository bootstrap | 项目目录外建立隔离 Git metadata | S20 |
| S21-002 | clean baseline snapshot | run 开始前记录基线 commit/digest | S21-001 |
| S21-003 | post-tool snapshot | 每个写操作后生成文件级快照 | S21-002 |
| S21-004 | diff/patch query | TUI 显示变更文件、增删行、摘要 | S21-003 |
| S21-005 | restore snapshot | 用户确认后恢复到指定 checkpoint | S21-004 |
| S21-006 | conflict detection | 外部修改导致 restore 时明确阻断 | S21-005 |
| S21-007 | `v0.1-snapshot-rc1` demo | 写入→查看 diff→恢复→验证文件内容 | S21-006 |
| S22-001 | TelemetryContext | project/session/run/turn/operation/correlation 可关联 | S21 |
| S22-002 | structured log query | 可按 run_id、level、message_code、时间查询本地日志 | S22-001 |
| S22-003 | call log projection | Provider/Tool/MCP/Skill/SubAgent 调用摘要可查 | S22-002 |
| S22-004 | health metrics | latency、error、queue depth、projection lag 可查 | S22-003 |
| S22-005 | audit projection | approval、skip、write、restore 有安全证据 | S22-004 |
| S22-006 | TUI Overview panel | 当前 run、进度、耗时、错误、连接状态 | S22-005 |
| S22-007 | TUI Skill/MCP/SubAgent/Memory panel | v0.1 展示基础投影；能力缺失显示明确 unavailable | S22-006 |
| S22-008 | support bundle | 日志脱敏、事件摘要、版本、health 打包 | S22-007 |
| S22-009 | `v0.1-observability-rc1` demo | 运行失败→查询证据→导出安全诊断包 | S22-008 |
| S23-001 | Project/Session 选择 | TUI 创建项目并恢复最近会话 | S09、S22 |
| S23-002 | 双 Provider 切换 | 两 adapter 完成相同任务，历史连续 | S10、S11 |
| S23-003 | 基础工具全量接入 | Read/Write/Edit/Bash/Glob/Grep/Task 都走 Gateway | S12～S16 |
| S23-004 | Spec 全量接入 | 正常五阶段和 skip-spec 均可完成 | S17、S18 |
| S23-005 | Rules Gate 接入 | Write/Edit 后检查并阻断违规变更 | S19 |
| S23-006 | Checkpoint/Snapshot 接入 | cancel/crash/timeout/restore 可恢复 | S20、S21 |
| S23-007 | TUI 全面状态投影 | session/run/tool/approval/spec/rules/snapshot 可切换 | S22 |
| S23-008 | `v0.1-mvp-integration` demo | 从输入需求到变更验证的完整单机流程 | S23-001～S23-007 |
| S24-001 | core integration suite | fake provider 全量回归 | S23 |
| S24-002 | crash/restart matrix | Core、TUI、Provider、Tool、DB 各类中断可恢复 | S24-001 |
| S24-003 | Secret scan | 仓库、日志、fixture、support bundle 扫描 | S24-002 |
| S24-004 | performance baseline | TUI 启动、事件提交、SQLite query、日志吞吐达标 | S24-003 |
| S24-005 | migration upgrade | 空库、旧库、失败 migration、回滚演练 | S24-004 |
| S24-006 | backup/restore | 备份 manifest、digest、seq 和恢复结果正确 | S24-005 |
| S24-007 | package install | Windows/macOS/Linux 本机安装后 `apex tui` 可用 | S24-006 |
| S24-008 | release candidate | `cargo fmt/clippy/test/audit` 全部通过 | S24-007 |
| S24-009 | v0.1.0 release | 二进制、SBOM、迁移说明、Runbook、已知限制齐全 | S24-008 |

## 2. 详细系统分析
<!-- DETAILS BEGIN -->
## S17-001：Spec aggregate/schema

### 1. 功能目标
本功能属于 S17。建立 requirements、design、tasks 工件及其审核、版本和追踪关系。 本项聚焦于Spec 工件、状态和追踪关系，必须形成可以独立启动、测试或演示的最小闭环。

### 2. 范围与边界
- 包含：Spec aggregate/schema所需契约、状态转换、错误返回、日志事件和自动化验收。
- 不包含：未列入本阶段的真实 Provider、跨阶段优化或视觉润色；依赖能力只能通过 Port、Fake 或 fixture 接入。
- 上游依赖：S16。依赖未 verified 时，本功能只能处于 blocked。
- 主要产物：工件、审核结果、状态变化和失效关系。

### 3. 输入、输出与处理流程
输入来自上游命令、查询、事件、配置、用户动作或 fixture，必须具备稳定标识、版本和必要授权上下文。

处理顺序：首先校验格式、当前状态、权限和依赖版本；其次，围绕功能 [Spec aggregate/schema] 完成输入校验、领域处理、持久化或投影、用户反馈和可重复验收。；然后产生领域事件或标准结果并更新必要投影；最后返回成功、等待、失败或取消结果，并记录阶段进度。

输出为工件、审核结果、状态变化和失效关系。计划中的最小验收为：feature、revision、stage、status 可持久化。

### 4. 状态、数据与一致性
- 状态变化必须遵守领域模型与事件规范，禁止直接修改 projection 绕过 domain command。
- SQLite Event Store，或本阶段明确声明的 Fake Store，是业务事实来源；日志只用于诊断。
- 外部副作用必须具有 operation_id、幂等策略、取消边界和恢复结论。
- 并发写入遵循单写者或 Write Claim；查询需要说明一致性级别和 cursor。

### 5. 异常、取消与恢复
必须设计输入无效、依赖不可用、权限拒绝、超时、用户取消、进程重启和重复提交路径。每类路径都要定义稳定 error_code、用户提示、是否可重试、是否产生事件和重启动作。不安全时进入明确的 blocked、failed 或 interrupted，不能静默成功。

### 6. 日志与安全要求
入口初始化日志，记录 stage.started、关键进度、stage.completed 或 stage.failed。日志采用非 JSON、非 key=value 的单行位置式格式：时间、级别、PID、任务链 UUID、线程或协程、logger、最后一级文件名和行号、主消息。不记录客户端地址；诊断上下文仅在 DEBUG 级别展示并置于主消息前，INFO 及以上隐藏。跳过必须有授权，上游变更必须使下游可检测失效。

### 7. 测试与可运行验收
- 必须提供 cargo 命令、CLI 子命令、集成测试或可重复 demo。
- 至少覆盖成功、失败、取消、重复调用以及适用的恢复或回滚路径。
- 日志断言任务链 UUID、稳定 message code、进度闭环、真实文件名和行号、无敏感信息泄漏。
- 回滚只能删除本阶段临时产物或恢复快照，不得删除用户数据或覆盖外部修改。

### 8. 后续代码注释要求
编码前必须通过本条目审核。实现代码需要为公共类型和函数、状态转换、协议字段、数据库迁移、异步任务边界、错误分支和安全判定添加中文详细注释。注释应解释设计原因、约束和失败恢复；复杂算法、并发或锁、取消传播、幂等键和安全边界还要写明前置条件、后置条件及不变量。测试代码需要说明 fixture 意图和验收理由。

### 9. 审核结论栏
- 需求理解：待审核
- 架构边界：待审核
- 接口和事件：待审核
- 异常与恢复：待审核
- 安全与日志：待审核
- 允许进入代码开发：否

<!-- END S17-001 -->
## S17-002：requirements artifact

### 1. 功能目标
本功能属于 S17。建立 requirements、design、tasks 工件及其审核、版本和追踪关系。 本项聚焦于Spec 工件、状态和追踪关系，必须形成可以独立启动、测试或演示的最小闭环。

### 2. 范围与边界
- 包含：requirements artifact所需契约、状态转换、错误返回、日志事件和自动化验收。
- 不包含：未列入本阶段的真实 Provider、跨阶段优化或视觉润色；依赖能力只能通过 Port、Fake 或 fixture 接入。
- 上游依赖：S17-001。依赖未 verified 时，本功能只能处于 blocked。
- 主要产物：工件、审核结果、状态变化和失效关系。

### 3. 输入、输出与处理流程
输入来自上游命令、查询、事件、配置、用户动作或 fixture，必须具备稳定标识、版本和必要授权上下文。

处理顺序：首先校验格式、当前状态、权限和依赖版本；其次，围绕功能 [requirements artifact] 完成输入校验、领域处理、持久化或投影、用户反馈和可重复验收。；然后产生领域事件或标准结果并更新必要投影；最后返回成功、等待、失败或取消结果，并记录阶段进度。

输出为工件、审核结果、状态变化和失效关系。计划中的最小验收为：生成带 frontmatter 的 requirements.md。

### 4. 状态、数据与一致性
- 状态变化必须遵守领域模型与事件规范，禁止直接修改 projection 绕过 domain command。
- SQLite Event Store，或本阶段明确声明的 Fake Store，是业务事实来源；日志只用于诊断。
- 外部副作用必须具有 operation_id、幂等策略、取消边界和恢复结论。
- 并发写入遵循单写者或 Write Claim；查询需要说明一致性级别和 cursor。

### 5. 异常、取消与恢复
必须设计输入无效、依赖不可用、权限拒绝、超时、用户取消、进程重启和重复提交路径。每类路径都要定义稳定 error_code、用户提示、是否可重试、是否产生事件和重启动作。不安全时进入明确的 blocked、failed 或 interrupted，不能静默成功。

### 6. 日志与安全要求
入口初始化日志，记录 stage.started、关键进度、stage.completed 或 stage.failed。日志采用非 JSON、非 key=value 的单行位置式格式：时间、级别、PID、任务链 UUID、线程或协程、logger、最后一级文件名和行号、主消息。不记录客户端地址；诊断上下文仅在 DEBUG 级别展示并置于主消息前，INFO 及以上隐藏。跳过必须有授权，上游变更必须使下游可检测失效。

### 7. 测试与可运行验收
- 必须提供 cargo 命令、CLI 子命令、集成测试或可重复 demo。
- 至少覆盖成功、失败、取消、重复调用以及适用的恢复或回滚路径。
- 日志断言任务链 UUID、稳定 message code、进度闭环、真实文件名和行号、无敏感信息泄漏。
- 回滚只能删除本阶段临时产物或恢复快照，不得删除用户数据或覆盖外部修改。

### 8. 后续代码注释要求
编码前必须通过本条目审核。实现代码需要为公共类型和函数、状态转换、协议字段、数据库迁移、异步任务边界、错误分支和安全判定添加中文详细注释。注释应解释设计原因、约束和失败恢复；复杂算法、并发或锁、取消传播、幂等键和安全边界还要写明前置条件、后置条件及不变量。测试代码需要说明 fixture 意图和验收理由。

### 9. 审核结论栏
- 需求理解：待审核
- 架构边界：待审核
- 接口和事件：待审核
- 异常与恢复：待审核
- 安全与日志：待审核
- 允许进入代码开发：否

<!-- END S17-002 -->
## S17-003：requirements review gate

### 1. 功能目标
本功能属于 S17。建立 requirements、design、tasks 工件及其审核、版本和追踪关系。 本项聚焦于输入事件、纯状态转换和终端渲染，必须形成可以独立启动、测试或演示的最小闭环。

### 2. 范围与边界
- 包含：requirements review gate所需契约、状态转换、错误返回、日志事件和自动化验收。
- 不包含：未列入本阶段的真实 Provider、跨阶段优化或视觉润色；依赖能力只能通过 Port、Fake 或 fixture 接入。
- 上游依赖：S17-002。依赖未 verified 时，本功能只能处于 blocked。
- 主要产物：AppState、Effect、渲染帧和用户提示。

### 3. 输入、输出与处理流程
输入来自上游命令、查询、事件、配置、用户动作或 fixture，必须具备稳定标识、版本和必要授权上下文。

处理顺序：首先校验格式、当前状态、权限和依赖版本；其次，围绕功能 [requirements review gate] 完成输入校验、领域处理、持久化或投影、用户反馈和可重复验收。；然后产生领域事件或标准结果并更新必要投影；最后返回成功、等待、失败或取消结果，并记录阶段进度。

输出为AppState、Effect、渲染帧和用户提示。计划中的最小验收为：用户 approve/reject/edit 后状态正确。

### 4. 状态、数据与一致性
- 状态变化必须遵守领域模型与事件规范，禁止直接修改 projection 绕过 domain command。
- SQLite Event Store，或本阶段明确声明的 Fake Store，是业务事实来源；日志只用于诊断。
- 外部副作用必须具有 operation_id、幂等策略、取消边界和恢复结论。
- 并发写入遵循单写者或 Write Claim；查询需要说明一致性级别和 cursor。

### 5. 异常、取消与恢复
必须设计输入无效、依赖不可用、权限拒绝、超时、用户取消、进程重启和重复提交路径。每类路径都要定义稳定 error_code、用户提示、是否可重试、是否产生事件和重启动作。不安全时进入明确的 blocked、failed 或 interrupted，不能静默成功。

### 6. 日志与安全要求
入口初始化日志，记录 stage.started、关键进度、stage.completed 或 stage.failed。日志采用非 JSON、非 key=value 的单行位置式格式：时间、级别、PID、任务链 UUID、线程或协程、logger、最后一级文件名和行号、主消息。不记录客户端地址；诊断上下文仅在 DEBUG 级别展示并置于主消息前，INFO 及以上隐藏。渲染层不得产生业务副作用，窄终端和 Unicode 不得越界。

### 7. 测试与可运行验收
- 必须提供 cargo 命令、CLI 子命令、集成测试或可重复 demo。
- 至少覆盖成功、失败、取消、重复调用以及适用的恢复或回滚路径。
- 日志断言任务链 UUID、稳定 message code、进度闭环、真实文件名和行号、无敏感信息泄漏。
- 回滚只能删除本阶段临时产物或恢复快照，不得删除用户数据或覆盖外部修改。

### 8. 后续代码注释要求
编码前必须通过本条目审核。实现代码需要为公共类型和函数、状态转换、协议字段、数据库迁移、异步任务边界、错误分支和安全判定添加中文详细注释。注释应解释设计原因、约束和失败恢复；复杂算法、并发或锁、取消传播、幂等键和安全边界还要写明前置条件、后置条件及不变量。测试代码需要说明 fixture 意图和验收理由。

### 9. 审核结论栏
- 需求理解：待审核
- 架构边界：待审核
- 接口和事件：待审核
- 异常与恢复：待审核
- 安全与日志：待审核
- 允许进入代码开发：否

<!-- END S17-003 -->
## S17-004：design artifact

### 1. 功能目标
本功能属于 S17。建立 requirements、design、tasks 工件及其审核、版本和追踪关系。 本项聚焦于Spec 工件、状态和追踪关系，必须形成可以独立启动、测试或演示的最小闭环。

### 2. 范围与边界
- 包含：design artifact所需契约、状态转换、错误返回、日志事件和自动化验收。
- 不包含：未列入本阶段的真实 Provider、跨阶段优化或视觉润色；依赖能力只能通过 Port、Fake 或 fixture 接入。
- 上游依赖：S17-003。依赖未 verified 时，本功能只能处于 blocked。
- 主要产物：工件、审核结果、状态变化和失效关系。

### 3. 输入、输出与处理流程
输入来自上游命令、查询、事件、配置、用户动作或 fixture，必须具备稳定标识、版本和必要授权上下文。

处理顺序：首先校验格式、当前状态、权限和依赖版本；其次，每项 requirement 必须映射 design、task 和 verification；缺失引用不能进入 ready。；然后产生领域事件或标准结果并更新必要投影；最后返回成功、等待、失败或取消结果，并记录阶段进度。

输出为工件、审核结果、状态变化和失效关系。计划中的最小验收为：生成 design.md 并嵌入 rules/acceptance。

### 4. 状态、数据与一致性
- 状态变化必须遵守领域模型与事件规范，禁止直接修改 projection 绕过 domain command。
- SQLite Event Store，或本阶段明确声明的 Fake Store，是业务事实来源；日志只用于诊断。
- 外部副作用必须具有 operation_id、幂等策略、取消边界和恢复结论。
- 并发写入遵循单写者或 Write Claim；查询需要说明一致性级别和 cursor。

### 5. 异常、取消与恢复
必须设计输入无效、依赖不可用、权限拒绝、超时、用户取消、进程重启和重复提交路径。每类路径都要定义稳定 error_code、用户提示、是否可重试、是否产生事件和重启动作。不安全时进入明确的 blocked、failed 或 interrupted，不能静默成功。

### 6. 日志与安全要求
入口初始化日志，记录 stage.started、关键进度、stage.completed 或 stage.failed。日志采用非 JSON、非 key=value 的单行位置式格式：时间、级别、PID、任务链 UUID、线程或协程、logger、最后一级文件名和行号、主消息。不记录客户端地址；诊断上下文仅在 DEBUG 级别展示并置于主消息前，INFO 及以上隐藏。跳过必须有授权，上游变更必须使下游可检测失效。

### 7. 测试与可运行验收
- 必须提供 cargo 命令、CLI 子命令、集成测试或可重复 demo。
- 至少覆盖成功、失败、取消、重复调用以及适用的恢复或回滚路径。
- 日志断言任务链 UUID、稳定 message code、进度闭环、真实文件名和行号、无敏感信息泄漏。
- 回滚只能删除本阶段临时产物或恢复快照，不得删除用户数据或覆盖外部修改。

### 8. 后续代码注释要求
编码前必须通过本条目审核。实现代码需要为公共类型和函数、状态转换、协议字段、数据库迁移、异步任务边界、错误分支和安全判定添加中文详细注释。注释应解释设计原因、约束和失败恢复；复杂算法、并发或锁、取消传播、幂等键和安全边界还要写明前置条件、后置条件及不变量。测试代码需要说明 fixture 意图和验收理由。

### 9. 审核结论栏
- 需求理解：待审核
- 架构边界：待审核
- 接口和事件：待审核
- 异常与恢复：待审核
- 安全与日志：待审核
- 允许进入代码开发：否

<!-- END S17-004 -->
## S17-005：design review gate

### 1. 功能目标
本功能属于 S17。建立 requirements、design、tasks 工件及其审核、版本和追踪关系。 本项聚焦于输入事件、纯状态转换和终端渲染，必须形成可以独立启动、测试或演示的最小闭环。

### 2. 范围与边界
- 包含：design review gate所需契约、状态转换、错误返回、日志事件和自动化验收。
- 不包含：未列入本阶段的真实 Provider、跨阶段优化或视觉润色；依赖能力只能通过 Port、Fake 或 fixture 接入。
- 上游依赖：S17-004。依赖未 verified 时，本功能只能处于 blocked。
- 主要产物：AppState、Effect、渲染帧和用户提示。

### 3. 输入、输出与处理流程
输入来自上游命令、查询、事件、配置、用户动作或 fixture，必须具备稳定标识、版本和必要授权上下文。

处理顺序：首先校验格式、当前状态、权限和依赖版本；其次，围绕功能 [design review gate] 完成输入校验、领域处理、持久化或投影、用户反馈和可重复验收。；然后产生领域事件或标准结果并更新必要投影；最后返回成功、等待、失败或取消结果，并记录阶段进度。

输出为AppState、Effect、渲染帧和用户提示。计划中的最小验收为：修改后产生 revision、下游失效。

### 4. 状态、数据与一致性
- 状态变化必须遵守领域模型与事件规范，禁止直接修改 projection 绕过 domain command。
- SQLite Event Store，或本阶段明确声明的 Fake Store，是业务事实来源；日志只用于诊断。
- 外部副作用必须具有 operation_id、幂等策略、取消边界和恢复结论。
- 并发写入遵循单写者或 Write Claim；查询需要说明一致性级别和 cursor。

### 5. 异常、取消与恢复
必须设计输入无效、依赖不可用、权限拒绝、超时、用户取消、进程重启和重复提交路径。每类路径都要定义稳定 error_code、用户提示、是否可重试、是否产生事件和重启动作。不安全时进入明确的 blocked、failed 或 interrupted，不能静默成功。

### 6. 日志与安全要求
入口初始化日志，记录 stage.started、关键进度、stage.completed 或 stage.failed。日志采用非 JSON、非 key=value 的单行位置式格式：时间、级别、PID、任务链 UUID、线程或协程、logger、最后一级文件名和行号、主消息。不记录客户端地址；诊断上下文仅在 DEBUG 级别展示并置于主消息前，INFO 及以上隐藏。渲染层不得产生业务副作用，窄终端和 Unicode 不得越界。

### 7. 测试与可运行验收
- 必须提供 cargo 命令、CLI 子命令、集成测试或可重复 demo。
- 至少覆盖成功、失败、取消、重复调用以及适用的恢复或回滚路径。
- 日志断言任务链 UUID、稳定 message code、进度闭环、真实文件名和行号、无敏感信息泄漏。
- 回滚只能删除本阶段临时产物或恢复快照，不得删除用户数据或覆盖外部修改。

### 8. 后续代码注释要求
编码前必须通过本条目审核。实现代码需要为公共类型和函数、状态转换、协议字段、数据库迁移、异步任务边界、错误分支和安全判定添加中文详细注释。注释应解释设计原因、约束和失败恢复；复杂算法、并发或锁、取消传播、幂等键和安全边界还要写明前置条件、后置条件及不变量。测试代码需要说明 fixture 意图和验收理由。

### 9. 审核结论栏
- 需求理解：待审核
- 架构边界：待审核
- 接口和事件：待审核
- 异常与恢复：待审核
- 安全与日志：待审核
- 允许进入代码开发：否

<!-- END S17-005 -->
## S17-006：tasks artifact

### 1. 功能目标
本功能属于 S17。建立 requirements、design、tasks 工件及其审核、版本和追踪关系。 本项聚焦于Spec 工件、状态和追踪关系，必须形成可以独立启动、测试或演示的最小闭环。

### 2. 范围与边界
- 包含：tasks artifact所需契约、状态转换、错误返回、日志事件和自动化验收。
- 不包含：未列入本阶段的真实 Provider、跨阶段优化或视觉润色；依赖能力只能通过 Port、Fake 或 fixture 接入。
- 上游依赖：S17-005。依赖未 verified 时，本功能只能处于 blocked。
- 主要产物：工件、审核结果、状态变化和失效关系。

### 3. 输入、输出与处理流程
输入来自上游命令、查询、事件、配置、用户动作或 fixture，必须具备稳定标识、版本和必要授权上下文。

处理顺序：首先校验格式、当前状态、权限和依赖版本；其次，围绕功能 [tasks artifact] 完成输入校验、领域处理、持久化或投影、用户反馈和可重复验收。；然后产生领域事件或标准结果并更新必要投影；最后返回成功、等待、失败或取消结果，并记录阶段进度。

输出为工件、审核结果、状态变化和失效关系。计划中的最小验收为：生成 tasks.md、依赖和写路径声明。

### 4. 状态、数据与一致性
- 状态变化必须遵守领域模型与事件规范，禁止直接修改 projection 绕过 domain command。
- SQLite Event Store，或本阶段明确声明的 Fake Store，是业务事实来源；日志只用于诊断。
- 外部副作用必须具有 operation_id、幂等策略、取消边界和恢复结论。
- 并发写入遵循单写者或 Write Claim；查询需要说明一致性级别和 cursor。

### 5. 异常、取消与恢复
必须设计输入无效、依赖不可用、权限拒绝、超时、用户取消、进程重启和重复提交路径。每类路径都要定义稳定 error_code、用户提示、是否可重试、是否产生事件和重启动作。不安全时进入明确的 blocked、failed 或 interrupted，不能静默成功。

### 6. 日志与安全要求
入口初始化日志，记录 stage.started、关键进度、stage.completed 或 stage.failed。日志采用非 JSON、非 key=value 的单行位置式格式：时间、级别、PID、任务链 UUID、线程或协程、logger、最后一级文件名和行号、主消息。不记录客户端地址；诊断上下文仅在 DEBUG 级别展示并置于主消息前，INFO 及以上隐藏。跳过必须有授权，上游变更必须使下游可检测失效。

### 7. 测试与可运行验收
- 必须提供 cargo 命令、CLI 子命令、集成测试或可重复 demo。
- 至少覆盖成功、失败、取消、重复调用以及适用的恢复或回滚路径。
- 日志断言任务链 UUID、稳定 message code、进度闭环、真实文件名和行号、无敏感信息泄漏。
- 回滚只能删除本阶段临时产物或恢复快照，不得删除用户数据或覆盖外部修改。

### 8. 后续代码注释要求
编码前必须通过本条目审核。实现代码需要为公共类型和函数、状态转换、协议字段、数据库迁移、异步任务边界、错误分支和安全判定添加中文详细注释。注释应解释设计原因、约束和失败恢复；复杂算法、并发或锁、取消传播、幂等键和安全边界还要写明前置条件、后置条件及不变量。测试代码需要说明 fixture 意图和验收理由。

### 9. 审核结论栏
- 需求理解：待审核
- 架构边界：待审核
- 接口和事件：待审核
- 异常与恢复：待审核
- 安全与日志：待审核
- 允许进入代码开发：否

<!-- END S17-006 -->
## S17-007：tasks review gate

### 1. 功能目标
本功能属于 S17。建立 requirements、design、tasks 工件及其审核、版本和追踪关系。 本项聚焦于输入事件、纯状态转换和终端渲染，必须形成可以独立启动、测试或演示的最小闭环。

### 2. 范围与边界
- 包含：tasks review gate所需契约、状态转换、错误返回、日志事件和自动化验收。
- 不包含：未列入本阶段的真实 Provider、跨阶段优化或视觉润色；依赖能力只能通过 Port、Fake 或 fixture 接入。
- 上游依赖：S17-006。依赖未 verified 时，本功能只能处于 blocked。
- 主要产物：AppState、Effect、渲染帧和用户提示。

### 3. 输入、输出与处理流程
输入来自上游命令、查询、事件、配置、用户动作或 fixture，必须具备稳定标识、版本和必要授权上下文。

处理顺序：首先校验格式、当前状态、权限和依赖版本；其次，围绕功能 [tasks review gate] 完成输入校验、领域处理、持久化或投影、用户反馈和可重复验收。；然后产生领域事件或标准结果并更新必要投影；最后返回成功、等待、失败或取消结果，并记录阶段进度。

输出为AppState、Effect、渲染帧和用户提示。计划中的最小验收为：确认后才能进入 implementation。

### 4. 状态、数据与一致性
- 状态变化必须遵守领域模型与事件规范，禁止直接修改 projection 绕过 domain command。
- SQLite Event Store，或本阶段明确声明的 Fake Store，是业务事实来源；日志只用于诊断。
- 外部副作用必须具有 operation_id、幂等策略、取消边界和恢复结论。
- 并发写入遵循单写者或 Write Claim；查询需要说明一致性级别和 cursor。

### 5. 异常、取消与恢复
必须设计输入无效、依赖不可用、权限拒绝、超时、用户取消、进程重启和重复提交路径。每类路径都要定义稳定 error_code、用户提示、是否可重试、是否产生事件和重启动作。不安全时进入明确的 blocked、failed 或 interrupted，不能静默成功。

### 6. 日志与安全要求
入口初始化日志，记录 stage.started、关键进度、stage.completed 或 stage.failed。日志采用非 JSON、非 key=value 的单行位置式格式：时间、级别、PID、任务链 UUID、线程或协程、logger、最后一级文件名和行号、主消息。不记录客户端地址；诊断上下文仅在 DEBUG 级别展示并置于主消息前，INFO 及以上隐藏。渲染层不得产生业务副作用，窄终端和 Unicode 不得越界。

### 7. 测试与可运行验收
- 必须提供 cargo 命令、CLI 子命令、集成测试或可重复 demo。
- 至少覆盖成功、失败、取消、重复调用以及适用的恢复或回滚路径。
- 日志断言任务链 UUID、稳定 message code、进度闭环、真实文件名和行号、无敏感信息泄漏。
- 回滚只能删除本阶段临时产物或恢复快照，不得删除用户数据或覆盖外部修改。

### 8. 后续代码注释要求
编码前必须通过本条目审核。实现代码需要为公共类型和函数、状态转换、协议字段、数据库迁移、异步任务边界、错误分支和安全判定添加中文详细注释。注释应解释设计原因、约束和失败恢复；复杂算法、并发或锁、取消传播、幂等键和安全边界还要写明前置条件、后置条件及不变量。测试代码需要说明 fixture 意图和验收理由。

### 9. 审核结论栏
- 需求理解：待审核
- 架构边界：待审核
- 接口和事件：待审核
- 异常与恢复：待审核
- 安全与日志：待审核
- 允许进入代码开发：否

<!-- END S17-007 -->
## S17-008：`v0.1-spec-plan-rc1` demo

### 1. 功能目标
本功能属于 S17。建立 requirements、design、tasks 工件及其审核、版本和追踪关系。 本项聚焦于Spec 工件、状态和追踪关系，必须形成可以独立启动、测试或演示的最小闭环。

### 2. 范围与边界
- 包含：`v0.1-spec-plan-rc1` demo所需契约、状态转换、错误返回、日志事件和自动化验收。
- 不包含：未列入本阶段的真实 Provider、跨阶段优化或视觉润色；依赖能力只能通过 Port、Fake 或 fixture 接入。
- 上游依赖：S17-007。依赖未 verified 时，本功能只能处于 blocked。
- 主要产物：工件、审核结果、状态变化和失效关系。

### 3. 输入、输出与处理流程
输入来自上游命令、查询、事件、配置、用户动作或 fixture，必须具备稳定标识、版本和必要授权上下文。

处理顺序：首先校验格式、当前状态、权限和依赖版本；其次，围绕功能 [`v0.1-spec-plan-rc1` demo] 完成输入校验、领域处理、持久化或投影、用户反馈和可重复验收。；然后产生领域事件或标准结果并更新必要投影；最后返回成功、等待、失败或取消结果，并记录阶段进度。

输出为工件、审核结果、状态变化和失效关系。计划中的最小验收为：需求→设计→任务三道门逐一确认。

### 4. 状态、数据与一致性
- 状态变化必须遵守领域模型与事件规范，禁止直接修改 projection 绕过 domain command。
- SQLite Event Store，或本阶段明确声明的 Fake Store，是业务事实来源；日志只用于诊断。
- 外部副作用必须具有 operation_id、幂等策略、取消边界和恢复结论。
- 并发写入遵循单写者或 Write Claim；查询需要说明一致性级别和 cursor。

### 5. 异常、取消与恢复
必须设计输入无效、依赖不可用、权限拒绝、超时、用户取消、进程重启和重复提交路径。每类路径都要定义稳定 error_code、用户提示、是否可重试、是否产生事件和重启动作。不安全时进入明确的 blocked、failed 或 interrupted，不能静默成功。

### 6. 日志与安全要求
入口初始化日志，记录 stage.started、关键进度、stage.completed 或 stage.failed。日志采用非 JSON、非 key=value 的单行位置式格式：时间、级别、PID、任务链 UUID、线程或协程、logger、最后一级文件名和行号、主消息。不记录客户端地址；诊断上下文仅在 DEBUG 级别展示并置于主消息前，INFO 及以上隐藏。跳过必须有授权，上游变更必须使下游可检测失效。

### 7. 测试与可运行验收
- 必须提供 cargo 命令、CLI 子命令、集成测试或可重复 demo。
- 至少覆盖成功、失败、取消、重复调用以及适用的恢复或回滚路径。
- 日志断言任务链 UUID、稳定 message code、进度闭环、真实文件名和行号、无敏感信息泄漏。
- 回滚只能删除本阶段临时产物或恢复快照，不得删除用户数据或覆盖外部修改。

### 8. 后续代码注释要求
编码前必须通过本条目审核。实现代码需要为公共类型和函数、状态转换、协议字段、数据库迁移、异步任务边界、错误分支和安全判定添加中文详细注释。注释应解释设计原因、约束和失败恢复；复杂算法、并发或锁、取消传播、幂等键和安全边界还要写明前置条件、后置条件及不变量。测试代码需要说明 fixture 意图和验收理由。

### 9. 审核结论栏
- 需求理解：待审核
- 架构边界：待审核
- 接口和事件：待审核
- 异常与恢复：待审核
- 安全与日志：待审核
- 允许进入代码开发：否

<!-- END S17-008 -->
## S18-001：implementation runner

### 1. 功能目标
本功能属于 S18。建立 implementation、verification、review、skip 和上游变更失效流程。 本项聚焦于领域功能、应用服务和可运行入口，必须形成可以独立启动、测试或演示的最小闭环。

### 2. 范围与边界
- 包含：implementation runner所需契约、状态转换、错误返回、日志事件和自动化验收。
- 不包含：未列入本阶段的真实 Provider、跨阶段优化或视觉润色；依赖能力只能通过 Port、Fake 或 fixture 接入。
- 上游依赖：S17。依赖未 verified 时，本功能只能处于 blocked。
- 主要产物：领域事件、查询投影、日志和验收结果。

### 3. 输入、输出与处理流程
输入来自上游命令、查询、事件、配置、用户动作或 fixture，必须具备稳定标识、版本和必要授权上下文。

处理顺序：首先校验格式、当前状态、权限和依赖版本；其次，围绕功能 [implementation runner] 完成输入校验、领域处理、持久化或投影、用户反馈和可重复验收。；然后产生领域事件或标准结果并更新必要投影；最后返回成功、等待、失败或取消结果，并记录阶段进度。

输出为领域事件、查询投影、日志和验收结果。计划中的最小验收为：按 tasks 顺序调用 Agent Loop/Tool。

### 4. 状态、数据与一致性
- 状态变化必须遵守领域模型与事件规范，禁止直接修改 projection 绕过 domain command。
- SQLite Event Store，或本阶段明确声明的 Fake Store，是业务事实来源；日志只用于诊断。
- 外部副作用必须具有 operation_id、幂等策略、取消边界和恢复结论。
- 并发写入遵循单写者或 Write Claim；查询需要说明一致性级别和 cursor。

### 5. 异常、取消与恢复
必须设计输入无效、依赖不可用、权限拒绝、超时、用户取消、进程重启和重复提交路径。每类路径都要定义稳定 error_code、用户提示、是否可重试、是否产生事件和重启动作。不安全时进入明确的 blocked、failed 或 interrupted，不能静默成功。

### 6. 日志与安全要求
入口初始化日志，记录 stage.started、关键进度、stage.completed 或 stage.failed。日志采用非 JSON、非 key=value 的单行位置式格式：时间、级别、PID、任务链 UUID、线程或协程、logger、最后一级文件名和行号、主消息。不记录客户端地址；诊断上下文仅在 DEBUG 级别展示并置于主消息前，INFO 及以上隐藏。必须保持 Port、幂等、取消、恢复和审计边界。

### 7. 测试与可运行验收
- 必须提供 cargo 命令、CLI 子命令、集成测试或可重复 demo。
- 至少覆盖成功、失败、取消、重复调用以及适用的恢复或回滚路径。
- 日志断言任务链 UUID、稳定 message code、进度闭环、真实文件名和行号、无敏感信息泄漏。
- 回滚只能删除本阶段临时产物或恢复快照，不得删除用户数据或覆盖外部修改。

### 8. 后续代码注释要求
编码前必须通过本条目审核。实现代码需要为公共类型和函数、状态转换、协议字段、数据库迁移、异步任务边界、错误分支和安全判定添加中文详细注释。注释应解释设计原因、约束和失败恢复；复杂算法、并发或锁、取消传播、幂等键和安全边界还要写明前置条件、后置条件及不变量。测试代码需要说明 fixture 意图和验收理由。

### 9. 审核结论栏
- 需求理解：待审核
- 架构边界：待审核
- 接口和事件：待审核
- 异常与恢复：待审核
- 安全与日志：待审核
- 允许进入代码开发：否

<!-- END S18-001 -->
## S18-002：artifact versioning

### 1. 功能目标
本功能属于 S18。建立 implementation、verification、review、skip 和上游变更失效流程。 本项聚焦于Spec 工件、状态和追踪关系，必须形成可以独立启动、测试或演示的最小闭环。

### 2. 范围与边界
- 包含：artifact versioning所需契约、状态转换、错误返回、日志事件和自动化验收。
- 不包含：未列入本阶段的真实 Provider、跨阶段优化或视觉润色；依赖能力只能通过 Port、Fake 或 fixture 接入。
- 上游依赖：S18-001。依赖未 verified 时，本功能只能处于 blocked。
- 主要产物：工件、审核结果、状态变化和失效关系。

### 3. 输入、输出与处理流程
输入来自上游命令、查询、事件、配置、用户动作或 fixture，必须具备稳定标识、版本和必要授权上下文。

处理顺序：首先校验格式、当前状态、权限和依赖版本；其次，围绕功能 [artifact versioning] 完成输入校验、领域处理、持久化或投影、用户反馈和可重复验收。；然后产生领域事件或标准结果并更新必要投影；最后返回成功、等待、失败或取消结果，并记录阶段进度。

输出为工件、审核结果、状态变化和失效关系。计划中的最小验收为：用户编辑/Agent 修改均有 revision/diff。

### 4. 状态、数据与一致性
- 状态变化必须遵守领域模型与事件规范，禁止直接修改 projection 绕过 domain command。
- SQLite Event Store，或本阶段明确声明的 Fake Store，是业务事实来源；日志只用于诊断。
- 外部副作用必须具有 operation_id、幂等策略、取消边界和恢复结论。
- 并发写入遵循单写者或 Write Claim；查询需要说明一致性级别和 cursor。

### 5. 异常、取消与恢复
必须设计输入无效、依赖不可用、权限拒绝、超时、用户取消、进程重启和重复提交路径。每类路径都要定义稳定 error_code、用户提示、是否可重试、是否产生事件和重启动作。不安全时进入明确的 blocked、failed 或 interrupted，不能静默成功。

### 6. 日志与安全要求
入口初始化日志，记录 stage.started、关键进度、stage.completed 或 stage.failed。日志采用非 JSON、非 key=value 的单行位置式格式：时间、级别、PID、任务链 UUID、线程或协程、logger、最后一级文件名和行号、主消息。不记录客户端地址；诊断上下文仅在 DEBUG 级别展示并置于主消息前，INFO 及以上隐藏。跳过必须有授权，上游变更必须使下游可检测失效。

### 7. 测试与可运行验收
- 必须提供 cargo 命令、CLI 子命令、集成测试或可重复 demo。
- 至少覆盖成功、失败、取消、重复调用以及适用的恢复或回滚路径。
- 日志断言任务链 UUID、稳定 message code、进度闭环、真实文件名和行号、无敏感信息泄漏。
- 回滚只能删除本阶段临时产物或恢复快照，不得删除用户数据或覆盖外部修改。

### 8. 后续代码注释要求
编码前必须通过本条目审核。实现代码需要为公共类型和函数、状态转换、协议字段、数据库迁移、异步任务边界、错误分支和安全判定添加中文详细注释。注释应解释设计原因、约束和失败恢复；复杂算法、并发或锁、取消传播、幂等键和安全边界还要写明前置条件、后置条件及不变量。测试代码需要说明 fixture 意图和验收理由。

### 9. 审核结论栏
- 需求理解：待审核
- 架构边界：待审核
- 接口和事件：待审核
- 异常与恢复：待审核
- 安全与日志：待审核
- 允许进入代码开发：否

<!-- END S18-002 -->
## S18-003：`/skip-spec` command

### 1. 功能目标
本功能属于 S18。建立 implementation、verification、review、skip 和上游变更失效流程。 本项聚焦于Spec 工件、状态和追踪关系，必须形成可以独立启动、测试或演示的最小闭环。

### 2. 范围与边界
- 包含：`/skip-spec` command所需契约、状态转换、错误返回、日志事件和自动化验收。
- 不包含：未列入本阶段的真实 Provider、跨阶段优化或视觉润色；依赖能力只能通过 Port、Fake 或 fixture 接入。
- 上游依赖：S18-002。依赖未 verified 时，本功能只能处于 blocked。
- 主要产物：工件、审核结果、状态变化和失效关系。

### 3. 输入、输出与处理流程
输入来自上游命令、查询、事件、配置、用户动作或 fixture，必须具备稳定标识、版本和必要授权上下文。

处理顺序：首先校验格式、当前状态、权限和依赖版本；其次，围绕功能 [`/skip-spec` command] 完成输入校验、领域处理、持久化或投影、用户反馈和可重复验收。；然后产生领域事件或标准结果并更新必要投影；最后返回成功、等待、失败或取消结果，并记录阶段进度。

输出为工件、审核结果、状态变化和失效关系。计划中的最小验收为：跳过决策带理由和 spec_skipped=true。

### 4. 状态、数据与一致性
- 状态变化必须遵守领域模型与事件规范，禁止直接修改 projection 绕过 domain command。
- SQLite Event Store，或本阶段明确声明的 Fake Store，是业务事实来源；日志只用于诊断。
- 外部副作用必须具有 operation_id、幂等策略、取消边界和恢复结论。
- 并发写入遵循单写者或 Write Claim；查询需要说明一致性级别和 cursor。

### 5. 异常、取消与恢复
必须设计输入无效、依赖不可用、权限拒绝、超时、用户取消、进程重启和重复提交路径。每类路径都要定义稳定 error_code、用户提示、是否可重试、是否产生事件和重启动作。不安全时进入明确的 blocked、failed 或 interrupted，不能静默成功。

### 6. 日志与安全要求
入口初始化日志，记录 stage.started、关键进度、stage.completed 或 stage.failed。日志采用非 JSON、非 key=value 的单行位置式格式：时间、级别、PID、任务链 UUID、线程或协程、logger、最后一级文件名和行号、主消息。不记录客户端地址；诊断上下文仅在 DEBUG 级别展示并置于主消息前，INFO 及以上隐藏。跳过必须有授权，上游变更必须使下游可检测失效。

### 7. 测试与可运行验收
- 必须提供 cargo 命令、CLI 子命令、集成测试或可重复 demo。
- 至少覆盖成功、失败、取消、重复调用以及适用的恢复或回滚路径。
- 日志断言任务链 UUID、稳定 message code、进度闭环、真实文件名和行号、无敏感信息泄漏。
- 回滚只能删除本阶段临时产物或恢复快照，不得删除用户数据或覆盖外部修改。

### 8. 后续代码注释要求
编码前必须通过本条目审核。实现代码需要为公共类型和函数、状态转换、协议字段、数据库迁移、异步任务边界、错误分支和安全判定添加中文详细注释。注释应解释设计原因、约束和失败恢复；复杂算法、并发或锁、取消传播、幂等键和安全边界还要写明前置条件、后置条件及不变量。测试代码需要说明 fixture 意图和验收理由。

### 9. 审核结论栏
- 需求理解：待审核
- 架构边界：待审核
- 接口和事件：待审核
- 异常与恢复：待审核
- 安全与日志：待审核
- 允许进入代码开发：否

<!-- END S18-003 -->
## S18-004：verification artifact

### 1. 功能目标
本功能属于 S18。建立 implementation、verification、review、skip 和上游变更失效流程。 本项聚焦于Spec 工件、状态和追踪关系，必须形成可以独立启动、测试或演示的最小闭环。

### 2. 范围与边界
- 包含：verification artifact所需契约、状态转换、错误返回、日志事件和自动化验收。
- 不包含：未列入本阶段的真实 Provider、跨阶段优化或视觉润色；依赖能力只能通过 Port、Fake 或 fixture 接入。
- 上游依赖：S18-003。依赖未 verified 时，本功能只能处于 blocked。
- 主要产物：工件、审核结果、状态变化和失效关系。

### 3. 输入、输出与处理流程
输入来自上游命令、查询、事件、配置、用户动作或 fixture，必须具备稳定标识、版本和必要授权上下文。

处理顺序：首先校验格式、当前状态、权限和依赖版本；其次，围绕功能 [verification artifact] 完成输入校验、领域处理、持久化或投影、用户反馈和可重复验收。；然后产生领域事件或标准结果并更新必要投影；最后返回成功、等待、失败或取消结果，并记录阶段进度。

输出为工件、审核结果、状态变化和失效关系。计划中的最小验收为：生成 verification.md 和逐条证据。

### 4. 状态、数据与一致性
- 状态变化必须遵守领域模型与事件规范，禁止直接修改 projection 绕过 domain command。
- SQLite Event Store，或本阶段明确声明的 Fake Store，是业务事实来源；日志只用于诊断。
- 外部副作用必须具有 operation_id、幂等策略、取消边界和恢复结论。
- 并发写入遵循单写者或 Write Claim；查询需要说明一致性级别和 cursor。

### 5. 异常、取消与恢复
必须设计输入无效、依赖不可用、权限拒绝、超时、用户取消、进程重启和重复提交路径。每类路径都要定义稳定 error_code、用户提示、是否可重试、是否产生事件和重启动作。不安全时进入明确的 blocked、failed 或 interrupted，不能静默成功。

### 6. 日志与安全要求
入口初始化日志，记录 stage.started、关键进度、stage.completed 或 stage.failed。日志采用非 JSON、非 key=value 的单行位置式格式：时间、级别、PID、任务链 UUID、线程或协程、logger、最后一级文件名和行号、主消息。不记录客户端地址；诊断上下文仅在 DEBUG 级别展示并置于主消息前，INFO 及以上隐藏。跳过必须有授权，上游变更必须使下游可检测失效。

### 7. 测试与可运行验收
- 必须提供 cargo 命令、CLI 子命令、集成测试或可重复 demo。
- 至少覆盖成功、失败、取消、重复调用以及适用的恢复或回滚路径。
- 日志断言任务链 UUID、稳定 message code、进度闭环、真实文件名和行号、无敏感信息泄漏。
- 回滚只能删除本阶段临时产物或恢复快照，不得删除用户数据或覆盖外部修改。

### 8. 后续代码注释要求
编码前必须通过本条目审核。实现代码需要为公共类型和函数、状态转换、协议字段、数据库迁移、异步任务边界、错误分支和安全判定添加中文详细注释。注释应解释设计原因、约束和失败恢复；复杂算法、并发或锁、取消传播、幂等键和安全边界还要写明前置条件、后置条件及不变量。测试代码需要说明 fixture 意图和验收理由。

### 9. 审核结论栏
- 需求理解：待审核
- 架构边界：待审核
- 接口和事件：待审核
- 异常与恢复：待审核
- 安全与日志：待审核
- 允许进入代码开发：否

<!-- END S18-004 -->
## S18-005：verification review gate

### 1. 功能目标
本功能属于 S18。建立 implementation、verification、review、skip 和上游变更失效流程。 本项聚焦于输入事件、纯状态转换和终端渲染，必须形成可以独立启动、测试或演示的最小闭环。

### 2. 范围与边界
- 包含：verification review gate所需契约、状态转换、错误返回、日志事件和自动化验收。
- 不包含：未列入本阶段的真实 Provider、跨阶段优化或视觉润色；依赖能力只能通过 Port、Fake 或 fixture 接入。
- 上游依赖：S18-004。依赖未 verified 时，本功能只能处于 blocked。
- 主要产物：AppState、Effect、渲染帧和用户提示。

### 3. 输入、输出与处理流程
输入来自上游命令、查询、事件、配置、用户动作或 fixture，必须具备稳定标识、版本和必要授权上下文。

处理顺序：首先校验格式、当前状态、权限和依赖版本；其次，verification 先进入 review_pending，只有用户确认或明确授权后才标记 completed。；然后产生领域事件或标准结果并更新必要投影；最后返回成功、等待、失败或取消结果，并记录阶段进度。

输出为AppState、Effect、渲染帧和用户提示。计划中的最小验收为：用户确认后 feature completed。

### 4. 状态、数据与一致性
- 状态变化必须遵守领域模型与事件规范，禁止直接修改 projection 绕过 domain command。
- SQLite Event Store，或本阶段明确声明的 Fake Store，是业务事实来源；日志只用于诊断。
- 外部副作用必须具有 operation_id、幂等策略、取消边界和恢复结论。
- 并发写入遵循单写者或 Write Claim；查询需要说明一致性级别和 cursor。

### 5. 异常、取消与恢复
必须设计输入无效、依赖不可用、权限拒绝、超时、用户取消、进程重启和重复提交路径。每类路径都要定义稳定 error_code、用户提示、是否可重试、是否产生事件和重启动作。不安全时进入明确的 blocked、failed 或 interrupted，不能静默成功。

### 6. 日志与安全要求
入口初始化日志，记录 stage.started、关键进度、stage.completed 或 stage.failed。日志采用非 JSON、非 key=value 的单行位置式格式：时间、级别、PID、任务链 UUID、线程或协程、logger、最后一级文件名和行号、主消息。不记录客户端地址；诊断上下文仅在 DEBUG 级别展示并置于主消息前，INFO 及以上隐藏。渲染层不得产生业务副作用，窄终端和 Unicode 不得越界。

### 7. 测试与可运行验收
- 必须提供 cargo 命令、CLI 子命令、集成测试或可重复 demo。
- 至少覆盖成功、失败、取消、重复调用以及适用的恢复或回滚路径。
- 日志断言任务链 UUID、稳定 message code、进度闭环、真实文件名和行号、无敏感信息泄漏。
- 回滚只能删除本阶段临时产物或恢复快照，不得删除用户数据或覆盖外部修改。

### 8. 后续代码注释要求
编码前必须通过本条目审核。实现代码需要为公共类型和函数、状态转换、协议字段、数据库迁移、异步任务边界、错误分支和安全判定添加中文详细注释。注释应解释设计原因、约束和失败恢复；复杂算法、并发或锁、取消传播、幂等键和安全边界还要写明前置条件、后置条件及不变量。测试代码需要说明 fixture 意图和验收理由。

### 9. 审核结论栏
- 需求理解：待审核
- 架构边界：待审核
- 接口和事件：待审核
- 异常与恢复：待审核
- 安全与日志：待审核
- 允许进入代码开发：否

<!-- END S18-005 -->
## S18-006：invalidation

### 1. 功能目标
本功能属于 S18。建立 implementation、verification、review、skip 和上游变更失效流程。 本项聚焦于Spec 工件、状态和追踪关系，必须形成可以独立启动、测试或演示的最小闭环。

### 2. 范围与边界
- 包含：invalidation所需契约、状态转换、错误返回、日志事件和自动化验收。
- 不包含：未列入本阶段的真实 Provider、跨阶段优化或视觉润色；依赖能力只能通过 Port、Fake 或 fixture 接入。
- 上游依赖：S18-005。依赖未 verified 时，本功能只能处于 blocked。
- 主要产物：工件、审核结果、状态变化和失效关系。

### 3. 输入、输出与处理流程
输入来自上游命令、查询、事件、配置、用户动作或 fixture，必须具备稳定标识、版本和必要授权上下文。

处理顺序：首先校验格式、当前状态、权限和依赖版本；其次，围绕功能 [invalidation] 完成输入校验、领域处理、持久化或投影、用户反馈和可重复验收。；然后产生领域事件或标准结果并更新必要投影；最后返回成功、等待、失败或取消结果，并记录阶段进度。

输出为工件、审核结果、状态变化和失效关系。计划中的最小验收为：上游 requirements/design 变更使下游失效。

### 4. 状态、数据与一致性
- 状态变化必须遵守领域模型与事件规范，禁止直接修改 projection 绕过 domain command。
- SQLite Event Store，或本阶段明确声明的 Fake Store，是业务事实来源；日志只用于诊断。
- 外部副作用必须具有 operation_id、幂等策略、取消边界和恢复结论。
- 并发写入遵循单写者或 Write Claim；查询需要说明一致性级别和 cursor。

### 5. 异常、取消与恢复
必须设计输入无效、依赖不可用、权限拒绝、超时、用户取消、进程重启和重复提交路径。每类路径都要定义稳定 error_code、用户提示、是否可重试、是否产生事件和重启动作。不安全时进入明确的 blocked、failed 或 interrupted，不能静默成功。

### 6. 日志与安全要求
入口初始化日志，记录 stage.started、关键进度、stage.completed 或 stage.failed。日志采用非 JSON、非 key=value 的单行位置式格式：时间、级别、PID、任务链 UUID、线程或协程、logger、最后一级文件名和行号、主消息。不记录客户端地址；诊断上下文仅在 DEBUG 级别展示并置于主消息前，INFO 及以上隐藏。跳过必须有授权，上游变更必须使下游可检测失效。

### 7. 测试与可运行验收
- 必须提供 cargo 命令、CLI 子命令、集成测试或可重复 demo。
- 至少覆盖成功、失败、取消、重复调用以及适用的恢复或回滚路径。
- 日志断言任务链 UUID、稳定 message code、进度闭环、真实文件名和行号、无敏感信息泄漏。
- 回滚只能删除本阶段临时产物或恢复快照，不得删除用户数据或覆盖外部修改。

### 8. 后续代码注释要求
编码前必须通过本条目审核。实现代码需要为公共类型和函数、状态转换、协议字段、数据库迁移、异步任务边界、错误分支和安全判定添加中文详细注释。注释应解释设计原因、约束和失败恢复；复杂算法、并发或锁、取消传播、幂等键和安全边界还要写明前置条件、后置条件及不变量。测试代码需要说明 fixture 意图和验收理由。

### 9. 审核结论栏
- 需求理解：待审核
- 架构边界：待审核
- 接口和事件：待审核
- 异常与恢复：待审核
- 安全与日志：待审核
- 允许进入代码开发：否

<!-- END S18-006 -->
## S18-007：`v0.1-spec-full-rc1` demo

### 1. 功能目标
本功能属于 S18。建立 implementation、verification、review、skip 和上游变更失效流程。 本项聚焦于Spec 工件、状态和追踪关系，必须形成可以独立启动、测试或演示的最小闭环。

### 2. 范围与边界
- 包含：`v0.1-spec-full-rc1` demo所需契约、状态转换、错误返回、日志事件和自动化验收。
- 不包含：未列入本阶段的真实 Provider、跨阶段优化或视觉润色；依赖能力只能通过 Port、Fake 或 fixture 接入。
- 上游依赖：S18-006。依赖未 verified 时，本功能只能处于 blocked。
- 主要产物：工件、审核结果、状态变化和失效关系。

### 3. 输入、输出与处理流程
输入来自上游命令、查询、事件、配置、用户动作或 fixture，必须具备稳定标识、版本和必要授权上下文。

处理顺序：首先校验格式、当前状态、权限和依赖版本；其次，围绕功能 [`v0.1-spec-full-rc1` demo] 完成输入校验、领域处理、持久化或投影、用户反馈和可重复验收。；然后产生领域事件或标准结果并更新必要投影；最后返回成功、等待、失败或取消结果，并记录阶段进度。

输出为工件、审核结果、状态变化和失效关系。计划中的最小验收为：正常流水线和 skip-spec 各完成一次。

### 4. 状态、数据与一致性
- 状态变化必须遵守领域模型与事件规范，禁止直接修改 projection 绕过 domain command。
- SQLite Event Store，或本阶段明确声明的 Fake Store，是业务事实来源；日志只用于诊断。
- 外部副作用必须具有 operation_id、幂等策略、取消边界和恢复结论。
- 并发写入遵循单写者或 Write Claim；查询需要说明一致性级别和 cursor。

### 5. 异常、取消与恢复
必须设计输入无效、依赖不可用、权限拒绝、超时、用户取消、进程重启和重复提交路径。每类路径都要定义稳定 error_code、用户提示、是否可重试、是否产生事件和重启动作。不安全时进入明确的 blocked、failed 或 interrupted，不能静默成功。

### 6. 日志与安全要求
入口初始化日志，记录 stage.started、关键进度、stage.completed 或 stage.failed。日志采用非 JSON、非 key=value 的单行位置式格式：时间、级别、PID、任务链 UUID、线程或协程、logger、最后一级文件名和行号、主消息。不记录客户端地址；诊断上下文仅在 DEBUG 级别展示并置于主消息前，INFO 及以上隐藏。跳过必须有授权，上游变更必须使下游可检测失效。

### 7. 测试与可运行验收
- 必须提供 cargo 命令、CLI 子命令、集成测试或可重复 demo。
- 至少覆盖成功、失败、取消、重复调用以及适用的恢复或回滚路径。
- 日志断言任务链 UUID、稳定 message code、进度闭环、真实文件名和行号、无敏感信息泄漏。
- 回滚只能删除本阶段临时产物或恢复快照，不得删除用户数据或覆盖外部修改。

### 8. 后续代码注释要求
编码前必须通过本条目审核。实现代码需要为公共类型和函数、状态转换、协议字段、数据库迁移、异步任务边界、错误分支和安全判定添加中文详细注释。注释应解释设计原因、约束和失败恢复；复杂算法、并发或锁、取消传播、幂等键和安全边界还要写明前置条件、后置条件及不变量。测试代码需要说明 fixture 意图和验收理由。

### 9. 审核结论栏
- 需求理解：待审核
- 架构边界：待审核
- 接口和事件：待审核
- 异常与恢复：待审核
- 安全与日志：待审核
- 允许进入代码开发：否

<!-- END S18-007 -->
## S19-001：rule source loader

### 1. 功能目标
本功能属于 S19。建立多来源规则加载、编译、增量检查、修复任务和 Verification Gate。 本项聚焦于规则来源、编译版本和验证门，必须形成可以独立启动、测试或演示的最小闭环。

### 2. 范围与边界
- 包含：rule source loader所需契约、状态转换、错误返回、日志事件和自动化验收。
- 不包含：未列入本阶段的真实 Provider、跨阶段优化或视觉润色；依赖能力只能通过 Port、Fake 或 fixture 接入。
- 上游依赖：S18。依赖未 verified 时，本功能只能处于 blocked。
- 主要产物：诊断项、检查结果、修复任务和门禁决策。

### 3. 输入、输出与处理流程
输入来自上游命令、查询、事件、配置、用户动作或 fixture，必须具备稳定标识、版本和必要授权上下文。

处理顺序：首先校验格式、当前状态、权限和依赖版本；其次，围绕功能 [rule source loader] 完成输入校验、领域处理、持久化或投影、用户反馈和可重复验收。；然后产生领域事件或标准结果并更新必要投影；最后返回成功、等待、失败或取消结果，并记录阶段进度。

输出为诊断项、检查结果、修复任务和门禁决策。计划中的最小验收为：加载 `apex/rules`、AGENTS/CLAUDE、全局规则。

### 4. 状态、数据与一致性
- 状态变化必须遵守领域模型与事件规范，禁止直接修改 projection 绕过 domain command。
- SQLite Event Store，或本阶段明确声明的 Fake Store，是业务事实来源；日志只用于诊断。
- 外部副作用必须具有 operation_id、幂等策略、取消边界和恢复结论。
- 并发写入遵循单写者或 Write Claim；查询需要说明一致性级别和 cursor。

### 5. 异常、取消与恢复
必须设计输入无效、依赖不可用、权限拒绝、超时、用户取消、进程重启和重复提交路径。每类路径都要定义稳定 error_code、用户提示、是否可重试、是否产生事件和重启动作。不安全时进入明确的 blocked、failed 或 interrupted，不能静默成功。

### 6. 日志与安全要求
入口初始化日志，记录 stage.started、关键进度、stage.completed 或 stage.failed。日志采用非 JSON、非 key=value 的单行位置式格式：时间、级别、PID、任务链 UUID、线程或协程、logger、最后一级文件名和行号、主消息。不记录客户端地址；诊断上下文仅在 DEBUG 级别展示并置于主消息前，INFO 及以上隐藏。错误级问题不得静默放行。

### 7. 测试与可运行验收
- 必须提供 cargo 命令、CLI 子命令、集成测试或可重复 demo。
- 至少覆盖成功、失败、取消、重复调用以及适用的恢复或回滚路径。
- 日志断言任务链 UUID、稳定 message code、进度闭环、真实文件名和行号、无敏感信息泄漏。
- 回滚只能删除本阶段临时产物或恢复快照，不得删除用户数据或覆盖外部修改。

### 8. 后续代码注释要求
编码前必须通过本条目审核。实现代码需要为公共类型和函数、状态转换、协议字段、数据库迁移、异步任务边界、错误分支和安全判定添加中文详细注释。注释应解释设计原因、约束和失败恢复；复杂算法、并发或锁、取消传播、幂等键和安全边界还要写明前置条件、后置条件及不变量。测试代码需要说明 fixture 意图和验收理由。

### 9. 审核结论栏
- 需求理解：待审核
- 架构边界：待审核
- 接口和事件：待审核
- 异常与恢复：待审核
- 安全与日志：待审核
- 允许进入代码开发：否

<!-- END S19-001 -->
## S19-002：rule compiler

### 1. 功能目标
本功能属于 S19。建立多来源规则加载、编译、增量检查、修复任务和 Verification Gate。 本项聚焦于规则来源、编译版本和验证门，必须形成可以独立启动、测试或演示的最小闭环。

### 2. 范围与边界
- 包含：rule compiler所需契约、状态转换、错误返回、日志事件和自动化验收。
- 不包含：未列入本阶段的真实 Provider、跨阶段优化或视觉润色；依赖能力只能通过 Port、Fake 或 fixture 接入。
- 上游依赖：S19-001。依赖未 verified 时，本功能只能处于 blocked。
- 主要产物：诊断项、检查结果、修复任务和门禁决策。

### 3. 输入、输出与处理流程
输入来自上游命令、查询、事件、配置、用户动作或 fixture，必须具备稳定标识、版本和必要授权上下文。

处理顺序：首先校验格式、当前状态、权限和依赖版本；其次，围绕功能 [rule compiler] 完成输入校验、领域处理、持久化或投影、用户反馈和可重复验收。；然后产生领域事件或标准结果并更新必要投影；最后返回成功、等待、失败或取消结果，并记录阶段进度。

输出为诊断项、检查结果、修复任务和门禁决策。计划中的最小验收为：规则解析、优先级、revision、diagnostic code。

### 4. 状态、数据与一致性
- 状态变化必须遵守领域模型与事件规范，禁止直接修改 projection 绕过 domain command。
- SQLite Event Store，或本阶段明确声明的 Fake Store，是业务事实来源；日志只用于诊断。
- 外部副作用必须具有 operation_id、幂等策略、取消边界和恢复结论。
- 并发写入遵循单写者或 Write Claim；查询需要说明一致性级别和 cursor。

### 5. 异常、取消与恢复
必须设计输入无效、依赖不可用、权限拒绝、超时、用户取消、进程重启和重复提交路径。每类路径都要定义稳定 error_code、用户提示、是否可重试、是否产生事件和重启动作。不安全时进入明确的 blocked、failed 或 interrupted，不能静默成功。

### 6. 日志与安全要求
入口初始化日志，记录 stage.started、关键进度、stage.completed 或 stage.failed。日志采用非 JSON、非 key=value 的单行位置式格式：时间、级别、PID、任务链 UUID、线程或协程、logger、最后一级文件名和行号、主消息。不记录客户端地址；诊断上下文仅在 DEBUG 级别展示并置于主消息前，INFO 及以上隐藏。错误级问题不得静默放行。

### 7. 测试与可运行验收
- 必须提供 cargo 命令、CLI 子命令、集成测试或可重复 demo。
- 至少覆盖成功、失败、取消、重复调用以及适用的恢复或回滚路径。
- 日志断言任务链 UUID、稳定 message code、进度闭环、真实文件名和行号、无敏感信息泄漏。
- 回滚只能删除本阶段临时产物或恢复快照，不得删除用户数据或覆盖外部修改。

### 8. 后续代码注释要求
编码前必须通过本条目审核。实现代码需要为公共类型和函数、状态转换、协议字段、数据库迁移、异步任务边界、错误分支和安全判定添加中文详细注释。注释应解释设计原因、约束和失败恢复；复杂算法、并发或锁、取消传播、幂等键和安全边界还要写明前置条件、后置条件及不变量。测试代码需要说明 fixture 意图和验收理由。

### 9. 审核结论栏
- 需求理解：待审核
- 架构边界：待审核
- 接口和事件：待审核
- 异常与恢复：待审核
- 安全与日志：待审核
- 允许进入代码开发：否

<!-- END S19-002 -->
## S19-003：changed-file detector

### 1. 功能目标
本功能属于 S19。建立多来源规则加载、编译、增量检查、修复任务和 Verification Gate。 本项聚焦于规则来源、编译版本和验证门，必须形成可以独立启动、测试或演示的最小闭环。

### 2. 范围与边界
- 包含：changed-file detector所需契约、状态转换、错误返回、日志事件和自动化验收。
- 不包含：未列入本阶段的真实 Provider、跨阶段优化或视觉润色；依赖能力只能通过 Port、Fake 或 fixture 接入。
- 上游依赖：S19-002。依赖未 verified 时，本功能只能处于 blocked。
- 主要产物：诊断项、检查结果、修复任务和门禁决策。

### 3. 输入、输出与处理流程
输入来自上游命令、查询、事件、配置、用户动作或 fixture，必须具备稳定标识、版本和必要授权上下文。

处理顺序：首先校验格式、当前状态、权限和依赖版本；其次，围绕功能 [changed-file detector] 完成输入校验、领域处理、持久化或投影、用户反馈和可重复验收。；然后产生领域事件或标准结果并更新必要投影；最后返回成功、等待、失败或取消结果，并记录阶段进度。

输出为诊断项、检查结果、修复任务和门禁决策。计划中的最小验收为：只检查本次变更文件。

### 4. 状态、数据与一致性
- 状态变化必须遵守领域模型与事件规范，禁止直接修改 projection 绕过 domain command。
- SQLite Event Store，或本阶段明确声明的 Fake Store，是业务事实来源；日志只用于诊断。
- 外部副作用必须具有 operation_id、幂等策略、取消边界和恢复结论。
- 并发写入遵循单写者或 Write Claim；查询需要说明一致性级别和 cursor。

### 5. 异常、取消与恢复
必须设计输入无效、依赖不可用、权限拒绝、超时、用户取消、进程重启和重复提交路径。每类路径都要定义稳定 error_code、用户提示、是否可重试、是否产生事件和重启动作。不安全时进入明确的 blocked、failed 或 interrupted，不能静默成功。

### 6. 日志与安全要求
入口初始化日志，记录 stage.started、关键进度、stage.completed 或 stage.failed。日志采用非 JSON、非 key=value 的单行位置式格式：时间、级别、PID、任务链 UUID、线程或协程、logger、最后一级文件名和行号、主消息。不记录客户端地址；诊断上下文仅在 DEBUG 级别展示并置于主消息前，INFO 及以上隐藏。错误级问题不得静默放行。

### 7. 测试与可运行验收
- 必须提供 cargo 命令、CLI 子命令、集成测试或可重复 demo。
- 至少覆盖成功、失败、取消、重复调用以及适用的恢复或回滚路径。
- 日志断言任务链 UUID、稳定 message code、进度闭环、真实文件名和行号、无敏感信息泄漏。
- 回滚只能删除本阶段临时产物或恢复快照，不得删除用户数据或覆盖外部修改。

### 8. 后续代码注释要求
编码前必须通过本条目审核。实现代码需要为公共类型和函数、状态转换、协议字段、数据库迁移、异步任务边界、错误分支和安全判定添加中文详细注释。注释应解释设计原因、约束和失败恢复；复杂算法、并发或锁、取消传播、幂等键和安全边界还要写明前置条件、后置条件及不变量。测试代码需要说明 fixture 意图和验收理由。

### 9. 审核结论栏
- 需求理解：待审核
- 架构边界：待审核
- 接口和事件：待审核
- 异常与恢复：待审核
- 安全与日志：待审核
- 允许进入代码开发：否

<!-- END S19-003 -->
## S19-004：format/lint/test adapter

### 1. 功能目标
本功能属于 S19。建立多来源规则加载、编译、增量检查、修复任务和 Verification Gate。 本项聚焦于规则来源、编译版本和验证门，必须形成可以独立启动、测试或演示的最小闭环。

### 2. 范围与边界
- 包含：format/lint/test adapter所需契约、状态转换、错误返回、日志事件和自动化验收。
- 不包含：未列入本阶段的真实 Provider、跨阶段优化或视觉润色；依赖能力只能通过 Port、Fake 或 fixture 接入。
- 上游依赖：S19-003。依赖未 verified 时，本功能只能处于 blocked。
- 主要产物：诊断项、检查结果、修复任务和门禁决策。

### 3. 输入、输出与处理流程
输入来自上游命令、查询、事件、配置、用户动作或 fixture，必须具备稳定标识、版本和必要授权上下文。

处理顺序：首先校验格式、当前状态、权限和依赖版本；其次，围绕功能 [format/lint/test adapter] 完成输入校验、领域处理、持久化或投影、用户反馈和可重复验收。；然后产生领域事件或标准结果并更新必要投影；最后返回成功、等待、失败或取消结果，并记录阶段进度。

输出为诊断项、检查结果、修复任务和门禁决策。计划中的最小验收为：运行规则命令并收集结果。

### 4. 状态、数据与一致性
- 状态变化必须遵守领域模型与事件规范，禁止直接修改 projection 绕过 domain command。
- SQLite Event Store，或本阶段明确声明的 Fake Store，是业务事实来源；日志只用于诊断。
- 外部副作用必须具有 operation_id、幂等策略、取消边界和恢复结论。
- 并发写入遵循单写者或 Write Claim；查询需要说明一致性级别和 cursor。

### 5. 异常、取消与恢复
必须设计输入无效、依赖不可用、权限拒绝、超时、用户取消、进程重启和重复提交路径。每类路径都要定义稳定 error_code、用户提示、是否可重试、是否产生事件和重启动作。不安全时进入明确的 blocked、failed 或 interrupted，不能静默成功。

### 6. 日志与安全要求
入口初始化日志，记录 stage.started、关键进度、stage.completed 或 stage.failed。日志采用非 JSON、非 key=value 的单行位置式格式：时间、级别、PID、任务链 UUID、线程或协程、logger、最后一级文件名和行号、主消息。不记录客户端地址；诊断上下文仅在 DEBUG 级别展示并置于主消息前，INFO 及以上隐藏。错误级问题不得静默放行。

### 7. 测试与可运行验收
- 必须提供 cargo 命令、CLI 子命令、集成测试或可重复 demo。
- 至少覆盖成功、失败、取消、重复调用以及适用的恢复或回滚路径。
- 日志断言任务链 UUID、稳定 message code、进度闭环、真实文件名和行号、无敏感信息泄漏。
- 回滚只能删除本阶段临时产物或恢复快照，不得删除用户数据或覆盖外部修改。

### 8. 后续代码注释要求
编码前必须通过本条目审核。实现代码需要为公共类型和函数、状态转换、协议字段、数据库迁移、异步任务边界、错误分支和安全判定添加中文详细注释。注释应解释设计原因、约束和失败恢复；复杂算法、并发或锁、取消传播、幂等键和安全边界还要写明前置条件、后置条件及不变量。测试代码需要说明 fixture 意图和验收理由。

### 9. 审核结论栏
- 需求理解：待审核
- 架构边界：待审核
- 接口和事件：待审核
- 异常与恢复：待审核
- 安全与日志：待审核
- 允许进入代码开发：否

<!-- END S19-004 -->
## S19-005：PostToolUse hook

### 1. 功能目标
本功能属于 S19。建立多来源规则加载、编译、增量检查、修复任务和 Verification Gate。 本项聚焦于Tool Gateway、工具契约和副作用隔离，必须形成可以独立启动、测试或演示的最小闭环。

### 2. 范围与边界
- 包含：PostToolUse hook所需契约、状态转换、错误返回、日志事件和自动化验收。
- 不包含：未列入本阶段的真实 Provider、跨阶段优化或视觉润色；依赖能力只能通过 Port、Fake 或 fixture 接入。
- 上游依赖：S19-004。依赖未 verified 时，本功能只能处于 blocked。
- 主要产物：ToolCall、标准结果、变更和快照摘要。

### 3. 输入、输出与处理流程
输入来自上游命令、查询、事件、配置、用户动作或 fixture，必须具备稳定标识、版本和必要授权上下文。

处理顺序：首先校验格式、当前状态、权限和依赖版本；其次，围绕功能 [PostToolUse hook] 完成输入校验、领域处理、持久化或投影、用户反馈和可重复验收。；然后产生领域事件或标准结果并更新必要投影；最后返回成功、等待、失败或取消结果，并记录阶段进度。

输出为ToolCall、标准结果、变更和快照摘要。计划中的最小验收为：每次 Write/Edit 后自动触发增量检查。

### 4. 状态、数据与一致性
- 状态变化必须遵守领域模型与事件规范，禁止直接修改 projection 绕过 domain command。
- SQLite Event Store，或本阶段明确声明的 Fake Store，是业务事实来源；日志只用于诊断。
- 外部副作用必须具有 operation_id、幂等策略、取消边界和恢复结论。
- 并发写入遵循单写者或 Write Claim；查询需要说明一致性级别和 cursor。

### 5. 异常、取消与恢复
必须设计输入无效、依赖不可用、权限拒绝、超时、用户取消、进程重启和重复提交路径。每类路径都要定义稳定 error_code、用户提示、是否可重试、是否产生事件和重启动作。不安全时进入明确的 blocked、failed 或 interrupted，不能静默成功。

### 6. 日志与安全要求
入口初始化日志，记录 stage.started、关键进度、stage.completed 或 stage.failed。日志采用非 JSON、非 key=value 的单行位置式格式：时间、级别、PID、任务链 UUID、线程或协程、logger、最后一级文件名和行号、主消息。不记录客户端地址；诊断上下文仅在 DEBUG 级别展示并置于主消息前，INFO 及以上隐藏。工具不得绕过 Gateway、权限、取消、超时和日志门禁。

### 7. 测试与可运行验收
- 必须提供 cargo 命令、CLI 子命令、集成测试或可重复 demo。
- 至少覆盖成功、失败、取消、重复调用以及适用的恢复或回滚路径。
- 日志断言任务链 UUID、稳定 message code、进度闭环、真实文件名和行号、无敏感信息泄漏。
- 回滚只能删除本阶段临时产物或恢复快照，不得删除用户数据或覆盖外部修改。

### 8. 后续代码注释要求
编码前必须通过本条目审核。实现代码需要为公共类型和函数、状态转换、协议字段、数据库迁移、异步任务边界、错误分支和安全判定添加中文详细注释。注释应解释设计原因、约束和失败恢复；复杂算法、并发或锁、取消传播、幂等键和安全边界还要写明前置条件、后置条件及不变量。测试代码需要说明 fixture 意图和验收理由。

### 9. 审核结论栏
- 需求理解：待审核
- 架构边界：待审核
- 接口和事件：待审核
- 异常与恢复：待审核
- 安全与日志：待审核
- 允许进入代码开发：否

<!-- END S19-005 -->
## S19-006：repair task

### 1. 功能目标
本功能属于 S19。建立多来源规则加载、编译、增量检查、修复任务和 Verification Gate。 本项聚焦于规则来源、编译版本和验证门，必须形成可以独立启动、测试或演示的最小闭环。

### 2. 范围与边界
- 包含：repair task所需契约、状态转换、错误返回、日志事件和自动化验收。
- 不包含：未列入本阶段的真实 Provider、跨阶段优化或视觉润色；依赖能力只能通过 Port、Fake 或 fixture 接入。
- 上游依赖：S19-005。依赖未 verified 时，本功能只能处于 blocked。
- 主要产物：诊断项、检查结果、修复任务和门禁决策。

### 3. 输入、输出与处理流程
输入来自上游命令、查询、事件、配置、用户动作或 fixture，必须具备稳定标识、版本和必要授权上下文。

处理顺序：首先校验格式、当前状态、权限和依赖版本；其次，围绕功能 [repair task] 完成输入校验、领域处理、持久化或投影、用户反馈和可重复验收。；然后产生领域事件或标准结果并更新必要投影；最后返回成功、等待、失败或取消结果，并记录阶段进度。

输出为诊断项、检查结果、修复任务和门禁决策。计划中的最小验收为：error 级问题生成修复子任务。

### 4. 状态、数据与一致性
- 状态变化必须遵守领域模型与事件规范，禁止直接修改 projection 绕过 domain command。
- SQLite Event Store，或本阶段明确声明的 Fake Store，是业务事实来源；日志只用于诊断。
- 外部副作用必须具有 operation_id、幂等策略、取消边界和恢复结论。
- 并发写入遵循单写者或 Write Claim；查询需要说明一致性级别和 cursor。

### 5. 异常、取消与恢复
必须设计输入无效、依赖不可用、权限拒绝、超时、用户取消、进程重启和重复提交路径。每类路径都要定义稳定 error_code、用户提示、是否可重试、是否产生事件和重启动作。不安全时进入明确的 blocked、failed 或 interrupted，不能静默成功。

### 6. 日志与安全要求
入口初始化日志，记录 stage.started、关键进度、stage.completed 或 stage.failed。日志采用非 JSON、非 key=value 的单行位置式格式：时间、级别、PID、任务链 UUID、线程或协程、logger、最后一级文件名和行号、主消息。不记录客户端地址；诊断上下文仅在 DEBUG 级别展示并置于主消息前，INFO 及以上隐藏。错误级问题不得静默放行。

### 7. 测试与可运行验收
- 必须提供 cargo 命令、CLI 子命令、集成测试或可重复 demo。
- 至少覆盖成功、失败、取消、重复调用以及适用的恢复或回滚路径。
- 日志断言任务链 UUID、稳定 message code、进度闭环、真实文件名和行号、无敏感信息泄漏。
- 回滚只能删除本阶段临时产物或恢复快照，不得删除用户数据或覆盖外部修改。

### 8. 后续代码注释要求
编码前必须通过本条目审核。实现代码需要为公共类型和函数、状态转换、协议字段、数据库迁移、异步任务边界、错误分支和安全判定添加中文详细注释。注释应解释设计原因、约束和失败恢复；复杂算法、并发或锁、取消传播、幂等键和安全边界还要写明前置条件、后置条件及不变量。测试代码需要说明 fixture 意图和验收理由。

### 9. 审核结论栏
- 需求理解：待审核
- 架构边界：待审核
- 接口和事件：待审核
- 异常与恢复：待审核
- 安全与日志：待审核
- 允许进入代码开发：否

<!-- END S19-006 -->
## S19-007：verification gate

### 1. 功能目标
本功能属于 S19。建立多来源规则加载、编译、增量检查、修复任务和 Verification Gate。 本项聚焦于Spec 工件、状态和追踪关系，必须形成可以独立启动、测试或演示的最小闭环。

### 2. 范围与边界
- 包含：verification gate所需契约、状态转换、错误返回、日志事件和自动化验收。
- 不包含：未列入本阶段的真实 Provider、跨阶段优化或视觉润色；依赖能力只能通过 Port、Fake 或 fixture 接入。
- 上游依赖：S19-006。依赖未 verified 时，本功能只能处于 blocked。
- 主要产物：工件、审核结果、状态变化和失效关系。

### 3. 输入、输出与处理流程
输入来自上游命令、查询、事件、配置、用户动作或 fixture，必须具备稳定标识、版本和必要授权上下文。

处理顺序：首先校验格式、当前状态、权限和依赖版本；其次，聚合规则诊断、测试结果、风险和变更范围，生成 Gate 决策并写入事件。；然后产生领域事件或标准结果并更新必要投影；最后返回成功、等待、失败或取消结果，并记录阶段进度。

输出为工件、审核结果、状态变化和失效关系。计划中的最小验收为：gate 阻断或放行有事件和安全理由。

### 4. 状态、数据与一致性
- 状态变化必须遵守领域模型与事件规范，禁止直接修改 projection 绕过 domain command。
- SQLite Event Store，或本阶段明确声明的 Fake Store，是业务事实来源；日志只用于诊断。
- 外部副作用必须具有 operation_id、幂等策略、取消边界和恢复结论。
- 并发写入遵循单写者或 Write Claim；查询需要说明一致性级别和 cursor。

### 5. 异常、取消与恢复
必须设计输入无效、依赖不可用、权限拒绝、超时、用户取消、进程重启和重复提交路径。每类路径都要定义稳定 error_code、用户提示、是否可重试、是否产生事件和重启动作。不安全时进入明确的 blocked、failed 或 interrupted，不能静默成功。

### 6. 日志与安全要求
入口初始化日志，记录 stage.started、关键进度、stage.completed 或 stage.failed。日志采用非 JSON、非 key=value 的单行位置式格式：时间、级别、PID、任务链 UUID、线程或协程、logger、最后一级文件名和行号、主消息。不记录客户端地址；诊断上下文仅在 DEBUG 级别展示并置于主消息前，INFO 及以上隐藏。跳过必须有授权，上游变更必须使下游可检测失效。

### 7. 测试与可运行验收
- 必须提供 cargo 命令、CLI 子命令、集成测试或可重复 demo。
- 至少覆盖成功、失败、取消、重复调用以及适用的恢复或回滚路径。
- 日志断言任务链 UUID、稳定 message code、进度闭环、真实文件名和行号、无敏感信息泄漏。
- 回滚只能删除本阶段临时产物或恢复快照，不得删除用户数据或覆盖外部修改。

### 8. 后续代码注释要求
编码前必须通过本条目审核。实现代码需要为公共类型和函数、状态转换、协议字段、数据库迁移、异步任务边界、错误分支和安全判定添加中文详细注释。注释应解释设计原因、约束和失败恢复；复杂算法、并发或锁、取消传播、幂等键和安全边界还要写明前置条件、后置条件及不变量。测试代码需要说明 fixture 意图和验收理由。

### 9. 审核结论栏
- 需求理解：待审核
- 架构边界：待审核
- 接口和事件：待审核
- 异常与恢复：待审核
- 安全与日志：待审核
- 允许进入代码开发：否

<!-- END S19-007 -->
## S19-008：rules quality panel

### 1. 功能目标
本功能属于 S19。建立多来源规则加载、编译、增量检查、修复任务和 Verification Gate。 本项聚焦于输入事件、纯状态转换和终端渲染，必须形成可以独立启动、测试或演示的最小闭环。

### 2. 范围与边界
- 包含：rules quality panel所需契约、状态转换、错误返回、日志事件和自动化验收。
- 不包含：未列入本阶段的真实 Provider、跨阶段优化或视觉润色；依赖能力只能通过 Port、Fake 或 fixture 接入。
- 上游依赖：S19-007。依赖未 verified 时，本功能只能处于 blocked。
- 主要产物：AppState、Effect、渲染帧和用户提示。

### 3. 输入、输出与处理流程
输入来自上游命令、查询、事件、配置、用户动作或 fixture，必须具备稳定标识、版本和必要授权上下文。

处理顺序：首先校验格式、当前状态、权限和依赖版本；其次，围绕功能 [rules quality panel] 完成输入校验、领域处理、持久化或投影、用户反馈和可重复验收。；然后产生领域事件或标准结果并更新必要投影；最后返回成功、等待、失败或取消结果，并记录阶段进度。

输出为AppState、Effect、渲染帧和用户提示。计划中的最小验收为：TUI 显示通过、警告、错误、修复中。

### 4. 状态、数据与一致性
- 状态变化必须遵守领域模型与事件规范，禁止直接修改 projection 绕过 domain command。
- SQLite Event Store，或本阶段明确声明的 Fake Store，是业务事实来源；日志只用于诊断。
- 外部副作用必须具有 operation_id、幂等策略、取消边界和恢复结论。
- 并发写入遵循单写者或 Write Claim；查询需要说明一致性级别和 cursor。

### 5. 异常、取消与恢复
必须设计输入无效、依赖不可用、权限拒绝、超时、用户取消、进程重启和重复提交路径。每类路径都要定义稳定 error_code、用户提示、是否可重试、是否产生事件和重启动作。不安全时进入明确的 blocked、failed 或 interrupted，不能静默成功。

### 6. 日志与安全要求
入口初始化日志，记录 stage.started、关键进度、stage.completed 或 stage.failed。日志采用非 JSON、非 key=value 的单行位置式格式：时间、级别、PID、任务链 UUID、线程或协程、logger、最后一级文件名和行号、主消息。不记录客户端地址；诊断上下文仅在 DEBUG 级别展示并置于主消息前，INFO 及以上隐藏。渲染层不得产生业务副作用，窄终端和 Unicode 不得越界。

### 7. 测试与可运行验收
- 必须提供 cargo 命令、CLI 子命令、集成测试或可重复 demo。
- 至少覆盖成功、失败、取消、重复调用以及适用的恢复或回滚路径。
- 日志断言任务链 UUID、稳定 message code、进度闭环、真实文件名和行号、无敏感信息泄漏。
- 回滚只能删除本阶段临时产物或恢复快照，不得删除用户数据或覆盖外部修改。

### 8. 后续代码注释要求
编码前必须通过本条目审核。实现代码需要为公共类型和函数、状态转换、协议字段、数据库迁移、异步任务边界、错误分支和安全判定添加中文详细注释。注释应解释设计原因、约束和失败恢复；复杂算法、并发或锁、取消传播、幂等键和安全边界还要写明前置条件、后置条件及不变量。测试代码需要说明 fixture 意图和验收理由。

### 9. 审核结论栏
- 需求理解：待审核
- 架构边界：待审核
- 接口和事件：待审核
- 异常与恢复：待审核
- 安全与日志：待审核
- 允许进入代码开发：否

<!-- END S19-008 -->
## S19-009：`v0.1-rules-rc1` demo

### 1. 功能目标
本功能属于 S19。建立多来源规则加载、编译、增量检查、修复任务和 Verification Gate。 本项聚焦于规则来源、编译版本和验证门，必须形成可以独立启动、测试或演示的最小闭环。

### 2. 范围与边界
- 包含：`v0.1-rules-rc1` demo所需契约、状态转换、错误返回、日志事件和自动化验收。
- 不包含：未列入本阶段的真实 Provider、跨阶段优化或视觉润色；依赖能力只能通过 Port、Fake 或 fixture 接入。
- 上游依赖：S19-008。依赖未 verified 时，本功能只能处于 blocked。
- 主要产物：诊断项、检查结果、修复任务和门禁决策。

### 3. 输入、输出与处理流程
输入来自上游命令、查询、事件、配置、用户动作或 fixture，必须具备稳定标识、版本和必要授权上下文。

处理顺序：首先校验格式、当前状态、权限和依赖版本；其次，围绕功能 [`v0.1-rules-rc1` demo] 完成输入校验、领域处理、持久化或投影、用户反馈和可重复验收。；然后产生领域事件或标准结果并更新必要投影；最后返回成功、等待、失败或取消结果，并记录阶段进度。

输出为诊断项、检查结果、修复任务和门禁决策。计划中的最小验收为：故意引入违规代码，流程自动阻断并修复。

### 4. 状态、数据与一致性
- 状态变化必须遵守领域模型与事件规范，禁止直接修改 projection 绕过 domain command。
- SQLite Event Store，或本阶段明确声明的 Fake Store，是业务事实来源；日志只用于诊断。
- 外部副作用必须具有 operation_id、幂等策略、取消边界和恢复结论。
- 并发写入遵循单写者或 Write Claim；查询需要说明一致性级别和 cursor。

### 5. 异常、取消与恢复
必须设计输入无效、依赖不可用、权限拒绝、超时、用户取消、进程重启和重复提交路径。每类路径都要定义稳定 error_code、用户提示、是否可重试、是否产生事件和重启动作。不安全时进入明确的 blocked、failed 或 interrupted，不能静默成功。

### 6. 日志与安全要求
入口初始化日志，记录 stage.started、关键进度、stage.completed 或 stage.failed。日志采用非 JSON、非 key=value 的单行位置式格式：时间、级别、PID、任务链 UUID、线程或协程、logger、最后一级文件名和行号、主消息。不记录客户端地址；诊断上下文仅在 DEBUG 级别展示并置于主消息前，INFO 及以上隐藏。错误级问题不得静默放行。

### 7. 测试与可运行验收
- 必须提供 cargo 命令、CLI 子命令、集成测试或可重复 demo。
- 至少覆盖成功、失败、取消、重复调用以及适用的恢复或回滚路径。
- 日志断言任务链 UUID、稳定 message code、进度闭环、真实文件名和行号、无敏感信息泄漏。
- 回滚只能删除本阶段临时产物或恢复快照，不得删除用户数据或覆盖外部修改。

### 8. 后续代码注释要求
编码前必须通过本条目审核。实现代码需要为公共类型和函数、状态转换、协议字段、数据库迁移、异步任务边界、错误分支和安全判定添加中文详细注释。注释应解释设计原因、约束和失败恢复；复杂算法、并发或锁、取消传播、幂等键和安全边界还要写明前置条件、后置条件及不变量。测试代码需要说明 fixture 意图和验收理由。

### 9. 审核结论栏
- 需求理解：待审核
- 架构边界：待审核
- 接口和事件：待审核
- 异常与恢复：待审核
- 安全与日志：待审核
- 允许进入代码开发：否

<!-- END S19-009 -->
## S20-001：context item 分类

### 1. 功能目标
本功能属于 S20。建立上下文分类、Token 预算、Checkpoint、压缩、前缀缓存和恢复。 本项聚焦于上下文条目、预算和 Checkpoint，必须形成可以独立启动、测试或演示的最小闭环。

### 2. 范围与边界
- 包含：context item 分类所需契约、状态转换、错误返回、日志事件和自动化验收。
- 不包含：未列入本阶段的真实 Provider、跨阶段优化或视觉润色；依赖能力只能通过 Port、Fake 或 fixture 接入。
- 上游依赖：S19。依赖未 verified 时，本功能只能处于 blocked。
- 主要产物：预算报告、摘要、版本和恢复上下文。

### 3. 输入、输出与处理流程
输入来自上游命令、查询、事件、配置、用户动作或 fixture，必须具备稳定标识、版本和必要授权上下文。

处理顺序：首先校验格式、当前状态、权限和依赖版本；其次，围绕功能 [context item 分类] 完成输入校验、领域处理、持久化或投影、用户反馈和可重复验收。；然后产生领域事件或标准结果并更新必要投影；最后返回成功、等待、失败或取消结果，并记录阶段进度。

输出为预算报告、摘要、版本和恢复上下文。计划中的最小验收为：system/rule/spec/history/tool/external 分层。

### 4. 状态、数据与一致性
- 状态变化必须遵守领域模型与事件规范，禁止直接修改 projection 绕过 domain command。
- SQLite Event Store，或本阶段明确声明的 Fake Store，是业务事实来源；日志只用于诊断。
- 外部副作用必须具有 operation_id、幂等策略、取消边界和恢复结论。
- 并发写入遵循单写者或 Write Claim；查询需要说明一致性级别和 cursor。

### 5. 异常、取消与恢复
必须设计输入无效、依赖不可用、权限拒绝、超时、用户取消、进程重启和重复提交路径。每类路径都要定义稳定 error_code、用户提示、是否可重试、是否产生事件和重启动作。不安全时进入明确的 blocked、failed 或 interrupted，不能静默成功。

### 6. 日志与安全要求
入口初始化日志，记录 stage.started、关键进度、stage.completed 或 stage.failed。日志采用非 JSON、非 key=value 的单行位置式格式：时间、级别、PID、任务链 UUID、线程或协程、logger、最后一级文件名和行号、主消息。不记录客户端地址；诊断上下文仅在 DEBUG 级别展示并置于主消息前，INFO 及以上隐藏。压缩不得丢失任务约束、工具事实和用户确认。

### 7. 测试与可运行验收
- 必须提供 cargo 命令、CLI 子命令、集成测试或可重复 demo。
- 至少覆盖成功、失败、取消、重复调用以及适用的恢复或回滚路径。
- 日志断言任务链 UUID、稳定 message code、进度闭环、真实文件名和行号、无敏感信息泄漏。
- 回滚只能删除本阶段临时产物或恢复快照，不得删除用户数据或覆盖外部修改。

### 8. 后续代码注释要求
编码前必须通过本条目审核。实现代码需要为公共类型和函数、状态转换、协议字段、数据库迁移、异步任务边界、错误分支和安全判定添加中文详细注释。注释应解释设计原因、约束和失败恢复；复杂算法、并发或锁、取消传播、幂等键和安全边界还要写明前置条件、后置条件及不变量。测试代码需要说明 fixture 意图和验收理由。

### 9. 审核结论栏
- 需求理解：待审核
- 架构边界：待审核
- 接口和事件：待审核
- 异常与恢复：待审核
- 安全与日志：待审核
- 允许进入代码开发：否

<!-- END S20-001 -->
## S20-002：token budget calculator

### 1. 功能目标
本功能属于 S20。建立上下文分类、Token 预算、Checkpoint、压缩、前缀缓存和恢复。 本项聚焦于上下文条目、预算和 Checkpoint，必须形成可以独立启动、测试或演示的最小闭环。

### 2. 范围与边界
- 包含：token budget calculator所需契约、状态转换、错误返回、日志事件和自动化验收。
- 不包含：未列入本阶段的真实 Provider、跨阶段优化或视觉润色；依赖能力只能通过 Port、Fake 或 fixture 接入。
- 上游依赖：S20-001。依赖未 verified 时，本功能只能处于 blocked。
- 主要产物：预算报告、摘要、版本和恢复上下文。

### 3. 输入、输出与处理流程
输入来自上游命令、查询、事件、配置、用户动作或 fixture，必须具备稳定标识、版本和必要授权上下文。

处理顺序：首先校验格式、当前状态、权限和依赖版本；其次，围绕功能 [token budget calculator] 完成输入校验、领域处理、持久化或投影、用户反馈和可重复验收。；然后产生领域事件或标准结果并更新必要投影；最后返回成功、等待、失败或取消结果，并记录阶段进度。

输出为预算报告、摘要、版本和恢复上下文。计划中的最小验收为：预算、保留区、超限原因可查询。

### 4. 状态、数据与一致性
- 状态变化必须遵守领域模型与事件规范，禁止直接修改 projection 绕过 domain command。
- SQLite Event Store，或本阶段明确声明的 Fake Store，是业务事实来源；日志只用于诊断。
- 外部副作用必须具有 operation_id、幂等策略、取消边界和恢复结论。
- 并发写入遵循单写者或 Write Claim；查询需要说明一致性级别和 cursor。

### 5. 异常、取消与恢复
必须设计输入无效、依赖不可用、权限拒绝、超时、用户取消、进程重启和重复提交路径。每类路径都要定义稳定 error_code、用户提示、是否可重试、是否产生事件和重启动作。不安全时进入明确的 blocked、failed 或 interrupted，不能静默成功。

### 6. 日志与安全要求
入口初始化日志，记录 stage.started、关键进度、stage.completed 或 stage.failed。日志采用非 JSON、非 key=value 的单行位置式格式：时间、级别、PID、任务链 UUID、线程或协程、logger、最后一级文件名和行号、主消息。不记录客户端地址；诊断上下文仅在 DEBUG 级别展示并置于主消息前，INFO 及以上隐藏。压缩不得丢失任务约束、工具事实和用户确认。

### 7. 测试与可运行验收
- 必须提供 cargo 命令、CLI 子命令、集成测试或可重复 demo。
- 至少覆盖成功、失败、取消、重复调用以及适用的恢复或回滚路径。
- 日志断言任务链 UUID、稳定 message code、进度闭环、真实文件名和行号、无敏感信息泄漏。
- 回滚只能删除本阶段临时产物或恢复快照，不得删除用户数据或覆盖外部修改。

### 8. 后续代码注释要求
编码前必须通过本条目审核。实现代码需要为公共类型和函数、状态转换、协议字段、数据库迁移、异步任务边界、错误分支和安全判定添加中文详细注释。注释应解释设计原因、约束和失败恢复；复杂算法、并发或锁、取消传播、幂等键和安全边界还要写明前置条件、后置条件及不变量。测试代码需要说明 fixture 意图和验收理由。

### 9. 审核结论栏
- 需求理解：待审核
- 架构边界：待审核
- 接口和事件：待审核
- 异常与恢复：待审核
- 安全与日志：待审核
- 允许进入代码开发：否

<!-- END S20-002 -->
## S20-003：Checkpoint schema

### 1. 功能目标
本功能属于 S20。建立上下文分类、Token 预算、Checkpoint、压缩、前缀缓存和恢复。 本项聚焦于上下文条目、预算和 Checkpoint，必须形成可以独立启动、测试或演示的最小闭环。

### 2. 范围与边界
- 包含：Checkpoint schema所需契约、状态转换、错误返回、日志事件和自动化验收。
- 不包含：未列入本阶段的真实 Provider、跨阶段优化或视觉润色；依赖能力只能通过 Port、Fake 或 fixture 接入。
- 上游依赖：S20-002。依赖未 verified 时，本功能只能处于 blocked。
- 主要产物：预算报告、摘要、版本和恢复上下文。

### 3. 输入、输出与处理流程
输入来自上游命令、查询、事件、配置、用户动作或 fixture，必须具备稳定标识、版本和必要授权上下文。

处理顺序：首先校验格式、当前状态、权限和依赖版本；其次，围绕功能 [Checkpoint schema] 完成输入校验、领域处理、持久化或投影、用户反馈和可重复验收。；然后产生领域事件或标准结果并更新必要投影；最后返回成功、等待、失败或取消结果，并记录阶段进度。

输出为预算报告、摘要、版本和恢复上下文。计划中的最小验收为：输入、稳定前缀、摘要、usage、revision 可保存。

### 4. 状态、数据与一致性
- 状态变化必须遵守领域模型与事件规范，禁止直接修改 projection 绕过 domain command。
- SQLite Event Store，或本阶段明确声明的 Fake Store，是业务事实来源；日志只用于诊断。
- 外部副作用必须具有 operation_id、幂等策略、取消边界和恢复结论。
- 并发写入遵循单写者或 Write Claim；查询需要说明一致性级别和 cursor。

### 5. 异常、取消与恢复
必须设计输入无效、依赖不可用、权限拒绝、超时、用户取消、进程重启和重复提交路径。每类路径都要定义稳定 error_code、用户提示、是否可重试、是否产生事件和重启动作。不安全时进入明确的 blocked、failed 或 interrupted，不能静默成功。

### 6. 日志与安全要求
入口初始化日志，记录 stage.started、关键进度、stage.completed 或 stage.failed。日志采用非 JSON、非 key=value 的单行位置式格式：时间、级别、PID、任务链 UUID、线程或协程、logger、最后一级文件名和行号、主消息。不记录客户端地址；诊断上下文仅在 DEBUG 级别展示并置于主消息前，INFO 及以上隐藏。压缩不得丢失任务约束、工具事实和用户确认。

### 7. 测试与可运行验收
- 必须提供 cargo 命令、CLI 子命令、集成测试或可重复 demo。
- 至少覆盖成功、失败、取消、重复调用以及适用的恢复或回滚路径。
- 日志断言任务链 UUID、稳定 message code、进度闭环、真实文件名和行号、无敏感信息泄漏。
- 回滚只能删除本阶段临时产物或恢复快照，不得删除用户数据或覆盖外部修改。

### 8. 后续代码注释要求
编码前必须通过本条目审核。实现代码需要为公共类型和函数、状态转换、协议字段、数据库迁移、异步任务边界、错误分支和安全判定添加中文详细注释。注释应解释设计原因、约束和失败恢复；复杂算法、并发或锁、取消传播、幂等键和安全边界还要写明前置条件、后置条件及不变量。测试代码需要说明 fixture 意图和验收理由。

### 9. 审核结论栏
- 需求理解：待审核
- 架构边界：待审核
- 接口和事件：待审核
- 异常与恢复：待审核
- 安全与日志：待审核
- 允许进入代码开发：否

<!-- END S20-003 -->
## S20-004：checkpoint materializer

### 1. 功能目标
本功能属于 S20。建立上下文分类、Token 预算、Checkpoint、压缩、前缀缓存和恢复。 本项聚焦于上下文条目、预算和 Checkpoint，必须形成可以独立启动、测试或演示的最小闭环。

### 2. 范围与边界
- 包含：checkpoint materializer所需契约、状态转换、错误返回、日志事件和自动化验收。
- 不包含：未列入本阶段的真实 Provider、跨阶段优化或视觉润色；依赖能力只能通过 Port、Fake 或 fixture 接入。
- 上游依赖：S20-003。依赖未 verified 时，本功能只能处于 blocked。
- 主要产物：预算报告、摘要、版本和恢复上下文。

### 3. 输入、输出与处理流程
输入来自上游命令、查询、事件、配置、用户动作或 fixture，必须具备稳定标识、版本和必要授权上下文。

处理顺序：首先校验格式、当前状态、权限和依赖版本；其次，围绕功能 [checkpoint materializer] 完成输入校验、领域处理、持久化或投影、用户反馈和可重复验收。；然后产生领域事件或标准结果并更新必要投影；最后返回成功、等待、失败或取消结果，并记录阶段进度。

输出为预算报告、摘要、版本和恢复上下文。计划中的最小验收为：turn 完成、cancel、tool 后生成 checkpoint。

### 4. 状态、数据与一致性
- 状态变化必须遵守领域模型与事件规范，禁止直接修改 projection 绕过 domain command。
- SQLite Event Store，或本阶段明确声明的 Fake Store，是业务事实来源；日志只用于诊断。
- 外部副作用必须具有 operation_id、幂等策略、取消边界和恢复结论。
- 并发写入遵循单写者或 Write Claim；查询需要说明一致性级别和 cursor。

### 5. 异常、取消与恢复
必须设计输入无效、依赖不可用、权限拒绝、超时、用户取消、进程重启和重复提交路径。每类路径都要定义稳定 error_code、用户提示、是否可重试、是否产生事件和重启动作。不安全时进入明确的 blocked、failed 或 interrupted，不能静默成功。

### 6. 日志与安全要求
入口初始化日志，记录 stage.started、关键进度、stage.completed 或 stage.failed。日志采用非 JSON、非 key=value 的单行位置式格式：时间、级别、PID、任务链 UUID、线程或协程、logger、最后一级文件名和行号、主消息。不记录客户端地址；诊断上下文仅在 DEBUG 级别展示并置于主消息前，INFO 及以上隐藏。压缩不得丢失任务约束、工具事实和用户确认。

### 7. 测试与可运行验收
- 必须提供 cargo 命令、CLI 子命令、集成测试或可重复 demo。
- 至少覆盖成功、失败、取消、重复调用以及适用的恢复或回滚路径。
- 日志断言任务链 UUID、稳定 message code、进度闭环、真实文件名和行号、无敏感信息泄漏。
- 回滚只能删除本阶段临时产物或恢复快照，不得删除用户数据或覆盖外部修改。

### 8. 后续代码注释要求
编码前必须通过本条目审核。实现代码需要为公共类型和函数、状态转换、协议字段、数据库迁移、异步任务边界、错误分支和安全判定添加中文详细注释。注释应解释设计原因、约束和失败恢复；复杂算法、并发或锁、取消传播、幂等键和安全边界还要写明前置条件、后置条件及不变量。测试代码需要说明 fixture 意图和验收理由。

### 9. 审核结论栏
- 需求理解：待审核
- 架构边界：待审核
- 接口和事件：待审核
- 异常与恢复：待审核
- 安全与日志：待审核
- 允许进入代码开发：否

<!-- END S20-004 -->
## S20-005：compaction strategy

### 1. 功能目标
本功能属于 S20。建立上下文分类、Token 预算、Checkpoint、压缩、前缀缓存和恢复。 本项聚焦于上下文条目、预算和 Checkpoint，必须形成可以独立启动、测试或演示的最小闭环。

### 2. 范围与边界
- 包含：compaction strategy所需契约、状态转换、错误返回、日志事件和自动化验收。
- 不包含：未列入本阶段的真实 Provider、跨阶段优化或视觉润色；依赖能力只能通过 Port、Fake 或 fixture 接入。
- 上游依赖：S20-004。依赖未 verified 时，本功能只能处于 blocked。
- 主要产物：预算报告、摘要、版本和恢复上下文。

### 3. 输入、输出与处理流程
输入来自上游命令、查询、事件、配置、用户动作或 fixture，必须具备稳定标识、版本和必要授权上下文。

处理顺序：首先校验格式、当前状态、权限和依赖版本；其次，优先加载稳定 Checkpoint，不足时按历史、工具输出、外部内容分级摘要。；然后产生领域事件或标准结果并更新必要投影；最后返回成功、等待、失败或取消结果，并记录阶段进度。

输出为预算报告、摘要、版本和恢复上下文。计划中的最小验收为：checkpoint-first，分级摘要兜底。

### 4. 状态、数据与一致性
- 状态变化必须遵守领域模型与事件规范，禁止直接修改 projection 绕过 domain command。
- SQLite Event Store，或本阶段明确声明的 Fake Store，是业务事实来源；日志只用于诊断。
- 外部副作用必须具有 operation_id、幂等策略、取消边界和恢复结论。
- 并发写入遵循单写者或 Write Claim；查询需要说明一致性级别和 cursor。

### 5. 异常、取消与恢复
必须设计输入无效、依赖不可用、权限拒绝、超时、用户取消、进程重启和重复提交路径。每类路径都要定义稳定 error_code、用户提示、是否可重试、是否产生事件和重启动作。不安全时进入明确的 blocked、failed 或 interrupted，不能静默成功。

### 6. 日志与安全要求
入口初始化日志，记录 stage.started、关键进度、stage.completed 或 stage.failed。日志采用非 JSON、非 key=value 的单行位置式格式：时间、级别、PID、任务链 UUID、线程或协程、logger、最后一级文件名和行号、主消息。不记录客户端地址；诊断上下文仅在 DEBUG 级别展示并置于主消息前，INFO 及以上隐藏。压缩不得丢失任务约束、工具事实和用户确认。

### 7. 测试与可运行验收
- 必须提供 cargo 命令、CLI 子命令、集成测试或可重复 demo。
- 至少覆盖成功、失败、取消、重复调用以及适用的恢复或回滚路径。
- 日志断言任务链 UUID、稳定 message code、进度闭环、真实文件名和行号、无敏感信息泄漏。
- 回滚只能删除本阶段临时产物或恢复快照，不得删除用户数据或覆盖外部修改。

### 8. 后续代码注释要求
编码前必须通过本条目审核。实现代码需要为公共类型和函数、状态转换、协议字段、数据库迁移、异步任务边界、错误分支和安全判定添加中文详细注释。注释应解释设计原因、约束和失败恢复；复杂算法、并发或锁、取消传播、幂等键和安全边界还要写明前置条件、后置条件及不变量。测试代码需要说明 fixture 意图和验收理由。

### 9. 审核结论栏
- 需求理解：待审核
- 架构边界：待审核
- 接口和事件：待审核
- 异常与恢复：待审核
- 安全与日志：待审核
- 允许进入代码开发：否

<!-- END S20-005 -->
## S20-006：prefix cache layout

### 1. 功能目标
本功能属于 S20。建立上下文分类、Token 预算、Checkpoint、压缩、前缀缓存和恢复。 本项聚焦于输入事件、纯状态转换和终端渲染，必须形成可以独立启动、测试或演示的最小闭环。

### 2. 范围与边界
- 包含：prefix cache layout所需契约、状态转换、错误返回、日志事件和自动化验收。
- 不包含：未列入本阶段的真实 Provider、跨阶段优化或视觉润色；依赖能力只能通过 Port、Fake 或 fixture 接入。
- 上游依赖：S20-005。依赖未 verified 时，本功能只能处于 blocked。
- 主要产物：AppState、Effect、渲染帧和用户提示。

### 3. 输入、输出与处理流程
输入来自上游命令、查询、事件、配置、用户动作或 fixture，必须具备稳定标识、版本和必要授权上下文。

处理顺序：首先校验格式、当前状态、权限和依赖版本；其次，围绕功能 [prefix cache layout] 完成输入校验、领域处理、持久化或投影、用户反馈和可重复验收。；然后产生领域事件或标准结果并更新必要投影；最后返回成功、等待、失败或取消结果，并记录阶段进度。

输出为AppState、Effect、渲染帧和用户提示。计划中的最小验收为：稳定前缀和易变后缀分离。

### 4. 状态、数据与一致性
- 状态变化必须遵守领域模型与事件规范，禁止直接修改 projection 绕过 domain command。
- SQLite Event Store，或本阶段明确声明的 Fake Store，是业务事实来源；日志只用于诊断。
- 外部副作用必须具有 operation_id、幂等策略、取消边界和恢复结论。
- 并发写入遵循单写者或 Write Claim；查询需要说明一致性级别和 cursor。

### 5. 异常、取消与恢复
必须设计输入无效、依赖不可用、权限拒绝、超时、用户取消、进程重启和重复提交路径。每类路径都要定义稳定 error_code、用户提示、是否可重试、是否产生事件和重启动作。不安全时进入明确的 blocked、failed 或 interrupted，不能静默成功。

### 6. 日志与安全要求
入口初始化日志，记录 stage.started、关键进度、stage.completed 或 stage.failed。日志采用非 JSON、非 key=value 的单行位置式格式：时间、级别、PID、任务链 UUID、线程或协程、logger、最后一级文件名和行号、主消息。不记录客户端地址；诊断上下文仅在 DEBUG 级别展示并置于主消息前，INFO 及以上隐藏。渲染层不得产生业务副作用，窄终端和 Unicode 不得越界。

### 7. 测试与可运行验收
- 必须提供 cargo 命令、CLI 子命令、集成测试或可重复 demo。
- 至少覆盖成功、失败、取消、重复调用以及适用的恢复或回滚路径。
- 日志断言任务链 UUID、稳定 message code、进度闭环、真实文件名和行号、无敏感信息泄漏。
- 回滚只能删除本阶段临时产物或恢复快照，不得删除用户数据或覆盖外部修改。

### 8. 后续代码注释要求
编码前必须通过本条目审核。实现代码需要为公共类型和函数、状态转换、协议字段、数据库迁移、异步任务边界、错误分支和安全判定添加中文详细注释。注释应解释设计原因、约束和失败恢复；复杂算法、并发或锁、取消传播、幂等键和安全边界还要写明前置条件、后置条件及不变量。测试代码需要说明 fixture 意图和验收理由。

### 9. 审核结论栏
- 需求理解：待审核
- 架构边界：待审核
- 接口和事件：待审核
- 异常与恢复：待审核
- 安全与日志：待审核
- 允许进入代码开发：否

<!-- END S20-006 -->
## S20-007：restore context

### 1. 功能目标
本功能属于 S20。建立上下文分类、Token 预算、Checkpoint、压缩、前缀缓存和恢复。 本项聚焦于Tool Gateway、工具契约和副作用隔离，必须形成可以独立启动、测试或演示的最小闭环。

### 2. 范围与边界
- 包含：restore context所需契约、状态转换、错误返回、日志事件和自动化验收。
- 不包含：未列入本阶段的真实 Provider、跨阶段优化或视觉润色；依赖能力只能通过 Port、Fake 或 fixture 接入。
- 上游依赖：S20-006。依赖未 verified 时，本功能只能处于 blocked。
- 主要产物：ToolCall、标准结果、变更和快照摘要。

### 3. 输入、输出与处理流程
输入来自上游命令、查询、事件、配置、用户动作或 fixture，必须具备稳定标识、版本和必要授权上下文。

处理顺序：首先校验格式、当前状态、权限和依赖版本；其次，围绕功能 [restore context] 完成输入校验、领域处理、持久化或投影、用户反馈和可重复验收。；然后产生领域事件或标准结果并更新必要投影；最后返回成功、等待、失败或取消结果，并记录阶段进度。

输出为ToolCall、标准结果、变更和快照摘要。计划中的最小验收为：重启后根据 checkpoint 恢复下一 turn。

### 4. 状态、数据与一致性
- 状态变化必须遵守领域模型与事件规范，禁止直接修改 projection 绕过 domain command。
- SQLite Event Store，或本阶段明确声明的 Fake Store，是业务事实来源；日志只用于诊断。
- 外部副作用必须具有 operation_id、幂等策略、取消边界和恢复结论。
- 并发写入遵循单写者或 Write Claim；查询需要说明一致性级别和 cursor。

### 5. 异常、取消与恢复
必须设计输入无效、依赖不可用、权限拒绝、超时、用户取消、进程重启和重复提交路径。每类路径都要定义稳定 error_code、用户提示、是否可重试、是否产生事件和重启动作。不安全时进入明确的 blocked、failed 或 interrupted，不能静默成功。

### 6. 日志与安全要求
入口初始化日志，记录 stage.started、关键进度、stage.completed 或 stage.failed。日志采用非 JSON、非 key=value 的单行位置式格式：时间、级别、PID、任务链 UUID、线程或协程、logger、最后一级文件名和行号、主消息。不记录客户端地址；诊断上下文仅在 DEBUG 级别展示并置于主消息前，INFO 及以上隐藏。工具不得绕过 Gateway、权限、取消、超时和日志门禁。

### 7. 测试与可运行验收
- 必须提供 cargo 命令、CLI 子命令、集成测试或可重复 demo。
- 至少覆盖成功、失败、取消、重复调用以及适用的恢复或回滚路径。
- 日志断言任务链 UUID、稳定 message code、进度闭环、真实文件名和行号、无敏感信息泄漏。
- 回滚只能删除本阶段临时产物或恢复快照，不得删除用户数据或覆盖外部修改。

### 8. 后续代码注释要求
编码前必须通过本条目审核。实现代码需要为公共类型和函数、状态转换、协议字段、数据库迁移、异步任务边界、错误分支和安全判定添加中文详细注释。注释应解释设计原因、约束和失败恢复；复杂算法、并发或锁、取消传播、幂等键和安全边界还要写明前置条件、后置条件及不变量。测试代码需要说明 fixture 意图和验收理由。

### 9. 审核结论栏
- 需求理解：待审核
- 架构边界：待审核
- 接口和事件：待审核
- 异常与恢复：待审核
- 安全与日志：待审核
- 允许进入代码开发：否

<!-- END S20-007 -->
## S20-008：`v0.1-context-rc1` demo

### 1. 功能目标
本功能属于 S20。建立上下文分类、Token 预算、Checkpoint、压缩、前缀缓存和恢复。 本项聚焦于上下文条目、预算和 Checkpoint，必须形成可以独立启动、测试或演示的最小闭环。

### 2. 范围与边界
- 包含：`v0.1-context-rc1` demo所需契约、状态转换、错误返回、日志事件和自动化验收。
- 不包含：未列入本阶段的真实 Provider、跨阶段优化或视觉润色；依赖能力只能通过 Port、Fake 或 fixture 接入。
- 上游依赖：S20-007。依赖未 verified 时，本功能只能处于 blocked。
- 主要产物：预算报告、摘要、版本和恢复上下文。

### 3. 输入、输出与处理流程
输入来自上游命令、查询、事件、配置、用户动作或 fixture，必须具备稳定标识、版本和必要授权上下文。

处理顺序：首先校验格式、当前状态、权限和依赖版本；其次，围绕功能 [`v0.1-context-rc1` demo] 完成输入校验、领域处理、持久化或投影、用户反馈和可重复验收。；然后产生领域事件或标准结果并更新必要投影；最后返回成功、等待、失败或取消结果，并记录阶段进度。

输出为预算报告、摘要、版本和恢复上下文。计划中的最小验收为：超长历史压缩后继续完成任务。

### 4. 状态、数据与一致性
- 状态变化必须遵守领域模型与事件规范，禁止直接修改 projection 绕过 domain command。
- SQLite Event Store，或本阶段明确声明的 Fake Store，是业务事实来源；日志只用于诊断。
- 外部副作用必须具有 operation_id、幂等策略、取消边界和恢复结论。
- 并发写入遵循单写者或 Write Claim；查询需要说明一致性级别和 cursor。

### 5. 异常、取消与恢复
必须设计输入无效、依赖不可用、权限拒绝、超时、用户取消、进程重启和重复提交路径。每类路径都要定义稳定 error_code、用户提示、是否可重试、是否产生事件和重启动作。不安全时进入明确的 blocked、failed 或 interrupted，不能静默成功。

### 6. 日志与安全要求
入口初始化日志，记录 stage.started、关键进度、stage.completed 或 stage.failed。日志采用非 JSON、非 key=value 的单行位置式格式：时间、级别、PID、任务链 UUID、线程或协程、logger、最后一级文件名和行号、主消息。不记录客户端地址；诊断上下文仅在 DEBUG 级别展示并置于主消息前，INFO 及以上隐藏。压缩不得丢失任务约束、工具事实和用户确认。

### 7. 测试与可运行验收
- 必须提供 cargo 命令、CLI 子命令、集成测试或可重复 demo。
- 至少覆盖成功、失败、取消、重复调用以及适用的恢复或回滚路径。
- 日志断言任务链 UUID、稳定 message code、进度闭环、真实文件名和行号、无敏感信息泄漏。
- 回滚只能删除本阶段临时产物或恢复快照，不得删除用户数据或覆盖外部修改。

### 8. 后续代码注释要求
编码前必须通过本条目审核。实现代码需要为公共类型和函数、状态转换、协议字段、数据库迁移、异步任务边界、错误分支和安全判定添加中文详细注释。注释应解释设计原因、约束和失败恢复；复杂算法、并发或锁、取消传播、幂等键和安全边界还要写明前置条件、后置条件及不变量。测试代码需要说明 fixture 意图和验收理由。

### 9. 审核结论栏
- 需求理解：待审核
- 架构边界：待审核
- 接口和事件：待审核
- 异常与恢复：待审核
- 安全与日志：待审核
- 允许进入代码开发：否

<!-- END S20-008 -->
## S21-001：shadow repository bootstrap

### 1. 功能目标
本功能属于 S21。建立项目目录外的 Shadow Git、基线、写后快照、差异、恢复和冲突阻断。 本项聚焦于领域功能、应用服务和可运行入口，必须形成可以独立启动、测试或演示的最小闭环。

### 2. 范围与边界
- 包含：shadow repository bootstrap所需契约、状态转换、错误返回、日志事件和自动化验收。
- 不包含：未列入本阶段的真实 Provider、跨阶段优化或视觉润色；依赖能力只能通过 Port、Fake 或 fixture 接入。
- 上游依赖：S20。依赖未 verified 时，本功能只能处于 blocked。
- 主要产物：领域事件、查询投影、日志和验收结果。

### 3. 输入、输出与处理流程
输入来自上游命令、查询、事件、配置、用户动作或 fixture，必须具备稳定标识、版本和必要授权上下文。

处理顺序：首先校验格式、当前状态、权限和依赖版本；其次，围绕功能 [shadow repository bootstrap] 完成输入校验、领域处理、持久化或投影、用户反馈和可重复验收。；然后产生领域事件或标准结果并更新必要投影；最后返回成功、等待、失败或取消结果，并记录阶段进度。

输出为领域事件、查询投影、日志和验收结果。计划中的最小验收为：项目目录外建立隔离 Git metadata。

### 4. 状态、数据与一致性
- 状态变化必须遵守领域模型与事件规范，禁止直接修改 projection 绕过 domain command。
- SQLite Event Store，或本阶段明确声明的 Fake Store，是业务事实来源；日志只用于诊断。
- 外部副作用必须具有 operation_id、幂等策略、取消边界和恢复结论。
- 并发写入遵循单写者或 Write Claim；查询需要说明一致性级别和 cursor。

### 5. 异常、取消与恢复
必须设计输入无效、依赖不可用、权限拒绝、超时、用户取消、进程重启和重复提交路径。每类路径都要定义稳定 error_code、用户提示、是否可重试、是否产生事件和重启动作。不安全时进入明确的 blocked、failed 或 interrupted，不能静默成功。

### 6. 日志与安全要求
入口初始化日志，记录 stage.started、关键进度、stage.completed 或 stage.failed。日志采用非 JSON、非 key=value 的单行位置式格式：时间、级别、PID、任务链 UUID、线程或协程、logger、最后一级文件名和行号、主消息。不记录客户端地址；诊断上下文仅在 DEBUG 级别展示并置于主消息前，INFO 及以上隐藏。必须保持 Port、幂等、取消、恢复和审计边界。

### 7. 测试与可运行验收
- 必须提供 cargo 命令、CLI 子命令、集成测试或可重复 demo。
- 至少覆盖成功、失败、取消、重复调用以及适用的恢复或回滚路径。
- 日志断言任务链 UUID、稳定 message code、进度闭环、真实文件名和行号、无敏感信息泄漏。
- 回滚只能删除本阶段临时产物或恢复快照，不得删除用户数据或覆盖外部修改。

### 8. 后续代码注释要求
编码前必须通过本条目审核。实现代码需要为公共类型和函数、状态转换、协议字段、数据库迁移、异步任务边界、错误分支和安全判定添加中文详细注释。注释应解释设计原因、约束和失败恢复；复杂算法、并发或锁、取消传播、幂等键和安全边界还要写明前置条件、后置条件及不变量。测试代码需要说明 fixture 意图和验收理由。

### 9. 审核结论栏
- 需求理解：待审核
- 架构边界：待审核
- 接口和事件：待审核
- 异常与恢复：待审核
- 安全与日志：待审核
- 允许进入代码开发：否

<!-- END S21-001 -->
## S21-002：clean baseline snapshot

### 1. 功能目标
本功能属于 S21。建立项目目录外的 Shadow Git、基线、写后快照、差异、恢复和冲突阻断。 本项聚焦于Tool Gateway、工具契约和副作用隔离，必须形成可以独立启动、测试或演示的最小闭环。

### 2. 范围与边界
- 包含：clean baseline snapshot所需契约、状态转换、错误返回、日志事件和自动化验收。
- 不包含：未列入本阶段的真实 Provider、跨阶段优化或视觉润色；依赖能力只能通过 Port、Fake 或 fixture 接入。
- 上游依赖：S21-001。依赖未 verified 时，本功能只能处于 blocked。
- 主要产物：ToolCall、标准结果、变更和快照摘要。

### 3. 输入、输出与处理流程
输入来自上游命令、查询、事件、配置、用户动作或 fixture，必须具备稳定标识、版本和必要授权上下文。

处理顺序：首先校验格式、当前状态、权限和依赖版本；其次，围绕功能 [clean baseline snapshot] 完成输入校验、领域处理、持久化或投影、用户反馈和可重复验收。；然后产生领域事件或标准结果并更新必要投影；最后返回成功、等待、失败或取消结果，并记录阶段进度。

输出为ToolCall、标准结果、变更和快照摘要。计划中的最小验收为：run 开始前记录基线 commit/digest。

### 4. 状态、数据与一致性
- 状态变化必须遵守领域模型与事件规范，禁止直接修改 projection 绕过 domain command。
- SQLite Event Store，或本阶段明确声明的 Fake Store，是业务事实来源；日志只用于诊断。
- 外部副作用必须具有 operation_id、幂等策略、取消边界和恢复结论。
- 并发写入遵循单写者或 Write Claim；查询需要说明一致性级别和 cursor。

### 5. 异常、取消与恢复
必须设计输入无效、依赖不可用、权限拒绝、超时、用户取消、进程重启和重复提交路径。每类路径都要定义稳定 error_code、用户提示、是否可重试、是否产生事件和重启动作。不安全时进入明确的 blocked、failed 或 interrupted，不能静默成功。

### 6. 日志与安全要求
入口初始化日志，记录 stage.started、关键进度、stage.completed 或 stage.failed。日志采用非 JSON、非 key=value 的单行位置式格式：时间、级别、PID、任务链 UUID、线程或协程、logger、最后一级文件名和行号、主消息。不记录客户端地址；诊断上下文仅在 DEBUG 级别展示并置于主消息前，INFO 及以上隐藏。工具不得绕过 Gateway、权限、取消、超时和日志门禁。

### 7. 测试与可运行验收
- 必须提供 cargo 命令、CLI 子命令、集成测试或可重复 demo。
- 至少覆盖成功、失败、取消、重复调用以及适用的恢复或回滚路径。
- 日志断言任务链 UUID、稳定 message code、进度闭环、真实文件名和行号、无敏感信息泄漏。
- 回滚只能删除本阶段临时产物或恢复快照，不得删除用户数据或覆盖外部修改。

### 8. 后续代码注释要求
编码前必须通过本条目审核。实现代码需要为公共类型和函数、状态转换、协议字段、数据库迁移、异步任务边界、错误分支和安全判定添加中文详细注释。注释应解释设计原因、约束和失败恢复；复杂算法、并发或锁、取消传播、幂等键和安全边界还要写明前置条件、后置条件及不变量。测试代码需要说明 fixture 意图和验收理由。

### 9. 审核结论栏
- 需求理解：待审核
- 架构边界：待审核
- 接口和事件：待审核
- 异常与恢复：待审核
- 安全与日志：待审核
- 允许进入代码开发：否

<!-- END S21-002 -->
## S21-003：post-tool snapshot

### 1. 功能目标
本功能属于 S21。建立项目目录外的 Shadow Git、基线、写后快照、差异、恢复和冲突阻断。 本项聚焦于Tool Gateway、工具契约和副作用隔离，必须形成可以独立启动、测试或演示的最小闭环。

### 2. 范围与边界
- 包含：post-tool snapshot所需契约、状态转换、错误返回、日志事件和自动化验收。
- 不包含：未列入本阶段的真实 Provider、跨阶段优化或视觉润色；依赖能力只能通过 Port、Fake 或 fixture 接入。
- 上游依赖：S21-002。依赖未 verified 时，本功能只能处于 blocked。
- 主要产物：ToolCall、标准结果、变更和快照摘要。

### 3. 输入、输出与处理流程
输入来自上游命令、查询、事件、配置、用户动作或 fixture，必须具备稳定标识、版本和必要授权上下文。

处理顺序：首先校验格式、当前状态、权限和依赖版本；其次，围绕功能 [post-tool snapshot] 完成输入校验、领域处理、持久化或投影、用户反馈和可重复验收。；然后产生领域事件或标准结果并更新必要投影；最后返回成功、等待、失败或取消结果，并记录阶段进度。

输出为ToolCall、标准结果、变更和快照摘要。计划中的最小验收为：每个写操作后生成文件级快照。

### 4. 状态、数据与一致性
- 状态变化必须遵守领域模型与事件规范，禁止直接修改 projection 绕过 domain command。
- SQLite Event Store，或本阶段明确声明的 Fake Store，是业务事实来源；日志只用于诊断。
- 外部副作用必须具有 operation_id、幂等策略、取消边界和恢复结论。
- 并发写入遵循单写者或 Write Claim；查询需要说明一致性级别和 cursor。

### 5. 异常、取消与恢复
必须设计输入无效、依赖不可用、权限拒绝、超时、用户取消、进程重启和重复提交路径。每类路径都要定义稳定 error_code、用户提示、是否可重试、是否产生事件和重启动作。不安全时进入明确的 blocked、failed 或 interrupted，不能静默成功。

### 6. 日志与安全要求
入口初始化日志，记录 stage.started、关键进度、stage.completed 或 stage.failed。日志采用非 JSON、非 key=value 的单行位置式格式：时间、级别、PID、任务链 UUID、线程或协程、logger、最后一级文件名和行号、主消息。不记录客户端地址；诊断上下文仅在 DEBUG 级别展示并置于主消息前，INFO 及以上隐藏。工具不得绕过 Gateway、权限、取消、超时和日志门禁。

### 7. 测试与可运行验收
- 必须提供 cargo 命令、CLI 子命令、集成测试或可重复 demo。
- 至少覆盖成功、失败、取消、重复调用以及适用的恢复或回滚路径。
- 日志断言任务链 UUID、稳定 message code、进度闭环、真实文件名和行号、无敏感信息泄漏。
- 回滚只能删除本阶段临时产物或恢复快照，不得删除用户数据或覆盖外部修改。

### 8. 后续代码注释要求
编码前必须通过本条目审核。实现代码需要为公共类型和函数、状态转换、协议字段、数据库迁移、异步任务边界、错误分支和安全判定添加中文详细注释。注释应解释设计原因、约束和失败恢复；复杂算法、并发或锁、取消传播、幂等键和安全边界还要写明前置条件、后置条件及不变量。测试代码需要说明 fixture 意图和验收理由。

### 9. 审核结论栏
- 需求理解：待审核
- 架构边界：待审核
- 接口和事件：待审核
- 异常与恢复：待审核
- 安全与日志：待审核
- 允许进入代码开发：否

<!-- END S21-003 -->
## S21-004：diff/patch query

### 1. 功能目标
本功能属于 S21。建立项目目录外的 Shadow Git、基线、写后快照、差异、恢复和冲突阻断。 本项聚焦于Tool Gateway、工具契约和副作用隔离，必须形成可以独立启动、测试或演示的最小闭环。

### 2. 范围与边界
- 包含：diff/patch query所需契约、状态转换、错误返回、日志事件和自动化验收。
- 不包含：未列入本阶段的真实 Provider、跨阶段优化或视觉润色；依赖能力只能通过 Port、Fake 或 fixture 接入。
- 上游依赖：S21-003。依赖未 verified 时，本功能只能处于 blocked。
- 主要产物：ToolCall、标准结果、变更和快照摘要。

### 3. 输入、输出与处理流程
输入来自上游命令、查询、事件、配置、用户动作或 fixture，必须具备稳定标识、版本和必要授权上下文。

处理顺序：首先校验格式、当前状态、权限和依赖版本；其次，围绕功能 [diff/patch query] 完成输入校验、领域处理、持久化或投影、用户反馈和可重复验收。；然后产生领域事件或标准结果并更新必要投影；最后返回成功、等待、失败或取消结果，并记录阶段进度。

输出为ToolCall、标准结果、变更和快照摘要。计划中的最小验收为：TUI 显示变更文件、增删行、摘要。

### 4. 状态、数据与一致性
- 状态变化必须遵守领域模型与事件规范，禁止直接修改 projection 绕过 domain command。
- SQLite Event Store，或本阶段明确声明的 Fake Store，是业务事实来源；日志只用于诊断。
- 外部副作用必须具有 operation_id、幂等策略、取消边界和恢复结论。
- 并发写入遵循单写者或 Write Claim；查询需要说明一致性级别和 cursor。

### 5. 异常、取消与恢复
必须设计输入无效、依赖不可用、权限拒绝、超时、用户取消、进程重启和重复提交路径。每类路径都要定义稳定 error_code、用户提示、是否可重试、是否产生事件和重启动作。不安全时进入明确的 blocked、failed 或 interrupted，不能静默成功。

### 6. 日志与安全要求
入口初始化日志，记录 stage.started、关键进度、stage.completed 或 stage.failed。日志采用非 JSON、非 key=value 的单行位置式格式：时间、级别、PID、任务链 UUID、线程或协程、logger、最后一级文件名和行号、主消息。不记录客户端地址；诊断上下文仅在 DEBUG 级别展示并置于主消息前，INFO 及以上隐藏。工具不得绕过 Gateway、权限、取消、超时和日志门禁。

### 7. 测试与可运行验收
- 必须提供 cargo 命令、CLI 子命令、集成测试或可重复 demo。
- 至少覆盖成功、失败、取消、重复调用以及适用的恢复或回滚路径。
- 日志断言任务链 UUID、稳定 message code、进度闭环、真实文件名和行号、无敏感信息泄漏。
- 回滚只能删除本阶段临时产物或恢复快照，不得删除用户数据或覆盖外部修改。

### 8. 后续代码注释要求
编码前必须通过本条目审核。实现代码需要为公共类型和函数、状态转换、协议字段、数据库迁移、异步任务边界、错误分支和安全判定添加中文详细注释。注释应解释设计原因、约束和失败恢复；复杂算法、并发或锁、取消传播、幂等键和安全边界还要写明前置条件、后置条件及不变量。测试代码需要说明 fixture 意图和验收理由。

### 9. 审核结论栏
- 需求理解：待审核
- 架构边界：待审核
- 接口和事件：待审核
- 异常与恢复：待审核
- 安全与日志：待审核
- 允许进入代码开发：否

<!-- END S21-004 -->
## S21-005：restore snapshot

### 1. 功能目标
本功能属于 S21。建立项目目录外的 Shadow Git、基线、写后快照、差异、恢复和冲突阻断。 本项聚焦于Tool Gateway、工具契约和副作用隔离，必须形成可以独立启动、测试或演示的最小闭环。

### 2. 范围与边界
- 包含：restore snapshot所需契约、状态转换、错误返回、日志事件和自动化验收。
- 不包含：未列入本阶段的真实 Provider、跨阶段优化或视觉润色；依赖能力只能通过 Port、Fake 或 fixture 接入。
- 上游依赖：S21-004。依赖未 verified 时，本功能只能处于 blocked。
- 主要产物：ToolCall、标准结果、变更和快照摘要。

### 3. 输入、输出与处理流程
输入来自上游命令、查询、事件、配置、用户动作或 fixture，必须具备稳定标识、版本和必要授权上下文。

处理顺序：首先校验格式、当前状态、权限和依赖版本；其次，围绕功能 [restore snapshot] 完成输入校验、领域处理、持久化或投影、用户反馈和可重复验收。；然后产生领域事件或标准结果并更新必要投影；最后返回成功、等待、失败或取消结果，并记录阶段进度。

输出为ToolCall、标准结果、变更和快照摘要。计划中的最小验收为：用户确认后恢复到指定 checkpoint。

### 4. 状态、数据与一致性
- 状态变化必须遵守领域模型与事件规范，禁止直接修改 projection 绕过 domain command。
- SQLite Event Store，或本阶段明确声明的 Fake Store，是业务事实来源；日志只用于诊断。
- 外部副作用必须具有 operation_id、幂等策略、取消边界和恢复结论。
- 并发写入遵循单写者或 Write Claim；查询需要说明一致性级别和 cursor。

### 5. 异常、取消与恢复
必须设计输入无效、依赖不可用、权限拒绝、超时、用户取消、进程重启和重复提交路径。每类路径都要定义稳定 error_code、用户提示、是否可重试、是否产生事件和重启动作。不安全时进入明确的 blocked、failed 或 interrupted，不能静默成功。

### 6. 日志与安全要求
入口初始化日志，记录 stage.started、关键进度、stage.completed 或 stage.failed。日志采用非 JSON、非 key=value 的单行位置式格式：时间、级别、PID、任务链 UUID、线程或协程、logger、最后一级文件名和行号、主消息。不记录客户端地址；诊断上下文仅在 DEBUG 级别展示并置于主消息前，INFO 及以上隐藏。工具不得绕过 Gateway、权限、取消、超时和日志门禁。

### 7. 测试与可运行验收
- 必须提供 cargo 命令、CLI 子命令、集成测试或可重复 demo。
- 至少覆盖成功、失败、取消、重复调用以及适用的恢复或回滚路径。
- 日志断言任务链 UUID、稳定 message code、进度闭环、真实文件名和行号、无敏感信息泄漏。
- 回滚只能删除本阶段临时产物或恢复快照，不得删除用户数据或覆盖外部修改。

### 8. 后续代码注释要求
编码前必须通过本条目审核。实现代码需要为公共类型和函数、状态转换、协议字段、数据库迁移、异步任务边界、错误分支和安全判定添加中文详细注释。注释应解释设计原因、约束和失败恢复；复杂算法、并发或锁、取消传播、幂等键和安全边界还要写明前置条件、后置条件及不变量。测试代码需要说明 fixture 意图和验收理由。

### 9. 审核结论栏
- 需求理解：待审核
- 架构边界：待审核
- 接口和事件：待审核
- 异常与恢复：待审核
- 安全与日志：待审核
- 允许进入代码开发：否

<!-- END S21-005 -->
## S21-006：conflict detection

### 1. 功能目标
本功能属于 S21。建立项目目录外的 Shadow Git、基线、写后快照、差异、恢复和冲突阻断。 本项聚焦于领域功能、应用服务和可运行入口，必须形成可以独立启动、测试或演示的最小闭环。

### 2. 范围与边界
- 包含：conflict detection所需契约、状态转换、错误返回、日志事件和自动化验收。
- 不包含：未列入本阶段的真实 Provider、跨阶段优化或视觉润色；依赖能力只能通过 Port、Fake 或 fixture 接入。
- 上游依赖：S21-005。依赖未 verified 时，本功能只能处于 blocked。
- 主要产物：领域事件、查询投影、日志和验收结果。

### 3. 输入、输出与处理流程
输入来自上游命令、查询、事件、配置、用户动作或 fixture，必须具备稳定标识、版本和必要授权上下文。

处理顺序：首先校验格式、当前状态、权限和依赖版本；其次，恢复前重算工作区文件摘要；发现外部修改时返回 conflict，禁止覆盖。；然后产生领域事件或标准结果并更新必要投影；最后返回成功、等待、失败或取消结果，并记录阶段进度。

输出为领域事件、查询投影、日志和验收结果。计划中的最小验收为：外部修改导致 restore 时明确阻断。

### 4. 状态、数据与一致性
- 状态变化必须遵守领域模型与事件规范，禁止直接修改 projection 绕过 domain command。
- SQLite Event Store，或本阶段明确声明的 Fake Store，是业务事实来源；日志只用于诊断。
- 外部副作用必须具有 operation_id、幂等策略、取消边界和恢复结论。
- 并发写入遵循单写者或 Write Claim；查询需要说明一致性级别和 cursor。

### 5. 异常、取消与恢复
必须设计输入无效、依赖不可用、权限拒绝、超时、用户取消、进程重启和重复提交路径。每类路径都要定义稳定 error_code、用户提示、是否可重试、是否产生事件和重启动作。不安全时进入明确的 blocked、failed 或 interrupted，不能静默成功。

### 6. 日志与安全要求
入口初始化日志，记录 stage.started、关键进度、stage.completed 或 stage.failed。日志采用非 JSON、非 key=value 的单行位置式格式：时间、级别、PID、任务链 UUID、线程或协程、logger、最后一级文件名和行号、主消息。不记录客户端地址；诊断上下文仅在 DEBUG 级别展示并置于主消息前，INFO 及以上隐藏。必须保持 Port、幂等、取消、恢复和审计边界。

### 7. 测试与可运行验收
- 必须提供 cargo 命令、CLI 子命令、集成测试或可重复 demo。
- 至少覆盖成功、失败、取消、重复调用以及适用的恢复或回滚路径。
- 日志断言任务链 UUID、稳定 message code、进度闭环、真实文件名和行号、无敏感信息泄漏。
- 回滚只能删除本阶段临时产物或恢复快照，不得删除用户数据或覆盖外部修改。

### 8. 后续代码注释要求
编码前必须通过本条目审核。实现代码需要为公共类型和函数、状态转换、协议字段、数据库迁移、异步任务边界、错误分支和安全判定添加中文详细注释。注释应解释设计原因、约束和失败恢复；复杂算法、并发或锁、取消传播、幂等键和安全边界还要写明前置条件、后置条件及不变量。测试代码需要说明 fixture 意图和验收理由。

### 9. 审核结论栏
- 需求理解：待审核
- 架构边界：待审核
- 接口和事件：待审核
- 异常与恢复：待审核
- 安全与日志：待审核
- 允许进入代码开发：否

<!-- END S21-006 -->
## S21-007：`v0.1-snapshot-rc1` demo

### 1. 功能目标
本功能属于 S21。建立项目目录外的 Shadow Git、基线、写后快照、差异、恢复和冲突阻断。 本项聚焦于Tool Gateway、工具契约和副作用隔离，必须形成可以独立启动、测试或演示的最小闭环。

### 2. 范围与边界
- 包含：`v0.1-snapshot-rc1` demo所需契约、状态转换、错误返回、日志事件和自动化验收。
- 不包含：未列入本阶段的真实 Provider、跨阶段优化或视觉润色；依赖能力只能通过 Port、Fake 或 fixture 接入。
- 上游依赖：S21-006。依赖未 verified 时，本功能只能处于 blocked。
- 主要产物：ToolCall、标准结果、变更和快照摘要。

### 3. 输入、输出与处理流程
输入来自上游命令、查询、事件、配置、用户动作或 fixture，必须具备稳定标识、版本和必要授权上下文。

处理顺序：首先校验格式、当前状态、权限和依赖版本；其次，围绕功能 [`v0.1-snapshot-rc1` demo] 完成输入校验、领域处理、持久化或投影、用户反馈和可重复验收。；然后产生领域事件或标准结果并更新必要投影；最后返回成功、等待、失败或取消结果，并记录阶段进度。

输出为ToolCall、标准结果、变更和快照摘要。计划中的最小验收为：写入→查看 diff→恢复→验证文件内容。

### 4. 状态、数据与一致性
- 状态变化必须遵守领域模型与事件规范，禁止直接修改 projection 绕过 domain command。
- SQLite Event Store，或本阶段明确声明的 Fake Store，是业务事实来源；日志只用于诊断。
- 外部副作用必须具有 operation_id、幂等策略、取消边界和恢复结论。
- 并发写入遵循单写者或 Write Claim；查询需要说明一致性级别和 cursor。

### 5. 异常、取消与恢复
必须设计输入无效、依赖不可用、权限拒绝、超时、用户取消、进程重启和重复提交路径。每类路径都要定义稳定 error_code、用户提示、是否可重试、是否产生事件和重启动作。不安全时进入明确的 blocked、failed 或 interrupted，不能静默成功。

### 6. 日志与安全要求
入口初始化日志，记录 stage.started、关键进度、stage.completed 或 stage.failed。日志采用非 JSON、非 key=value 的单行位置式格式：时间、级别、PID、任务链 UUID、线程或协程、logger、最后一级文件名和行号、主消息。不记录客户端地址；诊断上下文仅在 DEBUG 级别展示并置于主消息前，INFO 及以上隐藏。工具不得绕过 Gateway、权限、取消、超时和日志门禁。

### 7. 测试与可运行验收
- 必须提供 cargo 命令、CLI 子命令、集成测试或可重复 demo。
- 至少覆盖成功、失败、取消、重复调用以及适用的恢复或回滚路径。
- 日志断言任务链 UUID、稳定 message code、进度闭环、真实文件名和行号、无敏感信息泄漏。
- 回滚只能删除本阶段临时产物或恢复快照，不得删除用户数据或覆盖外部修改。

### 8. 后续代码注释要求
编码前必须通过本条目审核。实现代码需要为公共类型和函数、状态转换、协议字段、数据库迁移、异步任务边界、错误分支和安全判定添加中文详细注释。注释应解释设计原因、约束和失败恢复；复杂算法、并发或锁、取消传播、幂等键和安全边界还要写明前置条件、后置条件及不变量。测试代码需要说明 fixture 意图和验收理由。

### 9. 审核结论栏
- 需求理解：待审核
- 架构边界：待审核
- 接口和事件：待审核
- 异常与恢复：待审核
- 安全与日志：待审核
- 允许进入代码开发：否

<!-- END S21-007 -->
## S22-001：TelemetryContext

### 1. 功能目标
本功能属于 S22。建立 TelemetryContext、日志查询、调用投影、健康指标、审计和支持包。 本项聚焦于上下文条目、预算和 Checkpoint，必须形成可以独立启动、测试或演示的最小闭环。

### 2. 范围与边界
- 包含：TelemetryContext所需契约、状态转换、错误返回、日志事件和自动化验收。
- 不包含：未列入本阶段的真实 Provider、跨阶段优化或视觉润色；依赖能力只能通过 Port、Fake 或 fixture 接入。
- 上游依赖：S21。依赖未 verified 时，本功能只能处于 blocked。
- 主要产物：预算报告、摘要、版本和恢复上下文。

### 3. 输入、输出与处理流程
输入来自上游命令、查询、事件、配置、用户动作或 fixture，必须具备稳定标识、版本和必要授权上下文。

处理顺序：首先校验格式、当前状态、权限和依赖版本；其次，围绕功能 [TelemetryContext] 完成输入校验、领域处理、持久化或投影、用户反馈和可重复验收。；然后产生领域事件或标准结果并更新必要投影；最后返回成功、等待、失败或取消结果，并记录阶段进度。

输出为预算报告、摘要、版本和恢复上下文。计划中的最小验收为：project/session/run/turn/operation/correlation 可关联。

### 4. 状态、数据与一致性
- 状态变化必须遵守领域模型与事件规范，禁止直接修改 projection 绕过 domain command。
- SQLite Event Store，或本阶段明确声明的 Fake Store，是业务事实来源；日志只用于诊断。
- 外部副作用必须具有 operation_id、幂等策略、取消边界和恢复结论。
- 并发写入遵循单写者或 Write Claim；查询需要说明一致性级别和 cursor。

### 5. 异常、取消与恢复
必须设计输入无效、依赖不可用、权限拒绝、超时、用户取消、进程重启和重复提交路径。每类路径都要定义稳定 error_code、用户提示、是否可重试、是否产生事件和重启动作。不安全时进入明确的 blocked、failed 或 interrupted，不能静默成功。

### 6. 日志与安全要求
入口初始化日志，记录 stage.started、关键进度、stage.completed 或 stage.failed。日志采用非 JSON、非 key=value 的单行位置式格式：时间、级别、PID、任务链 UUID、线程或协程、logger、最后一级文件名和行号、主消息。不记录客户端地址；诊断上下文仅在 DEBUG 级别展示并置于主消息前，INFO 及以上隐藏。压缩不得丢失任务约束、工具事实和用户确认。

### 7. 测试与可运行验收
- 必须提供 cargo 命令、CLI 子命令、集成测试或可重复 demo。
- 至少覆盖成功、失败、取消、重复调用以及适用的恢复或回滚路径。
- 日志断言任务链 UUID、稳定 message code、进度闭环、真实文件名和行号、无敏感信息泄漏。
- 回滚只能删除本阶段临时产物或恢复快照，不得删除用户数据或覆盖外部修改。

### 8. 后续代码注释要求
编码前必须通过本条目审核。实现代码需要为公共类型和函数、状态转换、协议字段、数据库迁移、异步任务边界、错误分支和安全判定添加中文详细注释。注释应解释设计原因、约束和失败恢复；复杂算法、并发或锁、取消传播、幂等键和安全边界还要写明前置条件、后置条件及不变量。测试代码需要说明 fixture 意图和验收理由。

### 9. 审核结论栏
- 需求理解：待审核
- 架构边界：待审核
- 接口和事件：待审核
- 异常与恢复：待审核
- 安全与日志：待审核
- 允许进入代码开发：否

<!-- END S22-001 -->
## S22-002：structured log query

### 1. 功能目标
本功能属于 S22。建立 TelemetryContext、日志查询、调用投影、健康指标、审计和支持包。 本项聚焦于日志上下文和文件写入，必须形成可以独立启动、测试或演示的最小闭环。

### 2. 范围与边界
- 包含：structured log query所需契约、状态转换、错误返回、日志事件和自动化验收。
- 不包含：未列入本阶段的真实 Provider、跨阶段优化或视觉润色；依赖能力只能通过 Port、Fake 或 fixture 接入。
- 上游依赖：S22-001。依赖未 verified 时，本功能只能处于 blocked。
- 主要产物：日志文件、运行链 UUID、序号和关闭结果。

### 3. 输入、输出与处理流程
输入来自上游命令、查询、事件、配置、用户动作或 fixture，必须具备稳定标识、版本和必要授权上下文。

处理顺序：首先校验格式、当前状态、权限和依赖版本；其次，围绕功能 [structured log query] 完成输入校验、领域处理、持久化或投影、用户反馈和可重复验收。；然后产生领域事件或标准结果并更新必要投影；最后返回成功、等待、失败或取消结果，并记录阶段进度。

输出为日志文件、运行链 UUID、序号和关闭结果。计划中的最小验收为：可按 run_id、level、message_code、时间查询本地日志。

### 4. 状态、数据与一致性
- 状态变化必须遵守领域模型与事件规范，禁止直接修改 projection 绕过 domain command。
- SQLite Event Store，或本阶段明确声明的 Fake Store，是业务事实来源；日志只用于诊断。
- 外部副作用必须具有 operation_id、幂等策略、取消边界和恢复结论。
- 并发写入遵循单写者或 Write Claim；查询需要说明一致性级别和 cursor。

### 5. 异常、取消与恢复
必须设计输入无效、依赖不可用、权限拒绝、超时、用户取消、进程重启和重复提交路径。每类路径都要定义稳定 error_code、用户提示、是否可重试、是否产生事件和重启动作。不安全时进入明确的 blocked、failed 或 interrupted，不能静默成功。

### 6. 日志与安全要求
入口初始化日志，记录 stage.started、关键进度、stage.completed 或 stage.failed。日志采用非 JSON、非 key=value 的单行位置式格式：时间、级别、PID、任务链 UUID、线程或协程、logger、最后一级文件名和行号、主消息。不记录客户端地址；诊断上下文仅在 DEBUG 级别展示并置于主消息前，INFO 及以上隐藏。不得使用 JSON 或 key=value，不得记录客户端地址、绝对路径和敏感正文。

### 7. 测试与可运行验收
- 必须提供 cargo 命令、CLI 子命令、集成测试或可重复 demo。
- 至少覆盖成功、失败、取消、重复调用以及适用的恢复或回滚路径。
- 日志断言任务链 UUID、稳定 message code、进度闭环、真实文件名和行号、无敏感信息泄漏。
- 回滚只能删除本阶段临时产物或恢复快照，不得删除用户数据或覆盖外部修改。

### 8. 后续代码注释要求
编码前必须通过本条目审核。实现代码需要为公共类型和函数、状态转换、协议字段、数据库迁移、异步任务边界、错误分支和安全判定添加中文详细注释。注释应解释设计原因、约束和失败恢复；复杂算法、并发或锁、取消传播、幂等键和安全边界还要写明前置条件、后置条件及不变量。测试代码需要说明 fixture 意图和验收理由。

### 9. 审核结论栏
- 需求理解：待审核
- 架构边界：待审核
- 接口和事件：待审核
- 异常与恢复：待审核
- 安全与日志：待审核
- 允许进入代码开发：否

<!-- END S22-002 -->
## S22-003：call log projection

### 1. 功能目标
本功能属于 S22。建立 TelemetryContext、日志查询、调用投影、健康指标、审计和支持包。 本项聚焦于日志上下文和文件写入，必须形成可以独立启动、测试或演示的最小闭环。

### 2. 范围与边界
- 包含：call log projection所需契约、状态转换、错误返回、日志事件和自动化验收。
- 不包含：未列入本阶段的真实 Provider、跨阶段优化或视觉润色；依赖能力只能通过 Port、Fake 或 fixture 接入。
- 上游依赖：S22-002。依赖未 verified 时，本功能只能处于 blocked。
- 主要产物：日志文件、运行链 UUID、序号和关闭结果。

### 3. 输入、输出与处理流程
输入来自上游命令、查询、事件、配置、用户动作或 fixture，必须具备稳定标识、版本和必要授权上下文。

处理顺序：首先校验格式、当前状态、权限和依赖版本；其次，围绕功能 [call log projection] 完成输入校验、领域处理、持久化或投影、用户反馈和可重复验收。；然后产生领域事件或标准结果并更新必要投影；最后返回成功、等待、失败或取消结果，并记录阶段进度。

输出为日志文件、运行链 UUID、序号和关闭结果。计划中的最小验收为：Provider/Tool/MCP/Skill/SubAgent 调用摘要可查。

### 4. 状态、数据与一致性
- 状态变化必须遵守领域模型与事件规范，禁止直接修改 projection 绕过 domain command。
- SQLite Event Store，或本阶段明确声明的 Fake Store，是业务事实来源；日志只用于诊断。
- 外部副作用必须具有 operation_id、幂等策略、取消边界和恢复结论。
- 并发写入遵循单写者或 Write Claim；查询需要说明一致性级别和 cursor。

### 5. 异常、取消与恢复
必须设计输入无效、依赖不可用、权限拒绝、超时、用户取消、进程重启和重复提交路径。每类路径都要定义稳定 error_code、用户提示、是否可重试、是否产生事件和重启动作。不安全时进入明确的 blocked、failed 或 interrupted，不能静默成功。

### 6. 日志与安全要求
入口初始化日志，记录 stage.started、关键进度、stage.completed 或 stage.failed。日志采用非 JSON、非 key=value 的单行位置式格式：时间、级别、PID、任务链 UUID、线程或协程、logger、最后一级文件名和行号、主消息。不记录客户端地址；诊断上下文仅在 DEBUG 级别展示并置于主消息前，INFO 及以上隐藏。不得使用 JSON 或 key=value，不得记录客户端地址、绝对路径和敏感正文。

### 7. 测试与可运行验收
- 必须提供 cargo 命令、CLI 子命令、集成测试或可重复 demo。
- 至少覆盖成功、失败、取消、重复调用以及适用的恢复或回滚路径。
- 日志断言任务链 UUID、稳定 message code、进度闭环、真实文件名和行号、无敏感信息泄漏。
- 回滚只能删除本阶段临时产物或恢复快照，不得删除用户数据或覆盖外部修改。

### 8. 后续代码注释要求
编码前必须通过本条目审核。实现代码需要为公共类型和函数、状态转换、协议字段、数据库迁移、异步任务边界、错误分支和安全判定添加中文详细注释。注释应解释设计原因、约束和失败恢复；复杂算法、并发或锁、取消传播、幂等键和安全边界还要写明前置条件、后置条件及不变量。测试代码需要说明 fixture 意图和验收理由。

### 9. 审核结论栏
- 需求理解：待审核
- 架构边界：待审核
- 接口和事件：待审核
- 异常与恢复：待审核
- 安全与日志：待审核
- 允许进入代码开发：否

<!-- END S22-003 -->
## S22-004：health metrics

### 1. 功能目标
本功能属于 S22。建立 TelemetryContext、日志查询、调用投影、健康指标、审计和支持包。 本项聚焦于诊断上下文、查询投影和运维视图，必须形成可以独立启动、测试或演示的最小闭环。

### 2. 范围与边界
- 包含：health metrics所需契约、状态转换、错误返回、日志事件和自动化验收。
- 不包含：未列入本阶段的真实 Provider、跨阶段优化或视觉润色；依赖能力只能通过 Port、Fake 或 fixture 接入。
- 上游依赖：S22-003。依赖未 verified 时，本功能只能处于 blocked。
- 主要产物：诊断、指标、审计证据和脱敏支持包。

### 3. 输入、输出与处理流程
输入来自上游命令、查询、事件、配置、用户动作或 fixture，必须具备稳定标识、版本和必要授权上下文。

处理顺序：首先校验格式、当前状态、权限和依赖版本；其次，围绕功能 [health metrics] 完成输入校验、领域处理、持久化或投影、用户反馈和可重复验收。；然后产生领域事件或标准结果并更新必要投影；最后返回成功、等待、失败或取消结果，并记录阶段进度。

输出为诊断、指标、审计证据和脱敏支持包。计划中的最小验收为：latency、error、queue depth、projection lag 可查。

### 4. 状态、数据与一致性
- 状态变化必须遵守领域模型与事件规范，禁止直接修改 projection 绕过 domain command。
- SQLite Event Store，或本阶段明确声明的 Fake Store，是业务事实来源；日志只用于诊断。
- 外部副作用必须具有 operation_id、幂等策略、取消边界和恢复结论。
- 并发写入遵循单写者或 Write Claim；查询需要说明一致性级别和 cursor。

### 5. 异常、取消与恢复
必须设计输入无效、依赖不可用、权限拒绝、超时、用户取消、进程重启和重复提交路径。每类路径都要定义稳定 error_code、用户提示、是否可重试、是否产生事件和重启动作。不安全时进入明确的 blocked、failed 或 interrupted，不能静默成功。

### 6. 日志与安全要求
入口初始化日志，记录 stage.started、关键进度、stage.completed 或 stage.failed。日志采用非 JSON、非 key=value 的单行位置式格式：时间、级别、PID、任务链 UUID、线程或协程、logger、最后一级文件名和行号、主消息。不记录客户端地址；诊断上下文仅在 DEBUG 级别展示并置于主消息前，INFO 及以上隐藏。只展示安全视图，不泄露敏感内容。

### 7. 测试与可运行验收
- 必须提供 cargo 命令、CLI 子命令、集成测试或可重复 demo。
- 至少覆盖成功、失败、取消、重复调用以及适用的恢复或回滚路径。
- 日志断言任务链 UUID、稳定 message code、进度闭环、真实文件名和行号、无敏感信息泄漏。
- 回滚只能删除本阶段临时产物或恢复快照，不得删除用户数据或覆盖外部修改。

### 8. 后续代码注释要求
编码前必须通过本条目审核。实现代码需要为公共类型和函数、状态转换、协议字段、数据库迁移、异步任务边界、错误分支和安全判定添加中文详细注释。注释应解释设计原因、约束和失败恢复；复杂算法、并发或锁、取消传播、幂等键和安全边界还要写明前置条件、后置条件及不变量。测试代码需要说明 fixture 意图和验收理由。

### 9. 审核结论栏
- 需求理解：待审核
- 架构边界：待审核
- 接口和事件：待审核
- 异常与恢复：待审核
- 安全与日志：待审核
- 允许进入代码开发：否

<!-- END S22-004 -->
## S22-005：audit projection

### 1. 功能目标
本功能属于 S22。建立 TelemetryContext、日志查询、调用投影、健康指标、审计和支持包。 本项聚焦于事件存储、事务和可重建投影，必须形成可以独立启动、测试或演示的最小闭环。

### 2. 范围与边界
- 包含：audit projection所需契约、状态转换、错误返回、日志事件和自动化验收。
- 不包含：未列入本阶段的真实 Provider、跨阶段优化或视觉润色；依赖能力只能通过 Port、Fake 或 fixture 接入。
- 上游依赖：S22-004。依赖未 verified 时，本功能只能处于 blocked。
- 主要产物：事件序列、投影版本、游标和恢复结果。

### 3. 输入、输出与处理流程
输入来自上游命令、查询、事件、配置、用户动作或 fixture，必须具备稳定标识、版本和必要授权上下文。

处理顺序：首先校验格式、当前状态、权限和依赖版本；其次，围绕功能 [audit projection] 完成输入校验、领域处理、持久化或投影、用户反馈和可重复验收。；然后产生领域事件或标准结果并更新必要投影；最后返回成功、等待、失败或取消结果，并记录阶段进度。

输出为事件序列、投影版本、游标和恢复结果。计划中的最小验收为：approval、skip、write、restore 有安全证据。

### 4. 状态、数据与一致性
- 状态变化必须遵守领域模型与事件规范，禁止直接修改 projection 绕过 domain command。
- SQLite Event Store，或本阶段明确声明的 Fake Store，是业务事实来源；日志只用于诊断。
- 外部副作用必须具有 operation_id、幂等策略、取消边界和恢复结论。
- 并发写入遵循单写者或 Write Claim；查询需要说明一致性级别和 cursor。

### 5. 异常、取消与恢复
必须设计输入无效、依赖不可用、权限拒绝、超时、用户取消、进程重启和重复提交路径。每类路径都要定义稳定 error_code、用户提示、是否可重试、是否产生事件和重启动作。不安全时进入明确的 blocked、failed 或 interrupted，不能静默成功。

### 6. 日志与安全要求
入口初始化日志，记录 stage.started、关键进度、stage.completed 或 stage.failed。日志采用非 JSON、非 key=value 的单行位置式格式：时间、级别、PID、任务链 UUID、线程或协程、logger、最后一级文件名和行号、主消息。不记录客户端地址；诊断上下文仅在 DEBUG 级别展示并置于主消息前，INFO 及以上隐藏。SQLite 事件是业务事实，事务不得产生半提交。

### 7. 测试与可运行验收
- 必须提供 cargo 命令、CLI 子命令、集成测试或可重复 demo。
- 至少覆盖成功、失败、取消、重复调用以及适用的恢复或回滚路径。
- 日志断言任务链 UUID、稳定 message code、进度闭环、真实文件名和行号、无敏感信息泄漏。
- 回滚只能删除本阶段临时产物或恢复快照，不得删除用户数据或覆盖外部修改。

### 8. 后续代码注释要求
编码前必须通过本条目审核。实现代码需要为公共类型和函数、状态转换、协议字段、数据库迁移、异步任务边界、错误分支和安全判定添加中文详细注释。注释应解释设计原因、约束和失败恢复；复杂算法、并发或锁、取消传播、幂等键和安全边界还要写明前置条件、后置条件及不变量。测试代码需要说明 fixture 意图和验收理由。

### 9. 审核结论栏
- 需求理解：待审核
- 架构边界：待审核
- 接口和事件：待审核
- 异常与恢复：待审核
- 安全与日志：待审核
- 允许进入代码开发：否

<!-- END S22-005 -->
## S22-006：TUI Overview panel

### 1. 功能目标
本功能属于 S22。建立 TelemetryContext、日志查询、调用投影、健康指标、审计和支持包。 本项聚焦于输入事件、纯状态转换和终端渲染，必须形成可以独立启动、测试或演示的最小闭环。

### 2. 范围与边界
- 包含：TUI Overview panel所需契约、状态转换、错误返回、日志事件和自动化验收。
- 不包含：未列入本阶段的真实 Provider、跨阶段优化或视觉润色；依赖能力只能通过 Port、Fake 或 fixture 接入。
- 上游依赖：S22-005。依赖未 verified 时，本功能只能处于 blocked。
- 主要产物：AppState、Effect、渲染帧和用户提示。

### 3. 输入、输出与处理流程
输入来自上游命令、查询、事件、配置、用户动作或 fixture，必须具备稳定标识、版本和必要授权上下文。

处理顺序：首先校验格式、当前状态、权限和依赖版本；其次，围绕功能 [TUI Overview panel] 完成输入校验、领域处理、持久化或投影、用户反馈和可重复验收。；然后产生领域事件或标准结果并更新必要投影；最后返回成功、等待、失败或取消结果，并记录阶段进度。

输出为AppState、Effect、渲染帧和用户提示。计划中的最小验收为：当前 run、进度、耗时、错误、连接状态。

### 4. 状态、数据与一致性
- 状态变化必须遵守领域模型与事件规范，禁止直接修改 projection 绕过 domain command。
- SQLite Event Store，或本阶段明确声明的 Fake Store，是业务事实来源；日志只用于诊断。
- 外部副作用必须具有 operation_id、幂等策略、取消边界和恢复结论。
- 并发写入遵循单写者或 Write Claim；查询需要说明一致性级别和 cursor。

### 5. 异常、取消与恢复
必须设计输入无效、依赖不可用、权限拒绝、超时、用户取消、进程重启和重复提交路径。每类路径都要定义稳定 error_code、用户提示、是否可重试、是否产生事件和重启动作。不安全时进入明确的 blocked、failed 或 interrupted，不能静默成功。

### 6. 日志与安全要求
入口初始化日志，记录 stage.started、关键进度、stage.completed 或 stage.failed。日志采用非 JSON、非 key=value 的单行位置式格式：时间、级别、PID、任务链 UUID、线程或协程、logger、最后一级文件名和行号、主消息。不记录客户端地址；诊断上下文仅在 DEBUG 级别展示并置于主消息前，INFO 及以上隐藏。渲染层不得产生业务副作用，窄终端和 Unicode 不得越界。

### 7. 测试与可运行验收
- 必须提供 cargo 命令、CLI 子命令、集成测试或可重复 demo。
- 至少覆盖成功、失败、取消、重复调用以及适用的恢复或回滚路径。
- 日志断言任务链 UUID、稳定 message code、进度闭环、真实文件名和行号、无敏感信息泄漏。
- 回滚只能删除本阶段临时产物或恢复快照，不得删除用户数据或覆盖外部修改。

### 8. 后续代码注释要求
编码前必须通过本条目审核。实现代码需要为公共类型和函数、状态转换、协议字段、数据库迁移、异步任务边界、错误分支和安全判定添加中文详细注释。注释应解释设计原因、约束和失败恢复；复杂算法、并发或锁、取消传播、幂等键和安全边界还要写明前置条件、后置条件及不变量。测试代码需要说明 fixture 意图和验收理由。

### 9. 审核结论栏
- 需求理解：待审核
- 架构边界：待审核
- 接口和事件：待审核
- 异常与恢复：待审核
- 安全与日志：待审核
- 允许进入代码开发：否

<!-- END S22-006 -->
## S22-007：TUI Skill/MCP/SubAgent/Memory panel

### 1. 功能目标
本功能属于 S22。建立 TelemetryContext、日志查询、调用投影、健康指标、审计和支持包。 本项聚焦于输入事件、纯状态转换和终端渲染，必须形成可以独立启动、测试或演示的最小闭环。

### 2. 范围与边界
- 包含：TUI Skill/MCP/SubAgent/Memory panel所需契约、状态转换、错误返回、日志事件和自动化验收。
- 不包含：未列入本阶段的真实 Provider、跨阶段优化或视觉润色；依赖能力只能通过 Port、Fake 或 fixture 接入。
- 上游依赖：S22-006。依赖未 verified 时，本功能只能处于 blocked。
- 主要产物：AppState、Effect、渲染帧和用户提示。

### 3. 输入、输出与处理流程
输入来自上游命令、查询、事件、配置、用户动作或 fixture，必须具备稳定标识、版本和必要授权上下文。

处理顺序：首先校验格式、当前状态、权限和依赖版本；其次，围绕功能 [TUI Skill/MCP/SubAgent/Memory panel] 完成输入校验、领域处理、持久化或投影、用户反馈和可重复验收。；然后产生领域事件或标准结果并更新必要投影；最后返回成功、等待、失败或取消结果，并记录阶段进度。

输出为AppState、Effect、渲染帧和用户提示。计划中的最小验收为：v0.1 展示基础投影；能力缺失显示明确 unavailable。

### 4. 状态、数据与一致性
- 状态变化必须遵守领域模型与事件规范，禁止直接修改 projection 绕过 domain command。
- SQLite Event Store，或本阶段明确声明的 Fake Store，是业务事实来源；日志只用于诊断。
- 外部副作用必须具有 operation_id、幂等策略、取消边界和恢复结论。
- 并发写入遵循单写者或 Write Claim；查询需要说明一致性级别和 cursor。

### 5. 异常、取消与恢复
必须设计输入无效、依赖不可用、权限拒绝、超时、用户取消、进程重启和重复提交路径。每类路径都要定义稳定 error_code、用户提示、是否可重试、是否产生事件和重启动作。不安全时进入明确的 blocked、failed 或 interrupted，不能静默成功。

### 6. 日志与安全要求
入口初始化日志，记录 stage.started、关键进度、stage.completed 或 stage.failed。日志采用非 JSON、非 key=value 的单行位置式格式：时间、级别、PID、任务链 UUID、线程或协程、logger、最后一级文件名和行号、主消息。不记录客户端地址；诊断上下文仅在 DEBUG 级别展示并置于主消息前，INFO 及以上隐藏。渲染层不得产生业务副作用，窄终端和 Unicode 不得越界。

### 7. 测试与可运行验收
- 必须提供 cargo 命令、CLI 子命令、集成测试或可重复 demo。
- 至少覆盖成功、失败、取消、重复调用以及适用的恢复或回滚路径。
- 日志断言任务链 UUID、稳定 message code、进度闭环、真实文件名和行号、无敏感信息泄漏。
- 回滚只能删除本阶段临时产物或恢复快照，不得删除用户数据或覆盖外部修改。

### 8. 后续代码注释要求
编码前必须通过本条目审核。实现代码需要为公共类型和函数、状态转换、协议字段、数据库迁移、异步任务边界、错误分支和安全判定添加中文详细注释。注释应解释设计原因、约束和失败恢复；复杂算法、并发或锁、取消传播、幂等键和安全边界还要写明前置条件、后置条件及不变量。测试代码需要说明 fixture 意图和验收理由。

### 9. 审核结论栏
- 需求理解：待审核
- 架构边界：待审核
- 接口和事件：待审核
- 异常与恢复：待审核
- 安全与日志：待审核
- 允许进入代码开发：否

<!-- END S22-007 -->
## S22-008：support bundle

### 1. 功能目标
本功能属于 S22。建立 TelemetryContext、日志查询、调用投影、健康指标、审计和支持包。 本项聚焦于诊断上下文、查询投影和运维视图，必须形成可以独立启动、测试或演示的最小闭环。

### 2. 范围与边界
- 包含：support bundle所需契约、状态转换、错误返回、日志事件和自动化验收。
- 不包含：未列入本阶段的真实 Provider、跨阶段优化或视觉润色；依赖能力只能通过 Port、Fake 或 fixture 接入。
- 上游依赖：S22-007。依赖未 verified 时，本功能只能处于 blocked。
- 主要产物：诊断、指标、审计证据和脱敏支持包。

### 3. 输入、输出与处理流程
输入来自上游命令、查询、事件、配置、用户动作或 fixture，必须具备稳定标识、版本和必要授权上下文。

处理顺序：首先校验格式、当前状态、权限和依赖版本；其次，收集版本、health、事件摘要、脱敏日志和复现命令，生成带 manifest 和 digest 的安全包。；然后产生领域事件或标准结果并更新必要投影；最后返回成功、等待、失败或取消结果，并记录阶段进度。

输出为诊断、指标、审计证据和脱敏支持包。计划中的最小验收为：日志脱敏、事件摘要、版本、health 打包。

### 4. 状态、数据与一致性
- 状态变化必须遵守领域模型与事件规范，禁止直接修改 projection 绕过 domain command。
- SQLite Event Store，或本阶段明确声明的 Fake Store，是业务事实来源；日志只用于诊断。
- 外部副作用必须具有 operation_id、幂等策略、取消边界和恢复结论。
- 并发写入遵循单写者或 Write Claim；查询需要说明一致性级别和 cursor。

### 5. 异常、取消与恢复
必须设计输入无效、依赖不可用、权限拒绝、超时、用户取消、进程重启和重复提交路径。每类路径都要定义稳定 error_code、用户提示、是否可重试、是否产生事件和重启动作。不安全时进入明确的 blocked、failed 或 interrupted，不能静默成功。

### 6. 日志与安全要求
入口初始化日志，记录 stage.started、关键进度、stage.completed 或 stage.failed。日志采用非 JSON、非 key=value 的单行位置式格式：时间、级别、PID、任务链 UUID、线程或协程、logger、最后一级文件名和行号、主消息。不记录客户端地址；诊断上下文仅在 DEBUG 级别展示并置于主消息前，INFO 及以上隐藏。只展示安全视图，不泄露敏感内容。

### 7. 测试与可运行验收
- 必须提供 cargo 命令、CLI 子命令、集成测试或可重复 demo。
- 至少覆盖成功、失败、取消、重复调用以及适用的恢复或回滚路径。
- 日志断言任务链 UUID、稳定 message code、进度闭环、真实文件名和行号、无敏感信息泄漏。
- 回滚只能删除本阶段临时产物或恢复快照，不得删除用户数据或覆盖外部修改。

### 8. 后续代码注释要求
编码前必须通过本条目审核。实现代码需要为公共类型和函数、状态转换、协议字段、数据库迁移、异步任务边界、错误分支和安全判定添加中文详细注释。注释应解释设计原因、约束和失败恢复；复杂算法、并发或锁、取消传播、幂等键和安全边界还要写明前置条件、后置条件及不变量。测试代码需要说明 fixture 意图和验收理由。

### 9. 审核结论栏
- 需求理解：待审核
- 架构边界：待审核
- 接口和事件：待审核
- 异常与恢复：待审核
- 安全与日志：待审核
- 允许进入代码开发：否

<!-- END S22-008 -->
## S22-009：`v0.1-observability-rc1` demo

### 1. 功能目标
本功能属于 S22。建立 TelemetryContext、日志查询、调用投影、健康指标、审计和支持包。 本项聚焦于诊断上下文、查询投影和运维视图，必须形成可以独立启动、测试或演示的最小闭环。

### 2. 范围与边界
- 包含：`v0.1-observability-rc1` demo所需契约、状态转换、错误返回、日志事件和自动化验收。
- 不包含：未列入本阶段的真实 Provider、跨阶段优化或视觉润色；依赖能力只能通过 Port、Fake 或 fixture 接入。
- 上游依赖：S22-008。依赖未 verified 时，本功能只能处于 blocked。
- 主要产物：诊断、指标、审计证据和脱敏支持包。

### 3. 输入、输出与处理流程
输入来自上游命令、查询、事件、配置、用户动作或 fixture，必须具备稳定标识、版本和必要授权上下文。

处理顺序：首先校验格式、当前状态、权限和依赖版本；其次，围绕功能 [`v0.1-observability-rc1` demo] 完成输入校验、领域处理、持久化或投影、用户反馈和可重复验收。；然后产生领域事件或标准结果并更新必要投影；最后返回成功、等待、失败或取消结果，并记录阶段进度。

输出为诊断、指标、审计证据和脱敏支持包。计划中的最小验收为：运行失败→查询证据→导出安全诊断包。

### 4. 状态、数据与一致性
- 状态变化必须遵守领域模型与事件规范，禁止直接修改 projection 绕过 domain command。
- SQLite Event Store，或本阶段明确声明的 Fake Store，是业务事实来源；日志只用于诊断。
- 外部副作用必须具有 operation_id、幂等策略、取消边界和恢复结论。
- 并发写入遵循单写者或 Write Claim；查询需要说明一致性级别和 cursor。

### 5. 异常、取消与恢复
必须设计输入无效、依赖不可用、权限拒绝、超时、用户取消、进程重启和重复提交路径。每类路径都要定义稳定 error_code、用户提示、是否可重试、是否产生事件和重启动作。不安全时进入明确的 blocked、failed 或 interrupted，不能静默成功。

### 6. 日志与安全要求
入口初始化日志，记录 stage.started、关键进度、stage.completed 或 stage.failed。日志采用非 JSON、非 key=value 的单行位置式格式：时间、级别、PID、任务链 UUID、线程或协程、logger、最后一级文件名和行号、主消息。不记录客户端地址；诊断上下文仅在 DEBUG 级别展示并置于主消息前，INFO 及以上隐藏。只展示安全视图，不泄露敏感内容。

### 7. 测试与可运行验收
- 必须提供 cargo 命令、CLI 子命令、集成测试或可重复 demo。
- 至少覆盖成功、失败、取消、重复调用以及适用的恢复或回滚路径。
- 日志断言任务链 UUID、稳定 message code、进度闭环、真实文件名和行号、无敏感信息泄漏。
- 回滚只能删除本阶段临时产物或恢复快照，不得删除用户数据或覆盖外部修改。

### 8. 后续代码注释要求
编码前必须通过本条目审核。实现代码需要为公共类型和函数、状态转换、协议字段、数据库迁移、异步任务边界、错误分支和安全判定添加中文详细注释。注释应解释设计原因、约束和失败恢复；复杂算法、并发或锁、取消传播、幂等键和安全边界还要写明前置条件、后置条件及不变量。测试代码需要说明 fixture 意图和验收理由。

### 9. 审核结论栏
- 需求理解：待审核
- 架构边界：待审核
- 接口和事件：待审核
- 异常与恢复：待审核
- 安全与日志：待审核
- 允许进入代码开发：否

<!-- END S22-009 -->
## S23-001：Project/Session 选择

### 1. 功能目标
本功能属于 S23。将项目、Provider、工具、Spec、Rules、Checkpoint、Snapshot 和 TUI 合并为 MVP。 本项聚焦于领域功能、应用服务和可运行入口，必须形成可以独立启动、测试或演示的最小闭环。

### 2. 范围与边界
- 包含：Project/Session 选择所需契约、状态转换、错误返回、日志事件和自动化验收。
- 不包含：未列入本阶段的真实 Provider、跨阶段优化或视觉润色；依赖能力只能通过 Port、Fake 或 fixture 接入。
- 上游依赖：S09、S22。依赖未 verified 时，本功能只能处于 blocked。
- 主要产物：领域事件、查询投影、日志和验收结果。

### 3. 输入、输出与处理流程
输入来自上游命令、查询、事件、配置、用户动作或 fixture，必须具备稳定标识、版本和必要授权上下文。

处理顺序：首先校验格式、当前状态、权限和依赖版本；其次，围绕功能 [Project/Session 选择] 完成输入校验、领域处理、持久化或投影、用户反馈和可重复验收。；然后产生领域事件或标准结果并更新必要投影；最后返回成功、等待、失败或取消结果，并记录阶段进度。

输出为领域事件、查询投影、日志和验收结果。计划中的最小验收为：TUI 创建项目并恢复最近会话。

### 4. 状态、数据与一致性
- 状态变化必须遵守领域模型与事件规范，禁止直接修改 projection 绕过 domain command。
- SQLite Event Store，或本阶段明确声明的 Fake Store，是业务事实来源；日志只用于诊断。
- 外部副作用必须具有 operation_id、幂等策略、取消边界和恢复结论。
- 并发写入遵循单写者或 Write Claim；查询需要说明一致性级别和 cursor。

### 5. 异常、取消与恢复
必须设计输入无效、依赖不可用、权限拒绝、超时、用户取消、进程重启和重复提交路径。每类路径都要定义稳定 error_code、用户提示、是否可重试、是否产生事件和重启动作。不安全时进入明确的 blocked、failed 或 interrupted，不能静默成功。

### 6. 日志与安全要求
入口初始化日志，记录 stage.started、关键进度、stage.completed 或 stage.failed。日志采用非 JSON、非 key=value 的单行位置式格式：时间、级别、PID、任务链 UUID、线程或协程、logger、最后一级文件名和行号、主消息。不记录客户端地址；诊断上下文仅在 DEBUG 级别展示并置于主消息前，INFO 及以上隐藏。必须保持 Port、幂等、取消、恢复和审计边界。

### 7. 测试与可运行验收
- 必须提供 cargo 命令、CLI 子命令、集成测试或可重复 demo。
- 至少覆盖成功、失败、取消、重复调用以及适用的恢复或回滚路径。
- 日志断言任务链 UUID、稳定 message code、进度闭环、真实文件名和行号、无敏感信息泄漏。
- 回滚只能删除本阶段临时产物或恢复快照，不得删除用户数据或覆盖外部修改。

### 8. 后续代码注释要求
编码前必须通过本条目审核。实现代码需要为公共类型和函数、状态转换、协议字段、数据库迁移、异步任务边界、错误分支和安全判定添加中文详细注释。注释应解释设计原因、约束和失败恢复；复杂算法、并发或锁、取消传播、幂等键和安全边界还要写明前置条件、后置条件及不变量。测试代码需要说明 fixture 意图和验收理由。

### 9. 审核结论栏
- 需求理解：待审核
- 架构边界：待审核
- 接口和事件：待审核
- 异常与恢复：待审核
- 安全与日志：待审核
- 允许进入代码开发：否

<!-- END S23-001 -->
## S23-002：双 Provider 切换

### 1. 功能目标
本功能属于 S23。将项目、Provider、工具、Spec、Rules、Checkpoint、Snapshot 和 TUI 合并为 MVP。 本项聚焦于Provider Port、流式响应和上下文边界，必须形成可以独立启动、测试或演示的最小闭环。

### 2. 范围与边界
- 包含：双 Provider 切换所需契约、状态转换、错误返回、日志事件和自动化验收。
- 不包含：未列入本阶段的真实 Provider、跨阶段优化或视觉润色；依赖能力只能通过 Port、Fake 或 fixture 接入。
- 上游依赖：S10、S11。依赖未 verified 时，本功能只能处于 blocked。
- 主要产物：标准响应、usage、错误分类和可恢复状态。

### 3. 输入、输出与处理流程
输入来自上游命令、查询、事件、配置、用户动作或 fixture，必须具备稳定标识、版本和必要授权上下文。

处理顺序：首先校验格式、当前状态、权限和依赖版本；其次，围绕功能 [双 Provider 切换] 完成输入校验、领域处理、持久化或投影、用户反馈和可重复验收。；然后产生领域事件或标准结果并更新必要投影；最后返回成功、等待、失败或取消结果，并记录阶段进度。

输出为标准响应、usage、错误分类和可恢复状态。计划中的最小验收为：两 adapter 完成相同任务，历史连续。

### 4. 状态、数据与一致性
- 状态变化必须遵守领域模型与事件规范，禁止直接修改 projection 绕过 domain command。
- SQLite Event Store，或本阶段明确声明的 Fake Store，是业务事实来源；日志只用于诊断。
- 外部副作用必须具有 operation_id、幂等策略、取消边界和恢复结论。
- 并发写入遵循单写者或 Write Claim；查询需要说明一致性级别和 cursor。

### 5. 异常、取消与恢复
必须设计输入无效、依赖不可用、权限拒绝、超时、用户取消、进程重启和重复提交路径。每类路径都要定义稳定 error_code、用户提示、是否可重试、是否产生事件和重启动作。不安全时进入明确的 blocked、failed 或 interrupted，不能静默成功。

### 6. 日志与安全要求
入口初始化日志，记录 stage.started、关键进度、stage.completed 或 stage.failed。日志采用非 JSON、非 key=value 的单行位置式格式：时间、级别、PID、任务链 UUID、线程或协程、logger、最后一级文件名和行号、主消息。不记录客户端地址；诊断上下文仅在 DEBUG 级别展示并置于主消息前，INFO 及以上隐藏。不得记录 Prompt 原文、Provider 原文和凭据明文。

### 7. 测试与可运行验收
- 必须提供 cargo 命令、CLI 子命令、集成测试或可重复 demo。
- 至少覆盖成功、失败、取消、重复调用以及适用的恢复或回滚路径。
- 日志断言任务链 UUID、稳定 message code、进度闭环、真实文件名和行号、无敏感信息泄漏。
- 回滚只能删除本阶段临时产物或恢复快照，不得删除用户数据或覆盖外部修改。

### 8. 后续代码注释要求
编码前必须通过本条目审核。实现代码需要为公共类型和函数、状态转换、协议字段、数据库迁移、异步任务边界、错误分支和安全判定添加中文详细注释。注释应解释设计原因、约束和失败恢复；复杂算法、并发或锁、取消传播、幂等键和安全边界还要写明前置条件、后置条件及不变量。测试代码需要说明 fixture 意图和验收理由。

### 9. 审核结论栏
- 需求理解：待审核
- 架构边界：待审核
- 接口和事件：待审核
- 异常与恢复：待审核
- 安全与日志：待审核
- 允许进入代码开发：否

<!-- END S23-002 -->
## S23-003：基础工具全量接入

### 1. 功能目标
本功能属于 S23。将项目、Provider、工具、Spec、Rules、Checkpoint、Snapshot 和 TUI 合并为 MVP。 本项聚焦于领域功能、应用服务和可运行入口，必须形成可以独立启动、测试或演示的最小闭环。

### 2. 范围与边界
- 包含：基础工具全量接入所需契约、状态转换、错误返回、日志事件和自动化验收。
- 不包含：未列入本阶段的真实 Provider、跨阶段优化或视觉润色；依赖能力只能通过 Port、Fake 或 fixture 接入。
- 上游依赖：S12～S16。依赖未 verified 时，本功能只能处于 blocked。
- 主要产物：领域事件、查询投影、日志和验收结果。

### 3. 输入、输出与处理流程
输入来自上游命令、查询、事件、配置、用户动作或 fixture，必须具备稳定标识、版本和必要授权上下文。

处理顺序：首先校验格式、当前状态、权限和依赖版本；其次，围绕功能 [基础工具全量接入] 完成输入校验、领域处理、持久化或投影、用户反馈和可重复验收。；然后产生领域事件或标准结果并更新必要投影；最后返回成功、等待、失败或取消结果，并记录阶段进度。

输出为领域事件、查询投影、日志和验收结果。计划中的最小验收为：Read/Write/Edit/Bash/Glob/Grep/Task 都走 Gateway。

### 4. 状态、数据与一致性
- 状态变化必须遵守领域模型与事件规范，禁止直接修改 projection 绕过 domain command。
- SQLite Event Store，或本阶段明确声明的 Fake Store，是业务事实来源；日志只用于诊断。
- 外部副作用必须具有 operation_id、幂等策略、取消边界和恢复结论。
- 并发写入遵循单写者或 Write Claim；查询需要说明一致性级别和 cursor。

### 5. 异常、取消与恢复
必须设计输入无效、依赖不可用、权限拒绝、超时、用户取消、进程重启和重复提交路径。每类路径都要定义稳定 error_code、用户提示、是否可重试、是否产生事件和重启动作。不安全时进入明确的 blocked、failed 或 interrupted，不能静默成功。

### 6. 日志与安全要求
入口初始化日志，记录 stage.started、关键进度、stage.completed 或 stage.failed。日志采用非 JSON、非 key=value 的单行位置式格式：时间、级别、PID、任务链 UUID、线程或协程、logger、最后一级文件名和行号、主消息。不记录客户端地址；诊断上下文仅在 DEBUG 级别展示并置于主消息前，INFO 及以上隐藏。必须保持 Port、幂等、取消、恢复和审计边界。

### 7. 测试与可运行验收
- 必须提供 cargo 命令、CLI 子命令、集成测试或可重复 demo。
- 至少覆盖成功、失败、取消、重复调用以及适用的恢复或回滚路径。
- 日志断言任务链 UUID、稳定 message code、进度闭环、真实文件名和行号、无敏感信息泄漏。
- 回滚只能删除本阶段临时产物或恢复快照，不得删除用户数据或覆盖外部修改。

### 8. 后续代码注释要求
编码前必须通过本条目审核。实现代码需要为公共类型和函数、状态转换、协议字段、数据库迁移、异步任务边界、错误分支和安全判定添加中文详细注释。注释应解释设计原因、约束和失败恢复；复杂算法、并发或锁、取消传播、幂等键和安全边界还要写明前置条件、后置条件及不变量。测试代码需要说明 fixture 意图和验收理由。

### 9. 审核结论栏
- 需求理解：待审核
- 架构边界：待审核
- 接口和事件：待审核
- 异常与恢复：待审核
- 安全与日志：待审核
- 允许进入代码开发：否

<!-- END S23-003 -->
## S23-004：Spec 全量接入

### 1. 功能目标
本功能属于 S23。将项目、Provider、工具、Spec、Rules、Checkpoint、Snapshot 和 TUI 合并为 MVP。 本项聚焦于Spec 工件、状态和追踪关系，必须形成可以独立启动、测试或演示的最小闭环。

### 2. 范围与边界
- 包含：Spec 全量接入所需契约、状态转换、错误返回、日志事件和自动化验收。
- 不包含：未列入本阶段的真实 Provider、跨阶段优化或视觉润色；依赖能力只能通过 Port、Fake 或 fixture 接入。
- 上游依赖：S17、S18。依赖未 verified 时，本功能只能处于 blocked。
- 主要产物：工件、审核结果、状态变化和失效关系。

### 3. 输入、输出与处理流程
输入来自上游命令、查询、事件、配置、用户动作或 fixture，必须具备稳定标识、版本和必要授权上下文。

处理顺序：首先校验格式、当前状态、权限和依赖版本；其次，围绕功能 [Spec 全量接入] 完成输入校验、领域处理、持久化或投影、用户反馈和可重复验收。；然后产生领域事件或标准结果并更新必要投影；最后返回成功、等待、失败或取消结果，并记录阶段进度。

输出为工件、审核结果、状态变化和失效关系。计划中的最小验收为：正常五阶段和 skip-spec 均可完成。

### 4. 状态、数据与一致性
- 状态变化必须遵守领域模型与事件规范，禁止直接修改 projection 绕过 domain command。
- SQLite Event Store，或本阶段明确声明的 Fake Store，是业务事实来源；日志只用于诊断。
- 外部副作用必须具有 operation_id、幂等策略、取消边界和恢复结论。
- 并发写入遵循单写者或 Write Claim；查询需要说明一致性级别和 cursor。

### 5. 异常、取消与恢复
必须设计输入无效、依赖不可用、权限拒绝、超时、用户取消、进程重启和重复提交路径。每类路径都要定义稳定 error_code、用户提示、是否可重试、是否产生事件和重启动作。不安全时进入明确的 blocked、failed 或 interrupted，不能静默成功。

### 6. 日志与安全要求
入口初始化日志，记录 stage.started、关键进度、stage.completed 或 stage.failed。日志采用非 JSON、非 key=value 的单行位置式格式：时间、级别、PID、任务链 UUID、线程或协程、logger、最后一级文件名和行号、主消息。不记录客户端地址；诊断上下文仅在 DEBUG 级别展示并置于主消息前，INFO 及以上隐藏。跳过必须有授权，上游变更必须使下游可检测失效。

### 7. 测试与可运行验收
- 必须提供 cargo 命令、CLI 子命令、集成测试或可重复 demo。
- 至少覆盖成功、失败、取消、重复调用以及适用的恢复或回滚路径。
- 日志断言任务链 UUID、稳定 message code、进度闭环、真实文件名和行号、无敏感信息泄漏。
- 回滚只能删除本阶段临时产物或恢复快照，不得删除用户数据或覆盖外部修改。

### 8. 后续代码注释要求
编码前必须通过本条目审核。实现代码需要为公共类型和函数、状态转换、协议字段、数据库迁移、异步任务边界、错误分支和安全判定添加中文详细注释。注释应解释设计原因、约束和失败恢复；复杂算法、并发或锁、取消传播、幂等键和安全边界还要写明前置条件、后置条件及不变量。测试代码需要说明 fixture 意图和验收理由。

### 9. 审核结论栏
- 需求理解：待审核
- 架构边界：待审核
- 接口和事件：待审核
- 异常与恢复：待审核
- 安全与日志：待审核
- 允许进入代码开发：否

<!-- END S23-004 -->
## S23-005：Rules Gate 接入

### 1. 功能目标
本功能属于 S23。将项目、Provider、工具、Spec、Rules、Checkpoint、Snapshot 和 TUI 合并为 MVP。 本项聚焦于规则来源、编译版本和验证门，必须形成可以独立启动、测试或演示的最小闭环。

### 2. 范围与边界
- 包含：Rules Gate 接入所需契约、状态转换、错误返回、日志事件和自动化验收。
- 不包含：未列入本阶段的真实 Provider、跨阶段优化或视觉润色；依赖能力只能通过 Port、Fake 或 fixture 接入。
- 上游依赖：S19。依赖未 verified 时，本功能只能处于 blocked。
- 主要产物：诊断项、检查结果、修复任务和门禁决策。

### 3. 输入、输出与处理流程
输入来自上游命令、查询、事件、配置、用户动作或 fixture，必须具备稳定标识、版本和必要授权上下文。

处理顺序：首先校验格式、当前状态、权限和依赖版本；其次，围绕功能 [Rules Gate 接入] 完成输入校验、领域处理、持久化或投影、用户反馈和可重复验收。；然后产生领域事件或标准结果并更新必要投影；最后返回成功、等待、失败或取消结果，并记录阶段进度。

输出为诊断项、检查结果、修复任务和门禁决策。计划中的最小验收为：Write/Edit 后检查并阻断违规变更。

### 4. 状态、数据与一致性
- 状态变化必须遵守领域模型与事件规范，禁止直接修改 projection 绕过 domain command。
- SQLite Event Store，或本阶段明确声明的 Fake Store，是业务事实来源；日志只用于诊断。
- 外部副作用必须具有 operation_id、幂等策略、取消边界和恢复结论。
- 并发写入遵循单写者或 Write Claim；查询需要说明一致性级别和 cursor。

### 5. 异常、取消与恢复
必须设计输入无效、依赖不可用、权限拒绝、超时、用户取消、进程重启和重复提交路径。每类路径都要定义稳定 error_code、用户提示、是否可重试、是否产生事件和重启动作。不安全时进入明确的 blocked、failed 或 interrupted，不能静默成功。

### 6. 日志与安全要求
入口初始化日志，记录 stage.started、关键进度、stage.completed 或 stage.failed。日志采用非 JSON、非 key=value 的单行位置式格式：时间、级别、PID、任务链 UUID、线程或协程、logger、最后一级文件名和行号、主消息。不记录客户端地址；诊断上下文仅在 DEBUG 级别展示并置于主消息前，INFO 及以上隐藏。错误级问题不得静默放行。

### 7. 测试与可运行验收
- 必须提供 cargo 命令、CLI 子命令、集成测试或可重复 demo。
- 至少覆盖成功、失败、取消、重复调用以及适用的恢复或回滚路径。
- 日志断言任务链 UUID、稳定 message code、进度闭环、真实文件名和行号、无敏感信息泄漏。
- 回滚只能删除本阶段临时产物或恢复快照，不得删除用户数据或覆盖外部修改。

### 8. 后续代码注释要求
编码前必须通过本条目审核。实现代码需要为公共类型和函数、状态转换、协议字段、数据库迁移、异步任务边界、错误分支和安全判定添加中文详细注释。注释应解释设计原因、约束和失败恢复；复杂算法、并发或锁、取消传播、幂等键和安全边界还要写明前置条件、后置条件及不变量。测试代码需要说明 fixture 意图和验收理由。

### 9. 审核结论栏
- 需求理解：待审核
- 架构边界：待审核
- 接口和事件：待审核
- 异常与恢复：待审核
- 安全与日志：待审核
- 允许进入代码开发：否

<!-- END S23-005 -->
## S23-006：Checkpoint/Snapshot 接入

### 1. 功能目标
本功能属于 S23。将项目、Provider、工具、Spec、Rules、Checkpoint、Snapshot 和 TUI 合并为 MVP。 本项聚焦于Tool Gateway、工具契约和副作用隔离，必须形成可以独立启动、测试或演示的最小闭环。

### 2. 范围与边界
- 包含：Checkpoint/Snapshot 接入所需契约、状态转换、错误返回、日志事件和自动化验收。
- 不包含：未列入本阶段的真实 Provider、跨阶段优化或视觉润色；依赖能力只能通过 Port、Fake 或 fixture 接入。
- 上游依赖：S20、S21。依赖未 verified 时，本功能只能处于 blocked。
- 主要产物：ToolCall、标准结果、变更和快照摘要。

### 3. 输入、输出与处理流程
输入来自上游命令、查询、事件、配置、用户动作或 fixture，必须具备稳定标识、版本和必要授权上下文。

处理顺序：首先校验格式、当前状态、权限和依赖版本；其次，围绕功能 [Checkpoint/Snapshot 接入] 完成输入校验、领域处理、持久化或投影、用户反馈和可重复验收。；然后产生领域事件或标准结果并更新必要投影；最后返回成功、等待、失败或取消结果，并记录阶段进度。

输出为ToolCall、标准结果、变更和快照摘要。计划中的最小验收为：cancel/crash/timeout/restore 可恢复。

### 4. 状态、数据与一致性
- 状态变化必须遵守领域模型与事件规范，禁止直接修改 projection 绕过 domain command。
- SQLite Event Store，或本阶段明确声明的 Fake Store，是业务事实来源；日志只用于诊断。
- 外部副作用必须具有 operation_id、幂等策略、取消边界和恢复结论。
- 并发写入遵循单写者或 Write Claim；查询需要说明一致性级别和 cursor。

### 5. 异常、取消与恢复
必须设计输入无效、依赖不可用、权限拒绝、超时、用户取消、进程重启和重复提交路径。每类路径都要定义稳定 error_code、用户提示、是否可重试、是否产生事件和重启动作。不安全时进入明确的 blocked、failed 或 interrupted，不能静默成功。

### 6. 日志与安全要求
入口初始化日志，记录 stage.started、关键进度、stage.completed 或 stage.failed。日志采用非 JSON、非 key=value 的单行位置式格式：时间、级别、PID、任务链 UUID、线程或协程、logger、最后一级文件名和行号、主消息。不记录客户端地址；诊断上下文仅在 DEBUG 级别展示并置于主消息前，INFO 及以上隐藏。工具不得绕过 Gateway、权限、取消、超时和日志门禁。

### 7. 测试与可运行验收
- 必须提供 cargo 命令、CLI 子命令、集成测试或可重复 demo。
- 至少覆盖成功、失败、取消、重复调用以及适用的恢复或回滚路径。
- 日志断言任务链 UUID、稳定 message code、进度闭环、真实文件名和行号、无敏感信息泄漏。
- 回滚只能删除本阶段临时产物或恢复快照，不得删除用户数据或覆盖外部修改。

### 8. 后续代码注释要求
编码前必须通过本条目审核。实现代码需要为公共类型和函数、状态转换、协议字段、数据库迁移、异步任务边界、错误分支和安全判定添加中文详细注释。注释应解释设计原因、约束和失败恢复；复杂算法、并发或锁、取消传播、幂等键和安全边界还要写明前置条件、后置条件及不变量。测试代码需要说明 fixture 意图和验收理由。

### 9. 审核结论栏
- 需求理解：待审核
- 架构边界：待审核
- 接口和事件：待审核
- 异常与恢复：待审核
- 安全与日志：待审核
- 允许进入代码开发：否

<!-- END S23-006 -->
## S23-007：TUI 全面状态投影

### 1. 功能目标
本功能属于 S23。将项目、Provider、工具、Spec、Rules、Checkpoint、Snapshot 和 TUI 合并为 MVP。 本项聚焦于输入事件、纯状态转换和终端渲染，必须形成可以独立启动、测试或演示的最小闭环。

### 2. 范围与边界
- 包含：TUI 全面状态投影所需契约、状态转换、错误返回、日志事件和自动化验收。
- 不包含：未列入本阶段的真实 Provider、跨阶段优化或视觉润色；依赖能力只能通过 Port、Fake 或 fixture 接入。
- 上游依赖：S22。依赖未 verified 时，本功能只能处于 blocked。
- 主要产物：AppState、Effect、渲染帧和用户提示。

### 3. 输入、输出与处理流程
输入来自上游命令、查询、事件、配置、用户动作或 fixture，必须具备稳定标识、版本和必要授权上下文。

处理顺序：首先校验格式、当前状态、权限和依赖版本；其次，围绕功能 [TUI 全面状态投影] 完成输入校验、领域处理、持久化或投影、用户反馈和可重复验收。；然后产生领域事件或标准结果并更新必要投影；最后返回成功、等待、失败或取消结果，并记录阶段进度。

输出为AppState、Effect、渲染帧和用户提示。计划中的最小验收为：session/run/tool/approval/spec/rules/snapshot 可切换。

### 4. 状态、数据与一致性
- 状态变化必须遵守领域模型与事件规范，禁止直接修改 projection 绕过 domain command。
- SQLite Event Store，或本阶段明确声明的 Fake Store，是业务事实来源；日志只用于诊断。
- 外部副作用必须具有 operation_id、幂等策略、取消边界和恢复结论。
- 并发写入遵循单写者或 Write Claim；查询需要说明一致性级别和 cursor。

### 5. 异常、取消与恢复
必须设计输入无效、依赖不可用、权限拒绝、超时、用户取消、进程重启和重复提交路径。每类路径都要定义稳定 error_code、用户提示、是否可重试、是否产生事件和重启动作。不安全时进入明确的 blocked、failed 或 interrupted，不能静默成功。

### 6. 日志与安全要求
入口初始化日志，记录 stage.started、关键进度、stage.completed 或 stage.failed。日志采用非 JSON、非 key=value 的单行位置式格式：时间、级别、PID、任务链 UUID、线程或协程、logger、最后一级文件名和行号、主消息。不记录客户端地址；诊断上下文仅在 DEBUG 级别展示并置于主消息前，INFO 及以上隐藏。渲染层不得产生业务副作用，窄终端和 Unicode 不得越界。

### 7. 测试与可运行验收
- 必须提供 cargo 命令、CLI 子命令、集成测试或可重复 demo。
- 至少覆盖成功、失败、取消、重复调用以及适用的恢复或回滚路径。
- 日志断言任务链 UUID、稳定 message code、进度闭环、真实文件名和行号、无敏感信息泄漏。
- 回滚只能删除本阶段临时产物或恢复快照，不得删除用户数据或覆盖外部修改。

### 8. 后续代码注释要求
编码前必须通过本条目审核。实现代码需要为公共类型和函数、状态转换、协议字段、数据库迁移、异步任务边界、错误分支和安全判定添加中文详细注释。注释应解释设计原因、约束和失败恢复；复杂算法、并发或锁、取消传播、幂等键和安全边界还要写明前置条件、后置条件及不变量。测试代码需要说明 fixture 意图和验收理由。

### 9. 审核结论栏
- 需求理解：待审核
- 架构边界：待审核
- 接口和事件：待审核
- 异常与恢复：待审核
- 安全与日志：待审核
- 允许进入代码开发：否

<!-- END S23-007 -->
## S23-008：`v0.1-mvp-integration` demo

### 1. 功能目标
本功能属于 S23。将项目、Provider、工具、Spec、Rules、Checkpoint、Snapshot 和 TUI 合并为 MVP。 本项聚焦于发布集成、故障演练和运维生命周期，必须形成可以独立启动、测试或演示的最小闭环。

### 2. 范围与边界
- 包含：`v0.1-mvp-integration` demo所需契约、状态转换、错误返回、日志事件和自动化验收。
- 不包含：未列入本阶段的真实 Provider、跨阶段优化或视觉润色；依赖能力只能通过 Port、Fake 或 fixture 接入。
- 上游依赖：S23-001～S23-007。依赖未 verified 时，本功能只能处于 blocked。
- 主要产物：报告、制品、manifest、digest 和恢复证明。

### 3. 输入、输出与处理流程
输入来自上游命令、查询、事件、配置、用户动作或 fixture，必须具备稳定标识、版本和必要授权上下文。

处理顺序：首先校验格式、当前状态、权限和依赖版本；其次，从用户需求依次执行 Project、Session、Agent、Tool、Spec、Rules 和恢复，验证最终变更与审计链。；然后产生领域事件或标准结果并更新必要投影；最后返回成功、等待、失败或取消结果，并记录阶段进度。

输出为报告、制品、manifest、digest 和恢复证明。计划中的最小验收为：从输入需求到变更验证的完整单机流程。

### 4. 状态、数据与一致性
- 状态变化必须遵守领域模型与事件规范，禁止直接修改 projection 绕过 domain command。
- SQLite Event Store，或本阶段明确声明的 Fake Store，是业务事实来源；日志只用于诊断。
- 外部副作用必须具有 operation_id、幂等策略、取消边界和恢复结论。
- 并发写入遵循单写者或 Write Claim；查询需要说明一致性级别和 cursor。

### 5. 异常、取消与恢复
必须设计输入无效、依赖不可用、权限拒绝、超时、用户取消、进程重启和重复提交路径。每类路径都要定义稳定 error_code、用户提示、是否可重试、是否产生事件和重启动作。不安全时进入明确的 blocked、failed 或 interrupted，不能静默成功。

### 6. 日志与安全要求
入口初始化日志，记录 stage.started、关键进度、stage.completed 或 stage.failed。日志采用非 JSON、非 key=value 的单行位置式格式：时间、级别、PID、任务链 UUID、线程或协程、logger、最后一级文件名和行号、主消息。不记录客户端地址；诊断上下文仅在 DEBUG 级别展示并置于主消息前，INFO 及以上隐藏。失败必须可定位、可重试、可回滚且不破坏用户数据。

### 7. 测试与可运行验收
- 必须提供 cargo 命令、CLI 子命令、集成测试或可重复 demo。
- 至少覆盖成功、失败、取消、重复调用以及适用的恢复或回滚路径。
- 日志断言任务链 UUID、稳定 message code、进度闭环、真实文件名和行号、无敏感信息泄漏。
- 回滚只能删除本阶段临时产物或恢复快照，不得删除用户数据或覆盖外部修改。

### 8. 后续代码注释要求
编码前必须通过本条目审核。实现代码需要为公共类型和函数、状态转换、协议字段、数据库迁移、异步任务边界、错误分支和安全判定添加中文详细注释。注释应解释设计原因、约束和失败恢复；复杂算法、并发或锁、取消传播、幂等键和安全边界还要写明前置条件、后置条件及不变量。测试代码需要说明 fixture 意图和验收理由。

### 9. 审核结论栏
- 需求理解：待审核
- 架构边界：待审核
- 接口和事件：待审核
- 异常与恢复：待审核
- 安全与日志：待审核
- 允许进入代码开发：否

<!-- END S23-008 -->
## S24-001：core integration suite

### 1. 功能目标
本功能属于 S24。完成集成回归、故障矩阵、Secret 扫描、性能、迁移、备份、安装和发布。 本项聚焦于发布集成、故障演练和运维生命周期，必须形成可以独立启动、测试或演示的最小闭环。

### 2. 范围与边界
- 包含：core integration suite所需契约、状态转换、错误返回、日志事件和自动化验收。
- 不包含：未列入本阶段的真实 Provider、跨阶段优化或视觉润色；依赖能力只能通过 Port、Fake 或 fixture 接入。
- 上游依赖：S23。依赖未 verified 时，本功能只能处于 blocked。
- 主要产物：报告、制品、manifest、digest 和恢复证明。

### 3. 输入、输出与处理流程
输入来自上游命令、查询、事件、配置、用户动作或 fixture，必须具备稳定标识、版本和必要授权上下文。

处理顺序：首先校验格式、当前状态、权限和依赖版本；其次，围绕功能 [core integration suite] 完成输入校验、领域处理、持久化或投影、用户反馈和可重复验收。；然后产生领域事件或标准结果并更新必要投影；最后返回成功、等待、失败或取消结果，并记录阶段进度。

输出为报告、制品、manifest、digest 和恢复证明。计划中的最小验收为：fake provider 全量回归。

### 4. 状态、数据与一致性
- 状态变化必须遵守领域模型与事件规范，禁止直接修改 projection 绕过 domain command。
- SQLite Event Store，或本阶段明确声明的 Fake Store，是业务事实来源；日志只用于诊断。
- 外部副作用必须具有 operation_id、幂等策略、取消边界和恢复结论。
- 并发写入遵循单写者或 Write Claim；查询需要说明一致性级别和 cursor。

### 5. 异常、取消与恢复
必须设计输入无效、依赖不可用、权限拒绝、超时、用户取消、进程重启和重复提交路径。每类路径都要定义稳定 error_code、用户提示、是否可重试、是否产生事件和重启动作。不安全时进入明确的 blocked、failed 或 interrupted，不能静默成功。

### 6. 日志与安全要求
入口初始化日志，记录 stage.started、关键进度、stage.completed 或 stage.failed。日志采用非 JSON、非 key=value 的单行位置式格式：时间、级别、PID、任务链 UUID、线程或协程、logger、最后一级文件名和行号、主消息。不记录客户端地址；诊断上下文仅在 DEBUG 级别展示并置于主消息前，INFO 及以上隐藏。失败必须可定位、可重试、可回滚且不破坏用户数据。

### 7. 测试与可运行验收
- 必须提供 cargo 命令、CLI 子命令、集成测试或可重复 demo。
- 至少覆盖成功、失败、取消、重复调用以及适用的恢复或回滚路径。
- 日志断言任务链 UUID、稳定 message code、进度闭环、真实文件名和行号、无敏感信息泄漏。
- 回滚只能删除本阶段临时产物或恢复快照，不得删除用户数据或覆盖外部修改。

### 8. 后续代码注释要求
编码前必须通过本条目审核。实现代码需要为公共类型和函数、状态转换、协议字段、数据库迁移、异步任务边界、错误分支和安全判定添加中文详细注释。注释应解释设计原因、约束和失败恢复；复杂算法、并发或锁、取消传播、幂等键和安全边界还要写明前置条件、后置条件及不变量。测试代码需要说明 fixture 意图和验收理由。

### 9. 审核结论栏
- 需求理解：待审核
- 架构边界：待审核
- 接口和事件：待审核
- 异常与恢复：待审核
- 安全与日志：待审核
- 允许进入代码开发：否

<!-- END S24-001 -->
## S24-002：crash/restart matrix

### 1. 功能目标
本功能属于 S24。完成集成回归、故障矩阵、Secret 扫描、性能、迁移、备份、安装和发布。 本项聚焦于事件存储、事务和可重建投影，必须形成可以独立启动、测试或演示的最小闭环。

### 2. 范围与边界
- 包含：crash/restart matrix所需契约、状态转换、错误返回、日志事件和自动化验收。
- 不包含：未列入本阶段的真实 Provider、跨阶段优化或视觉润色；依赖能力只能通过 Port、Fake 或 fixture 接入。
- 上游依赖：S24-001。依赖未 verified 时，本功能只能处于 blocked。
- 主要产物：事件序列、投影版本、游标和恢复结果。

### 3. 输入、输出与处理流程
输入来自上游命令、查询、事件、配置、用户动作或 fixture，必须具备稳定标识、版本和必要授权上下文。

处理顺序：首先校验格式、当前状态、权限和依赖版本；其次，对 Core、TUI、Provider、Tool 和 DB 注入终止、超时、断连和半写入故障。；然后产生领域事件或标准结果并更新必要投影；最后返回成功、等待、失败或取消结果，并记录阶段进度。

输出为事件序列、投影版本、游标和恢复结果。计划中的最小验收为：Core、TUI、Provider、Tool、DB 各类中断可恢复。

### 4. 状态、数据与一致性
- 状态变化必须遵守领域模型与事件规范，禁止直接修改 projection 绕过 domain command。
- SQLite Event Store，或本阶段明确声明的 Fake Store，是业务事实来源；日志只用于诊断。
- 外部副作用必须具有 operation_id、幂等策略、取消边界和恢复结论。
- 并发写入遵循单写者或 Write Claim；查询需要说明一致性级别和 cursor。

### 5. 异常、取消与恢复
必须设计输入无效、依赖不可用、权限拒绝、超时、用户取消、进程重启和重复提交路径。每类路径都要定义稳定 error_code、用户提示、是否可重试、是否产生事件和重启动作。不安全时进入明确的 blocked、failed 或 interrupted，不能静默成功。

### 6. 日志与安全要求
入口初始化日志，记录 stage.started、关键进度、stage.completed 或 stage.failed。日志采用非 JSON、非 key=value 的单行位置式格式：时间、级别、PID、任务链 UUID、线程或协程、logger、最后一级文件名和行号、主消息。不记录客户端地址；诊断上下文仅在 DEBUG 级别展示并置于主消息前，INFO 及以上隐藏。SQLite 事件是业务事实，事务不得产生半提交。

### 7. 测试与可运行验收
- 必须提供 cargo 命令、CLI 子命令、集成测试或可重复 demo。
- 至少覆盖成功、失败、取消、重复调用以及适用的恢复或回滚路径。
- 日志断言任务链 UUID、稳定 message code、进度闭环、真实文件名和行号、无敏感信息泄漏。
- 回滚只能删除本阶段临时产物或恢复快照，不得删除用户数据或覆盖外部修改。

### 8. 后续代码注释要求
编码前必须通过本条目审核。实现代码需要为公共类型和函数、状态转换、协议字段、数据库迁移、异步任务边界、错误分支和安全判定添加中文详细注释。注释应解释设计原因、约束和失败恢复；复杂算法、并发或锁、取消传播、幂等键和安全边界还要写明前置条件、后置条件及不变量。测试代码需要说明 fixture 意图和验收理由。

### 9. 审核结论栏
- 需求理解：待审核
- 架构边界：待审核
- 接口和事件：待审核
- 异常与恢复：待审核
- 安全与日志：待审核
- 允许进入代码开发：否

<!-- END S24-002 -->
## S24-003：Secret scan

### 1. 功能目标
本功能属于 S24。完成集成回归、故障矩阵、Secret 扫描、性能、迁移、备份、安装和发布。 本项聚焦于安全策略、授权上下文和审批生命周期，必须形成可以独立启动、测试或演示的最小闭环。

### 2. 范围与边界
- 包含：Secret scan所需契约、状态转换、错误返回、日志事件和自动化验收。
- 不包含：未列入本阶段的真实 Provider、跨阶段优化或视觉润色；依赖能力只能通过 Port、Fake 或 fixture 接入。
- 上游依赖：S24-002。依赖未 verified 时，本功能只能处于 blocked。
- 主要产物：决策、理由、审批事件和安全报告。

### 3. 输入、输出与处理流程
输入来自上游命令、查询、事件、配置、用户动作或 fixture，必须具备稳定标识、版本和必要授权上下文。

处理顺序：首先校验格式、当前状态、权限和依赖版本；其次，围绕功能 [Secret scan] 完成输入校验、领域处理、持久化或投影、用户反馈和可重复验收。；然后产生领域事件或标准结果并更新必要投影；最后返回成功、等待、失败或取消结果，并记录阶段进度。

输出为决策、理由、审批事件和安全报告。计划中的最小验收为：仓库、日志、fixture、support bundle 扫描。

### 4. 状态、数据与一致性
- 状态变化必须遵守领域模型与事件规范，禁止直接修改 projection 绕过 domain command。
- SQLite Event Store，或本阶段明确声明的 Fake Store，是业务事实来源；日志只用于诊断。
- 外部副作用必须具有 operation_id、幂等策略、取消边界和恢复结论。
- 并发写入遵循单写者或 Write Claim；查询需要说明一致性级别和 cursor。

### 5. 异常、取消与恢复
必须设计输入无效、依赖不可用、权限拒绝、超时、用户取消、进程重启和重复提交路径。每类路径都要定义稳定 error_code、用户提示、是否可重试、是否产生事件和重启动作。不安全时进入明确的 blocked、failed 或 interrupted，不能静默成功。

### 6. 日志与安全要求
入口初始化日志，记录 stage.started、关键进度、stage.completed 或 stage.failed。日志采用非 JSON、非 key=value 的单行位置式格式：时间、级别、PID、任务链 UUID、线程或协程、logger、最后一级文件名和行号、主消息。不记录客户端地址；诊断上下文仅在 DEBUG 级别展示并置于主消息前，INFO 及以上隐藏。高风险操作默认拒绝，任何放行必须可审计。

### 7. 测试与可运行验收
- 必须提供 cargo 命令、CLI 子命令、集成测试或可重复 demo。
- 至少覆盖成功、失败、取消、重复调用以及适用的恢复或回滚路径。
- 日志断言任务链 UUID、稳定 message code、进度闭环、真实文件名和行号、无敏感信息泄漏。
- 回滚只能删除本阶段临时产物或恢复快照，不得删除用户数据或覆盖外部修改。

### 8. 后续代码注释要求
编码前必须通过本条目审核。实现代码需要为公共类型和函数、状态转换、协议字段、数据库迁移、异步任务边界、错误分支和安全判定添加中文详细注释。注释应解释设计原因、约束和失败恢复；复杂算法、并发或锁、取消传播、幂等键和安全边界还要写明前置条件、后置条件及不变量。测试代码需要说明 fixture 意图和验收理由。

### 9. 审核结论栏
- 需求理解：待审核
- 架构边界：待审核
- 接口和事件：待审核
- 异常与恢复：待审核
- 安全与日志：待审核
- 允许进入代码开发：否

<!-- END S24-003 -->
## S24-004：performance baseline

### 1. 功能目标
本功能属于 S24。完成集成回归、故障矩阵、Secret 扫描、性能、迁移、备份、安装和发布。 本项聚焦于发布集成、故障演练和运维生命周期，必须形成可以独立启动、测试或演示的最小闭环。

### 2. 范围与边界
- 包含：performance baseline所需契约、状态转换、错误返回、日志事件和自动化验收。
- 不包含：未列入本阶段的真实 Provider、跨阶段优化或视觉润色；依赖能力只能通过 Port、Fake 或 fixture 接入。
- 上游依赖：S24-003。依赖未 verified 时，本功能只能处于 blocked。
- 主要产物：报告、制品、manifest、digest 和恢复证明。

### 3. 输入、输出与处理流程
输入来自上游命令、查询、事件、配置、用户动作或 fixture，必须具备稳定标识、版本和必要授权上下文。

处理顺序：首先校验格式、当前状态、权限和依赖版本；其次，围绕功能 [performance baseline] 完成输入校验、领域处理、持久化或投影、用户反馈和可重复验收。；然后产生领域事件或标准结果并更新必要投影；最后返回成功、等待、失败或取消结果，并记录阶段进度。

输出为报告、制品、manifest、digest 和恢复证明。计划中的最小验收为：TUI 启动、事件提交、SQLite query、日志吞吐达标。

### 4. 状态、数据与一致性
- 状态变化必须遵守领域模型与事件规范，禁止直接修改 projection 绕过 domain command。
- SQLite Event Store，或本阶段明确声明的 Fake Store，是业务事实来源；日志只用于诊断。
- 外部副作用必须具有 operation_id、幂等策略、取消边界和恢复结论。
- 并发写入遵循单写者或 Write Claim；查询需要说明一致性级别和 cursor。

### 5. 异常、取消与恢复
必须设计输入无效、依赖不可用、权限拒绝、超时、用户取消、进程重启和重复提交路径。每类路径都要定义稳定 error_code、用户提示、是否可重试、是否产生事件和重启动作。不安全时进入明确的 blocked、failed 或 interrupted，不能静默成功。

### 6. 日志与安全要求
入口初始化日志，记录 stage.started、关键进度、stage.completed 或 stage.failed。日志采用非 JSON、非 key=value 的单行位置式格式：时间、级别、PID、任务链 UUID、线程或协程、logger、最后一级文件名和行号、主消息。不记录客户端地址；诊断上下文仅在 DEBUG 级别展示并置于主消息前，INFO 及以上隐藏。失败必须可定位、可重试、可回滚且不破坏用户数据。

### 7. 测试与可运行验收
- 必须提供 cargo 命令、CLI 子命令、集成测试或可重复 demo。
- 至少覆盖成功、失败、取消、重复调用以及适用的恢复或回滚路径。
- 日志断言任务链 UUID、稳定 message code、进度闭环、真实文件名和行号、无敏感信息泄漏。
- 回滚只能删除本阶段临时产物或恢复快照，不得删除用户数据或覆盖外部修改。

### 8. 后续代码注释要求
编码前必须通过本条目审核。实现代码需要为公共类型和函数、状态转换、协议字段、数据库迁移、异步任务边界、错误分支和安全判定添加中文详细注释。注释应解释设计原因、约束和失败恢复；复杂算法、并发或锁、取消传播、幂等键和安全边界还要写明前置条件、后置条件及不变量。测试代码需要说明 fixture 意图和验收理由。

### 9. 审核结论栏
- 需求理解：待审核
- 架构边界：待审核
- 接口和事件：待审核
- 异常与恢复：待审核
- 安全与日志：待审核
- 允许进入代码开发：否

<!-- END S24-004 -->
## S24-005：migration upgrade

### 1. 功能目标
本功能属于 S24。完成集成回归、故障矩阵、Secret 扫描、性能、迁移、备份、安装和发布。 本项聚焦于事件存储、事务和可重建投影，必须形成可以独立启动、测试或演示的最小闭环。

### 2. 范围与边界
- 包含：migration upgrade所需契约、状态转换、错误返回、日志事件和自动化验收。
- 不包含：未列入本阶段的真实 Provider、跨阶段优化或视觉润色；依赖能力只能通过 Port、Fake 或 fixture 接入。
- 上游依赖：S24-004。依赖未 verified 时，本功能只能处于 blocked。
- 主要产物：事件序列、投影版本、游标和恢复结果。

### 3. 输入、输出与处理流程
输入来自上游命令、查询、事件、配置、用户动作或 fixture，必须具备稳定标识、版本和必要授权上下文。

处理顺序：首先校验格式、当前状态、权限和依赖版本；其次，围绕功能 [migration upgrade] 完成输入校验、领域处理、持久化或投影、用户反馈和可重复验收。；然后产生领域事件或标准结果并更新必要投影；最后返回成功、等待、失败或取消结果，并记录阶段进度。

输出为事件序列、投影版本、游标和恢复结果。计划中的最小验收为：空库、旧库、失败 migration、回滚演练。

### 4. 状态、数据与一致性
- 状态变化必须遵守领域模型与事件规范，禁止直接修改 projection 绕过 domain command。
- SQLite Event Store，或本阶段明确声明的 Fake Store，是业务事实来源；日志只用于诊断。
- 外部副作用必须具有 operation_id、幂等策略、取消边界和恢复结论。
- 并发写入遵循单写者或 Write Claim；查询需要说明一致性级别和 cursor。

### 5. 异常、取消与恢复
必须设计输入无效、依赖不可用、权限拒绝、超时、用户取消、进程重启和重复提交路径。每类路径都要定义稳定 error_code、用户提示、是否可重试、是否产生事件和重启动作。不安全时进入明确的 blocked、failed 或 interrupted，不能静默成功。

### 6. 日志与安全要求
入口初始化日志，记录 stage.started、关键进度、stage.completed 或 stage.failed。日志采用非 JSON、非 key=value 的单行位置式格式：时间、级别、PID、任务链 UUID、线程或协程、logger、最后一级文件名和行号、主消息。不记录客户端地址；诊断上下文仅在 DEBUG 级别展示并置于主消息前，INFO 及以上隐藏。SQLite 事件是业务事实，事务不得产生半提交。

### 7. 测试与可运行验收
- 必须提供 cargo 命令、CLI 子命令、集成测试或可重复 demo。
- 至少覆盖成功、失败、取消、重复调用以及适用的恢复或回滚路径。
- 日志断言任务链 UUID、稳定 message code、进度闭环、真实文件名和行号、无敏感信息泄漏。
- 回滚只能删除本阶段临时产物或恢复快照，不得删除用户数据或覆盖外部修改。

### 8. 后续代码注释要求
编码前必须通过本条目审核。实现代码需要为公共类型和函数、状态转换、协议字段、数据库迁移、异步任务边界、错误分支和安全判定添加中文详细注释。注释应解释设计原因、约束和失败恢复；复杂算法、并发或锁、取消传播、幂等键和安全边界还要写明前置条件、后置条件及不变量。测试代码需要说明 fixture 意图和验收理由。

### 9. 审核结论栏
- 需求理解：待审核
- 架构边界：待审核
- 接口和事件：待审核
- 异常与恢复：待审核
- 安全与日志：待审核
- 允许进入代码开发：否

<!-- END S24-005 -->
## S24-006：backup/restore

### 1. 功能目标
本功能属于 S24。完成集成回归、故障矩阵、Secret 扫描、性能、迁移、备份、安装和发布。 本项聚焦于Tool Gateway、工具契约和副作用隔离，必须形成可以独立启动、测试或演示的最小闭环。

### 2. 范围与边界
- 包含：backup/restore所需契约、状态转换、错误返回、日志事件和自动化验收。
- 不包含：未列入本阶段的真实 Provider、跨阶段优化或视觉润色；依赖能力只能通过 Port、Fake 或 fixture 接入。
- 上游依赖：S24-005。依赖未 verified 时，本功能只能处于 blocked。
- 主要产物：ToolCall、标准结果、变更和快照摘要。

### 3. 输入、输出与处理流程
输入来自上游命令、查询、事件、配置、用户动作或 fixture，必须具备稳定标识、版本和必要授权上下文。

处理顺序：首先校验格式、当前状态、权限和依赖版本；其次，备份包含 schema 版本、最大事件序号、文件摘要、版本和 manifest，临时恢复校验后再替换正式数据。；然后产生领域事件或标准结果并更新必要投影；最后返回成功、等待、失败或取消结果，并记录阶段进度。

输出为ToolCall、标准结果、变更和快照摘要。计划中的最小验收为：备份 manifest、digest、seq 和恢复结果正确。

### 4. 状态、数据与一致性
- 状态变化必须遵守领域模型与事件规范，禁止直接修改 projection 绕过 domain command。
- SQLite Event Store，或本阶段明确声明的 Fake Store，是业务事实来源；日志只用于诊断。
- 外部副作用必须具有 operation_id、幂等策略、取消边界和恢复结论。
- 并发写入遵循单写者或 Write Claim；查询需要说明一致性级别和 cursor。

### 5. 异常、取消与恢复
必须设计输入无效、依赖不可用、权限拒绝、超时、用户取消、进程重启和重复提交路径。每类路径都要定义稳定 error_code、用户提示、是否可重试、是否产生事件和重启动作。不安全时进入明确的 blocked、failed 或 interrupted，不能静默成功。

### 6. 日志与安全要求
入口初始化日志，记录 stage.started、关键进度、stage.completed 或 stage.failed。日志采用非 JSON、非 key=value 的单行位置式格式：时间、级别、PID、任务链 UUID、线程或协程、logger、最后一级文件名和行号、主消息。不记录客户端地址；诊断上下文仅在 DEBUG 级别展示并置于主消息前，INFO 及以上隐藏。工具不得绕过 Gateway、权限、取消、超时和日志门禁。

### 7. 测试与可运行验收
- 必须提供 cargo 命令、CLI 子命令、集成测试或可重复 demo。
- 至少覆盖成功、失败、取消、重复调用以及适用的恢复或回滚路径。
- 日志断言任务链 UUID、稳定 message code、进度闭环、真实文件名和行号、无敏感信息泄漏。
- 回滚只能删除本阶段临时产物或恢复快照，不得删除用户数据或覆盖外部修改。

### 8. 后续代码注释要求
编码前必须通过本条目审核。实现代码需要为公共类型和函数、状态转换、协议字段、数据库迁移、异步任务边界、错误分支和安全判定添加中文详细注释。注释应解释设计原因、约束和失败恢复；复杂算法、并发或锁、取消传播、幂等键和安全边界还要写明前置条件、后置条件及不变量。测试代码需要说明 fixture 意图和验收理由。

### 9. 审核结论栏
- 需求理解：待审核
- 架构边界：待审核
- 接口和事件：待审核
- 异常与恢复：待审核
- 安全与日志：待审核
- 允许进入代码开发：否

<!-- END S24-006 -->
## S24-007：package install

### 1. 功能目标
本功能属于 S24。完成集成回归、故障矩阵、Secret 扫描、性能、迁移、备份、安装和发布。 本项聚焦于发布集成、故障演练和运维生命周期，必须形成可以独立启动、测试或演示的最小闭环。

### 2. 范围与边界
- 包含：package install所需契约、状态转换、错误返回、日志事件和自动化验收。
- 不包含：未列入本阶段的真实 Provider、跨阶段优化或视觉润色；依赖能力只能通过 Port、Fake 或 fixture 接入。
- 上游依赖：S24-006。依赖未 verified 时，本功能只能处于 blocked。
- 主要产物：报告、制品、manifest、digest 和恢复证明。

### 3. 输入、输出与处理流程
输入来自上游命令、查询、事件、配置、用户动作或 fixture，必须具备稳定标识、版本和必要授权上下文。

处理顺序：首先校验格式、当前状态、权限和依赖版本；其次，围绕功能 [package install] 完成输入校验、领域处理、持久化或投影、用户反馈和可重复验收。；然后产生领域事件或标准结果并更新必要投影；最后返回成功、等待、失败或取消结果，并记录阶段进度。

输出为报告、制品、manifest、digest 和恢复证明。计划中的最小验收为：Windows/macOS/Linux 本机安装后 `apex tui` 可用。

### 4. 状态、数据与一致性
- 状态变化必须遵守领域模型与事件规范，禁止直接修改 projection 绕过 domain command。
- SQLite Event Store，或本阶段明确声明的 Fake Store，是业务事实来源；日志只用于诊断。
- 外部副作用必须具有 operation_id、幂等策略、取消边界和恢复结论。
- 并发写入遵循单写者或 Write Claim；查询需要说明一致性级别和 cursor。

### 5. 异常、取消与恢复
必须设计输入无效、依赖不可用、权限拒绝、超时、用户取消、进程重启和重复提交路径。每类路径都要定义稳定 error_code、用户提示、是否可重试、是否产生事件和重启动作。不安全时进入明确的 blocked、failed 或 interrupted，不能静默成功。

### 6. 日志与安全要求
入口初始化日志，记录 stage.started、关键进度、stage.completed 或 stage.failed。日志采用非 JSON、非 key=value 的单行位置式格式：时间、级别、PID、任务链 UUID、线程或协程、logger、最后一级文件名和行号、主消息。不记录客户端地址；诊断上下文仅在 DEBUG 级别展示并置于主消息前，INFO 及以上隐藏。失败必须可定位、可重试、可回滚且不破坏用户数据。

### 7. 测试与可运行验收
- 必须提供 cargo 命令、CLI 子命令、集成测试或可重复 demo。
- 至少覆盖成功、失败、取消、重复调用以及适用的恢复或回滚路径。
- 日志断言任务链 UUID、稳定 message code、进度闭环、真实文件名和行号、无敏感信息泄漏。
- 回滚只能删除本阶段临时产物或恢复快照，不得删除用户数据或覆盖外部修改。

### 8. 后续代码注释要求
编码前必须通过本条目审核。实现代码需要为公共类型和函数、状态转换、协议字段、数据库迁移、异步任务边界、错误分支和安全判定添加中文详细注释。注释应解释设计原因、约束和失败恢复；复杂算法、并发或锁、取消传播、幂等键和安全边界还要写明前置条件、后置条件及不变量。测试代码需要说明 fixture 意图和验收理由。

### 9. 审核结论栏
- 需求理解：待审核
- 架构边界：待审核
- 接口和事件：待审核
- 异常与恢复：待审核
- 安全与日志：待审核
- 允许进入代码开发：否

<!-- END S24-007 -->
## S24-008：release candidate

### 1. 功能目标
本功能属于 S24。完成集成回归、故障矩阵、Secret 扫描、性能、迁移、备份、安装和发布。 本项聚焦于发布集成、故障演练和运维生命周期，必须形成可以独立启动、测试或演示的最小闭环。

### 2. 范围与边界
- 包含：release candidate所需契约、状态转换、错误返回、日志事件和自动化验收。
- 不包含：未列入本阶段的真实 Provider、跨阶段优化或视觉润色；依赖能力只能通过 Port、Fake 或 fixture 接入。
- 上游依赖：S24-007。依赖未 verified 时，本功能只能处于 blocked。
- 主要产物：报告、制品、manifest、digest 和恢复证明。

### 3. 输入、输出与处理流程
输入来自上游命令、查询、事件、配置、用户动作或 fixture，必须具备稳定标识、版本和必要授权上下文。

处理顺序：首先校验格式、当前状态、权限和依赖版本；其次，围绕功能 [release candidate] 完成输入校验、领域处理、持久化或投影、用户反馈和可重复验收。；然后产生领域事件或标准结果并更新必要投影；最后返回成功、等待、失败或取消结果，并记录阶段进度。

输出为报告、制品、manifest、digest 和恢复证明。计划中的最小验收为：`cargo fmt/clippy/test/audit` 全部通过。

### 4. 状态、数据与一致性
- 状态变化必须遵守领域模型与事件规范，禁止直接修改 projection 绕过 domain command。
- SQLite Event Store，或本阶段明确声明的 Fake Store，是业务事实来源；日志只用于诊断。
- 外部副作用必须具有 operation_id、幂等策略、取消边界和恢复结论。
- 并发写入遵循单写者或 Write Claim；查询需要说明一致性级别和 cursor。

### 5. 异常、取消与恢复
必须设计输入无效、依赖不可用、权限拒绝、超时、用户取消、进程重启和重复提交路径。每类路径都要定义稳定 error_code、用户提示、是否可重试、是否产生事件和重启动作。不安全时进入明确的 blocked、failed 或 interrupted，不能静默成功。

### 6. 日志与安全要求
入口初始化日志，记录 stage.started、关键进度、stage.completed 或 stage.failed。日志采用非 JSON、非 key=value 的单行位置式格式：时间、级别、PID、任务链 UUID、线程或协程、logger、最后一级文件名和行号、主消息。不记录客户端地址；诊断上下文仅在 DEBUG 级别展示并置于主消息前，INFO 及以上隐藏。失败必须可定位、可重试、可回滚且不破坏用户数据。

### 7. 测试与可运行验收
- 必须提供 cargo 命令、CLI 子命令、集成测试或可重复 demo。
- 至少覆盖成功、失败、取消、重复调用以及适用的恢复或回滚路径。
- 日志断言任务链 UUID、稳定 message code、进度闭环、真实文件名和行号、无敏感信息泄漏。
- 回滚只能删除本阶段临时产物或恢复快照，不得删除用户数据或覆盖外部修改。

### 8. 后续代码注释要求
编码前必须通过本条目审核。实现代码需要为公共类型和函数、状态转换、协议字段、数据库迁移、异步任务边界、错误分支和安全判定添加中文详细注释。注释应解释设计原因、约束和失败恢复；复杂算法、并发或锁、取消传播、幂等键和安全边界还要写明前置条件、后置条件及不变量。测试代码需要说明 fixture 意图和验收理由。

### 9. 审核结论栏
- 需求理解：待审核
- 架构边界：待审核
- 接口和事件：待审核
- 异常与恢复：待审核
- 安全与日志：待审核
- 允许进入代码开发：否

<!-- END S24-008 -->
## S24-009：v0.1.0 release

### 1. 功能目标
本功能属于 S24。完成集成回归、故障矩阵、Secret 扫描、性能、迁移、备份、安装和发布。 本项聚焦于发布集成、故障演练和运维生命周期，必须形成可以独立启动、测试或演示的最小闭环。

### 2. 范围与边界
- 包含：v0.1.0 release所需契约、状态转换、错误返回、日志事件和自动化验收。
- 不包含：未列入本阶段的真实 Provider、跨阶段优化或视觉润色；依赖能力只能通过 Port、Fake 或 fixture 接入。
- 上游依赖：S24-008。依赖未 verified 时，本功能只能处于 blocked。
- 主要产物：报告、制品、manifest、digest 和恢复证明。

### 3. 输入、输出与处理流程
输入来自上游命令、查询、事件、配置、用户动作或 fixture，必须具备稳定标识、版本和必要授权上下文。

处理顺序：首先校验格式、当前状态、权限和依赖版本；其次，围绕功能 [v0.1.0 release] 完成输入校验、领域处理、持久化或投影、用户反馈和可重复验收。；然后产生领域事件或标准结果并更新必要投影；最后返回成功、等待、失败或取消结果，并记录阶段进度。

输出为报告、制品、manifest、digest 和恢复证明。计划中的最小验收为：二进制、SBOM、迁移说明、Runbook、已知限制齐全。

### 4. 状态、数据与一致性
- 状态变化必须遵守领域模型与事件规范，禁止直接修改 projection 绕过 domain command。
- SQLite Event Store，或本阶段明确声明的 Fake Store，是业务事实来源；日志只用于诊断。
- 外部副作用必须具有 operation_id、幂等策略、取消边界和恢复结论。
- 并发写入遵循单写者或 Write Claim；查询需要说明一致性级别和 cursor。

### 5. 异常、取消与恢复
必须设计输入无效、依赖不可用、权限拒绝、超时、用户取消、进程重启和重复提交路径。每类路径都要定义稳定 error_code、用户提示、是否可重试、是否产生事件和重启动作。不安全时进入明确的 blocked、failed 或 interrupted，不能静默成功。

### 6. 日志与安全要求
入口初始化日志，记录 stage.started、关键进度、stage.completed 或 stage.failed。日志采用非 JSON、非 key=value 的单行位置式格式：时间、级别、PID、任务链 UUID、线程或协程、logger、最后一级文件名和行号、主消息。不记录客户端地址；诊断上下文仅在 DEBUG 级别展示并置于主消息前，INFO 及以上隐藏。失败必须可定位、可重试、可回滚且不破坏用户数据。

### 7. 测试与可运行验收
- 必须提供 cargo 命令、CLI 子命令、集成测试或可重复 demo。
- 至少覆盖成功、失败、取消、重复调用以及适用的恢复或回滚路径。
- 日志断言任务链 UUID、稳定 message code、进度闭环、真实文件名和行号、无敏感信息泄漏。
- 回滚只能删除本阶段临时产物或恢复快照，不得删除用户数据或覆盖外部修改。

### 8. 后续代码注释要求
编码前必须通过本条目审核。实现代码需要为公共类型和函数、状态转换、协议字段、数据库迁移、异步任务边界、错误分支和安全判定添加中文详细注释。注释应解释设计原因、约束和失败恢复；复杂算法、并发或锁、取消传播、幂等键和安全边界还要写明前置条件、后置条件及不变量。测试代码需要说明 fixture 意图和验收理由。

### 9. 审核结论栏
- 需求理解：待审核
- 架构边界：待审核
- 接口和事件：待审核
- 异常与恢复：待审核
- 安全与日志：待审核
- 允许进入代码开发：否

<!-- END S24-009 -->
